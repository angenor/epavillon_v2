# Progression

**Ce fichier est la mémoire du projet entre deux sessions Claude Code.**

Le contexte d'une session se perd ; le dépôt reste. Toute session commence par lire ce fichier et se termine par le mettre à jour. Une session qui ne le fait pas oblige la suivante à tout redécouvrir.

Il ne porte plus que ce qui se lit **en arrivant** : l'état général, l'avancement écran par écran, et ce qui bloque. Le détail vit dans [`progression/`](progression/) — **on n'en ouvre que le fichier utile à la tâche du jour.**

**Dernière mise à jour** : 21 août 2026 — 🏁 **B4 EST LIVRÉ (T001–T160)** : **les huit histoires, les trente-sept routes.** Le dépôt, la machine à états, la liste du comité, la fiche d'évaluation et son voile, l'espace organisation, la correction et le renvoi, les pièces, l'historique et la reprise v1. **37 routes montées sur 37**, frappées sur la vraie application, et **37 opérations documentées** — deux comptes écrits à deux endroits, et c'est l'un d'eux qui a attrapé un scope écrit mais non monté, muet en 404. **Le voile de l'aveugle n'est pas un filtre** : quand il est baissé, la requête des revues des pairs n'est pas exécutée, et le test cherche la note d'un pair dans la charge utile **sérialisée**. Le décompte, lui, est lu — compter n'ancre pas. **La consolidation des notes est appelée dans la transaction, et les agrégats sont relus en base** : sans elle, le classement du comité serait faux **sans qu'aucune erreur ne le signale**. **L'espace organisation se prouve par un balayage de la charge utile entière** — ni note, ni rang, ni nom de membre du comité : vérifier champ par champ laisserait passer celui qu'on ajoutera demain. **Corriger un dossier retenu ne déplace pas sa séance** : le test change titre, créneau, format et durée, puis relit la séance champ par champ — déplacer une séance à quarante inscrits ne se rattrape pas. **La reprise v1 n'émet aucun événement** : elle écrit dans le journal sans passer par la mise à jour de l'état, donc sans réveiller le déclencheur — huit mille courriels évités. **Six écarts nouveaux, n° 105 à 110**, dont trois corrigeaient du code déjà livré : le refus de plafond de note sortait en 500 là où le contrat annonce un 422 ; un dossier **retenu** était fermé à la correction sans que rien ne le demande ; et le contrat du front demande deux choses incompatibles dès qu'un membre du comité écrit au déposant. **524 tests au total**, `cargo fmt`, `clippy -D warnings` et `cargo test --workspace` au vert, **aucune arête** vers `identity`, `org` ni `event`, **aucun travail différé** — vérifié, pas supposé. [Écarts et vérifications](progression/ecrans/b4-propositions.md). Prochain pas : **B5, les séances et le planificateur**. Avant cela, le même jour : 🟨 **B4 — PHASE 5 LIVRÉE (T077–T093)** : **le comité pilote sa liste.** **Tout l'écran en une réponse** — les lignes de la vue de pilotage telle quelle, et **les sept facettes comptées sur ces mêmes lignes** : demandées à part, elles seraient mesurées à un autre instant, et le « Retenu (17) » du filtre finirait par ne plus correspondre aux lignes affichées. **Le test recompte** filtre par filtre au lieu de comparer à des nombres écrits à la main. **Le périmètre est vérifié deux fois**, et ses trois états ne se confondent pas : global, détaché, et **vide → refus explicite, jamais une liste vide**. **Six identifiants forgés mènent au même refus qu'un inexistant**, dont quatre qui désignent des objets bien réels. **L'affectation groupée rend compte de chaque dossier** — appliqués + écartés = la sélection — et **douze dossiers confiés émettent douze événements, jamais un pour le lot**. **Le moment annoncé depuis US2 est arrivé** : `GET /proposals/{id}` est monté, six chemins littéraux entrent en concurrence réelle avec lui, et le découpage a tenu. **Deux écarts nouveaux, n° 104 et n° 105** ; le n° 104 aurait rendu au déposant la note du comité, que FR-077 lui interdit — le masquage est à la source. **16 routes sur 37, 91 tests du module, 491 au total**, `cargo fmt`, `clippy -D warnings` et `cargo test --workspace` au vert. [Écarts et vérifications](progression/ecrans/b4-propositions.md). Prochain pas : **phase 6, US4, T094–T115**. Avant cela, le même jour : 🏁 **B4 — LE JALON DU PROMPT EST ATTEINT (T001–T076)** : **l'appel à propositions de la COP31 reçoit des dossiers.** Une organisation dépose, corrige et retire ; la machine à états tient. **US2 livrée** : les transitions offertes tiennent en **une requête**, et un même dossier rend **trois menus différents** selon qui regarde — la déposante retire, le noteur demande des corrections, le décideur retient ou rejette. **Une transition acceptée écrit UNE ligne d'outbox, pas deux** : le déclencheur émet déjà, et compter est le seul contrôle qui dise quelque chose d'un doublon. **La colonne de décision est écrasée, le journal garde tout** — un renvoi sans motif l'efface, et c'est ce qui interdit à un écran de la lire. **Une correction mesurée plutôt que supposée** : Actix ne rend pas 405 quand la méthode ne correspond pas, il poursuit et le chemin finit en 404 — le risque de capture d'un chemin littéral n'existe donc que si les méthodes se recouvrent, ce qui arrivera à US4. **10 routes sur 37, 81 tests du module, 481 au total**, `make check-back` au vert. [Écarts et vérifications](progression/ecrans/b4-propositions.md). Prochain pas : **phase 5, US3, T077–T092**. Avant cela, le même jour : **B4 — PHASE 3 LIVRÉE (T031–T062)** : **US1 est livrée**, du premier enregistrement automatique à la confirmation de dépôt, **avec le même numéro de bout en bout**. **6 routes montées sur 37**, frappées sur la vraie application. **Un écart nouveau qui bloquait la fonctionnalité entière, n° 102** : la spécification avait vu que l'adresse d'URL manquait au premier enregistrement, **elle n'avait pas vu que les trois textes obligatoires butent sur le même vide** — le formulaire commence par les organisations, et `platform.i18n_text` refuse un français vide. Le service pose un texte provisoire, refusé au dépôt et effacé à la recomposition. **Un second, n° 103** : un intervenant connu sans compte « reste modifiable » d'après le contrat, ce que le plan n'avait pas prévu — ne rien écrire aurait produit un enregistrement qui réussit et ne change rien, sans un mot. **Les trois refus de recevabilité sortent en 200 avec leur valeur**, classés avant l'écriture, et une course est **reclassée** plutôt que lue au texte du déclencheur. **69 tests du module, 469 au total**, `make check-back` au vert. [Écarts et vérifications](progression/ecrans/b4-propositions.md). Prochain pas : **phase 4, US2, T063–T077**. Avant cela, le même jour : **B4 — PHASES 1 ET 2 LIVRÉES (T001–T030)** : `backend/crates/modules/programme` existe et **ne dépend de personne**, et tout ce que les huit histoires partagent est posé — six codes d'erreur au catalogue, trois contrats d'événements, **dix règles pures du domaine**, les seules lectures hors schéma réunies, les deux écritures hors schéma isolées, la résolution d'ascendance sur ses **trois** niveaux, le montage et la fabrique de test. **Le contrat d'événements du module ne porte aucune charge utile de changement d'état, et son en-tête dit pourquoi** : le déclencheur émet déjà les huit, et un service zélé enverrait tout en double — deux courriels par transition, visibles seulement en production. **`/organizations` est refactorisé — du code livré est modifié** : `org` expose ses routes du préfixe séparément, `api` compose une seule fois, aucune route n'a changé de chemin, et les vingt et une routes de B2 restent atteignables. **43 tests du module, 440 au total**, `make check-back` au vert, aucune arête entre crates de module. [Écarts et vérifications](progression/ecrans/b4-propositions.md). Prochain pas : **phase 3, US1, T031–T053**. Avant cela, le 20/08 : **B4 EST SPÉCIFIÉ, PLANIFIÉ ET DÉCOUPÉ** : [`specs/004-propositions/`](../specs/004-propositions/spec.md) — 8 histoires, 17 cas limites, **103 exigences**, 27 critères, **aucune modification du SQL**. **Huit écarts, n° 93 à 100**, dont trois qui auraient cassé à l'exécution : le déclencheur d'état **émet déjà** l'événement de domaine (l'inverse de B3 — un service qui émettrait aussi enverrait tout en double) ; l'adresse d'URL d'un dossier est **obligatoire, unique par édition, et absente du contrat du formulaire**, si bien que le tout premier enregistrement d'un brouillon échoue ; et **rien n'appelle la consolidation des notes**, laissant le classement du comité figé sans qu'aucune erreur ne le signale. **Le plan est écrit le même jour** — 24 décisions, 37 routes, 3 événements émis par le service contre huit par la base, **aucun travail différé**, une entorse et deux écritures hors schéma justifiées, et **un neuvième écart** : le chemin que le prompt voulait pour les transitions offertes est déjà celui du journal. **Et découpé le même jour** — **160 tâches en 11 phases, dont 46 de test**, quatre jalons, et **le jalon que le prompt demande à T001–T076**. [Écarts et vérifications](progression/ecrans/b4-propositions.md). Prochain pas : `/speckit-implement`. Avant cela, le même jour : **B3 EST LIVRÉ (T001–T129)**. Le module Événements est complet : **les huit histoires**, **37 routes** frappées sur la vraie application, **6 événements de domaine** émis — et les six silences tenus, ce qui compte autant. **L'appel à propositions de la COP31 peut être ouvert** : édition, périmètre, appel et sa grille, avec le refus qui **sauve les notes** — retirer un critère porteur de notes réussirait en base et détruirait l'argumentaire d'une décision de sélection ; c'est l'unique entorse assumée du module. Le public voit une édition, ses échéances et ses trois visuels **en une requête** — les écarts n° 25 et n° 26 se referment. Lieux, salles, canal du direct, calendrier, journées spéciales et comité sont écrivables ; la programmation ne se publie pas avec un conflit ouvert, et republier est sans effet. **Une dérogation, assumée et bornée** : poser les thématiques d'un fil écrit dans le référentiel partagé — la seule écriture hors du schéma `event`, isolée dans un fichier, le contrôle mécanique du plan étant trop large d'un schéma. **Deux arbitrages attendent toujours le commanditaire** : la granularité du calendrier d'une série de webinaires, et les permissions du rôle de programmation. Prochain pas : **B4 — les propositions**. [Écarts et vérifications](progression/ecrans/b3-evenements.md). Avant cela, le même jour : **B3 : PHASE 4 IMPLÉMENTÉE (T042–T053)**. **US2 est livrée** — le périmètre d'administration borne aussi les événements : la liste ne montre que ce qu'on administre, les facettes se comptent sur ces mêmes lignes, un compte sans droit reçoit un **refus** et non une page vide, et six identifiants forgés mènent tous au même refus qu'un identifiant inexistant. Le détail d'une édition rend ses **six onglets en une réponse**, lus sur une seule connexion dans une transaction en lecture seule, pour que leurs décomptes soient cohérents entre eux. **Trois constats de l'écriture** : le contrat du front documente une permission d'évaluation qui **n'existe pas** dans le modèle — le code réel a été retenu, et il vit au titre de **critère de liste**, pas de garde ; une jointure vers le schéma des personnes s'était glissée hors du fichier de frontière, retirée le jour même ; et l'heure d'ouverture du pavillon est rendue en texte **par la base**, faute d'un format standard qui corresponde au contrat. 69 tests sur le module, `cargo test --workspace` au vert, clippy sans avertissement, aucune arête entre crates de module, aucune écriture hors du schéma `event`. **T049 reste ouverte, et c'est exact** : elle attend les six routes d'onglet des phases 5, 7 et 9. Prochain pas : **phase 5 — US3, l'appel unique et sa grille (T054–T068)**, qui referme le jalon demandé. [Écarts et vérifications](progression/ecrans/b3-evenements.md). Avant cela, le même jour : **B3 : PHASES 1 À 3 IMPLÉMENTÉES (T001–T041)**. **US1 est livrée** — une édition de COP existe, avec le sigle exigé dès que le pavillon est tenu, la valeur proposée qui accompagne le refus, la traduction des six contraintes du modèle chacune sur son champ, et un calendrier **daté dans le fuseau de l'édition**. C'est l'entité dont B4, B5 et B6 dépendent tous, et l'écart n° 9 est traité de bout en bout. **Trois constats de l'écriture** : une tâche des fondations n'avait pas été écrite — un `cd` en échec avait avalé le fichier sans que rien ne le dise, et le code compilait parce que rien ne le référençait ; la traduction des refus de la base branche sur le **nom de la contrainte** et jamais sur le texte du message, ce qui oblige le dépôt à rendre l'erreur brute au service ; et le diagnostic de type de PostgreSQL rend le nom du domaine **nu**, mesuré sur la base après qu'une hypothèse contraire eut fait sortir un refus de formulaire en erreur anonyme. 49 tests sur le module, `make check-back` au vert, aucune arête entre crates de module, aucune écriture hors du schéma `event`. Prochain pas : **phase 4 — US2, le périmètre borne aussi les événements (T042–T053)**. [Écarts et vérifications](progression/ecrans/b3-evenements.md). Avant cela, le même jour : **B3 : PHASES 1 ET 2 IMPLÉMENTÉES (T001–T024)**. Le crate `backend/crates/modules/event` existe, il ne dépend d'aucun autre module, et les fondations tiennent : **trois codes d'erreur** au catalogue du noyau, la traduction des contraintes du module — **y compris le cas des domaines, où le nom de contrainte est celui du domaine et non de la colonne, et où seul le nom de type est fiable** —, **six charges utiles d'événements**, les **six résolutions d'ascendance** et l'assistant qui résout **avant** de vérifier le périmètre, le montage dans les deux binaires avec le préfixe `/admin/planner` **composé une seule fois dans `api`** (le défaut de B2 traité avant qu'il ne se reproduise), la clôture automatique des appels échus et son réarmage au démarrage du worker, et le harnais de test avec ses deux éditions — la COP31 et celle **sans pavillon**. `make check-back` au vert, `cargo tree -p event` sans arête vers `identity` ni `org`. **Une divergence assumée avec le découpage** : le harnais du module ne monte pas la vraie application — le crate `api` en dépendrait, et `cargo tree` ferait échouer le contrôle. Les tests d'application vivent dans `crates/api/tests/`, comme en B2. Prochain pas : **phase 3 — US1, l'édition, son sigle et son calendrier (T025–T041)**. [Écarts et vérifications](progression/ecrans/b3-evenements.md). Avant cela, le même jour : **B3 EST DÉCOUPÉ : 129 TÂCHES EN 11 PHASES, DONT 31 TESTS**, une phase par histoire et **quatre jalons de livraison**. **Trois avertissements sont écrits en tête du découpage**, chacun parce qu'il coûterait cher découvert en chemin : **supprimer un critère d'évaluation RÉUSSIT en détruisant les notes** — le refus et le test qui vérifie que les notes sont toujours là après vont ensemble, sans la seconde moitié le test ne prouve rien ; le préfixe du planificateur **sera partagé avec B5** et se compose dès les fondations ; et trois chemins littéraux doivent être déclarés avant leur homologue paramétré, sans quoi « public » est capturé comme une adresse d'URL. **Le découpage a fait apparaître une dépendance que le plan n'avait pas nommée** : la phase du périmètre bloque les trois phases d'onglets, parce que **toute écriture d'onglet rend la composition entière**. **Le jalon que le prompt demande est T001–T068** — édition, périmètre, appel et grille : à ce point, la campagne de la COP31 peut être ouverte. Prochain pas : `/speckit-implement`. [Écarts et vérifications](progression/ecrans/b3-evenements.md). Avant cela, le même jour : **B3 EST PLANIFIÉ : 20 DÉCISIONS, 37 ROUTES, UNE SEULE ENTORSE.** `plan.md`, `research.md`, `data-model.md`, `contracts/` et `quickstart.md` — contrôle constitutionnel passé **deux fois**, **aucune dépendance nouvelle**, et **aucun SQL composé dynamiquement**, contrairement à B2. **Deux écarts de plus, n° 91 et 92, dont un que seul le plan pouvait révéler** : **supprimer un critère d'évaluation détruit les notes, en silence** — la clé étrangère est `ON DELETE CASCADE`, l'ordre réussit, et l'argumentaire d'une décision de sélection disparaît, précisément ce que la v1 n'avait pas ; le service compte et refuse, et **c'est l'unique entorse au principe VIII de tout le plan**. Le second : **cinq tables du module ne sont pas auditées** — journées, fils, lieux, salles, canaux —, consigné et non compensé. **Quatre décisions structurantes.** Les **jours civils se calculent en base, dans le fuseau de l'édition** : deux bases de fuseaux dans une même chaîne, c'est exactement ce qui a fait tomber le formulaire du front sur `Europe/Geneva`. **Tout ce qui détache une séance le compte AVANT de détacher** — les clés sont `ON DELETE SET NULL`, après coup le chiffre serait zéro. Le préfixe du planificateur est **composé une seule fois dans `api`**, avant que B5 n'y vienne : le patron posé en B1 pour `/people`, appliqué avant que les trois routes muettes de B2 ne se reproduisent. Et la **publication annonce au lieu d'écrire**, en portant le **prédicat exact** des séances, pour que le chiffre annoncé et l'effet obtenu viennent du même raisonnement. Prochain pas : `/speckit-tasks`. [Écarts et vérifications](progression/ecrans/b3-evenements.md). Avant cela, le même jour : **B3 EST SPÉCIFIÉ : LE MODULE ÉVÉNEMENTS.** [`specs/003-evenements/`](../specs/003-evenements/spec.md) — **8 histoires, 16 cas limites, 99 exigences, 25 critères de réussite, et AUCUNE modification du SQL** : `060_events.sql` a été relu en entier, ses huit sections couvrent déjà tout le prompt. **Trois vérifications ont changé la spécification.** (1) **Aucun déclencheur de ce module n'émet d'événement de domaine** — deux déclencheurs d'audit, aucune fonction `SECURITY DEFINER` : le piège n° 1 de B1 (anonymisation) et de B2 (fusion), où la base émet déjà ce que le service s'apprête à émettre, **n'existe pas ici**, et c'est vérifié plutôt que supposé. (2) **Le rôle de programmation ne détient AUCUNE permission du module** — l'écart n° 5 d'A10 supposait le contraire ; un chargé de programmation ne peut ni créer un fil, ni déclarer une salle, ni poser un canal. Consigné, non corrigé : ce serait modifier le modèle. (3) **Publier la programmation exige deux écritures dans deux schémas** — la date sur l'édition, la visibilité sur chaque séance, que la vue publique filtre. **Tranché par l'outbox** : B3 contrôle, pose la date et annonce ; la Programmation consomme. Et la publication est gardée par la permission de **planifier**, celle que le modèle attribue au rôle chargé de publier le programme — le garde vivant dans le noyau depuis B1, aucune arête entre crates n'en découle. **L'écart n° 9 est traité par l'option A** : le sigle reste facultatif en base et devient obligatoire **dans le service** pour une édition tenant un pavillon, vérifié sur l'état **résultant** de l'écriture, avec **une valeur par défaut proposée** — un refus qui ne propose rien fait chercher une convention que personne n'a écrite. **Deux écarts d'A3 se referment** : les éditions publiques deviennent une ressource portant la règle « ni brouillon ni annulée » (n° 26), et la page d'une édition embarque ses trois déclinaisons d'image (n° 25). **Quatre écarts nouveaux, n° 87 à 90.** Deux points restent à arbitrer avec le commanditaire : la granularité du calendrier d'une série de webinaires (302 journées vides sur `paco2027`) et les permissions du rôle de programmation. Aucun code Rust : `backend/crates/modules/` contient `identity` et `org`. [Écarts et vérifications](progression/ecrans/b3-evenements.md). Avant cela, le même jour : **B2 EST CLOS : LES 129 TÂCHES SONT FAITES, `make check` PASSE EN ENTIER.** T124 exécuté — base détruite, schéma rechargé de zéro, `nuxt typecheck`, `npm run build`, `cargo fmt --check`, `clippy -D warnings` et `cargo test --workspace` en une seule passe. **249 tests.** Trois corrections avant cela. (1) **« Remettre dans la file » ne remettait rien** : l'écran range les paires de doublons sur la date de relecture, que *toute* décision renseigne, et le service ne savait reprendre qu'une paire déjà **reportée** — jamais une paire **écartée**, c'est-à-dire jamais le cas qui compte. Un report posé sur une paire déjà sortie de la file l'y ramène désormais, quelle qu'ait été la décision ; **une paire fusionnée, elle, ne se rejuge plus du tout**. (2) **`useApi.ts` repasse sous le garde-fou** : 1019 → 807 lignes, le bloc `proposals` sorti dans son propre fichier. (3) **`nuxt typecheck` ne vérifiait plus rien** : `vue-router` était épinglé sur la branche 4 quand Nuxt 4.5 dépend de la 5, et `vue-tsc` échouait à lire sa configuration **avant** d'avoir vérifié le moindre type. Aligné, vérifié au navigateur dans les deux langues. — **DEUX CORRECTIONS DE FRONT APRÈS LA VÉRIFICATION DE B2 AU NAVIGATEUR.** (1) **L'écran d'acceptation d'une invitation existe** : `app/pages/invitation.vue` referme le 404 des liens d'invitation — le défaut que B1 avait déjà payé —, sur le chemin exact que compose `mail.rs`, **sans exiger de session** puisque le jeton est la preuve d'adresse. Les cinq issues sont éprouvées au navigateur, français et anglais. (2) **Le bouton « Examiner la fusion » navigue** : le défaut consigné ne se reproduit dans aucune des huit conditions éprouvées, et `localePath()`, mis en cause, rend le bon chemin dans les deux langues — mesuré, pas supposé. Seule fragilité réelle, corrigée : la promesse de `navigateTo` n'était pas attendue. Les deux constats faits en chemin sont dans [`progression/points-bloques.md`](progression/points-bloques.md). — **B2 EST LIVRÉ : LE MODULE ORGANISATIONS TOURNE.** Les 129 tâches de `specs/002-organisations/tasks.md` sont faites : le crate `backend/crates/modules/org` porte **21 routes, 6 travaux différés, 6 événements, 11 codes d'erreur et 30 fichiers de test — dont un qui frappe les vingt et une routes sur la vraie application**. `cargo test --workspace` passe avec **246 tests**, contre 141 à la fin de B1. Clippy passe sans un avertissement, aucun fichier de `backend/` ne dépasse 1000 lignes, et `cargo tree -p org` ne montre **aucune arête** vers un autre crate de module. **La cible chiffrée est tenue, et largement** : la recherche répond en **55 ms au 95ᵉ centile sur 5 001 fiches**, pour une cible de 150 — médiane 48 ms, pire cas 64. Aucun des trois remèdes prévus n'a servi, et **aucune ligne de `docs/database/` n'a été modifiée**, comme annoncé. **Le défaut n° 1 de la v1 est pris par les quatre bouts** : les cinq façons de désigner l'IFDD ramènent la même fiche, les deux lectures de recherche rendent enfin des résultats différents sur la même requête, le doublon exact se refuse en **nommant la fiche existante**, et la fusion répare ce qui a été créé en double sans rien perdre. **Les trois pièges que le plan avait cherchés n'ont rien cassé** — c'est tout l'intérêt de les avoir cherchés avant : la fonction de fusion émet elle-même son événement et marque elle-même la paire (le service ne le refait pas, et **le dit à l'endroit où l'on serait tenté d'ajouter la ligne**) ; l'unicité des adhésions ignore le statut, donc la demande est un **unique ordre** avec reprise conditionnelle, éprouvé sur cent demandes concurrentes ; et les arbitrages de fusion viennent **après** l'appel — une fusion arbitrant le nom légal aboutit, et échouerait dans l'autre ordre. **Le service de jetons est remonté dans le noyau à comportement constant** : les quatre tests de B1 sur la vérification d'adresse, la réinitialisation et le rejeu **n'ont pas été réécrits** et restent verts. Le défaut n° 81 est corrigé — **une personne créée par invitation obtient désormais un compte et son lien** —, et le test de B1 qui affirmait l'inverse est réécrit **en disant pourquoi il avait tort**. **Cinq défauts que seul le code pouvait révéler**, dont deux que seule une requête HTTP réelle pouvait montrer : **trois routes sur vingt et une étaient muettes** — deux `web::scope` du même préfixe ne se complètent pas, Actix retient le premier et rend 404 —, et une charge utile exigeait un identifiant que le chemin portait déjà. Le premier est désormais tenu par un test qui frappe les vingt et une routes sur la vraie application. Les trois autres : cent créations concurrentes sortaient en « service indisponible » parce que la transaction perdante retenait **deux** connexions du pool avant d'être rendue ; la liste du back-office était **vide sur une base neuve**, jointe par l'intérieur à une projection qu'un travail différé seul rafraîchit ; et une faute d'orthographe connue se faisait passer pour une dénomination posée par la base, la normalisation effaçant les accents qui la distinguaient du nom légal. **Une réserve, mesurée et assumée** : le décompte de transfert d'une fusion est exact au chiffre près sur **dix-sept lignes du registre sur dix-huit** — la dernière est déplacée par un trigger d'adhésion avant que la boucle n'y arrive ; rien n'est perdu, et corriger l'écart demanderait de reproduire l'effet d'un trigger dans un décompte, ce que le principe VIII interdit. **Éprouvé au navigateur le même jour** : la chaîne du courriel va jusqu'à **Mailpit** — invitation, travail en file avec son jeton, worker, relais du site, courriel lu ; le premier essai a échoué et **la reprise a réussi**. L'acceptation **sans session** rend l'adhésion active et marque l'adresse vérifiée ; le rejeu dit « déjà utilisé ». Côté site, rien n'a régressé, et **deux constats en sont sortis pour B7** : le lien d'invitation mène à une page qui n'existe pas — le défaut que B1 avait déjà payé —, et le bouton « Examiner la fusion » ne navigue pas. Reste une vérification : `make check` en entier, qui détruit la base. [Écarts et vérifications](progression/ecrans/b2-organisations.md). Avant cela, le même jour : **B2 EST SPÉCIFIÉ, PLANIFIÉ ET DÉCOUPÉ : `specs/002-organisations/`** — `tasks.md` porte **129 tâches en 11 phases, dont 29 tests**, une phase par histoire utilisateur et **quatre jalons de livraison**, dont le premier — **T001–T035** — suffit à *prévenir* le défaut n° 1 de la v1 et se démontre en une minute sur la fiche que le semis fournit déjà. Deux avertissements sont écrits en tête du découpage : **la phase 2 touche du code livré et éprouvé**, et **l'ordre des deux écritures d'une fusion est l'inverse de celui que la documentation annonçait encore**. Avant cela, le même jour : **le plan**, qui porte **19 décisions techniques**, 21 routes, 11 codes d'erreur, 6 événements, 6 travaux différés, et passe le contrôle des dix principes **deux fois** avec **une seule entorse justifiée** : le décompte de transfert d'une fusion compose son SQL depuis le registre des références, seule façon que le chiffre annoncé avant et le chiffre rendu après viennent du même raisonnement. Trois décisions structurantes : **la lecture filtrée SUR-LIT** — la limite est appliquée à l'intérieur de la fonction, donc filtrer après coup rendrait moins de lignes que demandé, en silence ; **le service de jetons à usage unique remonte dans le noyau**, trois des cinq finalités du modèle n'appartenant pas à `identity` et les durées par finalité y vivant déjà depuis B1 ; **la cible de 150 ms se mesure avant de se traiter**, avec un semis à distribution réaliste, 5 000 noms tirés au hasard rendant la mesure excellente et fausse. **Six écarts de plus, n° 81 à 86**, dont un défaut de B1 que seul B2 pouvait révéler : **une personne créée par invitation ne pouvait pas se créer de compte** — le courriel serait parti, le lien aurait marché, et l'invité n'aurait jamais pu se connecter. La règle de frontière qui vaudra pour B3 à B6 est posée : un module **lit** hors de son schéma quand la question porte sur ses propres entités, il n'y **écrit** jamais et n'appelle jamais un autre module. Avant cela, le même jour : **la spécification** — 8 histoires utilisateur, 14 cas limites, **84 exigences fonctionnelles**, 18 critères de réussite, **aucune modification du SQL**. L'écart n° 23 devient **deux lectures au-dessus de la même fonction de base**, qui n'est pas touchée : celle destinée à une personne n'admet que les fiches ressemblant à ce qui a été tapé, celle de la revue des doublons rend tous les signaux — et le domaine **continue d'alimenter le score** de la première, ce qui est son seul emploi juste ici. **Les quatre obligations relevées en A11 sont traitées, et la première est corrigée** : les arbitrages de champ d'une fusion viennent **APRÈS** l'appel et non avant, l'unicité du nom ne portant que sur les fiches vivantes — tant que la fiche absorbée est vivante, la survivante ne peut pas reprendre son nom légal. **Onze écarts, n° 70 à 80**, dont trois qui auraient cassé à l'exécution : cet ordre, l'**adresse d'URL non arbitrable** (unique sans condition de statut, et la libérer casserait la promesse même de la fusion), et l'**unicité des adhésions qui porte aussi sur les révoquées** — une personne refusée ne pourrait plus jamais redemander à rejoindre. Deux constats d'autorisation : la permission de consultation des organisations est **détenue par le rôle d'utilisateur ordinaire** et ne peut donc pas garder le back-office seule, et **rien n'attribue ce rôle aujourd'hui** — garder la recherche derrière elle refuserait tout nouvel inscrit. Le piège n° 1 d'`identity` se répète à l'identique : **la fonction de fusion émet elle-même son événement**. Deux choses tranchées : le score de confiance est recalculé par un **travail différé et non par un trigger** — c'est une aide au tri, pas un invariant —, et **le retrait du dernier référent actif est refusé** tant qu'aucun autre n'a été désigné. Aucun code Rust : `backend/crates/modules/` ne contient toujours que `identity`. [Écarts et vérifications](progression/ecrans/b2-organisations.md). Avant cela, le même jour : **B1 EST TERMINÉ : LES 126 TÂCHES SONT FAITES, ET LE JALON QUI OUVRE L'APPEL À PROPOSITIONS DE LA COP31 EST COMPLET (phases 9, 10 et 11, T100–T126).** `make check` passe **en entier depuis la racine**, avec **141 tests**, **143 requêtes préparées** et **zéro avertissement** : base détruite et rechargée de zéro, seize schémas, rapport de frontières vide, projections analytiques rafraîchies, site compilé. Plus gros fichier de `backend/` : **588 lignes** ; aucune arête entre deux crates de module. **La chaîne différée est éprouvée, plus supposée** : la purge des jetons devient une tâche récurrente **qui se replanifie elle-même** — le démarrage du worker ne fait que **réarmer** la chaîne au cas où sa dernière occurrence serait morte avant d'avoir posé la suivante —, `GET /api/health` rend les quatorze indicateurs de la vue d'exploitation **sans recalculer un seul seuil**, et `GET /api/ready` **sollicite réellement le pool**. Le point de contrôle est tenu à la main : inscription provoquée, worker arrêté (rien ne part), worker relancé — **un seul courriel**. **Une demande d'effacement s'honore sans détruire les statistiques d'une COP passée** : la file RGPD exige la portée **globale**, ou 403 — jamais une file filtrée, qui donnerait l'illusion d'un traitement complet à qui n'en voit qu'un morceau ; l'anonymisation ne répond qu'à une demande d'**effacement** ; et le service **n'émet pas** `identity.person.anonymized`, parce que la fonction de base l'émet déjà — le piège n° 1 du module. **La documentation OpenAPI est engendrée, jamais écrite** : 26 chemins annotés auprès de leur gestionnaire, et les **20 codes d'erreur stables** engendrés depuis le catalogue du noyau, si bien qu'un code ajouté apparaît au prochain démarrage et qu'un code oublié n'existe pas. **Deux défauts trouvés en éprouvant à la main, et non par un test** : (1) **trois exemplaires du même courriel** sont partis pour une seule inscription — le relais du site retenait l'identifiant du message **après** l'envoi, alors que le doublon réel est **concurrent** ; il est désormais réservé **avant**, et rendu si l'envoi échoue ; (2) **`platform.audit_log.actor_label` n'était renseignée par personne**, alors que la colonne existe pour « rester lisible après anonymisation RGPD » — sans elle, une personne qui exerce son droit à l'effacement fait devenir « Utilisateur anonymisé » toutes ses décisions passées ; le modèle est corrigé. Quinze écarts, n° 55 à 69. **Puis, hors lot : les en-têtes CORS sont posés**, avancés de B7 — ils manquaient, et sans eux aucun appel de navigateur n'aboutissait. Les **huit histoires** sont désormais éprouvées depuis un vrai navigateur, ce qui montre ce que `curl` ne peut pas montrer : les cookies de session **invisibles à `document.cookie`** et pourtant envoyés, et le jeton de rafraîchissement **confiné à `/api/auth`** par son `Path`. Le site n'a rien perdu au passage — `NUXT_PUBLIC_API_BASE` reste vide, les écrans tournent toujours sur les données simulées. Avant cela, le même jour : **UN WEBINAIRE SE CONFIE À SON RESPONSABLE SANS LUI OUVRIR LE RESTE DE LA PLATEFORME (phase 8, T089–T099).** Le scénario est éprouvé à la main : une administratrice détachée sur une édition attribue **là**, et nulle part ailleurs. **Le retrait exige le droit qu'il aurait fallu pour accorder**, sur la portée de l'attribution visée — sans cette seconde moitié, elle défait un rôle global qu'elle n'aurait jamais pu poser. **Le refus du trigger ressort mot pour mot** : le modèle sait déjà dire quelles portées un rôle admet, et le reformuler produirait un second libellé qui se périmerait. **Le retrait pose trois colonnes et ne supprime jamais.** Les options d'attribution se bornent à ce que l'appelant peut réellement accorder, et ce qui est hors d'atteinte reste **visible et désactivé**. **110 tests.** **Deux défauts trouvés en éprouvant à la main, et non par un test** : le refus de portée **divulguait les rôles de la personne visée** à n'importe quel compte connecté, et une **valeur** hors liste se faisait passer pour un **champ** fautif, faisant souligner à l'écran une case inexistante. Avant cela, le même jour : **UN COMPTE PERDU SE RÉCUPÈRE SANS PASSER PAR L'ASSISTANCE (phase 7, T081–T088).** Le cycle est éprouvé à la main : demande, courriel reçu dans **Mailpit**, lien suivi, mot de passe choisi — et **l'ancien mot de passe ne vaut plus rien**, la session ouverte avec lui rendant `null`. **Deux statuts, et ils ne disent pas la même chose** : un jeton refusé sort en **200** avec son discriminant, un mot de passe refusé en **422** sur le champ `password`. **Le jeton est revérifié à l'envoi** et pas seulement au contrôle préalable ; un mot de passe refusé **ne brûle pas le lien**. Un changement abouti remet le compteur d'échecs à zéro, **lève le verrou** et **coupe toutes les sessions**. **88 tests.** Six écarts, dont un qui datait de la phase précédente et se voyait dans la boîte aux lettres : **les liens des courriels menaient à des pages inexistantes**, le site traduisant ses chemins là où les courriels citaient le nom du fichier de page. Avant cela, le même jour : **QUELQU'UN S'INSCRIT, REÇOIT SON COURRIEL ET SE CONNECTE (phase 6, T068–T080).** La chaîne complète est éprouvée à la main : inscription, courriel reçu dans **Mailpit**, lien suivi, adresse vérifiée, connexion — et le même lien rejoué rend « déjà utilisé ». **La réponse invariable tient par le message ET par le temps** : créer un compte coûte un hachage de cinquante à cent millisecondes, ne rien créer coûte une lecture d'une milliseconde, donc le mot de passe est haché **dans les deux cas**. **Le jeton en clair ne vit que dans la charge utile du travail différé**, née dans la transaction du changement d'état et vidée dès l'envoi réussi : ni la table des jetons ni l'outbox ne le connaissent, et un test le cherche. **Le trou connu de la file est refermé avant le premier envoi** — un travail mort gardait sa charge utile —, sans coûter leur diagnostic aux modules dont elle est la seule matière. **75 tests.** Cinq écarts, dont un qui trompe : **`localhost` n'est pas `127.0.0.1`**, et un envoi sur deux échouait en disant seulement « relais injoignable ». Avant cela, le même jour : **LE PÉRIMÈTRE D'ADMINISTRATION EST TENU PAR L'API (phase 5, T059–T067).** La règle métier n° 8 quitte l'écran : huit routes de lecture gardées, et un administrateur détaché sur une édition ne voit que ce qu'on lui a confié — **y compris quand il forge un identifiant dans l'URL**. **Les trois cas du périmètre restent distincts** : global, éditions listées, et **aucun droit → refus explicite**, jamais une liste vide — c'est le défaut silencieux que le modèle décrit et qu'un garde testant « pas global » aurait reproduit. Une fiche hors périmètre est **marquée** (`in_scope: false`), pas cachée : la taire ferait croire à la disparition de quelqu'un qu'on sait présent. **L'origine d'une permission n'existe nulle part en base** — la fonction du modèle rend `(permission, portée)`, assez pour autoriser, pas pour expliquer — et se recompose depuis les attributions. Le noyau gagne la permission testée **sur n'importe quelle portée**, dont les cinq modules suivants auront besoin dès leur première liste. **59 tests**, `make check-back` au vert, plus gros fichier à 422 lignes. **Le jalon T001–T067, celui qui débloque B2 à B6, est fermé.** Un écart laissé ouvert : `GET /people` n'est pas bornée par le périmètre parce que le contrat ne le demande pas. Avant cela, le même jour : **LES DEUX ÉCARTS ASSUMÉS SONT REFERMÉS.** (1) Les écritures du compteur d'échecs avaient quitté la porte du noyau parce que les quatre allers-retours d'une transaction se mesurent et trahiraient l'existence d'un compte. Le raisonnement était juste, la conclusion fausse : **une transaction ne coûte que si on l'attend**. Lancée avant le hachage, elle se replie derrière lui — **1,4 % d'écart de temps** mesuré, contre 10 % tolérés —, et toute écriture du jalon repasse par la porte unique. (2) **`X-Forwarded-For` n'est plus cru sur parole** : `TRUSTED_PROXIES` déclare les mandataires, le défaut n'en croit aucun, et la chaîne annoncée se remonte de droite à gauche jusqu'au premier maillon inconnu. Le tout vit dans `kernel::net`, pour que les cinq modules suivants n'aient pas à le refaire. Au passage la colonne `inet` cesse de traverser en texte. **52 tests**. Le troisième écart reste ouvert à la demande du commanditaire — `useApi.ts` et ses 1 008 lignes sont un chantier de front. Avant cela, le même jour : **les phases 3 et 4 sont livrées, auditées et corrigées.** Un second audit croisé — huit lecteurs, huit angles, chacun suivi d'un sceptique — a rendu **45 constats confirmés sur 59, plus 9 trouvés par le critique de complétude, tous traités**. **Un défaut de gravité haute**, trouvé par trois lecteurs sur huit : un verrou échu ne rendait qu'**un** essai au compte au lieu de tous — quelqu'un qui s'était verrouillé en cherchant son mot de passe n'avait plus droit qu'à une tentative par quart d'heure, à vie. **Deux courses fermées** : deux renouvellements simultanés ouvraient deux sessions, deux échecs simultanés posaient deux verrous ; dans les deux cas c'est la base qui décide désormais. **Un corps mal formé sortait en 400 avec le texte anglais de serde** alors que le contrat annonçait le point livré. **Et rien ne traversait HTTP** : `crates/api` devient bibliothèque et binaire pour qu'un test monte **la vraie application**, intergiciels compris — neuf cas de plus, **42 tests** au total. Quatorze affirmations fausses ont été corrigées dans les documents, sur dix-huit fichiers, dont un relevé du modèle faux sur cinq de ses six chiffres parce que la requête de recomptage oubliait un schéma. La constitution passe en **1.0.1**. Avant cela, le même jour : **ON SE CONNECTE, ET LA SESSION TIENT (phases 3 et 4).** 58 tâches sur 126, `make check-back` au vert avec **52 tests**. Les **six issues de connexion** sortent en 200 dans l'ordre imposé — mot de passe d'abord, puis verrou, suspension, adresse non vérifiée, second facteur. La session vit, tourne et se coupe : jeton d'accès signé Ed25519 de quinze minutes **sans aucune permission dedans**, jeton de rafraîchissement dont la base ne garde que l'empreinte, rotation qui chaîne les lignes, et **rejeu qui révoque toutes les sessions**. **Le test de discrétion a échoué au premier essai, et il avait raison** : 22,8 % d'écart de temps entre une adresse inconnue et un mot de passe faux, là où la spécification en tolère dix. Le hachage était identique des deux côtés à 0,2 % près — la différence venait de ce qu'une adresse connue **écrit** son compteur d'échecs et qu'une adresse inconnue n'écrit rien. Le hachage part désormais sur un fil dédié et l'écriture est lancée avant de l'attendre : **écart ramené à 1,5 %**. Six écarts de plus, dont la clé de signature qui se lit en 32 octets et non en PEM, le renouvellement qui **ne repousse pas** l'échéance, et `citext` que PostgreSQL rabat silencieusement sur `text` — la comparaison d'adresse serait redevenue sensible à la casse. [Écarts et vérifications](progression/ecrans/b1-socle-identite.md). Avant cela, le même jour : **le socle est debout, AUDITÉ et corrigé.** Un audit croisé du code contre les documents a rendu **53 constats confirmés sur 80, tous traités** : trois choses étaient réellement cassées sans que rien ne le montre — une adresse mal écrite sortait en **500** parce que le contrat citait le mauvais SQLSTATE, **Jaeger ne recevait aucune trace** (aucun span ouvert, puis un exportateur qui mourait faute de réacteur sur son fil), et **le worker n'obéissait pas à `Ctrl-C`**. Trois garanties étaient fausses, dont la « porte unique d'écriture » que le noyau enfreignait lui-même. Le modèle des tests porte maintenant l'empreinte du SQL qu'il contient, et ne se périme plus en silence. Dix documents portaient des décomptes périmés. Deux dettes sont tranchées et datées : le jeton en clair d'un travail mort (avant les courriels) et l'absence de CORS (avec B7). Avant cela, le même jour : **`backend/` EXISTE ET TOURNE — les phases 1 et 2 sont implémentées** (T001–T035 sur 126). Workspace Cargo à cinq crates — `kernel`, `contracts`, `modules/identity`, `api`, `worker` —, 2 837 lignes de Rust, aucun fichier au-dessus de 332. `make check-back` est au vert : mise en forme, `clippy -D warnings` sans un seul avertissement, 3 tests d'intégration sur base réelle et jetable. Le noyau porte ses onze pièces, dont **l'unique porte d'écriture qui pose l'acteur et l'identifiant de requête elle-même** et **l'empreinte factice calculée au démarrage**. **Le garde d'autorisation est bien dans `kernel`** : aucune arête entre deux crates de module. Éprouvé pour de vrai — l'API répond, `X-Request-Id` va jusqu'au corps d'erreur, une écriture d'origine inconnue est refusée, et un événement de l'outbox est relayé en moins d'une seconde puis **non retraité** au rejeu. Sept écarts relevés en implémentant, dont trois qui engagent la suite : **SQLx passe en 0.9** et Rust est épinglé en 1.97.1 ; une écriture **sans** en-tête d'origine passe ; le contrôle de frontières de `quickstart.md` compte une ligne de trop. [Écarts et vérifications](progression/ecrans/b1-socle-identite.md). Avant cela, le même jour : **B1 spécifié, PLANIFIÉ et DÉCOUPÉ** — `tasks.md` porte **126 tâches en 11 phases, dont 23 tests**, une phase par histoire utilisateur. Le découpage a fait apparaître une décision d'architecture qui engage les cinq modules suivants : **le garde d'autorisation vit dans le noyau**, `has_permission()` et `administered_events()` étant des fonctions SQL que tous les modules doivent appeler alors qu'aucun crate de module ne peut dépendre d'`identity`. Trois jalons de livraison sont posés, dont **T001–T067, celui qui débloque B2 à B6**. Détail du plan : (`specs/001-socle-identite/`). Le plan porte 16 décisions techniques et passe le contrôle des dix principes de la constitution deux fois, sans violation. Trois décisions structurent le reste : **le jeton d'accès est signé et ne porte aucune permission** — c'est le modèle qui l'a décidé, `identity.sessions` ne portant qu'une empreinte de jeton de rafraîchissement ; **un jeton rejoué révoque toutes les sessions** ; **l'API n'enverra aucun courriel elle-même**. **Une contrainte d'hébergement est arrivée en cours de plan** : l'API et le site sont sur deux serveurs, et seul celui du site a le droit d'émettre du courriel — le worker compose et remet au site par un appel HTTP privé, le noyau exposant un *contrat* d'envoi pour que la bascule en Rust, le jour de l'autorisation, ne touche aucun module. Onze écarts relevés en tout. Toujours aucun code Rust. Détail de la spécification : (`specs/001-socle-identite/`, par `/speckit-specify`) : 8 histoires utilisateur priorisées, 64 exigences fonctionnelles, 13 critères de réussite mesurables. Les quatre exigences du prompt sont traitées — seuil et durée du verrou dans la configuration du service (5 échecs, 15 min), une durée de jeton par finalité (24 h / 1 h), l'adresse non vérifiée qui interdit la connexion sans nouveau statut de personne, et l'ordre imposé des contrôles. Sept écarts relevés en l'écrivant, dont la discrétion qui se trahit par le TEMPS de réponse et le courriel transactionnel sans propriétaire avant B6. **Le point laissé ouvert par B0 est clos** : `identity.administered_events` filtre sur la bonne permission. Aucun code Rust : `backend/` n'existe toujours pas. **`CLAUDE.md` gagne une section** — les questions au commanditaire se posent en mots simples. Avant cela, le même jour : **B0 : la constitution de l'API est ratifiée en 1.0.0** (`.specify/memory/constitution.md`) : dix principes non négociables, deux sections de contraintes, une procédure d'amendement. Trois vérifications restent mécaniques et bloquantes — rapport de frontières vide, `make check` au vert, aucune arête entre deux crates de module. Sept écarts relevés en l'écrivant, dont le filtre de permission de `identity.administered_events`, à vérifier en B1. Aucun code Rust : `backend/` n'existe pas encore. Le même jour : **GitHub Spec Kit est installé et branché sur Claude Code** : CLI `specify` 0.16.5, `.specify/` et les dix compétences `.claude/skills/speckit-*`. Les commandes prennent un tiret depuis la 0.16 (`/speckit-specify`), `docs/PROMPTS_DEVELOPPEMENT.md` est corrigé. Aucun prompt B n'est commencé — la constitution reste à écrire. Avant cela, le 19 août : **la page publique d'une édition est refondue (A3)** : le dépôt monte dans le bandeau, la page passe à deux colonnes, la frise des échéances à la verticale, et `UiButton` gagne une variante `glass` bornée au média. La barre d'ancres essayée au passage a été retirée à la demande du commanditaire. Avant cela, le même jour : **la progression est découpée dans [`progression/`](progression/)**, un fichier par écran, par jour de journal et par jour de décisions. Avant cela, le même jour : accueil public et vitrine administrable (A15), le module `content` qui la porte, l'ouverture du verre dépoli dans la charte, les visuels réels repris de la plateforme v1, l'historique des éditions passé au rail-affiche, et les trois formats d'image d'une édition (32:9, 16:9, 1:1).

---

## État général

| Domaine | État |
|---------|------|
| Modèle de données | ✅ Complet et validé — 19 fichiers, 16 schémas, 143 tables, chargement vérifié sur PostgreSQL 17 + pgvector. Le décompte exact et sa requête vivent dans [`README.md`](README.md) |
| Cadrage et décisions | ✅ Complet — 14 ADR, périmètre du jalon arrêté. Audité le 16/08, corrigé |
| Environnement local | ✅ Monté et vérifié le 16/08 — `ops/docker-compose.dev.yml`, `ops/garage.toml`, `Makefile`, `.gitignore`, `.env.example`. Cinq services démarrés, schéma chargé, `make check` au vert |
| Front | 🟨 **Écran d'acceptation d'une invitation le 20/08 (B7, par anticipation)** — `app/pages/invitation.vue`, 1 fabrique d'API (`composables/api/invitation.ts`), 1 contrat (`AcceptInvitationResult`), 1 écriture simulée et **trois jetons de démonstration**, 2 fichiers de traduction. Elle referme le 404 des liens d'invitation, **sans exiger de session** : le jeton est la preuve d'adresse. Éprouvée au navigateur en français et en anglais sur les cinq issues. **Treizième écran métier le 19/08 : l'accueil public et sa vitrine administrable (A15)** — 4 pages (`app/pages/index.vue`, qui remplace la redirection du 17/08, et `app/pages/admin/vitrine/`), 17 composants (`app/components/home/`, `app/components/admin/showcase/`), 3 utilitaires purs, 3 fichiers de contrats, 3 fichiers de mocks, 2 fabriques d'API, 6 fichiers de traduction. **Le modèle a été complété d'abord** : nouveau schéma `content` (`115_content.sql`), vues `event.v_public_editions` et `programme.v_edition_stats`, rôle `video` ajouté à `media.attachment_role`. **Douzième écran métier le 18/08 : les messages d'incident (A13)** — 3 pages sous `app/pages/admin/incidents/`, 6 composants sous `app/components/admin/incidents/`, 1 utilitaire pur (`utils/incident-list.ts`), 1 fichier de contrats (`types/admin-incidents.ts`), 1 fichier de mocks d'écran (`mocks/admin-incidents.ts`), 1 fabrique d'API, 4 fichiers de traduction, plus le raccourci « Signaler un débordement » dans le panneau de séance du planificateur. **Deux compléments du modèle** : la nature « débordement sur le créneau suivant », qui manquait au vocabulaire, et `live.event_incidents()`, qui rend les cinq états d'un message là où le modèle ne savait lire que les actifs. **Onzième écran métier le 18/08 : les utilisateurs, leurs rôles SCOPÉS et le RGPD (A12)** — 3 pages sous `app/pages/admin/utilisateurs/`, 10 composants sous `app/components/admin/users/`, 2 utilitaires purs (`utils/role-scope.ts`, `utils/user-list.ts`), 1 fichier de contrats (`types/admin-users.ts`), 1 dossier de mocks en quatre fichiers (`mocks/admin-users/`), 2 mocks d'entité (`mocks/platform.ts`, `mocks/privacy.ts`), 1 fabrique d'API, 10 fichiers de traduction. Le RBAC scopé de `030_identity.sql` — la réponse au défaut structurel de la v1, huit rôles globaux dans un ENUM — reçoit enfin sa surface : une pastille ne porte jamais un rôle sans sa PORTÉE. **Deux corrections du modèle** : la révocation d'un rôle ne disait ni qui ni pourquoi, et `identity.role.assign` n'était détenue par aucun rôle. **Dixième écran métier le 18/08 : les organisations et la fusion des doublons (A11)** — 4 pages sous `app/pages/admin/organisations/`, 9 composants sous `app/components/admin/organizations/`, 2 utilitaires purs (`utils/organization-list.ts`, `utils/organization-merge.ts`), 1 fichier de contrats (`types/admin-organizations.ts`), **1 dossier de mocks en cinq fichiers** (`mocks/admin-organizations/`), 1 fabrique d'API (`composables/api/admin-organizations.ts`), 8 fichiers de traduction (4 × 2 locales). `analytics.mv_organization_scorecard`, présente dans le modèle depuis le premier jour et affichée nulle part, devient la liste du back-office ; le décompte de fusion se lit dans `org.organization_references` plutôt que dans une liste écrite à la main. **Une correction du modèle** : le registre ne dédoublonnait pas les domaines de courriel. **Révision du tableau de bord le 18/08, sur demande du commanditaire : ApexCharts remplace les cinq graphiques dessinés à la main, et six indicateurs de tête ouvrent la zone des chiffres** — 1 greffon client (`plugins/apexcharts.client.ts`, import dynamique), 1 composable (`useChartTheme`, qui lit les jetons de design dans le DOM), 1 enveloppe (`UiChart`), 2 composants (`AdminStatCard`, `AdminKeyFigures`), 3 composants réécrits (courbe quotidienne, répartition, entonnoir), et la moyenne mobile du modèle enfin tracée. **Neuvième écran métier le 18/08 : le planificateur de créneaux (A9)** — 1 page `app/pages/admin/programmation.vue`, 5 composants sous `app/components/admin/planner/`, 1 utilitaire pur (`utils/planner.ts`), 1 fichier de contrats (`types/admin-planner.ts`), 1 fichier de mocks (`admin-planner.ts`), 2 fichiers de traduction, 1 fabrique d'API (`composables/api/planner.ts`) ; **vue-cal passe en mode éditable**, salles en colonnes, glisser-déposer et redimensionnement, et `detect_conflicts()` cesse de signaler comme bloquant ce qui n'occupe pas le stand. **Huitième écran métier le 18/08, et le plus dense du jalon : la fiche d'évaluation d'une proposition (A8)** — 1 page `app/pages/admin/propositions/[id].vue`, 10 composants sous `app/components/admin/review/`, 1 utilitaire pur (`utils/review-scoring.ts`, qui rejoue `refresh_proposal_score()` en direct), 1 fichier de contrats (`types/admin-review.ts`), 1 fichier de mocks (`proposal-review.ts`, qui applique le VOILE de l'évaluation en aveugle à la source), 2 fichiers de traduction ; `useApi()` se découpe pour la première fois (`composables/api/proposal-review.ts`) et son périmètre d'administration cesse de tester des noms de rôles. **Septième écran métier le 18/08 : la liste des propositions du back-office (A7)** — 1 page `app/pages/admin/propositions/index.vue`, 4 composants sous `app/components/admin/proposals/`, 2 utilitaires purs (`utils/proposal-list.ts`, `utils/permissions.ts`), 1 fichier de contrats (`types/admin-proposals.ts`), 4 fichiers de mocks (`permissions.ts`, `proposal-workflow.ts`, `proposal-reads.ts`, `admin-proposals.ts`), 2 fichiers de traduction ; `v_proposal_dashboard` gagne treize colonnes et `programme.unread_proposals_for()` apparaît, `UiTable` gagne `hideBelow` et `rowLabelKey`. Socle posé le 16/08 — Nuxt 4.5, TypeScript strict, Tailwind v4, Pinia, i18n fr/en, jetons de design, **trois layouts** (`public`, `admin`, `auth`). **Sixième écran métier le 17/08, et premier du back-office : le tableau de bord (A6)** — 1 page `app/pages/admin/index.vue`, 5 composants sous `app/components/admin/` (sélecteur d'événement, file d'actions, entonnoir, courbe, répartition, santé), 2 fichiers de types (`types/analytics.ts`, `types/admin-dashboard.ts`), 3 fichiers de mocks (`incidents.ts`, `analytics.ts`, `admin-dashboard.ts`), 2 fichiers de traduction ; le store `admin-scope` charge désormais le périmètre, le sélecteur d'événement passe de la navigation latérale à la tête de page et vit dans l'URL, `130_analytics.sql` gagne `mv_daily_registrations` et `080_live.sql` `active_incidents_for_event()`. **Cinquième écran métier le 17/08 : l'espace organisation (A5)** — 2 pages sous `app/pages/organization/`, 7 composants sous `app/components/workspace/`, 1 utilitaire pur (`utils/proposal-timeline.ts`), 2 fichiers de types (`types/engagement.ts`, `types/organization-workspace.ts`), 3 fichiers de mocks (`reminders.ts`, `organization-workspace.ts`, `proposals/history.ts`), 4 fichiers de traduction ; `org.memberships` gagne `invited_by`/`invited_at`, et `UiStatusTimeline` deux corrections. **Quatrième écran métier le 17/08 : le formulaire de soumission (A4)** — 1 page gardée, 11 composants sous `app/components/proposal/`, 1 composable d'enregistrement automatique, 16 fichiers de traduction découpés par étape ; `UiStepper` corrigé (débordement à 375 px, navigation en avant) et `utils/datetime.ts` complété de `instantFromWallClock()`. **Troisième écran métier le 17/08 : la page publique d'une édition (A3)** — 1 page + la redirection d'accueil, 10 composants sous `app/components/event/`, vue-cal introduit en lecture seule, 4 fichiers de traduction, quatre éditions de plus dans les mocks. **Deuxième écran métier le 17/08 : le rattachement à une organisation (A2)** — 1 page, 5 composants, la recherche du modèle rejouée dans les mocks, 4 fichiers de traduction. **Premier écran métier le 17/08 : l'authentification (A1)** — 5 pages, 6 composants, un store de session, les comptes et jetons simulés. **Types du modèle dérivés le 16/08** : 14 fichiers, 62 tables du jalon, 2 vues, 8 retours de fonction, puis alignés le 16/08 sur les deux corrections du modèle (`AdministeredEvents`, `ProposalDashboardRow`). **Données simulées le 16/08** : 22 fichiers sous `app/mocks/`, 13 organisations dont un doublon, 25 personnes, 40 propositions, 32 revues, 30 sessions, 60 inscriptions ; couche d'accès `useApi()` réécrite. **Bibliothèque d'interface le 16/08** : 38 composants sous `app/components/ui/`, les 27 du prompt plus 11 briques ; guide de style vivant à `/style-guide`, 7 sections ancrées, rendu sur données réelles. `make check-front` au vert |
| Outillage de la phase B | ✅ Installé le 20/08 — GitHub Spec Kit 0.16.5, intégration `claude`, scripts `sh`. `.specify/` (gabarits, scripts, workflow, constitution) et `.claude/skills/speckit-*` (dix compétences). **Constitution ratifiée le 20/08 en 1.0.0** — dix principes, [écarts et vérifications](progression/ecrans/b0-constitution.md) |
| API | 🏁 **B4 LIVRÉ le 21/08 (T001–T160)** — `backend/crates/modules/programme` : **les huit histoires, 37 routes sur 37**, montées, frappées sur la vraie application et documentées. Voile de l'aveugle qui n'exécute pas la requête plutôt que de filtrer son résultat, consolidation des notes appelée dans la transaction et agrégats relus, espace organisation prouvé par balayage de la charge utile entière, séance strictement inchangée quand le dossier retenu est corrigé, reprise v1 sans un seul événement émis. **Six écarts nouveaux, n° 105 à 110**, dont trois corrigent du code déjà livré. 524 tests, `cargo fmt`, `clippy -D warnings` et `cargo test --workspace` au vert, aucune arête vers `identity`, `org` ni `event`, aucun travail différé. Prochain pas : **B5, les séances**. Avant cela, le même jour : 🟨 **B4 — PHASE 5 LIVRÉE le 21/08 (T077–T093)** — `backend/crates/modules/programme` : **US3 livrée**, le comité pilote sa liste. Tout l'écran en une réponse, sept facettes comptées sur les mêmes lignes, non-lus par la fonction du modèle, périmètre vérifié deux fois et **vide → refus explicite**, six identifiants forgés au même refus qu'un inexistant, affectation groupée rendant compte de chaque dossier, **douze événements pour douze dossiers**. **16 routes montées sur 37**, dont `GET /proposals/{id}` — le découpage des chemins littéraux tient. **Deux écarts nouveaux, n° 104 et n° 105.** 91 tests du module, 491 au total, `cargo fmt`, `clippy -D warnings` et `cargo test --workspace` au vert. Prochain pas : **phase 6, T094–T115**. Avant cela, le même jour : 🏁 **B4 — JALON DU PROMPT ATTEINT le 21/08 (T001–T076)** — `backend/crates/modules/programme` : **US1 et US2 livrées**, l'appel de la COP31 reçoit des dossiers. Transitions offertes en une requête et trois menus selon le lecteur, une seule ligne d'outbox par transition, deux refus du garde distingués par le moment, action groupée évaluée dossier par dossier. **10 routes montées sur 37**, deux écarts nouveaux (n° 102 et n° 103). 81 tests du module, 481 au total, `make check-back` au vert. Prochain pas : **phase 5, T077–T092**. Avant cela, le même jour : 🟨 **B4 — PHASE 3 LIVRÉE le 21/08 (T031–T062)** — `backend/crates/modules/programme` : **US1 livrée** (brouillon, enregistrement automatique, dérivation d'adresse d'URL avec réessai suffixé, heure murale convertie en base, trois refus de recevabilité en 200 portant leur valeur, dépôt), **6 routes montées sur 37**, deux écarts nouveaux (n° 102 et n° 103). 69 tests du module, 469 au total, `make check-back` au vert. Prochain pas : **phase 4, T063–T077**. Avant cela, le même jour : 🟨 **B4 — PHASES 1 ET 2 LIVRÉES le 21/08 (T001–T030)** — `backend/crates/modules/programme` : crate créé sans aucune arête vers un autre module, fondations posées (six codes d'erreur, trois contrats d'événements, dix règles pures du domaine, lectures hors schéma réunies, deux écritures hors schéma isolées, ascendance sur trois niveaux, montage, harnais de test). **`/organizations` refactorisé — du code livré modifié**, les vingt et une routes de B2 restent atteignables. 43 tests du module, 440 au total, `make check-back` au vert. Prochain pas : **phase 3, T031–T053**. Avant cela, le 20/08 : 🟨 **B4 SPÉCIFIÉ le 20/08** — [`specs/004-propositions/`](../specs/004-propositions/spec.md) : 8 histoires, 17 cas limites, **103 exigences**, 27 critères, **aucune modification du SQL** — `070_programme_proposals.sql` relu en entier —, puis **planifié le même jour** : 24 décisions, 37 routes, aucun travail différé, une entorse justifiée. **Neuf écarts, n° 93 à 101.** Découpé le même jour : **160 tâches, 46 tests, quatre jalons**. Prochain pas : `/speckit-implement`. Avant cela, le même jour : **B3 LIVRÉ le 20/08 (T001–T129)** — `backend/crates/modules/event` : les huit histoires, 37 routes, 6 événements de domaine, 3 codes d'erreur, 1 travail différé. Le jalon que le prompt demandait — ouvrir l'appel à propositions de la COP31 — est atteint. Avant cela — PHASE 4 LIVRÉE le 20/08 (T042–T053)** — `backend/crates/modules/event` : **US2 livrée** (liste bornée par le périmètre avec ses facettes, détail des six onglets en une réponse, sélecteur du back-office, refus sur URL forgée), **6 routes montées sur 37**, 69 tests, `cargo test --workspace` au vert. **T049 reste ouverte** : les six routes d'onglet qu'elle vise naissent en phases 5, 7 et 9. Prochain pas : **phase 5, T054–T068**. Avant cela, le même jour : 🟨 **B3 — PHASES 1 À 3 LIVRÉES le 20/08 (T001–T041)** — `backend/crates/modules/event` : fondations posées, **US1 livrée** (création et modification d'une édition, règle du sigle avec valeur proposée, six contraintes traduites, calendrier dans le fuseau de l'édition), 3 routes montées sur 37, 49 tests, `make check-back` au vert. Prochain pas : **phase 4, T042–T053**. Avant cela, le même jour : 🟨 **B3 — PHASES 1 ET 2 LIVRÉES le 20/08 (T001–T024)** — `backend/crates/modules/event` : crate créé, fondations posées (erreurs, traduction PostgreSQL, contrats d'événements, ascendance, montage, travail différé, harnais de test), `make check-back` au vert, aucune arête entre crates de module. Prochain pas : **phase 3, T025–T041**. Avant cela, le même jour : 🟨 **B3 SPÉCIFIÉ, PLANIFIÉ ET DÉCOUPÉ le 20/08** — [`specs/003-evenements/`](../specs/003-evenements/tasks.md) : **129 tâches en 11 phases, dont 31 tests**, quatre jalons, et le jalon qui ouvre l'appel de la COP31 à **T001–T068**. Prochain pas : `/speckit-implement`. Avant cela, le même jour : [`specs/003-evenements/`](../specs/003-evenements/plan.md) : **20 décisions techniques, 37 routes, 6 événements, 3 codes d'erreur, 1 travail différé**, contrôle constitutionnel passé deux fois avec **une entorse justifiée**, et **aucun SQL composé dynamiquement**. **Six écarts, n° 87 à 92**, dont **n° 91 — supprimer un critère d'évaluation détruit les notes en silence** (`ON DELETE CASCADE`), que le service refuse désormais. Prochain pas : `/speckit-tasks`. Avant cela, le même jour : [`specs/003-evenements/`](../specs/003-evenements/spec.md) : **8 histoires, 16 cas limites, 99 exigences, 25 critères, aucune modification du SQL.** Quatre écarts, **n° 87 à 90**, dont trois qui engagent le plan : aucun déclencheur du module n'émet d'événement de domaine (le piège n° 1 de B1 et B2 n'existe pas ici) ; le rôle de programmation ne détient aucune permission du module ; publier la programmation traverse deux schémas et passe donc par **l'outbox**, la publication étant gardée par la permission de planifier. **L'écart n° 9 est traité dans le service, pas dans la base** — sigle obligatoire pour une édition à pavillon, avec valeur par défaut proposée. Deux écarts d'A3 refermés (n° 25 et 26). [Écarts et vérifications](progression/ecrans/b3-evenements.md). Prochain pas : `/speckit-plan`. Avant cela, le même jour : ✅ **B2 LIVRÉ** — `backend/crates/modules/org` : **21 routes, 6 travaux différés, 6 événements, 11 codes d'erreur, 30 fichiers de test** et **246 tests verts**. Clippy sans avertissement, aucun fichier au-dessus de 1000 lignes, aucune arête entre deux crates de module. **Recherche à 55 ms au 95ᵉ centile sur 5 001 fiches** (cible 150), **sans modifier une ligne de SQL**. Le service de jetons est remonté dans le noyau **à comportement constant** — les quatre tests de B1 restent verts sans être réécrits —, et le défaut n° 81 est corrigé : une personne créée par invitation peut enfin se créer un compte. **Chaîne du courriel éprouvée jusqu'à Mailpit au navigateur**, acceptation sans session comprise. Reste `make check` en entier, qui détruit la base. [Écarts et vérifications](progression/ecrans/b2-organisations.md). Avant cela, le même jour : 🟨 **B2 spécifié, planifié et découpé** — [`specs/002-organisations/`](../specs/002-organisations/spec.md) : 84 exigences, 19 décisions techniques, 21 routes, aucune modification du SQL, dix-sept écarts (n° 70 à 86). Contrôle constitutionnel passé deux fois, une entorse justifiée. **129 tâches en 11 phases, dont 29 tests.** Prochain pas : `/speckit-implement`. [Écarts et vérifications](progression/ecrans/b2-organisations.md). Avant cela, le même jour : ✅ **B1 TERMINÉ — les 126 tâches, `make check` au vert en entier** : **le jalon qui ouvre l'appel à propositions de la COP31 est complet.** **141 tests**, **143 requêtes préparées**, zéro avertissement, plus gros fichier à **588 lignes**, aucune arête entre deux crates de module. La chaîne différée est **éprouvée** — purge récurrente qui se replanifie elle-même, `GET /api/health` sur les quatorze indicateurs du modèle, `GET /api/ready` qui sollicite le pool —, la file RGPD exige la portée **globale** ou 403, l'effacement conserve les compteurs de participation, et la documentation OpenAPI est **engendrée** : 26 chemins, 20 codes d'erreur. **Les en-têtes CORS sont posés, avancés de B7 : les huit histoires sont éprouvées depuis un vrai navigateur.** Une dette reste pour **B7** : le mock du site range la fenêtre de validité invalide sous `scope_not_allowed`. Avant cela, le même jour, les **phases 1 à 8** : on s'inscrit, on reçoit son courriel, on se connecte, **on récupère un compte perdu**, la session tourne et se coupe, **un rôle se confie et se retire sur sa portée**, et **le back-office se partage sans se dupliquer**. 99 tâches sur 126 (T001–T099) — **le jalon qui débloque B2 à B6 est fermé** —, **110 tests**. L'attribution se vérifie sur la portée **visée** et le retrait sur celle de **l'attribution visée** ; le refus du trigger ressort **mot pour mot** ; une attribution retirée n'est **jamais supprimée**. La réinitialisation tient le cycle entier : jeton **revérifié à l'envoi**, mot de passe refusé en **422** quand un jeton refusé sort en **200**, et un changement abouti qui lève le verrou et coupe toutes les sessions. La chaîne du courriel — API, file de travaux, worker, route privée du site, SMTP — est éprouvée jusqu'à Mailpit ; la réponse d'inscription est invariable en message **et en temps** ; le jeton en clair ne vit que dans la charge utile du travail, vidée dès l'envoi. La règle métier n° 8 est tenue par l'API : huit routes de lecture gardées, les trois cas du périmètre distincts jusqu'au bout, et une URL forgée qui ne mène nulle part. Avant cela, le même jour : 58 tâches sur 126 (T001–T058), `make check-back` au vert, **52 tests** dont 35 sur base réelle et 10 qui traversent HTTP. Les six issues de connexion, la rotation du jeton, la détection de rejeu, et la discrétion tenue **en message et en temps** — écart mesuré 1,5 %. **Audité et corrigé le même jour** : 45 constats confirmés, tous traités, dont un verrou échu qui ne rendait qu'un essai au compte, deux courses de concurrence, un corps mal formé sans code stable, et l'absence de tout test traversant HTTP. Avant cela, **phases 1 et 2 implémentées PUIS auditées le 20/08** : `backend/` existe, cinq crates, 35 tâches sur 126, `make check-back` au vert, 3 tests sur base réelle. **53 constats d'audit confirmés, tous traités.** Le socle est éprouvé — contexte d'audit, intergiciels, relais d'outbox et garde d'idempotence. Reste les phases 7 à 11, à commencer par US5 (retrouver l'accès à un compte). Avant cela, **B1 spécifié, planifié et découpé le 20/08** — `specs/001-socle-identite/` : `spec.md` (8 histoires, 64 exigences, 13 critères), `plan.md`, `research.md` (16 décisions), `data-model.md`, `contracts/`, `quickstart.md`, **`tasks.md` (126 tâches, 23 tests)**. Contrôle constitutionnel passé deux fois, aucune violation. [Écarts et vérifications](progression/ecrans/b1-socle-identite.md). Prochain pas : **phase 9 — US7, les effets différés partent une fois et une seule (T100–T107)** : la purge récurrente des jetons, la remontée des travaux en échec sur la route de santé, et les garanties de la chaîne différée éprouvées plutôt que supposées |

L'environnement local, ce qui y a été vérifié et les écarts relevés : [`progression/environnement-local.md`](progression/environnement-local.md).

---

## Front — suivi des prompts

Un écran = un fichier. Il porte ce qui a été livré, les écarts relevés entre le modèle et l'interface, et ce qui a été vérifié. Chaque écart est soit un défaut du modèle, soit un défaut de l'interface — il se tranche, il ne se contourne pas par une conversion. Les prompts correspondants sont dans [PROMPTS_DEVELOPPEMENT.md](PROMPTS_DEVELOPPEMENT.md).

| Prompt | Écran | État | Détail |
|--------|-------|------|--------|
| A0.1 | Socle Nuxt, Tailwind, i18n, jetons | ✅ 16/08 | [écarts et vérifications](progression/ecrans/a0.1-socle.md) |
| A0.2 | Types TypeScript dérivés du SQL | ✅ 16/08 | [écarts et vérifications](progression/ecrans/a0.2-types.md) |
| A0.3 | Données simulées | ✅ 16/08 | [écarts et vérifications](progression/ecrans/a0.3-donnees-simulees.md) |
| A0.4 | Composants d'interface + page de guide de style | ✅ 16/08 | [écarts et vérifications](progression/ecrans/a0.4-composants.md) |
| A1 | Authentification | ✅ 17/08 | [écarts et vérifications](progression/ecrans/a1-authentification.md) |
| A2 | Rattachement à une organisation | ✅ 17/08 | [écarts et vérifications](progression/ecrans/a2-organisation.md) |
| A3 | Page publique de l'événement | ✅ 17/08 · refondue 19/08 | [écarts et vérifications](progression/ecrans/a3-evenement-public.md) |
| A4 | Formulaire de soumission | ✅ 17/08 | [écarts et vérifications](progression/ecrans/a4-soumission.md) |
| A5 | Espace organisation | ✅ 17/08 | [écarts et vérifications](progression/ecrans/a5-espace-organisation.md) |
| A6 | Tableau de bord back-office | ✅ 17/08 | [écarts et vérifications](progression/ecrans/a6-tableau-de-bord.md) |
| A7 | Liste des propositions | ✅ 18/08 | [écarts et vérifications](progression/ecrans/a7-propositions.md) |
| A8 | Fiche d'évaluation | ✅ 18/08 | [écarts et vérifications](progression/ecrans/a8-evaluation.md) |
| A9 | Planificateur de créneaux | ✅ 18/08 | [écarts et vérifications](progression/ecrans/a9-planificateur.md) |
| A10 | Gestion des événements | ✅ 18/08 | [écarts et vérifications](progression/ecrans/a10-evenements.md) |
| A11 | Organisations et fusion | ✅ 18/08 | [écarts et vérifications](progression/ecrans/a11-organisations-fusion.md) |
| A12 | Utilisateurs et rôles | ✅ 18/08 | [écarts et vérifications](progression/ecrans/a12-utilisateurs-roles.md) |
| A13 | Messages d'incident | ✅ 18/08 | [écarts et vérifications](progression/ecrans/a13-incidents.md) |
| A14 | Page « En cours de maintenance » | ✅ 18/08 | [écarts et vérifications](progression/ecrans/a14-maintenance.md) |
| A15 | Accueil public et vitrine administrable | ✅ 19/08 | [écarts et vérifications](progression/ecrans/a15-accueil.md) |

Les pièges transverses, ceux qui ne tiennent à aucun écran : [`progression/pieges.md`](progression/pieges.md).

---

## API — suivi des prompts

Le tableau et les obligations d'API relevées en écrivant les écrans : [`progression/api.md`](progression/api.md). **B0 est fait le 20/08** — la constitution est ratifiée en 1.0.0. **B1 est spécifié le même jour** — [`specs/001-socle-identite/`](../specs/001-socle-identite/spec.md), ses [écarts et vérifications](progression/ecrans/b1-socle-identite.md). **Le plan et le découpage sont écrits le même jour** — 16 décisions, contrôle constitutionnel sans violation, 126 tâches. **B1 EST TERMINÉ le même jour — les 126 tâches, et `make check` passe en entier depuis la racine avec 141 tests et zéro avertissement.** **B2 est spécifié, planifié ET découpé le même jour** — [`specs/002-organisations/`](../specs/002-organisations/spec.md), 84 exigences et 19 décisions techniques, [écarts et vérifications](progression/ecrans/b2-organisations.md) : deux lectures pour l'écart n° 23, l'ordre des écritures de la fusion corrigé, le service de jetons remonté dans le noyau, dix-sept écarts n° 70 à 86, et **129 tâches en 11 phases**. Avant cela, **les phases 1 à 8** : `backend/` et ses cinq crates existent, on s'inscrit, on reçoit son courriel, on se connecte, **on récupère un compte perdu**, la session tourne et se coupe, **un rôle se confie et se retire sur sa portée**, et le périmètre d'administration est tenu par l'API. `make check-back` était au vert avec **110 tests**, et **le jalon T001–T067, celui qui débloque B2 à B6, est fermé**. **B2 EST LIVRÉ le même jour** — 21 routes, 29 fichiers de test, la recherche à 55 ms sur 5 001 fiches, et le défaut n° 1 de la v1 pris par les quatre bouts. **B3 est spécifié le même jour** — [`specs/003-evenements/`](../specs/003-evenements/spec.md), 99 exigences, aucune modification du SQL, **et planifié le même jour** — 20 décisions, 37 routes, une entorse justifiée, six écarts n° 87 à 92, et l'écart n° 9 traité dans le service. **Et découpé le même jour** — 129 tâches en 11 phases, dont 31 tests. **B3 EST LIVRÉ le 20/08** — 37 routes, 6 événements de domaine, 32 fichiers de test, et l'appel à propositions de la COP31 peut être ouvert. **B4 est spécifié le même jour** — [`specs/004-propositions/`](../specs/004-propositions/spec.md), 103 exigences et aucune modification du SQL, [écarts et vérifications](progression/ecrans/b4-propositions.md) : huit écarts **n° 93 à 100**, dont le déclencheur d'état qui **émet déjà** son événement de domaine — l'inverse de B3, et le retour du piège n° 1 de B1 et B2 —, l'adresse d'URL d'un dossier **que le formulaire ne porte pas** alors que la base l'exige et la rend unique par édition, et **l'absence de tout appel à la consolidation des notes**, qui fige le classement du comité sans erreur. Les douze exigences du prompt sont traitées ; deux points restent au plan — la frontière de la création d'une personne inconnue, et le découpage de la fiche d'évaluation, qui compose onze tables. **Le plan est écrit le même jour** — 24 décisions, 37 routes, une entorse et deux écritures hors schéma justifiées, la création d'une personne inconnue tranchée en dérogation bornée, et le préfixe `/organizations` refactorisé avant que les trois routes muettes de B2 ne se reproduisent. **Et découpé le même jour** — 160 tâches en 11 phases, le jalon qui fait recevoir les dossiers de la COP31 à T001–T076, et une question qui attend toujours le commanditaire : qui, dans une organisation, peut retirer un dossier déposé par une collègue. Le prochain pas est **`/speckit-implement`**.

---

## Où trouver le reste

| Question | Fichier |
|----------|---------|
| Qu'a fait la session d'hier ? | [`progression/journal/`](progression/journal/) — un fichier par jour, le plus récent d'abord |
| Pourquoi cette décision d'interface, de nommage, de découpage ? | [`progression/decisions/`](progression/decisions/) — un fichier par jour |
| Quelles tables ont bougé, et pourquoi ? | [`progression/modele.md`](progression/modele.md) |
| Quels écarts sur l'écran que je reprends ? | [`progression/ecrans/`](progression/ecrans/) — le fichier de son prompt |
| Qu'est-ce qui attend une réponse du commanditaire ? | [`progression/points-bloques.md`](progression/points-bloques.md) |
| Qu'est-ce qui a déjà coûté une erreur ? | [`progression/pieges.md`](progression/pieges.md) |

### Journal

| Jour | Fichier |
|------|---------|
| 21 août 2026 | [`journal/2026-08-21.md`](progression/journal/2026-08-21.md) |
| 20 août 2026 | [`journal/2026-08-20.md`](progression/journal/2026-08-20.md) |
| 19 août 2026 | [`journal/2026-08-19.md`](progression/journal/2026-08-19.md) |
| 18 août 2026 | [`journal/2026-08-18.md`](progression/journal/2026-08-18.md) |
| 17 août 2026 | [`journal/2026-08-17.md`](progression/journal/2026-08-17.md) |
| 16 août 2026 | [`journal/2026-08-16.md`](progression/journal/2026-08-16.md) |

---

## Points bloqués

Trois questions au commanditaire attendent depuis le 16/08, et quelques points restent en suspens : [`progression/points-bloques.md`](progression/points-bloques.md).

---

## Mettre à jour ce dispositif en fin de session

1. **Le journal du jour** — `progression/journal/<date>.md`, une ligne : ce qui a été fait, ce qui bloque, ce qui vient. Le fichier du jour se crée s'il n'existe pas, et se déclare dans le tableau ci-dessus.
2. **Le fichier de l'écran travaillé** — `progression/ecrans/<prompt>.md` : ce qui a été livré, les écarts relevés, ce qui a été vérifié et comment. Un nouveau prompt ouvre un nouveau fichier, déclaré dans le suivi ci-dessus.
3. **Les décisions du jour** — `progression/decisions/<date>.md`, si quelque chose a été tranché.
4. **`progression/modele.md`** — si un fichier de `docs/database/` a changé.
5. **Ici** — l'état général, la ligne de suivi du prompt, et la date de dernière mise à jour en tête.

Rien d'autre n'a besoin d'être touché. **Ne pas regrossir ce fichier** : il se lit en entier à chaque session, c'est ce qui lui donne sa valeur.
