# Modifications du modèle de données

> Extrait de la [progression](../PROGRESSION.md). Le modèle se corrige avant l'écran, jamais l'inverse.

Toute modification d'un fichier de `docs/database/` se note ici. C'est ce qui permet de savoir, plus tard, pourquoi le schéma diffère de ce que décrit le cadrage.

| Date | Fichier | Changement | Motif |
|------|---------|------------|-------|
| 08-21 | `110_engagement.sql` | **Fonction de lecture `engagement.session_reminder_schedule(uuid)`** ajoutée au § 6. `STABLE`, rend une ligne par (décalage, canal) : décalage **en minutes**, canal, instant d'envoi, état consolidé, **nombre** de destinataires, motif dominant, dernier envoi — du décalage le plus lointain au plus proche. **Ni table, ni colonne, ni type** : la surface du modèle ne change pas. | L'agrégat des rappels a **deux** lecteurs — la lecture par séance du module Engagement et la composition de l'espace organisation servie par `programme`. Deux agrégations écrites séparément divergeraient au premier ajustement de la règle de consolidation, et **la divergence serait silencieuse** : un nombre de destinataires faux ressemble à un nombre juste. C'est le précédent exact de `media.attached_image()`, que trois modules appellent, et dont l'en-tête du fichier média porte mot pour mot ce raisonnement. **Ce qu'elle ne rend jamais** : ni identifiant de personne, ni nom, ni adresse — la garantie est portée par la **signature** de la fonction, pas par la discipline de ses appelants. **Impose `down -v`** : le schéma n'est chargé qu'au premier démarrage du conteneur, et sans destruction du volume la compilation échoue sur une fonction que SQLx ne trouve pas, avec un message sans rapport avec la cause. |
| 08-19 | `050_media.sql` | **Valeur `thumbnail` ajoutée à l'ENUM `media.attachment_role`** (entre `video` et `attachment`), avec son commentaire | Une édition porte trois déclinaisons ; l'énumération n'avait aucun rôle pour la vignette carrée. `avatar` et `logo` désignent une personne et une organisation, les détourner aurait rendu illisible le seul endroit qui dit à quoi sert un fichier. **UN RÔLE DIT UN USAGE, JAMAIS UNE FORME** : `square` aurait figé le ratio dans le vocabulaire, et le jour où un réseau passe au 4:5 il aurait fallu un rôle de plus pour le même usage. |
| 08-19 | `050_media.sql` | **Deux colonnes sur `media.attachable_roles` : `expected_aspect_ratio` et `aspect_ratio_tolerance`**, plus le contrôle correspondant dans `tg_validate_attachment()` | La forme exigée se déclare là où se déclarent déjà le type MIME et le poids — sinon un écran vérifierait seul et finirait par accepter ce que l'API refuse. Tolérance de 2 % : un 1920×1080 passe, un 4:3 présenté comme un 16:9 est refusé. **Le contrôle ne s'applique qu'aux objets MESURÉS** — un PDF n'a pas de ratio, et une image dont le relevé de dimensions a échoué passe : refuser là transformerait une panne de traitement en refus de téléversement. Ajouter un ratio 4:5 reste une INSERTION, jamais une migration. |
| 08-19 | `050_media.sql` | **`event.events` déclare trois rôles au lieu d'un** : `banner` 32:9 (3.5556), `cover` 16:9 (1.7778), `thumbnail` 1:1 — `gallery` inchangé | Un recadrage automatique décapite les intervenants d'une photographie de conférence ; les trois sont donc téléversés à la main. Facultatifs et indépendants : aucun ne se déduit d'un autre, déduire c'est recadrer. Les autres rôles gardent `expected_aspect_ratio` NULL — le périmètre s'arrête à l'édition. |
| 08-19 | `060_events.sql` | **`event.v_public_editions` expose `cover` et `thumbnail`** en plus de `banner` | La vue ne choisit pas pour l'écran : seul celui-ci sait de quelle largeur il dispose, et un repli décidé en base serait le même pour une fiche pleine largeur et pour une vignette de 48 px. L'accueil prend le 16:9, la fiche du back-office le 32:9. |
| 08-19 | **`115_content.sql` — NOUVEAU FICHIER, NOUVEAU SCHÉMA `content`** | `content.highlights` (contenus mis en avant : emplacement, nature en taxonomie, ordre, fenêtre de diffusion, attribution, rattachement édition/séance/lien), la vue `content.v_showcase`, les trois rôles média (`banner`, `video`, `cover`), la permission `content.highlight.manage`, la taxonomie `highlight_nature` (6 termes) et le module enregistré dans `platform.modules` | **La page d'accueil demandée par le commanditaire n'avait RIEN derrière elle** : aucune table de témoignage, de mise en avant ni de contenu éditorial dans les 18 fichiers. En v1, ces contenus vivaient dans deux tables Supabase divergentes — `user_testimonials` et `video_testimonials`, qui ne partageaient ni leur colonne de contexte, ni leur état de modération, ni leur titre, le frontend les fusionnant à la main avec un champ `type` inventé côté client — plus une table `innovations_practices`, plus **cinq composants Vue** où les annonces étaient écrites en dur avec leurs dates, leurs images et leurs identifiants. **Une seule table** ici : la nature du contenu est un vocabulaire OUVERT et vit en taxonomie, pas dans un ENUM ni dans un fichier i18n (défaut n° 1 de la v1, la v1 affichait « Innovation/Bonne pratique » en français figé dans le composant). **Ni `publication` ni `engagement` ne convenaient** : le premier porte un cycle éditorial, un quota et des révisions, et son drapeau est éteint ; le second porte la relation aux personnes. Le module est délibérément petit et ne dépend d'aucun autre pour fonctionner — ses cinq liens sont facultatifs et tous nommés `xmod_fk_` |
| 08-19 | `115_content.sql` | **`sort_order` et la fenêtre `starts_at`/`ends_at`** portés par la base, plus le trigger `tg_highlight_normalize` (dérive l'édition depuis la séance, refuse la contradiction, date la première publication) | **Les deux défauts de la v1 que la table corrige nommément.** (1) *L'ordre* : aucune colonne ne le portait, le carrousel suivait `created_at DESC` et l'IFDD ne décidait pas ce qui passe en premier. (2) *La fenêtre* : les cinq widgets d'annonce apparaissaient et disparaissaient selon des dates comparées **en JavaScript, dans le composant** — une annonce périmée survivait partout où quelqu'un avait oublié la comparaison. Le filtre temporel est maintenant dans `v_showcase` : ce que la vue rend est ce qui doit s'afficher aujourd'hui. Le trigger sur `session_id`/`event_id` n'est pas décoratif — une diapositive classée sous une édition alors qu'elle pointe une séance d'une autre **échapperait au périmètre d'administration des deux** (règle métier n° 8) |
| 08-19 | `050_media.sql` | **Valeur `video` ajoutée à l'ENUM `media.attachment_role`** (entre `avatar` et `attachment`), avec neuf lignes de commentaire | L'énumération ne portait que des rôles d'image et de document — `cover`, `banner`, `logo`, `gallery`, `document`, `avatar`, `attachment` — alors que la plateforme **stocke déjà des enregistrements de séance** (`live.streams.recording_asset_id`) et fait maintenant défiler des fonds vidéo. Le détourner sur `attachment`, le fourre-tout, aurait rendu illisible le seul endroit du modèle qui dit à quoi sert un fichier ; et `allowed_mime_prefixes` n'aurait pas suffi, la clé de `attachable_roles` étant `(schéma, table, rôle)` — un seul `attachment` par table |
| 08-19 | `060_events.sql` | **Nouvelle vue `event.v_public_editions`** (§ 9) : série, pays, bannière, `temporal_state` calculé, et l'appel à propositions avec `call_is_open` et son **échéance effective** (prolongation comprise) | **Aucune vue n'existait dans le schéma `event`**, et l'historique des éditions est un écran à lui seul : l'accueil le déroule en frise, la programmation s'en sert de sélecteur. Chaque écran recomposait la même jointure et calculait l'état temporel à sa façon. **La v1 en est morte** : son accueil filtrait sur `upcoming|ongoing` avant de trier sur `completed`, si bien qu'aucun événement passé ne s'y affichait jamais. Le critère de publicité est celui du modèle — ni brouillon ni annulée — et non `programme_published_at`, qui ne dit que la visibilité du PROGRAMME. `temporal_state` reprend mot pour mot la sémantique de `v_public_schedule` : deux écrans qui parlent du même temps doivent employer le même vocabulaire |
| 08-19 | `075_programme_sessions.sql` | **Nouvelle vue `programme.v_edition_stats`** : nombre d'activités publiées, activités diffusées, organisations distinctes, bornes réelles du programme, par édition | Le compte d'activités appartenait naturellement à `v_public_editions` — **et il l'a d'abord été, à tort**. `060_events.sql` se charge AVANT `075` : la vue ne se créait pas. Ce n'est pas un contretemps mais un signal d'architecture, et la correction va dans le bon sens : `programme` connaît son édition, jamais l'inverse. Les deux vues se joignent sur `event_id`, l'API compose, et l'écran reste servi en une requête. **Une édition sans programme publié n'a aucune ligne** : l'absence vaut zéro, le front doit le traiter |
| 08-18 | `900_seed.sql` | **Drapeau `directory.enabled` ajouté** (§ 2) — « Annuaire des organisations et des personnes, et profils publics — l'espace Communauté », éteint comme ses cinq voisins | **L'annuaire était le seul des six modules fermés sans drapeau de module.** Le prompt A14 fait servir la page de maintenance PAR LE ROUTAGE, à partir de `<module>.enabled` : sans drapeau, l'espace Communauté n'aurait pu être fermé qu'en écrivant la règle dans la page — exactement ce que le prompt interdit. `publications`, `negotiation`, `training`, `messaging` et `tools` étaient déjà semés (depuis A10) ; `CLAUDE.md` affirmait le contraire pour formations et messagerie, et a été corrigé. **Le nom retenu est `directory` et non `community`** : la barre de navigation parle de Communauté parce que c'est ce qu'un visiteur y cherche, mais le modèle parle d'annuaire (`identity.people.is_directory_visible`, les fiches d'organisation) — un seul espace, deux noms, et le drapeau porte celui du modèle |
| 08-18 | `080_live.sql` | **Nouvelle fonction `live.event_incidents(event_id, at)`** (§ 5) : tous les incidents d'une édition, toutes portées et **tous états** confondus — `active`, `scheduled`, `draft`, `expired`, `unpublished` —, avec la cible résolue (`target_label`), l'auteur de la publication (`published_by_name`) et la trace de dépublication. **`live.active_incidents_for_event()` est réécrite au-dessus d'elle** : même signature, même contrat de colonnes, un simple `WHERE state = 'active'` | **L'écran A13 doit montrer ce qui NE parle PAS.** Un brouillon en attente de décision, un message programmé, un bandeau retiré à la main : la fonction existante les filtrait tous. La recomposer côté API aurait dupliqué le balayage de portée — journées, séances, organisations qui y animent — que l'en-tête de `080_live.sql` avertit précisément de ne pas dupliquer, et l'état des quatre conditions cumulées aurait fini par différer d'un écran à l'autre. C'est ce que faisait la v1 avec son booléen `is_active` basculé à la main |
| 08-18 | `080_live.sql` | **Terme `overrun` ajouté à la taxonomie `incident_kind`** (§ 7, `sort_order` 35) : « Débordement sur le créneau suivant » / « Overrun into the next slot », avec quatre lignes de commentaire | **Le cas que le commanditaire nomme en premier n'existait pas dans le vocabulaire.** Un débordement n'est ni un `delay` — il en cause un, ailleurs — ni un `schedule_change`, qui suppose une décision prise : c'est un fait constaté en direct. Le ranger sous l'un des deux aurait rendu tout historique d'incidents inexploitable, et le vocabulaire est OUVERT précisément pour ça |
| 08-18 | `030_identity.sql` | `identity.role_assignments` gagne **`revoked_by`** (FK vers `people`, `ON DELETE SET NULL`) et **`revoked_reason`**, plus la contrainte **`ck_role_assignment_revocation`** — ni l'un ni l'autre tant que `revoked_at` est nul — et deux `COMMENT ON COLUMN` qui séparent le motif de l'octroi de celui du retrait | **Le prompt A12 demande « motif, historique des attributions ET DES RÉVOCATIONS ».** La table portait `note` — le motif de l'OCTROI — et `revoked_at`, une date nue. Qui avait retiré le rôle ne se retrouvait que dans `platform.audit_log`, et POURQUOI, nulle part : c'est pourtant la question qu'on pose six mois plus tard. La contrainte n'est pas décorative — une attribution vivante portant « fin de mission » se lirait comme révoquée dans toute liste qui ne teste pas la date |
| 08-18 | `030_identity.sql` | **`identity.role.assign` est accordée au rôle `admin`** (une ligne de `role_permissions`, précédée de dix lignes de commentaire) | **La permission existait dans le catalogue depuis le premier jour et AUCUNE ligne ne l'accordait à personne** : seul `super_admin` la détenait, par l'effet du trigger `tg_permissions_grant_super_admin`. Le back-office des rôles n'aurait donc été utilisable que par le compte technique pivot, et confier un webinaire à son responsable aurait demandé de sortir de l'application — exactement ce que la v2 corrige. **Aucune élévation** : la portée de l'attribution suit celle de l'administrateur, et `super_admin` n'admet que la portée globale (`allowed_scopes`). Éprouvé sur la base rechargée, voir les vérifications plus bas |
| 08-18 | `040_organizations.sql` | Le registre `org.organization_references` déclare `organization_domains` avec **`dedupe_on = '{domain}'`** au lieu de `'{}'` ; commentaire de six lignes à l'appui | **Trouvé en éprouvant l'écran de fusion, pas en relisant le SQL.** Deux fiches en doublon déclarent presque toujours LE MÊME domaine — c'est même le signal qui les a fait remonter (§ 3). Sans dédoublonnage, `merge_organizations()` basculait la ligne source telle quelle et la fiche absorbante se retrouvait avec deux lignes pour `osed-sahel.org`, l'une vérifiée et l'autre non. Aucun index ne l'interdit : `ux_organization_domains_verified` ne porte que sur les domaines VÉRIFIÉS. La fusion fabriquait donc un doublon interne à l'instant où elle en corrigeait un autre. Le décompte de l'écran est passé de « 1 domaine transféré » à « 1 supprimé, déjà présent » — et c'est bien la seconde phrase qui est vraie |
| 08-18 | `060_events.sql` | `event.events` gagne **`latitude` et `longitude`** (`numeric(9,6)`, bornées par `CHECK`) et la contrainte **`ck_events_coordinates`** — les deux ensemble ou aucun — plus un `COMMENT ON COLUMN` | **Retour du commanditaire sur A10 : « en plus de l'adresse, ajouter les coordonnées GPS, facultatif ».** Rien ne les portait. Deux `numeric` plutôt qu'un type géométrique : PostGIS n'est pas une dépendance du projet et rien ne demande de calcul spatial — seulement d'ouvrir un plan à la bonne épingle. Une adresse de parc des expositions ne se géocode pas : celle du Hangar de Belém place un marqueur à deux kilomètres du pavillon. Une latitude seule ne désigne rien, d'où la contrainte de paire |
| 08-18 | `900_seed.sql` | **`reference.countries` passe de 29 à 249 entrées** — la norme ISO 3166-1 entière, en section « 3 bis ». Les 29 lignes curées restent en tête et priment (`ON CONFLICT (iso2) DO NOTHING`) | **Retour du commanditaire sur A10 : « la liste des pays doit être plus exhaustive ».** Une conférence des Nations unies se tient où les Nations unies la placent — Bakou, Dubaï, Belém —, et une liste réduite à l'espace francophone obligeait à modifier le semis avant chaque édition hors zone. Codes vérifiés mécaniquement (249 entités, trois codes uniques chacune, concordance avec les 29 lignes curées, tout fuseau valide) ; noms issus des données CLDR, jamais recopiés à la main. **`oif_status` reste `none` sur les 220 ajouts** : la liste des 88 États et gouvernements fait autorité chez l'OIF et n'est pas devinée ici |
| 08-18 | `070_programme_proposals.sql` | **`v_proposal_dashboard` gagne treize colonnes** (§ 7) : `format`, `activity_type_code`, `organization_acronym`, `organization_country_code`, `organization_country`, `co_organizer_count`, `theme_codes`, `themes`, `reviewer_ids`, `reviewers` (nom, échéance, date de remise), `overdue_reviews`, `next_review_due_at`, `read_count` ; jointure sur `reference.countries` ; six `COMMENT ON COLUMN` | La vue portait l'avancement des revues et le classement, **et rien de ce qui identifie un dossier dans un tableau de quarante lignes**. L'écran devait recharger `proposals`, `proposal_organizations`, `entity_terms`, `taxonomy_terms`, `review_assignments` et `people`, puis refaire les correspondances lui-même — c'est-à-dire perdre la raison d'être de la vue (répondre à un écran en une requête) et **réécrire dans le frontend la correspondance code → libellé de thématique, défaut n°1 de la v1**. Même correction que celle déjà faite sur `v_public_schedule` le 17/08 |
| 08-18 | `070_programme_proposals.sql` | **Nouvelle fonction `programme.unread_proposals_for(person_id, event_id)`** (§ 7) : les dossiers d'une édition que cette personne n'a jamais ouverts, `'{}'` et jamais `NULL` | L'indicateur « non consulté » du back-office. **Ce n'est pas une propriété du dossier mais de la relation entre un dossier et un LECTEUR** : la même ligne est lue par l'un et pas par l'autre, une vue sans paramètre ne peut pas le dire, et la faire dépendre de `current_setting('app.actor_id')` rendrait son résultat invisible à la relecture — deux requêtes identiques, deux réponses |
| 08-17 | `130_analytics.sql` | **Nouvelle projection `analytics.mv_daily_registrations`** (§ 4 bis) : inscriptions aux activités par (jour, événement), série continue, avec liste d'attente, annulations, présents, personnes distinctes, cumul et moyenne mobile ; index unique `ux_mv_daily_registrations` (condition du rafraîchissement `CONCURRENTLY`) et index par événement ; ajoutée à `analytics.refresh_all()` dans l'ordre de dépendance | **« Courbe des inscriptions » n'était répondable par aucune projection existante.** `mv_daily_signups` porte le même mot et compte tout autre chose — des CRÉATIONS DE COMPTE sur toute la plateforme — et **ne porte pas `event_id`** : un administrateur détaché sur une seule édition (règle métier n° 8) n'en lit rien qui le concerne. Éprouvée en base sur jeu synthétique : série continue avec ses jours à zéro, cumul exact, annulations comptées à part et jamais soustraites |
| 08-17 | `080_live.sql` | **Nouvelle fonction `live.active_incidents_for_event(event_id, at)`** (§ 5) : les incidents actifs d'une édition, toutes portées confondues — globale, édition, journée, séance, organisation qui y anime —, avec `target_id` et `target_label` résolus | `live.active_incidents(session)` REMONTE la hiérarchie depuis une séance ; le back-office a besoin de la DESCENDRE depuis une édition. Sans elle, le tableau de bord (A6) et l'écran des messages d'incident (A13) recomposeraient chacun ce balayage, et le premier incident de portée `organization` oublié par l'un des deux passerait inaperçu là où il est publié. Éprouvée en base : les quatre portées remontent, triées par gravité décroissante, un bandeau dont la fenêtre est close est écarté |
| 08-17 | `070_programme_proposals.sql` | `tg_check_submission_eligibility()` : la fenêtre de l'appel n'est plus vérifiée que pour un **PREMIER dépôt** — `TG_OP = 'INSERT'` ou `OLD.status = 'draft'`. Le plafond par organisation et l'exigence de vérification restent contrôlés dans tous les cas ; en-tête de la fonction réécrit | Le comité demande ses corrections APRÈS la clôture de l'appel — c'est le cas normal, l'évaluation commençant quand l'appel se ferme. Le contrôle indifférencié refusait donc `changes_requested → submitted`, c'est-à-dire **le renvoi d'un dossier que le comité venait lui-même de réclamer**. L'organisation se retrouvait avec un dossier définitivement bloqué et un écran lui affichant « 1 point à corriger » sans aucune issue — le défaut ne se voyait pas tant qu'aucun écran ne permettait de corriger. Éprouvé en base sur un appel clos : renvoi accepté, premier dépôt toujours refusé. Le plafond n'avait pas à être relâché : il compte des DOSSIERS, pas des envois, et s'exclut déjà lui-même par `p.id <> NEW.id` |
| 08-17 | `040_organizations.sql` | `org.memberships` gagne **`invited_by`** et **`invited_at`**, la contrainte `ck_memberships_invitation` (les deux vont ensemble) et **deux index partiels** — `ix_memberships_requests` (demandes à trancher) et `ix_memberships_invitations` (invitations à honorer) ; en-tête du § 4 et deux `COMMENT ON COLUMN` ajoutés | Le prompt A5 demande l'invitation d'un membre par son adresse, et le modèle ne pouvait pas la porter. **Une adhésion `pending` a deux origines OPPOSÉES** : une personne qui demande à rejoindre attend qu'un référent l'accepte ; une personne qu'un référent invite attend, elle, d'accepter. Le statut seul ne dit pas laquelle — la file de modération d'un référent aurait donc mêlé ses propres invitations aux demandes reçues, et **il aurait approuvé une adhésion que l'intéressé n'a jamais acceptée**. Les deux colonnes portent la DIRECTION de la demande, ce qui suffit à séparer les deux files ; l'invitation elle-même voyage par `identity.one_time_tokens`, finalité `invitation`, prévue depuis l'origine. Un statut supplémentaire dans l'ENUM aurait été le mauvais geste : la direction n'est pas un état du cycle de vie, et deux sources pour un même fait divergent toujours |
| 08-17 | `070_programme_proposals.sql` | `proposals.target_audience` (i18n_text) devient **`target_audiences platform.i18n_text[] NOT NULL DEFAULT '{}'`** ; deux `COMMENT ON COLUMN` ajoutés, dont un sur `detailed_presentation` qui déclare son contenu HTML restreint et l'assainissement attendu de l'API | Le commanditaire veut ajouter les publics visés un par un, « pour pouvoir les afficher de façon plus intéressante après validation ». Une chaîne unique ne le permet pas : elle s'imprime telle quelle, ne se compte pas, ne se filtre pas et se découpe à la virgule par quiconque essaie — c'est exactement ce que faisait la v1 dans ses gabarits. Le tableau d'un DOMAINE conserve la garantie qui compte : `platform.is_i18n_text` s'applique **à chaque élément**, donc le français reste exigé sur chaque public — vérifié en base, un tableau dont une entrée n'a que de l'anglais est refusé. Une table de liaison aurait été plus lourde sans rien apporter : ces libellés n'ont ni identité, ni ordre propre, ni rattachement ailleurs |
| 08-17 | `060_events.sql` | `calls_for_proposals` gagne **quatre colonnes** — `min_duration_minutes` (45), `max_duration_minutes` (150), `daily_start_time` (09:00), `daily_end_time` (17:00) — et deux contraintes : `ck_calls_duration_bounds` (la durée par défaut doit être proposable, et les bornes tenir dans 15–600) et `ck_calls_daily_window`. `default_duration_minutes` passe de 90 à **60** | Retours du commanditaire sur A4 : « le temps habituel c'est 60 min, 2 h 30 max, 45 min minimum, de 9 h à 17 h ». Ces quatre valeurs sont des règles de CAMPAGNE et non de dossier : le pavillon d'une COP tient des séances d'une heure, un cycle de webinaires en voudra d'autres. Les écrire en dur dans l'écran aurait figé « 9 h – 17 h » pour toutes les éditions à venir, y compris celles qui n'ont pas de stand — et rien n'aurait pu les vérifier ailleurs que dans un navigateur. Le `CHECK` large de `programme.proposals.duration_minutes` (15 à 600) reste en place : c'est un garde-fou de DONNÉES, pas une règle d'appel, et les deux ne disent pas la même chose |
| 08-17 | `070_programme_proposals.sql` | `COMMENT ON COLUMN programme.proposals.reference_code` réécrit : le numéro est attribué à la **création** de la ligne par `tg_assign_reference_code` (`BEFORE INSERT`), donc dès le brouillon | Le commentaire annonçait « généré à la soumission » et décrivait donc autre chose que son propre trigger. L'écart n'est pas cosmétique : il commande le comportement de l'écran de dépôt. Numéro attribué à l'insertion, l'interface peut l'annoncer dès le premier enregistrement automatique et la confirmation d'envoi affiche LE MÊME numéro ; numéro attribué à la soumission, il aurait fallu attendre la fin du parcours pour donner à l'organisation de quoi désigner son dossier. Le code SQL était juste, sa documentation non — et c'est la documentation que lit la session suivante |
| 08-17 | `075_programme_sessions.sql` | `v_public_schedule` § 6 : trois colonnes de plus — `organization_country_code` (ISO2), `organization_country` (`i18n_text`) et `themes` (jsonb prêt à afficher) ; jointure sur `reference.countries` ; en-tête du § 6 réécrit avec la règle « cette vue répond à l'écran EN UNE REQUÊTE » | Écarts n° 14 et 15, ouverts depuis le 16/08 et à trancher **avant A3**. La vue promettait de répondre à un écran en une requête et ne tenait pas : la carte de séance devait charger `organizations` + `countries` pour un seul mot, et la taxonomie entière pour des pastilles. `theme_codes` est CONSERVÉ à côté de `themes` — les deux ne servent pas à la même chose : un tableau de codes se compare et s'indexe (filtre), un jsonb porte le libellé traduit et la couleur (affichage). N'exposer que les codes, c'était obliger chaque écran à refaire la correspondance : la façon exacte dont les libellés de thématiques se sont figés dans le frontend de la v1 |
| 08-17 | `020_reference.sql` | Fonction `reference.term_badges(schéma, table, id, taxonomie)` (§ 4) : rend `[{code, label, color, icon}]`, termes inactifs exclus, `'[]'` et jamais `NULL` | Même raison que `media.attached_image()` : le rattachement aux termes est polymorphe, et trois vues qui déroulent la jointure à la main sont trois occasions de diverger. Elle sera consommée par la programmation publique, la fiche d'activité et les publications. Fonction séparée de `terms_of()` plutôt qu'un paramètre de plus : les deux ont des types de retour incompatibles, et l'appelant choisit selon qu'il FILTRE ou qu'il AFFICHE |
| 08-17 | `040_organizations.sql` | `find_similar_organizations()` § 5 : deux signaux de correspondance ajoutés — `word_similarity` (opérateur `<%`) et préfixe littéral —, pondérés respectivement 0,6 et 0,55 ; en-tête du fichier et du § 5 réécrits | **Mesuré sur la base, pas supposé** : `similarity('institut', 'institut de la francophonie pour le developpement durable')` vaut **0,17**, sous le seuil de 0,3 — `similarity()` se normalise sur la longueur des DEUX chaînes. La fonction ne renvoyait donc **aucun résultat** à qui tapait le début du nom complet, geste le plus naturel qui soit, et l'écran lui proposait de créer la fiche qui existait déjà. C'est le défaut n° 1 de la v1 reproduit par le verrou V2 censé le corriger. Les deux signaux nouveaux sont volontairement pondérés SOUS une correspondance sur le nom entier : reconnaître un fragment ne vaut pas reconnaître un nom, et ils ne doivent jamais franchir seuls le seuil de 85 qui affirme « c'est la même » |
| 08-17 | `040_organizations.sql` | `find_similar_organizations()` § 5 : quatre colonnes de plus — `organization_type_code`, `city`, `verified_at`, `member_count` (adhésions actives) ; départage des dénominations à score égal (`name_rank`) | La fonction ne rendait que de quoi DÉSIGNER une fiche, pas de quoi la RECONNAÎTRE. Le prompt A2 demande nom, sigle, pays, nombre de membres et sceau de vérification sur chaque résultat : sans ces colonnes, l'écran rechargeait `memberships` et `organizations` à chaque frappe, ou renonçait — et l'utilisateur tranchait entre deux fiches homonymes sans rien pour les distinguer. Le départage évite d'annoncer « trouvée sous : <faute d'orthographe connue> » quand une dénomination correcte a le même score |
| 08-16 | `075_programme_sessions.sql` | Rôle d'attachement `('programme','sessions','cover')` ajouté à `media.attachable_roles` | `programme.proposals` déclarait son rôle `cover` depuis l'origine, `programme.sessions` **aucun** : le trigger `tg_attachments_validate` refusait donc toute image sur une séance. Or c'est la séance, pas le dossier, que le public regarde dans la programmation. Un rôle sur la séance — plutôt qu'un simple renvoi à la proposition — permet à l'IFDD de remplacer le visuel à la publication **sans réécrire le dossier déposé**, qui reste la pièce contractuelle |
| 08-16 | `050_media.sql` | Fonction `media.attached_image(schéma, table, id, rôle)` (§ 4) | Le rattachement est polymorphe : sans elle, chaque vue qui montre une vignette réécrit la même jointure latérale, la même composition d'URL et le même ordre de repli du texte alternatif. Trois écritures, trois occasions de diverger — et la première divergence est silencieuse, une image manquante ressemblant à une entité sans image. Elle rend TOUJOURS l'URL de l'original en plus des variantes : la génération est asynchrone, et sans ce champ la carte resterait trouée entre le téléversement et le passage du worker |
| 08-16 | `075_programme_sessions.sql` | `v_public_schedule` expose `proposal_id` et `cover` (§ 6) | La vue ne joignait pas `proposals` et n'exposait pas même `s.proposal_id` : **il était impossible d'y remonter jusqu'à la couverture du dossier d'origine**. `cover` applique le repli — couverture de la séance, à défaut celle de la proposition. C'est la règle et non une commodité : l'image est jointe au DÉPÔT, et personne ne revient en téléverser une seconde après l'acceptation |
| 08-16 | `900_seed.sql` | Suppression des semis `media.public_base_url` et `media.default_bucket` | Ils portaient d'autres valeurs que `050_media.sql` § 8 (`cdn.…` / `epavillon-public` contre `media.…` / `epavillon`) et, ce fichier étant chargé après, son `ON CONFLICT (key) DO NOTHING` les écartait en silence. **Vérifié sur la base** : les valeurs actives ont toujours été celles de `050`. Ces deux lignes ne servaient qu'à tromper qui lit le fichier de semis pour connaître la configuration |
| 08-16 | `030_identity.sql` | `administered_events()` : `COALESCE` sur les deux agrégats (`false`, `'{}'`) et `array_agg(DISTINCT …)` ; en-tête et `COMMENT ON` réécrits | Écart n°5. La fonction agrège sans `GROUP BY` : sans aucune attribution, `is_global` **et** `event_ids` valaient `NULL`. Deux conséquences — un garde testant `NOT is_global` confondait « aucun droit » et « administrateur d'un événement », et `event_id = ANY(NULL)` vaut `NULL`, donc le filtre d'accès **ne renvoyait rien sans lever d'erreur**. Le `DISTINCT` évite qu'une personne cumulant `admin` et `programmer` sur la même édition la voie comptée deux fois |
| 08-16 | `070_programme_proposals.sql` | `v_proposal_dashboard` : `title` expose désormais le `i18n_text` brut, la valeur résolue passe sous `title_text` ; deux `COMMENT ON COLUMN` ajoutés | Écart n°1. Le même nom de champ portait un `text` dans la vue et un `i18n_text` sur la table ; `resolveI18nText()` appliqué dessus rendait une chaîne vide **sans erreur**. `title` désigne maintenant partout la même chose — la table, `v_public_schedule` et cette vue — et la liste du back-office change de langue sans requête. La valeur résolue reste disponible pour ce que le JSON ne sait pas faire : trier, filtrer et exporter en SQL |
| 08-16 | `075_programme_sessions.sql` | Suppression des contraintes d'exclusion `ex_sessions_no_room_overlap` et `ex_sessions_no_broadcast_overlap` ; remplacées par `detect_conflicts()` et `publication_readiness()` | Le commanditaire : les chevauchements ne doivent pas être bloqués, l'admin réorganise par glisser-déposer |
| 08-16 | `060_events.sql` | Index unique `ux_calls_one_per_event` — un seul appel à propositions par édition | Une version antérieure en autorisait plusieurs ; le commanditaire a tranché |
| 08-16 | `070` et `075` | Ajout de `proposal_organizations` et `session_organizations` (porteur principal, co-organisateurs, partenaires, soutiens) | La co-organisation n'existait pas dans le modèle ; elle est courante en réalité |
| 08-16 | `040_organizations.sql` | Colonne `dedupe_on` dans `organization_references` + déduplication dans `merge_organizations()` | Sans elle, la fusion échouait dès que deux organisations co-organisaient la même activité |
| 08-16 | `060_events.sql` | Ajout de `event.programme_tracks` + `programme.session_tracks` ; `event_days` ramené à son rôle de calendrier | Une journée spéciale n'occupe pas forcément le jour entier et peut déborder sur deux jours |
| 08-16 | `060` et `075` | Ajout de `event.broadcast_channels` et de `sessions.broadcast_channel_id` | Règle « un seul direct à la fois » : le canal devient une ressource réservable |
| 08-16 | `030_identity.sql` | Rôle `admin` attribuable sur la portée `event` + `administered_events()` + trigger de contrôle de portée | Confier un événement à un responsable sans lui ouvrir le reste (cas rencontré en v1) |
| 08-16 | `010_platform.sql` | Ajout de `entity_history()` ; historique dérivé du journal d'audit | Le commanditaire demande l'historique des modifications d'activité |
| 08-16 | `125_training.sql` | Nouveau module Formations (12 tables) | Remonté au produit minimum viable à la demande du commanditaire |
| 08-16 | `030_identity.sql` | `privacy_requests.due_at` passé de colonne générée à `DEFAULT` | `timestamptz + interval` est STABLE, donc interdit dans une expression `GENERATED` |
