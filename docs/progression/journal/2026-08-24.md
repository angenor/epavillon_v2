# Journal — 2026-08-24

> Extrait de la [progression](../../PROGRESSION.md). Une ligne par session, la plus récente en haut.

| Date | Session | Fait | À suivre |
|------|---------|------|----------|
| 2026-08-24 | **B8 — LE MODULE VITRINE EXISTE : le bandeau d'accueil dit vrai, et le back-office enregistre** | **LE CRATE `content` EST ÉCRIT** — 2 201 lignes, **11 routes** : la vitrine publique (`GET /home`) et les dix routes du back-office. C'est ce qui manquait quand une diapositive retirée réapparaissait au rechargement : l'écran écrivait en mémoire, et rien n'atteignait la base. Le site est branché — `pending` devient `call`/`send` —, et **les routes en attente d'API tombent de 19 à 8**. **Cinq décisions** : l'aperçu du formulaire NE passe PAS par `v_showcase` (la vue ne rend que le publié dans sa fenêtre, l'éditeur compose un brouillon) et résout les mêmes jointures sur les valeurs en cours de saisie ; `broadcast_state` n'est pas `status` — publié hors fenêtre se dit `scheduled` ou `expired` ; l'ordre se RENUMÉROTE après chaque échange, sans quoi une insertion tombe sur un rang pris et l'ordre devient celui de `id`, le défaut du carrousel de la v1 ; `event_id` nul est la contrainte la PLUS FORTE et son refus se DIT (403 avec message) là où un hors-périmètre rend 404 ; les neuf refus de validation sortent en 200, comme le contrat du site le prévoit. **TROIS CONTENUS RÉELS EN BASE, DEUX IMAGES ET UNE VIDÉO DANS LE STOCKAGE**, passés par l'API réelle et non par une insertion : le worker a fabriqué les déclinaisons, les objets sont `ready`. **Le semis a révélé un manque de l'environnement local** : aucun média téléversé n'était visible: le modèle compose ses URL en CHEMIN, Garage n'ouvre ses objets qu'en SOUS-DOMAINE et son API S3 exige une signature. D'où un relais nginx dans le compose (`ops/media-proxy.conf`), et `media.public_base_url` pointé dessus **dans la base de développement seulement**. **Vérifié** : `fmt`, `clippy -D warnings`, **858 tests**, `typecheck`, `build`, `check-db-safe`, `check-api-contract` ; contre l'API réelle — liste, retrait, ordre, duplication, quatre refus de validation en une requête ; **au navigateur, le retrait TIENT AU RECHARGEMENT**, et l'accueil affiche les trois diapositives avec la vidéo qui joue. | **Aucun test d'intégration Rust pour ce module** : les onze routes ont été éprouvées à la main contre l'API réelle. Le **téléversement depuis le formulaire** reste à écrire — les emplacements de média sont rendus, joindre un fichier depuis l'écran ne l'est pas. Le menu d'actions ne réagit pas au clic SIMULÉ par l'outil d'automatisation (il répond au clavier) : à revérifier à la souris. |
| 2026-08-24 | **La colonne « À venir » devient automatique — `home_aside` quitte le modèle** | **ARBITRAGE DU COMMANDITAIRE** : la colonne de gauche ne se compose plus depuis le back-office. Elle affiche les ÉVÉNEMENTS À VENIR dès qu'il en existe un, puis la FRISE DES ACTIVITÉS RETENUES dès qu'une programmation est publiée — les deux viennent de l'API réelle (`GET /events/public`, `GET /schedule`). Le bandeau d'ouverture, lui, garde la vitrine. **CONSÉQUENCE TIRÉE JUSQU'AU MODÈLE** : `home_aside` n'a plus aucun rendu, et `115_content.sql` écrit lui-même qu'« un emplacement sans rendu n'existe pas ». L'ENUM `content.highlight_placement` ne porte plus qu'une valeur ; le back-office perd ses deux onglets, son sélecteur d'emplacement et l'aiguillage de son aperçu ; `HomeScreen.aside`, `HomeAsidePin`, quatre épingles d'exemple, leurs identifiants, leurs rattachements média et leurs clés de traduction disparaissent. **Base alignée À CHAUD** — `DROP VIEW`, recréation du type, `ALTER COLUMN … USING`, vue recréée à l'identique depuis le fichier : un rechargement aurait détruit le seul compte capable de se connecter. `content.highlights` est vide, aucune ligne ne pouvait être perdue. **Chargement de zéro revérifié sur une base jetable** : 178 tables, aucune erreur, le type ne porte plus qu'une valeur. `make check-db-safe` conforme, `typecheck`, `build` et `make check-api-contract` au vert, et l'accueil comme `/admin/vitrine` éprouvés au navigateur. | **Le chantier suivant est le module `content` lui-même** : le bandeau d'ouverture lit toujours des données d'exemple, faute de crate Rust. Il faut la lecture publique de la vitrine, l'écriture du back-office — c'est elle qui manque quand une diapositive retirée réapparaît —, puis semer trois contenus réels en base, deux images et une vidéo dans le stockage. |
| 2026-08-24 | **Une écriture d'exemple ne conserve rien, et le bandeau le dit maintenant** | **FAUX BUG SIGNALÉ PAR LE COMMANDITAIRE** : retirer une épingle depuis `/admin/vitrine?emplacement=panneau` ne persiste pas. **Ce n'est pas un défaut de l'écran** — le schéma `content` n'a AUCUN crate Rust, aucune route de vitrine n'existe dans le contrat engendré (`grep showcase app/types/api.ts` : zéro), et les dix appels de cet écran passent par `pending()`. Les gestes du back-office de la vitrine — publier, retirer, archiver, réordonner, dupliquer — modifient des objets en mémoire et disparaissent au rechargement. **Le bandeau ne pouvait pas le faire deviner** : il annonçait des données LUES d'exemple, quand le problème est qu'une ÉCRITURE simulée ne conserve rien alors que l'écran répond « c'est fait ». `useMockData()` suit désormais les chemins d'écriture à part (`hasWrites`), `pending` lui transmet son type, et le bandeau ajoute une ligne franche : « Ce que vous modifiez ici n'est enregistré nulle part. » `typecheck` et `build` au vert ; le bandeau lui-même n'a pas été vu au navigateur, faute d'un compte d'administration utilisable sur la base de développement. | **La question de fond reste entière** : écrire le module `content` (crate Rust, routes de la vitrine et de l'accueil) pour que la vitrine persiste, ou continuer sans. Même situation pour `live` (messages d'incident) et `analytics` (tableau de bord). |
| 2026-08-24 | **Le panneau « À venir » montre enfin ce qu'il promet** | **L'ÉCART SIGNALÉ PAR LE COMMANDITAIRE EST CORRIGÉ** : le panneau latéral de l'accueil n'affichait que les encarts de `/admin/vitrine?emplacement=panneau`. Il porte désormais **les événements à venir** — le prochain en carte pleine, avec son compte à rebours, son lieu et le volume de son programme — puis **la frise des activités retenues**, groupée par journée, sur un rail vertical. Les épingles éditoriales **restent, en dernier** : les retirer aurait laissé l'emplacement « panneau » du back-office sans surface d'affichage, et un administrateur composerait un contenu invisible. Maquette validée d'abord, puis appliquée. **Deux fichiers neufs de logique pure et deux composants neufs** ; `AsideEdition` et `AsideSession` refondus. **Trois pièges traités** : « maintenant » vient de `generated_at` et non de l'horloge du navigateur — sinon le rendu serveur et l'hydratation divergent d'une seconde et l'écran se redessine ; la journée se calcule dans le fuseau de la SÉANCE, sans quoi une séance de Belém bascule du 17 au 18 novembre selon l'endroit d'où on regarde la page ; et la durée d'une édition ne se dit que d'un pavillon — appliquée à un cycle de webinaires étalé sur l'année, elle annonçait « 302 jours ». **Éprouvé au navigateur en 1440 × 900 et en 390 × 844**, sur données d'exemple : trois défauts trouvés à la capture et corrigés — « heure de Belem » sans accent faute de transmettre la ville de l'édition, la durée absurde, et l'intitulé complet qui répétait le libellé qu'on venait de lire. `typecheck`, `build` et `make check-api-contract` au vert. | **Une question au commanditaire** : la frise doit-elle suivre une seule édition — celle en cours — ou continuer de mêler toutes les éditions comme aujourd'hui ? Elle nomme l'édition sur chaque carte quand elles diffèrent. **La base de développement reste vide** : ces trois blocs ne montrent quelque chose qu'avec les données d'exemple ou après création de la COP31. |
| 2026-08-24 | **B7 — l'accueil dit vrai sur trois blocs, et le bandeau ne ment plus** | **L'ACCUEIL PUBLIC EST BRANCHÉ SUR L'API POUR TROIS DE SES CINQ BLOCS** — les éditions, les prochaines séances et les chiffres de chaque édition. Le quatrième et le cinquième — les deux blocs de la vitrine — restent en données d'exemple, faute de crate `content`, et le bandeau ne désigne plus qu'eux. **`GET /schedule` accepte désormais une lecture SANS édition** : l'accueil compose une liste des prochaines séances toutes COP confondues et n'a aucune édition à nommer. Elle est **bornée dans les deux sens** — les séances passées sont écartées, le nombre de lignes plafonné à 50 (200 au maximum) —, sans quoi la page d'accueil rendrait la programmation entière de toutes les éditions de l'histoire de la plateforme à chaque affichage. **`par_adresse` ne prend AUCUN plafond** : elle cherche une séance précise, et la borner ferait rendre « introuvable » pour une séance qui existe mais arrive tard dans le programme. **Les chiffres du programme ne coûtent aucun appel** : chaque ligne de `GET /events/public` les portait déjà côté Rust, et c'est le type du site qui les ignorait — cinq colonnes ajoutées à `PublicEditionRow`, et l'accueil compose ses statistiques à partir de la liste qu'il tient en main. **Un défaut réel corrigé, invisible autrement** : le middleware qui vide la liste des données d'exemple se rejoue à l'HYDRATATION, sur la route que le serveur vient de rendre — il effaçait le marquage une fraction de seconde avant que le bandeau ne se rende, si bien que celui-ci ne s'affichait JAMAIS alors que le marquage, lui, avait bien lieu. Il compare maintenant `to` et `from`. **Éprouvé au navigateur** : `GET /api/events/public` et `GET /api/schedule` partent réellement, l'accueil affiche « Aucun appel à propositions ouvert » — ce qui est vrai, la base ne porte aucune édition — et le bandeau nomme la seule route qui manque. `npm run typecheck`, `npm run build`, `make check-api-contract` et les 7 tests de la programmation publique au vert. | **La base de développement est VIDE de données métier** : 249 pays, 74 termes, 13 drapeaux, 4 séries, 1 organisation, mais **zéro édition et zéro séance**. Les écrans branchés affichent donc leurs états vides, ce qui est exact mais ne montre rien. Deux voies : créer la COP31 depuis le back-office — le parcours que le jalon vise —, ou semer un jeu de démonstration en base. **À trancher avec le commanditaire.** Restent les deux crates non écrits (`live`, `content`) et le tableau de bord, tous trois en `pending()`. |
| 2026-08-24 | **B7 — le back-office s'ouvre, et une adhésion dit à quel titre** | **DEUX BLOCAGES LEVÉS, DONT UN CIRCULAIRE.** Le back-office refusait l'accès à un administrateur GLOBAL sur une plateforme neuve : `canAdminister` testait la **liste des événements** au lieu du **périmètre**, si bien qu'il fallait une édition pour entrer dans l'écran où l'on crée la première édition. Le contrat déclarait pourtant les trois cas depuis le premier jour, dans l'en-tête de `useApi.ts`. Un `computed` corrigé, **dix écrans débloqués**. Et le compte d'amorçage du semis **n'a pas de mot de passe** — c'est voulu, mais aucun jeton d'activation n'est posé non plus : personne ne pouvait se connecter. Le rôle a été accordé à la main, une fois, avec son motif inscrit dans la colonne `note`. **UNE ADHÉSION ACTIVE PORTE TOUJOURS SA FONCTION** (`ck_memberships_job_title`), sur demande du commanditaire — portée par le MODÈLE et non par les écrans, car trois chemins mènent à une adhésion active et trois validations auraient divergé. **Elle n'est pas exigée en attente**, et c'est l'arbitrage : un référent qui invite ne connaît pas l'intitulé exact de la personne qu'il vise, et le lui faire inventer produirait une donnée fausse que personne ne corrigerait. C'est l'invitée qui la déclare **en acceptant** — l'écran, qui acceptait automatiquement au chargement, la demande désormais avant. **Contrainte posée à chaud** plutôt que par un rechargement : celui-ci aurait détruit le seul compte capable de se connecter. Le chargement de zéro est vérifié **sur une base jetable** — 17 schémas, 159 tables. **100 tests du module Organisations au vert**, `cargo fmt` et `clippy -D warnings` sans un avertissement. | **Créer la COP31 depuis le back-office**, au navigateur : c'est le parcours que le jalon vise, et rien ne l'a encore éprouvé bout à bout — édition, calendrier, appel à propositions. **Deux questions ouvertes** : le semis doit-il poser un jeton d'activation pour le compte d'amorçage, ou la mise en service passe-t-elle par une commande dédiée ? Et le tableau de bord, sans aucune édition, invite à « choisir une édition en tête de page » alors qu'il n'y en a aucune à choisir — il devrait proposer d'en créer une. |
