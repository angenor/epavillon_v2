# Journal — 2026-09-05

> Extrait de la [progression](../../PROGRESSION.md). Une ligne par session, la plus récente en haut.

| Date | Session | Fait | À suivre |
|------|---------|------|----------|
| 2026-09-05 | **Une diapositive ne pouvait plus être modifiée : `PATCH` manquait aux méthodes annoncées par CORS** | Signalé après coup, sur `/admin/vitrine/<id>` : « L'enregistrement n'a pas abouti », et en console *« méthode manquante dans l'en-tête `Access-Control-Allow-Methods` »*. `middleware/cors.rs` annonçait `GET, POST, PUT, DELETE, OPTIONS` — **et une seule route de tout le dépôt emploie `PATCH`**, la modification d'une diapositive. Le refus vient du NAVIGATEUR, pas de l'API : le préalable est bloqué, l'appel ne part pas, aucun code ne revient, et l'écran affiche donc son message de **panne réseau**, qui désigne le mauvais coupable. `curl` n'a jamais eu le problème — il n'envoie pas de préalable, ce qui est exactement pourquoi le manque a pu vivre. Le test `entetes_cors.rs` ne contrôlait que `POST` et `DELETE` : il vérifie maintenant **les six verbes un par un**, en nommant celui qui manque. Consigné dans [`pieges.md`](../pieges.md) | **L'API doit être redémarrée** pour que la correction prenne : elle tourne dans un terminal, avec le binaire d'avant |
| 2026-09-05 | **Le téléversement d'image de la vitrine : la chaîne existait depuis dix jours, l'écran ne l'appelait pas** | Signalé comme « impossible de téléverser une image, la boîte ne s'ouvre même pas » sur `/admin/vitrine/nouveau`. **Le bouton était `disabled`**, sous la mention « le téléversement arrive à la prochaine étape » — un reste écrit avant que le dépôt existe. Il existe depuis le **26/08** : `MediaImageField` porte le choix du fichier, le recadrage au rapport du rôle, le texte alternatif exigé par `ck_assets_alt_text_required` et le dépôt par `POST /media/assets`, et les trois déclinaisons d'une édition s'en servent. La vitrine affichait ses emplacements et un bouton mort : **le travail était fait, à un écran de distance**. `AdminShowcaseMediaPanel` ne dessine plus de bouton à lui — chaque emplacement d'**image** est le composant partagé. **Le rattachement passe par la route générique**, `PUT /media/attachments` sur `('content','highlights',<id>)`, jumelle de `adminEvents.saveImages()` ; son corps (`AttachmentBatch`, `AttachmentAssignment`) a été **remonté du composable des éditions vers `types/media.ts`**, où il était déclaré en local. **Trois décisions** : le lot n'emporte que les emplacements **modifiés** — il vide tout rôle qu'il nomme, donc réaffirmer une image inchangée réécrirait son rattachement et nommer le fond vidéo le **détacherait** ; l'ordre s'inverse entre les deux pages — la diapositive d'abord à la création, les médias d'abord en modification, et la page de création **retient la ligne créée** pour qu'un second envoi n'en fasse pas une deuxième ; la vitrine ne sème **aucun rapport attendu** (`115_content.sql` § 5), le recadrage y est donc libre. `MediaImageField` gagne un second événement, `update:image`, sans lequel l'aperçu vivant du formulaire ne pouvait pas dessiner ce qu'il venait d'enregistrer. **Le fond vidéo reste fermé, seul** : 200 Mio ne passent pas d'un seul tenant sans reprise, et son bouton le dit désormais en propre au lieu d'une mention de phase. **Deux autres mentions « phase B » périmées reformulées** (`types/content.ts`, `mocks/content.ts`). `CLAUDE.md` gagne une section **« Réutiliser avant d'écrire »** — c'est le défaut que cette session corrige, et il se reproduira | **Vérifié : `npm run typecheck` et `npm run build` passent** (`make check-front`, contrat d'API compris). **Non vérifié au navigateur** — la session d'automatisation a été arrêtée avant d'avoir pu se connecter. **À éprouver à la main** : déposer une image sur les deux emplacements, à la création et en modification, et confirmer que le rattachement tient au rechargement. La **photo d'intervenant** (`SpeakerDialog`) est le prochain candidat à l'alignement : elle plafonne à 5 Mio **écrits en dur** au lieu de lire `media.attachable_roles`, et ne dépose rien. Le fichier d'écran : [`b8-vitrine.md`](../ecrans/b8-vitrine.md) |
