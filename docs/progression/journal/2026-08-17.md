# Journal — 2026-08-17

> Extrait de la [progression](../../PROGRESSION.md). Une ligne par session, la plus récente en haut.

| Date | Session | Fait | À suivre |
|------|---------|------|----------|
| 2026-08-17 | Tableau de bord du back-office (A6) | **LE MODÈLE A ÉTÉ CORRIGÉ D'ABORD, pour la sixième fois du jalon, et sur deux points**. **(1) « Courbe des inscriptions » n'était pas répondable par édition** : `mv_daily_signups` compte des CRÉATIONS DE COMPTE sur toute la plateforme et ne porte pas `event_id` — un administrateur détaché sur une seule COP n'en lit rien qui le concerne, et les deux sens du mot « inscription » se confondaient depuis le début. `130_analytics.sql` gagne **`mv_daily_registrations`** (§ 4 bis), série continue par (jour, événement), inscrite à `refresh_all`. **(2) Les incidents actifs d'une ÉDITION ne se lisaient nulle part** : `live.active_incidents(session)` REMONTE la hiérarchie depuis une séance ; le back-office a besoin de la DESCENDRE. `080_live.sql` gagne **`live.active_incidents_for_event()`**, symétrique, avec la cible résolue. Base rechargée de zéro, les deux objets éprouvés sur jeu synthétique (série continue, cumul, quatre portées d'incident, bandeau expiré écarté), `make check` au vert. **1 page** `app/pages/admin/index.vue`, **5 composants** sous `app/components/admin/`, **1 store étendu** (`admin-scope` charge et tient le périmètre), **2 fichiers de contrats**, **3 fichiers de mocks**, **2 fichiers de traduction**. **LE SÉLECTEUR D'ÉVÉNEMENT A QUITTÉ LA NAVIGATION LATÉRALE** pour la tête de page : sur écran étroit, la latérale est un tiroir, et le périmètre d'un écran ne peut pas être caché derrière un bouton. Il vit dans l'URL (`?evenement=`), comme l'organisation d'A5. **Un compte détaché sur une seule édition n'a ni liste, ni mention, ni compteur** — la clé `nav.admin.eventScope.restricted` a été supprimée : « votre compte n'administre qu'un seul événement » est exactement l'aveu que le prompt interdit. **Cinq défauts trouvés en chemin, corrigés** (les deux derniers pendant la reprise au navigateur : fuseau affiché « heure de Belem » sans accent, et URL forgée hors périmètre qui restait affichée) : `middleware/auth.ts` redirigeait vers `/auth/login`, **une adresse qui n'existe pas** (404 mesuré) — la page déclare `/connexion` par `defineI18nRoute`, il fallait le NOM de route ; `UiSelect`, `UiInput` et `UiTextarea` ne transmettaient pas `hide-label` à `UiFormField` ; les inscriptions simulées de la COP31 étaient **entièrement datées dans le futur** et ont été antidatées de trente jours, jusqu'au jour de publication du programme. | A7 — liste des propositions. `useApi.ts` atteint 822 lignes : le découper par domaine avant A8 |
| 2026-08-17 | Modifier son dossier | Retour du commanditaire : « une personne doit pouvoir modifier son activité ». **LA RÈGLE QU'IL A DONNÉE N'EST PAS CELLE QU'ON AURAIT DÉDUITE DU MODÈLE** — « tant que l'évènement n'est pas terminé, il peut modifier » : ce n'est pas l'état du DOSSIER qui ferme la porte, c'est la fin de l'ÉDITION. Un dossier déposé, en évaluation ou même retenu reste donc modifiable, ce que la machine à états seule n'aurait jamais laissé conclure. **LE MODÈLE A ÉTÉ CORRIGÉ D'ABORD, pour la cinquième fois du jalon, et le défaut était grave** : `tg_check_submission_eligibility()` contrôlait la fenêtre de l'appel sur TOUT passage à `submitted`, renvoi après correction compris. Or le comité demande ses corrections APRÈS la clôture — c'est le cas normal, l'évaluation commençant quand l'appel se ferme. **Le dossier que le comité venait lui-même de réclamer ne pouvait donc pas lui être renvoyé** : l'organisation restait devant un écran affichant « 1 point à corriger » et aucune issue. La fenêtre ne vaut plus que pour un PREMIER dépôt (`INSERT` ou `OLD.status = 'draft'`) ; le plafond, lui, reste vérifié dans les deux cas — il compte des dossiers, pas des envois. Base rechargée, **les deux comportements éprouvés en base** sur un appel clos : renvoi après correction accepté, premier dépôt toujours refusé. **UN SEUL FORMULAIRE**, celui du dépôt, rouvert sur `?dossier=<id>` — mêmes sept étapes, mêmes validations, même enregistrement automatique. La lecture est une RECOMPOSITION et non un `SELECT` : le formulaire travaille sur une structure d'écran (français, heures murales, clés de liste) quand la base range la même chose dans cinq tables. Trois conversions comptent — l'heure murale rendue au fuseau de l'ÉDITION (sans quoi un créneau de 14:30 à Belém se rouvrirait à 11:30 depuis Dakar), les textes ramenés à leur français, et le `has_account` d'un intervenant qui **reverrouille son identité**. **TROIS ISSUES SELON L'ÉTAT, et le bouton les nomme** : « Envoyer le dossier » (brouillon), « Renvoyer au comité » (corrections demandées), « Enregistrer les modifications » (tout le reste — il n'existe aucune transition vers soi-même, et un dossier en évaluation ne repart pas au comité pour une faute de frappe). **Deux avertissements portés à l'écran plutôt que découverts après coup** : un dossier en évaluation est relu par le comité, qui verra les modifications dans l'historique ; et corriger un dossier RETENU **ne déplace pas l'activité programmée** — la séance garde créneau, salle et inscriptions, le modèle séparant volontairement le dossier de son occurrence. **Deux incohérences du jeu de données corrigées en chemin, toutes deux révélées par l'usage** : Verdéo Solutions, seule organisation portant un dossier en corrections demandées, n'avait AUCUN compte — le cas central de l'espace organisation n'était atteignable par personne ; et ce dossier n'avait ni intervenant ni créneau, alors qu'il avait forcément été déposé — il n'aurait donc jamais pu passer la validation du dépôt. Vérifié dans un navigateur : le dossier COP31-00025 rouvert, corrigé, **renvoyé au comité avec le MÊME numéro** malgré l'échéance dépassée, et sa confirmation propre (« Votre dossier est renvoyé au comité », et non « déposé ») ; un dossier retenu modifié, enregistré sans changer d'état, dont la séance n'a pas bougé (10 novembre 2027, 14:00 — 15:30) ; un dossier de la COP30 qui affiche « Cette édition est terminée » à la place du bouton ; anglais complet, zéro clé brute, `make check-db` et `make check-front` verts | Prompt A6 — tableau de bord du back-office. **Deux obligations d'API nouvelles (n° 38 et 39)** : la recomposition d'un dossier en brouillon appartient à l'API, et le renvoi après correction est une ROUTE distincte du dépôt — la confondre avec lui, c'est réintroduire le contrôle de fenêtre qu'on vient de retirer |
| 2026-08-17 | Espace organisation (A5) | **LE MODÈLE A ÉTÉ CORRIGÉ D'ABORD, pour la quatrième fois du jalon** : « invitation par adresse » n'était pas exprimable. Une adhésion `pending` a DEUX ORIGINES OPPOSÉES — une personne qui DEMANDE attend un référent, une personne qu'on INVITE attend d'accepter elle-même — et rien ne les distinguait. La file de modération d'un référent mêlait donc ses propres invitations aux demandes reçues, et **il aurait approuvé une adhésion que l'intéressé n'a jamais acceptée**. `org.memberships` gagne `invited_by`, `invited_at`, une contrainte de cohérence et **deux index partiels, un par file d'attente**. Base rechargée de zéro, `make check-db` vert, contrainte éprouvée en base (date sans auteur refusée, invitation complète acceptée avec `is_primary` faux). **Deux pages** : `/mon-organisation` (vue d'ensemble) et `/mon-organisation/dossiers/:id` (suivi d'un dossier, quatre onglets). Deux plutôt qu'une **parce qu'un dossier se DÉSIGNE** — on s'écrit « regarde COP31-00001 », la confirmation de dépôt y renvoie désormais, et un panneau dépliant n'a pas d'adresse. **Sept composants** sous `app/components/workspace/` et **un utilitaire pur** (`utils/proposal-timeline.ts`) qui compose la frise depuis le JOURNAL DES TRANSITIONS et non depuis la proposition : deux dates sur cinq étapes vivent sur la ligne, le reste est dans `proposal_transitions`. **LES QUATRE RAPPELS SONT DÉRIVÉS, PAS ÉCRITS** : la matérialisation compte une ligne par destinataire, par canal et par décalage — sept inscrits d'une seule séance en produisent vingt-huit —, donc `mocks/reminders.ts` rejoue `schedule_session_reminders()` en TypeScript, comme `organization-search.ts` rejoue `find_similar_organizations()`. **UN DOSSIER DE LA COP30 A ÉTÉ AJOUTÉ AU JEU, et c'est ce qui rend l'écran démontrable** : toutes les séances de la COP31 se tiennent en novembre 2027, donc TOUS les rappels y sont à venir et l'exigence « distinguer les envoyés des à-venir » n'avait aucune donnée. La séance `cop30Adaptation` existait déjà, portée par le ROAC et terminée, mais SANS dossier d'origine ; elle en a un désormais, plus sept inscriptions dont cinq présentes, une absente et une annulée — les seuls `attended`/`no_show` du jeu, et ils ne sont vrais que là. **Trois écritures** (invitation, réponse à un message, marquage résolu) plus une quatrième trouvée en chemin : accepter ou refuser une demande d'adhésion, deux boutons qui ne faisaient rien et qu'il fallait brancher ou retirer. **Trois défauts corrigés là où ils étaient** : `UiStatusTimeline` écrivait « en attente » sous une étape FRANCHIE dont la date est inconnue (contresens visible sur tout dossier retenu), ses `detail` débordaient sur les colonnes voisines en orientation horizontale (nouvelle propriété `hideDetails`), et l'historique rendait `under_review` et `2027-11-09T14:00:00-03:00` bruts — le journal d'audit livré à la place de l'historique. Vérifié dans un navigateur : les quatre rappels de la COP30 tous « Envoyé » avec leurs six destinataires à l'heure de Belém, le fil de discussion avec réponse envoyée et résolution posée puis retirée puis reposée, l'invitation par adresse d'une personne inconnue, l'acceptation d'une demande qui fait tomber le bloc d'actions de 3 à 2, l'état vide sur une organisation créée en séance (« Rien ne vous attend » + l'appel en cours à 44 jours), 375 px sans défilement horizontal, thème clair, anglais complet URL comprises, zéro clé brute, `make check-db-safe` et `make check-front` verts | Prompt A6 — tableau de bord du back-office. **Cinq obligations d'API relevées (n° 33 à 37)**, dont deux qui comptent : aucune vue ne récapitule les rappels d'une séance par décalage, et rien ne dit qui peut marquer une demande de correction « résolue » |
| 2026-08-17 | Publics visés en liste, présentation détaillée en texte riche | Deux demandes du commanditaire, **et les deux ont commencé par le modèle**. **(1) `proposals.target_audience` DEVIENT `target_audiences`, un TABLEAU** de `platform.i18n_text` : « Ministères, ONG, journalistes » écrit d'un trait ne se réaffiche pas — il s'imprime tel quel, ne se compte pas, ne se filtre pas, et finit découpé à la virgule par le premier gabarit qui essaie, ce que faisait la v1. **La contrainte du domaine s'applique élément par élément** — vérifié en base : un tableau dont une entrée n'a pas de français est refusé —, donc le français reste exigé sur chaque public. Les 27 publics des données simulées ont été convertis en autant d'entrées. L'étape 2 les ajoute un par un (Entrée **ou virgule** valident, la virgule étant le geste naturel de qui écrit une énumération), les rend en jetons retirables, refuse les doublons, et le récapitulatif les affiche en pastilles. **(2) LA PRÉSENTATION DÉTAILLÉE PASSE EN TEXTE RICHE** — `@tiptap/vue-3` 3.30, nouveau composant `UiRichText` dans la bibliothèque, plus `UiRichContent` pour la lecture. **Ce que l'éditeur NE permet pas est le cœur de la décision** : ni police, ni taille, ni couleur, ni alignement, ni surlignage. Le contenu ne porte que de la STRUCTURE — « ceci est un sous-titre », « ceci est une liste » —, l'apparence est décidée à l'affichage par les jetons. Sans cela, deux campagnes suffisent à produire un programme où chaque activité a sa typographie, et le premier thème sombre le rend illisible : un texte peint en noir reste noir sur fond noir. **Une seule feuille `.rich-text` habille la saisie ET la lecture** (`main.css`), avec une règle `[style] { color: inherit !important }` qui dépeint ce qui arrive collé d'un traitement de texte. Sous-titres limités aux niveaux 3 et 4 : le h1 est le titre de l'activité, et laisser choisir h1 dans un corps de texte casse la navigation au lecteur d'écran. **Le compteur porte sur le TEXTE, pas sur le balisage** — compter le HTML ferait grossir le décompte à chaque mise en gras. Rendu client seulement (`<ClientOnly>` avec un cadre de repli à la bonne hauteur) : ProseMirror manipule le DOM. Cinq icônes ajoutées à `UiIcon`, une démonstration ajoutée au guide de style. **Le HTML n'est pas de confiance et c'est écrit trois fois** — dans le SQL, dans l'éditeur, dans `UiRichContent` : l'éditeur limite ce qu'on TAPE, pas ce qu'on COLLE, et l'assainissement est une obligation de l'API (prompt B4, écart n° 32). Base rechargée de zéro, `make check-db` et `make check-front` verts, parcours vérifié dans le navigateur | Prompt A5 — espace organisation. **Obligation d'API à ne pas perdre** : assainir le HTML de `detailed_presentation` à l'écriture, avec la liste blanche de l'éditeur |
| 2026-08-17 | Retours du commanditaire sur A4 | Sept demandes, toutes tenues. **(1) LE MODÈLE A ÉTÉ CORRIGÉ D'ABORD, pour la troisième fois du jalon** : « 60 min habituel, 45 min minimum, 2 h 30 maximum, de 9 h à 17 h » n'était exprimable nulle part — `calls_for_proposals` gagne donc `min_duration_minutes`, `max_duration_minutes`, `daily_start_time`, `daily_end_time`, deux contraintes de cohérence, et sa durée par défaut passe de 90 à 60. Écrites en dur dans l'écran, ces quatre valeurs auraient figé les horaires d'un pavillon pour toutes les éditions à venir. Base rechargée de zéro, `make check-db` vert. L'étape 5 propose désormais des durées **calculées depuis l'appel** (45 à 150 par quarts d'heure, 60 marquée « durée habituelle ») et refuse un créneau qui déborderait de la plage d'accueil — c'est la FIN qui doit tenir avant 17 h, début plus durée. **(2) LE BOUTON « SUIVANT » BLOQUE** tant que l'étape courante porte une erreur, avec le compte des points à corriger sous le bouton éteint : un bouton désactivé sans écriteau est une porte fermée sans explication. Contrepartie assumée d'une décision antérieure — les erreurs de l'étape courante s'affichent désormais dès la première modification du dossier, faute de quoi on ne pourrait plus quitter l'étape pour découvrir ce qui cloche. La barre d'étapes reste NAVIGABLE : c'est l'avancée pas à pas qui exige un dossier propre, pas la consultation. **(3) L'ANTI-DOUBLON D'INTERVENANT, qui est le vrai morceau** : la saisie de l'adresse interroge l'annuaire (`people.primary_email`, clé de rapprochement du modèle) et propose la personne trouvée. Retenue, elle remplit tout — et **si elle a un COMPTE, son identité se verrouille** : civilité, nom, prénom, adresse, notice et photo appartiennent à son titulaire. Fonction et organisation restent modifiables, et ce n'est pas une inconséquence : ce sont les INSTANTANÉS de l'activité (`job_title_snapshot`, `organization_snapshot`), que le modèle distingue explicitement de la fiche de la personne. Une personne connue SANS compte reste corrigible — personne d'autre ne l'entretient. **On ne rend jamais l'annuaire entier** : une plateforme ne diffuse pas sa liste de contacts pour remplir un formulaire, et `knownSpeakers()` a cédé la place à `lookupSpeaker(email)`. **(4) CIVILITÉ, FONCTION ET ORGANISATION DEVIENNENT OBLIGATOIRES** pour un intervenant, **(5) CRÉNEAU ET DURÉE AUSSI** — aucune de ces cinq colonnes n'est `NOT NULL` en base : ce sont des exigences de dossier, et le programme imprimé les justifie (« Mme Awa Sow Fall, directrice exécutive, ROAC »). **(6) LA PHOTO S'AFFICHE**, dans le dialogue et dans la liste : une photo choisie sans être montrée est une photo qu'on ne peut pas vérifier. L'aperçu passe par `URL.createObjectURL()`, révoqué au retrait, et **ne part jamais à l'API**. **(7) L'ÉTAPE DES DOCUMENTS EST MASQUÉE** — le comité n'aura pas le temps de lire des pièces jointes : une seule ligne commentée dans `PROPOSAL_FORM_STEPS`, le composant, sa validation et ses traductions restent en place. Le formulaire compte six étapes. Un écart de plus relevé (n° 31) : rien dans le modèle ne dit qui peut modifier l'identité d'un intervenant — l'écran a tranché, l'API devra le porter. Vérifié dans un navigateur : les six étapes, le blocage avec ses trois points signalés, le profil verrouillé de Boureima Ouédraogo (champs en lecture seule, badge « Compte de la plateforme » dans la liste), une intervenante inconnue créée avec sa photo et sa vignette, la durée de 120 min refusée à 16 h et acceptée à 14 h, `make check-db` et `make check-front` verts | Prompt A5 — espace organisation. La règle « figée après validation » (écart n° 31) s'y appliquera : c'est là qu'un dossier accepté se consulte |
| 2026-08-17 | Formulaire de soumission (A4) | **Une page** `app/pages/proposal/form.vue` (`/deposer-une-proposition`, `/en/submit-a-proposal`), **gardée par le middleware d'A2** (`requires-organization`, raison `proposal`) — la première page à l'être, comme le prévoyait la règle transverse. **Onze composants** sous `app/components/proposal/` : les sept étapes, plus `Deadline` (l'encart permanent), `Autosave`, `IssueSummary` et `SpeakerDialog`. **Un composable** `useProposalDraft()` : enregistrement automatique 1,5 s après la dernière frappe, une seule écriture en vol, **et aucune écriture avant la première modification** — sinon ouvrir la page créerait une ligne vide et ferait avancer le compteur du plafond pour une visite. **Un fichier de contrats** (`types/proposal-form.ts`), **un fichier de règles pures** (`utils/proposal-draft.ts` : brouillon vierge, validation, refus de fichier), **un fichier de mocks** (`mocks/proposal-submission.ts`) qui rejoue la numérotation, la recevabilité et les deux écritures. **16 fichiers de traduction** (8 × 2 locales), découpés par étape comme le prescrit `CLAUDE.md`. **LE NUMÉRO DE DOSSIER EXISTE DÈS LE BROUILLON, et c'est le SQL qui le dit** : `tg_assign_reference_code` est un trigger `BEFORE INSERT`, alors que le `COMMENT ON` de la colonne annonçait « généré à la soumission ». Le commentaire a été corrigé — c'est la seule modification du modèle de cette session, base rechargée de zéro, `make check-db` vert. Conséquence d'écran : l'encart d'enregistrement annonce « COP31-00041 » dès la première frappe, et la confirmation d'envoi affiche LE MÊME numéro, pas un second. **Trois obligations d'API relevées** (n° 27 à 29), dont une qui compte : `min_speakers` et `max_speakers` sont des données de l'appel que **AUCUN trigger ne vérifie** — l'écran les applique seul aujourd'hui. **Deux corrections de composants trouvées en chemin** : `UiStepper` faisait défiler LA PAGE au lieu de lui-même sous 375 px (sa largeur minimale automatique valait celle de ses sept cellules bout à bout — `min-w-0` manquant, exactement le piège relevé au A3 sur les cartes de programmation), et il n'autorisait le clic que vers l'arrière, alors qu'un dossier se remplit dans le désordre (propriété `allowSkipAhead`). **`utils/datetime.ts` gagne `instantFromWallClock()`**, l'inverse de `wallClockInZone()` : sans elle, un créneau saisi depuis Dakar aurait valu 14:30 heure de Dakar, soit 11:30 au pavillon, sans qu'aucune erreur ne soit levée. Vérifié dans un navigateur : parcours complet jusqu'à la confirmation (`COP31-00041`, copiable), l'enregistrement automatique et son horodatage « 14:36, heure de Dakar » (fuseau de la PERSONNE, pas de l'édition), l'envoi d'un dossier vide qui renvoie sur l'étape en défaut et met le focus sur le champ, les cinq erreurs regroupées en tête, le co-organisateur ajouté par la recherche d'A2, l'intervenant reconnu par son adresse, 375 px sans défilement horizontal du corps de page, thème clair, anglais complet, zéro clé brute, `make check-front` vert | Prompt A5 — espace organisation. **Le bouton « Suivre mon dossier » de la confirmation n'est pas rendu tant qu'A5 n'existe pas** : on ne promet pas un lien mort. Le téléversement réel des documents et des photos attend le prompt B6 — les fichiers sont retenus et contrôlés, pas déposés |
| 2026-08-17 | Approfondissement de la palette | Le commanditaire trouvait l'interface trop sobre au regard de `ifdd.francophonie.org`. **Le relevé du site institutionnel est contre-intuitif, et il a dicté la solution** : ce qui rend ce site plus foncé n'est PAS son fond de page — blanc à 43 % — mais l'alternance avec de très grands aplats presque noirs, un indigo `#1D1A4C` qui couvre près d'un cinquième de la hauteur de l'accueil et qui sert AUSSI de couleur de titre. Ce site n'emploie d'ailleurs **nulle part** le cyan `#00A1E4`. Or notre charte porte déjà cette couleur : `--ifdd-bleu-riche` (#1d1a5b), à deux points de l'indigo institutionnel, décliné en rampe complète et **réclamé par aucun rôle**. Assombrir n'a donc demandé aucune couleur nouvelle, seulement de brancher des rôles sur des paliers existants. **Quatre leviers** : les titres passent au bleu riche (`--color-heading`, une seule règle `h1`…`h6` dans `main.css` — sa spécificité 0,0,1 laisse tout utilitaire de composant l'emporter, donc les titres restent blancs sur les aplats foncés sans exception à écrire) ; **le pied de page devient un aplat institutionnel**, seul grand bloc que porte chaque écran ; le fond de page passe de `gris-50` à `gris-100`, car à un point de luminance du blanc des cartes il ne séparait rien ; l'accent descend d'un palier (cyan-800 en texte, cyan-700 en aplat), le cyan restant la couleur de l'information et de l'action. **Les aplats institutionnels ne s'inversent pas au thème sombre** — un aplat est un bloc de mise en page, pas une surface de thème : d'où des rôles `--color-surface-inverse*` distincts de `--color-surface-*`. **Deux rôles manquants ajoutés** : `--color-scrim` et `--color-ring-contrast`, qui suppriment les six `bg-black/50` et `ring-black/10` ne réagissant à aucun thème. **L'audit de contraste a trouvé deux régressions AA, corrigées avant livraison** : `--color-text-subtle` tombait à 4,46:1 sur le nouveau fond (une trentaine d'occurrences) et `--color-focus` à 2,97:1 sur les surfaces creuses — donc la navigation au clavier de la page de connexion, entièrement en surface creuse. Corrigés à 5,68/5,18 et 5,39/4,91, plus `--color-warning` (4,50 → 7,40) et `--color-info-solid` (4,23 → 6,23). **Deux demi-paliers dérivés ajoutés à la rampe de gris** (`gris-150`, `gris-650`) : descendre le texte discret jusqu'à `gris-700` l'aurait confondu avec le texte atténué et la hiérarchie du texte y aurait perdu un cran. Aucune couleur de charte touchée. Défaut collatéral trouvé et corrigé : `--color-surface-hover` valait le fond de page, soit 1,00:1 — tout élément survolable posé sur la page avait perdu son retour visuel. Vérifié : 5 pages × 2 thèmes, ~2 300 éléments mesurés par compositage réel, **zéro manquement en thème sombre**, 101 titres relevés sans un seul illisible, `npm run build` vert | Le commanditaire juge le rendu. Passif non traité, antérieur : la démonstration de puce désactivée du guide de style, à 4,46:1 |
| 2026-08-17 | Retours du commanditaire sur A3 | Cinq observations, toutes tenues. **(1) LA PROGRAMMATION SORT DE LA PAGE D'ÉDITION et devient la page `/programmations` (`/en/programmes`)** : elle porte un sélecteur d'édition, et le laisser sous le titre « COP31 — Belém » revenait à afficher le programme du cycle PACO comme s'il était celui de la COP31. Le sujet d'un écran ne peut pas être une chose et son sélecteur une autre. La page d'édition ne garde qu'un **renvoi** (`components/event/ProgrammeLink.vue`) qui annonce ce qu'il y a derrière — « 20 activités publiées · 8 jours » — et part avec son édition déjà sélectionnée (`?edition=<slug>`). **(2) L'ÉDITION CONSULTÉE EST NOMMÉE EN TITRE** sur la nouvelle page, avec ses dates et son lieu, au-dessus des filtres : c'est la correction de fond du même défaut, et elle rend impossible de lire une activité sans savoir à quoi elle appartient. Le bandeau « vous consultez une archive » disparaît, il n'a plus d'objet. **(3) LES ÉCHÉANCES PASSENT AVANT L'ENCART D'APPEL** : la frise tient en cent pixels et répond d'un coup d'œil à « où en est-on », que l'encart ne fait ensuite que développer. **(4) LA FRISE NE SE REJOIGNAIT PAS — corrigé dans `UiStatusTimeline`** : en horizontal le trait partait de `50 % + 1,5 rem`, une valeur qui supposait des pastilles centrées dans leur colonne alors qu'elles sont calées à gauche ; en vertical il démarrait 8 px sous la pastille. La géométrie se déduit désormais de la seule pastille (`size-7`) — départ à 1,75 rem, axe à 0,8125 rem — et **les deux bouts touchent**, mesuré au pixel dans le navigateur en horizontal (132 → 412, marqueur suivant à 412) comme en vertical à 375 px. **(5) NAVIGATION** : « Programme » devient « **Programmations** » au pluriel et mène à la nouvelle page ; « Accueil » est retiré — le logo est déjà le lien d'accueil, et l'accueil ne fait que rediriger. Traductions déplacées de `pages/event.public.programme.json` vers `pages/programme.json` (clé racine `programme`), conformément à la règle « la clé racine est le nom du fichier ». **La couverture d'une activité passe en COLONNE DE DROITE**, en pendant du rail de date et d'heure : en bandeau pleine largeur, un 16:9 mesurait près de sept cents pixels de haut sur une carte de programmation et repoussait le titre sous la ligne de flottaison. La colonne fait 20 rem, l'image la remplit sur toute la hauteur et **n'allonge jamais la carte** — son cadre y est en position absolue. Deux pièges relevés en chemin, tous deux documentés dans le composant : donner des proportions à une image qui remplit la hauteur, dans une colonne de largeur `auto`, fait dériver la largeur de la hauteur puis l'inverse — la carte gonflait à 597 px ; et `min-w-0` ne neutralise la largeur minimale automatique que d'un **élément de grille**, si bien que repasser la carte en bloc sous 640 px la faisait déborder de l'écran d'un téléphone. **Les points de rupture de la carte sont désormais des requêtes de conteneur** (`@container`) et non ceux de la fenêtre : à `lg:` près, un écran de 1440 px donnait trois colonnes à une carte large de 591 px dans le guide de style, et le titre tombait sur trois lignes dans 219 px. `UiImage` gagne pour cela deux propriétés — `ratio="auto"` (le ratio est posé en style inline, qu'aucune classe utilitaire ne peut redéfinir) et `frameClass`, qui atteint le cadre plutôt que la racine. **Le bandeau de légende est retiré et son composant supprimé** : il énumérait les six états et les journées spéciales, et chacun de ces repères est déjà porté par l'activité elle-même — `UiSessionCard` écrit son état en toutes lettres et nomme ses journées spéciales avec leur pastille de couleur, le bloc du calendrier fait de même. L'exigence du prompt A3 — « un repère qui ne repose pas uniquement sur la couleur » — reste donc tenue là où elle compte, sur l'activité, et non dans un bandeau qui répétait avant de lire. Vérifié dans le navigateur : les deux langues, le sélecteur d'édition COP31 ↔ PACO 2026 avec titre et dates qui suivent, l'URL qui porte l'édition, la vue calendrier, 375 px sans défilement horizontal, `npm run typecheck` et `npm run build` verts | Prompt A4 — formulaire de soumission. Le lien « Déposer une proposition » reste le seul lien mort de l'écran |
| 2026-08-17 | Page publique de l'événement (A3) | **LE MODÈLE A ÉTÉ CORRIGÉ D'ABORD, comme le demandait le tableau des points bloqués** : les écarts n° 14 et 15 — pays de l'organisation et libellés de thématiques absents de `v_public_schedule` — étaient à trancher AVANT cet écran, et ils l'ont été. La vue expose désormais `organization_country_code`, `organization_country` et `themes` ; `020_reference.sql` gagne **`reference.term_badges()`**, jumelle de `terms_of()` : l'une filtre (tableau de codes indexable), l'autre affiche (libellé traduit et couleur, venus de la base). Base rechargée de zéro, `make check-db` vert, fonction éprouvée terme par terme. **Conséquence immédiate et visible** : `UiSessionCard` ne reçoit plus ni `themes` ni `organizationCountry` en propriétés de contournement — elle les lit sur sa ligne —, et le guide de style a perdu les deux recompositions qui les fabriquaient. **Une page** `app/pages/event/[slug].vue` (`/evenements/[slug]`, `/en/events/[slug]`) et **la page d'accueil remplacée** par la redirection vers l'édition en cours, choisie par les données et non par une constante. **Dix composants** sous `app/components/event/` : `Hero`, `CallBanner`, `Milestones`, `SpecialDays`, `Programme`, `ProgrammeFilters`, `ProgrammeGrid`, `ProgrammeCalendar`, `ProgrammeLegend`, `SessionDetail`. **vue-cal 4.10 introduit** — la bibliothèque que le planificateur A9 réutilisera —, en lecture seule, avec **le piège du fuseau réglé une fois pour toutes** : vue-cal place ses blocs à l'heure de la MACHINE, d'où `wallClockInZone()` qui convertit vers le fuseau de l'ÉDITION avant de lui passer quoi que ce soit. **Les deux vues partagent tout** : mêmes données, mêmes filtres, même sélection, et le jour affiché au calendrier EST le filtre de jour de la liste. **Le sélecteur d'année** distingue conférences et « autres activités » par `event_series.kind`, jamais par une liste de slugs. **Quatre éditions ajoutées aux mocks** — COP30 (Belém 2025), COP29 (Bakou 2024, fuseau `Asia/Baku`), le cycle de webinaires PACO 2026 (hors COP, sans pavillon donc sans appel) et PACO 2027 (annoncé, programme non publié) : sans elles, quatre états de l'écran n'avaient aucune donnée — séance terminée, appel clos, activité hors COP, programme à venir. **Deux corrections de composants trouvées en chemin** : `UiSelect` affichait « facultatif » sur un filtre et sa valeur « tous » n'était pas re-sélectionnable ; `UiStatusTimeline` affichait une heure sur une étape datée au JOUR (`results_expected_at` est une `date`) — « 15 novembre 2026 à 09:00 », heure née de la conversion de fuseau et que personne n'avait saisie. Vérifié dans un navigateur : les trois états de l'encart d'appel dont deux sur données réelles, le rebours à 44 jours, la bascule grille/calendrier avec conservation des filtres, le passage d'une année à l'autre et le retour, les webinaires PACO avec leur changement d'heure d'été (`UTC−5` en février, `UTC−4` en avril), 375 px en thème sombre sans défilement horizontal, thème clair, anglais complet, zéro clé brute, `make check-front` vert | Prompt A4 — formulaire de soumission. **Il crée la page `/deposer-une-proposition`, vers laquelle l'encart d'appel pointe déjà** : le lien est le seul de cet écran à ne mener nulle part, et il cesse de l'être avec A4. Deux obligations d'API relevées (n° 25 et 26) |
| 2026-08-17 | Rattachement à une organisation (A2) | **LE MODÈLE A ÉTÉ CORRIGÉ AVANT L'ÉCRAN, et c'était nécessaire** : mesuré sur la base, `find_similar_organizations('institut')` ne renvoyait **RIEN** — `similarity()` compare deux chaînes entières et rend 0,17 face à un nom long, sous le seuil de 0,3. Autrement dit, qui tapait le début du nom complet ne trouvait rien et se voyait proposer de créer la fiche existante : le défaut n° 1 de la v1, reproduit par la fonction censée le corriger. `040_organizations.sql` § 5 gagne donc **deux signaux de plus** (`word_similarity`, préfixe littéral, pondérés plus bas qu'une correspondance sur le nom entier) et **quatre colonnes** (`organization_type_code`, `city`, `verified_at`, `member_count`) sans lesquelles l'écran rechargeait une table à chaque frappe pour afficher « 4 membres ». Base rechargée de zéro, `make check-db` vert, résultats vérifiés terme par terme. **Une page** `app/pages/organization/join.vue` (`/rattachement-organisation`, `/en/join-organization`), **cinq composants** sous `app/components/organization/` — `MatchCard`, `DomainSuggestion`, `CreateForm`, `DuplicateWarning`, `MembershipSummary`, `JoinDialog`. **`mocks/organization-search.ts`** rejoue la fonction SQL en TypeScript — trigrammes, `word_similarity`, seuils relevés sur la base (`show_limit()`) — et rend **exactement les mêmes scores** : 60,0 / 125,0 / 100,0 / 51,4 / 55,0 sur les cinq termes comparés. **Deux personnes ajoutées au jeu** : Fatoumata Sy (adresse `@ujfc.org`, domaine vérifié en rattachement automatique) et son compte. **Cinq défauts trouvés en chemin et corrigés là où ils étaient** : `I18nText` promettait une valeur pour toute locale, `UiModal` ne s'ouvrait jamais si elle était montée déjà ouverte, `UiFormField` annonçait « facultatif » sur une barre de recherche, le layout public affichait « Se connecter » à qui venait de se connecter, et `ACCOUNT(1)` valait `DUPLICATE.osed` dans les mocks. **Le rattachement est FACULTATIF, et une garde le rend obligatoire là où il l'est** (arbitrage du commanditaire, en cours de session) : sortie explicite sur l'écran, middleware nommé `requires-organization` déclaré par les pages concernées, store `membership` qui répond à la question « cette personne peut-elle agir au nom d'une organisation ? » au même endroit pour tout le monde, et journal d'écritures dans les mocks pour que l'étape franchie le reste. **Le parcours a été branché** : rien ne menait à cet écran — la vérification d'adresse renvoie désormais vers `/connexion?redirect=<rattachement>`, et une entrée « Mon organisation » le garde atteignable ensuite. Vérifié dans un navigateur : les trois moments du prompt, le doublon OSED remonté deux fois par « OSED », le rattachement immédiat par domaine, la demande en attente, le refus de doublon exact rendu comme une information et non comme un mur, le parcours inscription → vérification → connexion → rattachement dans les deux langues, 375 px en thème sombre sans défilement horizontal (bouton mesuré à 44 px), anglais complet, zéro clé brute, `make check-front` vert | Prompt A3 — page publique de l'événement. **À trancher avant elle** : les écarts n° 14 et 15 (pays et thématiques absents de `v_public_schedule`), toujours ouverts. Deux obligations d'API écrites plus bas (n° 21 et 22) |
| 2026-08-17 | Authentification (A1) | **Cinq écrans** sous `app/pages/auth/` — connexion, création de compte, vérification d'adresse, mot de passe oublié, réinitialisation — aux URL françaises (`/connexion`, `/inscription`, `/verification-adresse`, `/mot-de-passe-oublie`, `/nouveau-mot-de-passe`) et anglaises, par `defineI18nRoute` : le FICHIER suit la convention de traduction (`pages/auth.login.json`), l'ADRESSE suit la langue. **Layout `auth`** sobre et centré, sans image de fond : logo, langue, thème, mentions légales, rien d'autre — chaque lien affiché est une occasion de quitter le parcours. **Six composants** sous `app/components/auth/` : `Screen` (tient à un seul endroit les états communs des cinq écrans), `Card`, `PasswordField` (révélation, remasquage à la perte du focus), `PasswordStrength`, `Federation` et `SecondFactor` — les deux emplacements prévus sans être implémentés —, `SignedInNotice`. **Un utilitaire pur** `utils/password-strength.ts` : trois exigences opposables — huit caractères, une majuscule, une minuscule — et, à part, une robustesse qui conseille sans bloquer. **Un store** `stores/auth.ts`, **un composable** `useResendCountdown()` (60 s), **deux middlewares** (`guest` ne redirige pas, `auth` retient la destination). **`mocks/auth.ts`** : 11 comptes dont quatre cas particuliers — second facteur, verrouillage, personne suspendue, adresse non vérifiée — et 6 jetons de courriel couvrant valide / périmé / déjà utilisé ; **26e personne ajoutée** à `people.ts`, inscrite et non vérifiée. `useApi()` gagne `send()` (écritures) et une section `auth` de 9 appels. **Aucune modification du modèle de données** : `030_identity.sql` couvrait tout ce dont les écrans avaient besoin. Vérifié dans un vrai navigateur : les six issues de connexion rendues une à une, les trois refus de jeton pour chacun des deux usages, l'inscription de bout en bout, le rebours de 60 s qui se termine et réactive le bouton, 375 px en thème sombre sans défilement horizontal, `make check-front` vert | Prompt A2 — rattachement à une organisation, qui reprend la personne créée ici (`primary_organization_id` nul). Trois obligations d'API écrites plus bas (n° 18 à 20) ; les pages `/mentions-legales`, `/confidentialite`, `/conditions-utilisation`, `/aide` sont liées mais n'existent pas encore |
