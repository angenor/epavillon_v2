# Journal — 2026-08-22

> Extrait de la [progression](../../PROGRESSION.md). Une ligne par session, la plus récente en haut.

| Date | Session | Fait | À suivre |
|------|---------|------|----------|
| 2026-08-22 | 🔌 **B7 — le raccordement : le site parle à l'API** | **Le client TypeScript est ENGENDRÉ**, jamais écrit — `make openapi`, par un **binaire** et non par `/api/docs` : la route ne décrit que les modules montés, et engendrer depuis une base de développement retirerait du client les chemins d'un module éteint ce jour-là. 156 chemins, 175 opérations, les 65 codes stables. **L'OpenAPI ne décrit pas ses corps** — elle les NOMME et confie leur définition au site ; ce lien, purement déclaratif, est désormais **vérifié** par `make check-api-contract`, branché sur `check-front` : 128 appels sur 156 chemins, 123 formes annoncées **toutes définies**, 19 routes en attente **nommées**. Un garde-fou de plus côté Rust : l'export **refuse un `operation_id` porté deux fois** — le défaut existait (`fiche`), et il produisait un TypeScript qui ne compilait pas, avec un message ne nommant ni la route ni le module. **152 écarts tranchés**, chacun avec sa preuve lue des deux côtés, et les verdicts « bloquant » ou « défaut de l'API » passés par une **contre-expertise** chargée de les réfuter — **quatorze renversés et trois ajoutés**, dont trois défauts d'API qui n'en étaient pas et auraient coûté du travail inutile en Rust. **Quatre routes manquaient au socle** et arrêtaient net trois écrans du jalon : pays, langues, taxonomies, drapeaux. Elles vivent dans le crate `api`, `reference` et `platform` étant transverses, et **les drapeaux sortent RÉSOLUS** — `is_feature_enabled()` tranche en base, rendre la table obligeait le site à réimplémenter le déploiement progressif et publiait les identifiants des personnes visées. **Sept corrections d'API dans des modules livrés** : la grille d'évaluation portée par l'appel public, l'espace organisation qui rendait 404 à un non-membre (200 + `null` : l'indiscernabilité ne demandait pas un statut d'erreur), la fiche de performance toujours pleine, **deux clés JSON émises en double** par un `flatten` — cela marchait par accident —, la santé d'exploitation ouverte à un administrateur détaché, les pays inactifs écartés, et cinq noms de contrat qui désignaient autre chose que ce que l'API rend. **Neuf périmètres corrigés côté site**, chacun relu par un second agent. **Deux défauts trouvés en ÉPROUVANT, pas en lisant** : une **instance d'erreur ne traverse pas le rendu serveur** — `devalue` refuse tout ce qui n'est pas un objet simple, et une erreur posée dans un store faisait rendre **500** à la page entière, au seul moment où ce chemin sert, c'est-à-dire quand l'API est en panne ; et **le rendu serveur ne peut pas renouveler un jeton**, le cookie de rafraîchissement étant limité à `/api/auth` — la rotation est donc un geste du navigateur, exclusivement. **Vérifié contre l'API réelle** : le parcours de session entier (inscription, refus avant vérification, connexion, deux cookies aux bonnes portées, `/auth/me`, **rotation `renewed`**), le refus d'origine avec son message français, **250 options de pays venues de la base** et non des mocks, le mode dégradé (`200` et bandeau français, là où c'était `500`), et la reprise sans rechargement. **`make check` passe en entier depuis la racine — base détruite et rechargée de zéro, 857 tests au vert, `clippy -D warnings` sans un avertissement.** **Un test intermittent de B6 corrigé au passage** : sa clé d'unicité porte l'horodatage **à la seconde**, et il relisait `now()` deux fois — une fois sur dix, les deux appels tombaient de part et d'autre d'une seconde et le test échouait en mesurant exactement le contraire de ce qu'il affirme. | **Le dépôt de fichiers** reste à brancher — photo d'intervenant et pièces d'un dossier sont saisies puis perdues, faute d'envoi composite. **Trois modules manquent à l'API** et ce n'est pas du raccordement : `live` (incidents), `content` (accueil et vitrine), `analytics` (tableau de bord) — leurs écrans lisent des exemples **et le disent**. Six obligations d'API consignées, dont **ajouter une dénomination ou un domaine**, dont dépend la règle métier n° 1. Une décision au commanditaire : une fois l'édition publiée, **republier ne publie plus rien**. |
| 2026-08-22 | 🏁 **B6 — phase 11 : le disque ne se remplit pas tout seul (US9)** | T185 à T198, et **T205 vérifiée**. **JALON 3 ATTEINT : les neuf histoires sont livrées.** **Un objet purgé a réellement quitté le stockage** — l'absence est vérifiée sur le stockage lui-même, jamais sur une relecture en base : une ligne peut porter son instant de purge alors que les octets sont toujours là, et c'est exactement ce qu'on écrirait en oubliant d'appeler le stockage. **La consommation baisse à la suppression logique, pas à la purge** : c'est le déclencheur de quota qui la rend, déclinaisons comprises, et la fenêtre de rétention ne coûte donc aucun octet. **L'écart n° 128 est refermé en disant deux** : le même fichier déposé par deux organisations ne donne qu'une ligne, et la suppression par la première est refusée **avec le nombre de fiches** qui l'utilisent — sans quoi elle ferait disparaître l'image de la seconde, sans que rien ne l'annonce. **Une purge dont l'objet a déjà disparu aboutit** : l'objectif est qu'il ne soit plus là, pas qu'il ait fallu l'enlever. **Un objet fautif ne bloque pas le disque** : chaque purge est comptée pour son compte, une clé qui résiste est tracée et reprise au passage suivant, et la replanification a lieu de toute façon. **Trois chaînes récurrentes de plus** — purge, réconciliation des quotas, partitions du journal d'expédition (écart n° 137) —, sur le patron de B1 : elles posent leur propre occurrence suivante, la clé d'unicité porte le créneau, et dix redémarrages dans la journée n'en produisent pas dix. **Deux défauts trouvés en écrivant les tests, et corrigés** : les travaux récurrents du média atterrissaient dans la file par défaut alors que leur gestionnaire écoute « media » — `NewJob::new()` pose la file par défaut, et le travail se serait empilé sans erreur ni trace ; et le harnais de test réservait **par gestionnaire**, ce qui faisait tenir au premier les travaux des deux autres dès qu'ils partagent une file. **Trois routes de back-office**, plates sous `/admin` et jamais dans un `web::scope` propre au module : le préfixe est partagé avec B5 et B6, et un scope de plus rendrait muettes les routes des autres. `cargo fmt --check`, `clippy -D warnings`, **856 tests au vert**, frontières de crates sans arête, aucune écriture hors schéma, aucun fichier au-dessus de mille lignes. | **Phase 12** — T199 à T209 : les trente-trois routes sur la vraie application, les seize codes d'erreur dans l'OpenAPI engendrée, les sept parcours du quickstart **sur Garage réel** (seul moment où S3 est exercé), et `make check` en entier depuis la racine. **T201 a changé de critère** : trois mises en file subsistent légitimement, celles des trois chaînes récurrentes. |
