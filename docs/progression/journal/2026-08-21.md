# Journal — 2026-08-21

> Extrait de la [progression](../../PROGRESSION.md). Une ligne par session, la plus récente en haut.

| Date | Session | Fait | À suivre |
|------|---------|------|----------|
| 2026-08-21 | **B6 — phase 10 : préférences et délivrabilité (US8)** | T166 à T184. **La plateforme cesse d'écrire à qui ne veut plus la lire**, et c'est la réputation du domaine qui est en jeu : une adresse en rebond dur qui continue de recevoir fait monter le taux de rebond **pour tous les envois**, confirmations d'inscription comprises. **L'écart n° 133 est refermé sans qu'aucune ligne d'un module livré ne change** : une adresse supprimée ne reçoit plus rien, la trace existe quand même et **dit pourquoi** — écarté n'est pas rebondi, le message n'est jamais parti. **Couper un canal laisse l'autre** : qui coupe le courriel garde l'avis à l'écran, et inversement — les confondre ferait d'un « plus de courriels » un « plus rien savoir ». **Couper un type critique est enregistré et sans effet, et la lecture le DIT** : refuser laisserait l'écran sans réponse à donner, et sans `is_overridable` la personne croirait s'être désabonnée d'une annulation de séance. **Trois faits de même nature forment UNE ligne portant un compte** — le modèle le décrit, aucune fonction ne le faisait — et **une ligne lue cesse de se regrouper**, l'index d'unicité étant partiel. **Le lien de rebond est relatif** : aucun nom d'hôte de préproduction n'entre en base. **Une annonce du fournisseur rejouée est ignorée**, jamais dupliquée ; un rebond **dur** inscrit l'adresse, un **souple** non — une boîte pleine n'est pas une adresse morte. **Une suppression échue se lève toute seule**, sans travail récurrent. **La porte d'ingestion suit son jeton** : montée elle réclame le bon, absente elle rend **404** et non 401 — une porte sans secret vaut mieux fermée, et un 401 l'annoncerait à qui sonde le port. **Cinq choses ont dû être décidées** : **une** exception à « la correspondance événement → avis est une donnée », parce que le catalogue nomme `programme.registration.confirmed` que **personne n'émet** — sans elle, toute inscription confirmée resterait sans avis, en silence ; la trace se retrouve par l'identifiant que **l'API** a remis au site, le contrat d'envoi du noyau n'en rapportant aucun — bâtir l'ingestion sur celui du fournisseur aurait donné un dispositif complet qui **n'aurait jamais rien trouvé** ; **deux audiences de diffusion et pas une troisième**, qu'il faudrait inventer ; la liste de suppression **n'a pas de travail de purge**, la fonction du modèle suffisant et un second dispositif divergeant en silence ; et l'épreuve de la garde frappe le **décorateur**, monter le module Organisations ici étant interdit par la frontière de crates — la limite est consignée plutôt que masquée. `fmt`, `clippy -D warnings` et **837 tests** au vert. Prochain pas : **phase 11, US9** — le disque ne se remplit pas tout seul. | **Phase 11, US9** — la purge des médias orphelins |
| 2026-08-21 | **B6 — phase 9 : les modèles de messages (US7)** | T152 à T165. Une faute d'orthographe se corrige au back-office, dans les deux langues, et **un retour arrière est une republication** — rien n'est jamais effacé pour le permettre. **Mesuré sur le texte du courriel réellement expédié**, en trois vagues sur trois décalages : sans révision publiée le texte de secours part et la trace porte un modèle **nul** ; la seconde révision publiée, le courriel change et la trace porte **son numéro** ; la première republiée, l'ancien texte revient. **Le piège du lien est refermé et relu EN BASE** : un `href="{{lien_participation}}"` survit à l'assainissement — pour un analyseur d'URL c'est une adresse relative, et la normaliser détruirait la variable, donnant un lien mort que **seule la réception** révèle. Un `<script>` disparaît, un `onclick` aussi, et une balise refusée **n'emporte pas le texte qu'elle contient**. **Une variable non promise fait refuser la publication, en la nommant** et en listant ce que le type promet — à l'envoi il serait trop tard pour corriger sans que personne n'ait rien reçu —, **y compris citée dans la seule version anglaise**. **Une variable manquante à l'exécution fait échouer l'envoi** : rien ne part, la ligne n'est pas marquée partie, et le motif la nomme. **Cinq choses ont dû être décidées** : la permission des modèles se tient sur la portée **globale** — un modèle sert toutes les éditions, et l'ouvrir à qui administre une COP laisserait réécrire les courriels de toutes les autres, l'administratrice d'édition étant explicitement refusée ; **écrire et publier sont deux gestes**, sans quoi une correction à moitié faite partirait à deux mille personnes ; **l'aperçu ne se refuse pas** sur une variable absente, contrairement à l'envoi — il sert à regarder une mise en page, et ce qui manque prend un exemple **visible** ; les variables citées sont **relevées**, jamais déclarées, et dans **toutes les langues** ; et la trace est annotée **après coup**, par `job_id`, parce qu'enrichir le message casserait les six sites de construction des modules livrés. **Un défaut réel a été trouvé et corrigé en chemin, et il était intermittent** : les épreuves d'envoi posaient l'échéance d'un travail depuis l'horloge de la **machine** quand la file compare à celle du **conteneur**. Quelques secondes de dérive suffisaient à placer `run_at` dans le futur de la base : aucun travail réservé, et un test rouge **une fois sur deux** sur une chaîne qui fonctionne — le symptôme le pire qui soit, puisqu'on finit par le relancer jusqu'à ce qu'il passe. L'heure vient désormais de la base, des deux côtés. `fmt`, `clippy -D warnings` et **824 tests** au vert. Prochain pas : **phase 10, US8** — chacun choisit ce qu'il reçoit, et une adresse morte cesse d'être sollicitée. | **Phase 10, US8** — préférences et délivrabilité |
| 2026-08-21 | **B6 — phase 8 : le rappel part une fois, et une seule (US5)** | T131 à T151. **JALON 2 atteint.** **Les rappels de la COP31 partent, et l'organisation lit ce qui part.** **Le piège annoncé était réel, et il est désamorcé par le vrai chemin** : `programme.registration.confirmed` n'est émis par personne, et le test le mesure — zéro annonce de ce type, trois annonces de **création** portant `status: registered`. Un consommateur branché sur le commentaire du modèle **dormirait pour toujours** ; celui-ci branche sur le **statut de la charge utile**. **Les annonces relayées sont celles que la base a réellement émises**, lues dans l'outbox et jamais fabriquées à la main : c'est la seule façon d'éprouver la charge utile du déclencheur plutôt que celle qu'on aurait imaginée. **Annuler puis réinscrire RÉACTIVE la ligne** — aucune ligne de plus, `cancelled` revient à `pending` : la recréer se heurterait à la clé d'unicité, en silence, et qui se désiste puis revient ne recevrait plus jamais rien. **Un report DÉPLACE**, il ne recrée pas : trois heures de décalage, six lignes suivies, celle qui était déjà partie inchangée. **Dix rejeux ne produisent ni rappel ni travail supplémentaire**, garde d'inbox volontairement absente du test — ce qui reste doit tenir tout seul. **Une matérialisation émet UNE annonce et exactement un travail par rappel**, jamais le double. **Un courriel par destinataire, et le travail rejoué n'en écrit pas de second** ; worker tué avant son marquage puis relancé : trois courriels, une seule fois. **Cinq choses ont dû être décidées** : le rappel est marqué **parti AVANT** l'envoi — un rappel manquant se voit et se rejoue, un rappel doublé est irrattrapable, et le prix de la mort brutale est écrit ; un report déplace **la ligne ET son travail**, un rappel portant deux horloges, d'où `kernel::jobs::reschedule_by()` — le module ne peut pas écrire `platform.jobs` sans sortir de son schéma ; une inscription reprise **garde son `job_id`**, le lui rendre à nul ferait une seconde mise en file que l'unicité absorberait, laissant la ligne **sans travail** ; des huit annonces de séance, **`postponed` annule et `rescheduled` déplace**, `live` et `completed` ne faisant rien ; et **deux motifs d'écart** sont ajoutés — un décalage passé sous le nez, un canal sans expédition —, sans quoi ces lignes resteraient en attente pour toujours, ce qui se lit « ça va partir ». **Un type sans modèle publié part quand même**, avec un texte de secours dont la trace porte `template_id` **nul** : c'est elle qui dit que personne n'a écrit ce courriel. **Un écart nouveau, n° 138** : aucune page de séance n'existe dans le front, et le lien de participation pointe donc la page de l'édition, ancrée sur la séance. `fmt`, `clippy -D warnings` et **817 tests** au vert. Prochain pas : **phase 9, US7** — les textes des courriels se corrigent sans redéploiement. | **Phase 9, US7** — les modèles de messages |
| 2026-08-21 | **B6 — phase 7 : le calendrier des rappels (US4)** | T116 à T130. **L'organisation lit ce qui part à ses inscrits, sans savoir à qui** : l'écart n° 34 est refermé, et celui que B4 avait laissé — n° 108 — avec lui. **T116, T117 et T118 étaient déjà faites** en phases 1 et 2 : le préfixe `/sessions` est composé une seule fois par l'API et un test frappe les routes gardées de B5. Vérifiées, cochées, pas retouchées. **Le test qui compte** : quarante inscrits et quatre décalages rendent **quatre** lignes portant chacune quarante destinataires — jamais cent soixante, l'erreur qui ressemble le plus à un résultat juste. **Le second** : le balayage de la charge utile **sérialisée entière** n'y trouve aucun identifiant de personne, aucun nom, aucune adresse — champ par champ laisserait passer celui qu'on ajoutera demain. **Deux canaux sont deux envois** : huit lignes et non quatre. **Une seule personne qui attend retient le groupe entier** : trente-neuf partis et un en attente donnent « en attente », jamais « parti ». **Le miroir Rust est confronté au SQL** sur six mélanges d'états posés sur les mêmes trois lignes — sans quoi le miroir serait un second dispositif à tenir d'accord avec le premier. **L'espace organisation cesse de rendre une liste vide** et appelle **la même fonction du modèle** : deux agrégations divergeraient en silence, un nombre de destinataires faux ressemblant à un nombre juste. **La garde n'est pas un périmètre** — une organisation n'administre rien : adhésion **active** à l'organisation qui anime, ou `programme.registration.manage` sur l'édition ; une adhésion révoquée ne suffit plus. **Trois choses ont dû être décidées** : le refus de garde est un **403** et non le 404 du reste du module, une séance figurant au programme public ; les **noms de clés** du contrat sont écrits deux fois — la fonction nomme `offset_minutes`, le front attend `offset_before` — et deux tests les confrontent en toutes lettres ; et le miroir de consolidation est éprouvé contre la base plutôt que contre lui-même. `fmt`, `clippy -D warnings` et **800 tests** au vert. | **Phase 8, US5** — le rappel part une fois, et une seule |
| 2026-08-21 | **B6 — phase 6 : l'administrateur décide ce qui part (US6)** | T101 à T115. **Une édition peut enfin avoir une règle de rappel**, et l'on sait laquelle s'applique. Cette histoire passe **avant** US4 et US5, contre l'ordre des priorités, et c'est assumé : rien ne sème de règle, et sans écriture ni le calendrier ni les envois ne se démontreraient autrement qu'en posant une ligne à la main en SQL — ce qui prouverait la lecture sans prouver le chemin réel. **L'écriture est l'instrument de mesure des deux histoires suivantes.** **Une LISTE de décalages, jamais un décalage seul** : les quatre valeurs du défaut sont cumulées, et une écriture qui n'en accepterait qu'une ferait croire le contraire jusqu'au jour de la séance. **La règle de séance remplace celle de l'édition sans s'y ajouter** — deux décalages posés, **deux** rendus et non six —, et la lecture rend l'**origine**, sans laquelle une règle de séance tronquée ne se distingue pas d'une règle d'édition tronquée. **Une seconde écriture modifie la première**, `count(*)` restant à un : l'unicité du modèle est une modification, jamais une erreur. **Trois choses ont dû être décidées** : l'écriture passe par **deux** fonctions parce que les deux index d'unicité sont **partiels** et qu'une clause `ON CONFLICT` ne peut en nommer qu'un — les fondre demanderait de composer le SQL, ce que ce dépôt ne fait nulle part ; le refus des décalages **nomme lequel des quatre cas** s'applique, dont le **doublon**, seul cas que la base ne refuse pas et que la clé d'unicité absorberait **en silence** ; et couper une règle **annule ses rappels encore à traiter** — dans cet ordre, la colonne étant `ON DELETE SET NULL` — sans décommander les travaux déjà en file, l'envoi relisant l'état de la ligne. **Couper et supprimer sont deux gestes distincts**, et les deux existent. `fmt`, `clippy -D warnings` et **788 tests** au vert ; les quatre contrôles mécaniques du quickstart aussi. | **Phase 7, US4** — le calendrier des rappels : une ligne par décalage et par canal, un **nombre** de destinataires et jamais un nom, et l'écart n° 34 qui se referme |
| 2026-08-21 | **B6 — phase 5 : un fichier se rattache à ce qu'il illustre (US3) — JALON 1 ATTEINT** | T083 à T100. **L'obligation que B3 avait laissée est refermée**, et c'est **sa** route qui le dit : les trois déclinaisons d'une édition s'enregistrent **en un geste**, et `GET /events/{slug}` les rend résolues — **sans qu'une ligne du module Événements change**, le rattachement vivant dans le schéma Média. **La mesure est dédoublée, et les deux moitiés comptent** : les trois appels exacts que le dépôt de données de B3 fait à la fonction du modèle, côté module ; et la route elle-même, sur la vraie application, avant et après le lot. Frapper cette route depuis le crate Média était impossible — la dépendance de développement vers l'API ferait échouer le contrôle de frontières, `cargo tree` listant aussi les dépendances de développement. **« Remplacer en lot » a dû être défini** : le lot décrit l'état voulu des rôles qu'il **nomme**, et ne touche à aucun autre. Trois conséquences d'un coup — les trois déclinaisons partent ensemble, une valeur nulle en retire **une** sans toucher aux deux autres, et une galerie **se réordonne** en renvoyant la même liste dans un autre ordre, si bien qu'**aucune route de réordonnancement n'a besoin d'exister**. **Les cinq refus du modèle sont traduits, jamais réimplémentés** : le déclencheur les lève sans nom de contrainte et trois partagent le même état d'erreur, alors le service les distingue **par ce qu'il a lui-même vérifié en amont** — et le refus de forme cite ses **quatre nombres**, dimensions reçues, rapport obtenu, rapport attendu, tolérance. Un objet image **sans dimensions relevées passe** sur un rôle qui impose une forme : c'est le relevé qui a échoué, pas le cadrage. **Détacher ne détruit pas l'objet**, relu après coup en base **et sur le stockage**. **Un arbitrage assumé** : la lecture des médias d'une entité ne masque pas les objets encore en traitement — les masquer ferait croire que le téléversement a échoué —, là où les lectures publiques, elles, ne rendent que le servable. **Un écart au contrat est consigné plutôt que comblé en douce** : le contrat annonce que le dépôt pose le rattachement dans la même transaction, aucune tâche ne le demande, et l'ajouter changerait le comportement d'une route livrée. `fmt`, `clippy -D warnings` et **777 tests** au vert ; les quatre contrôles mécaniques du quickstart aussi. **Jalon 1 atteint, T001 à T100** : un fichier entre, devient servable, et se rattache. | **Phase 6, US6** — l'écriture des règles de rappel, qui passe avant US4 et US5 : rien n'en sème, et sans elle ni le calendrier ni les envois ne se démontrent |
| 2026-08-21 | **B6 — phase 4 : le fichier devient servable sans faire attendre personne (US2)** | T068 à T082. **Le traitement différé.** Une photographie déposée s'affiche **tout de suite** — l'adresse de l'original est là, la liste des déclinaisons est **vide mais présente** —, puis le worker la mesure, la fait analyser, en fabrique trois tailles et la passe à l'état servable, sans nouveau geste. **Le travail est consommé, jamais créé** : c'est le déclencheur du modèle qui l'enfile, et rien dans ce module n'appelle `enqueue` ni `emit`. **Un défaut bloquant a été trouvé en chemin, et il était entièrement silencieux** : `platform.claim_jobs()` filtre strictement sur la file, et le worker ne réservait que `default` — or le déclencheur enfile dans « media ». Le travail serait resté en file **pour toujours**, sans erreur ni trace. **La file devient une déclaration du gestionnaire**, et le worker écoute exactement celles que ses gestionnaires nomment ; aucun module livré ne change. **Le test qui compte est au vert, et par le vrai chemin** : worker tué **avant son marquage**, travail rendu à la file par `reclaim_stalled()` — charge utile intacte, ce qu'un travail déjà marqué réussi n'aurait plus —, puis relancé ; `count(*)` sur `media.renditions` rend **trois**, jamais six. L'épreuve est faite **deux fois**, parce que deux gardes différentes la tiennent : la sortie immédiate sur un objet déjà servable, et le relevé des déclinaisons déjà faites sur un objet resté en traitement. **Le redimensionnement vit sur une tâche bloquante dédiée**, comme le hachage de mot de passe en B1. **Trois autres choses ont dû être décidées** : la durée d'un MP4 se lit dans sa boîte `mvhd` plutôt qu'en ajoutant un décodeur — soixante lignes, aucun binaire externe, et l'absence de relevé reste **déclarée** pour les autres conteneurs ; le motif d'un échec n'est écrit **sur aucune colonne nouvelle** mais lu là où la file et la déclinaison l'écrivent déjà, l'objet ne passant en échec qu'au **dernier essai** ; et la transparence se mesure **sur les pixels**, un canal alpha opaque n'étant pas de la transparence — s'y fier produirait un PNG là où un JPEG est cinq fois plus léger. **Le moteur `none` écrit « non pris en charge » et son nom, jamais « sain »** : l'objet devient bien servable, et la trace dit que personne n'a regardé. `fmt`, `clippy -D warnings` et **765 tests** au vert ; les trois contrôles mécaniques du quickstart aussi. | **Phase 5, US3** — le rattachement : table blanche, rôle exclusif, forme attendue, et **l'obligation que B3 a laissée**, les trois déclinaisons d'une édition |
| 2026-08-21 | **B6 — phase 3 : un fichier entre dans la plateforme (US1)** | T050 à T067. **Le premier octet de la plateforme.** Dépôt en un seul geste, corps composite, métadonnées **avant** le fichier — ce qui permet de refuser un type, un poids ou un droit sans avoir lu un octet. **L'empreinte est calculée pendant la réception** : un contenu déjà connu rend l'objet existant et **n'écrit rien**, ce que le test mesure sur le nombre de lignes, sur l'identifiant rendu et sur le stockage lui-même. **Le service n'émet rien et n'enfile rien** — après un dépôt, `outbox_events` compte **une** ligne et `platform.jobs` **une**, jamais deux ; une déduplication n'en produit **aucune**. **Le quota est opposable dans les deux sens** : le contrôle préalable et le refus de `tg_enforce_quota` sortent sous le **même** code, et les trois chiffres partent dans le message français que l'écran affiche — le corps d'erreur du noyau ne porte pas de charge structurée, et lui en ajouter une pour un seul cas modifierait le noyau. **Trois choses ont dû être décidées en chemin.** Le contrat de stockage a gagné `put_stream` : sans lui, un fond vidéo de deux cents mégaoctets tiendrait entier en mémoire, et la promesse du plan serait fausse. Côté S3, le flux passe par un **tampon local** — SigV4 signe l'empreinte de la charge utile, qui doit donc être connue avant le premier octet. Et le corps composite d'Actix **n'est pas `Send`** : la lecture reste sur le fil de la requête et pousse ses tranches dans un **canal borné à quatre**, que le dépôt consomme sur une tâche — la contre-pression est ce qui tient la promesse du flux. **Les gardes sont éprouvées à la base** : un hors-périmètre et un inexistant rendent le **même** refus, mot pour mot ; le logo exige un référent et non un simple membre ; une combinaison sans garde est refusée ; et la garde **fermée** des Formations écrit son motif dans la trace. `fmt`, `clippy -D warnings` et la suite complète au vert. | **Phase 4 — US2** : le traitement différé. Mesure, analyse, déclinaisons, passage à l'état servable — et le test qui compte : worker arrêté puis relancé, `count(*)` sur `media.renditions` rend le nombre configuré, jamais le double. |
| 2026-08-21 | **B6 — phases 1 et 2 : le socle des deux crates** | **Le schéma a changé une fois, et une seule** : `engagement.session_reminder_schedule()` ajoutée à `110_engagement.sql` § 6 — une fonction de lecture, ni table ni colonne ni type. Base **détruite et rechargée**, seize schémas vérifiés, fonction présente. **Deux crates créés**, `media` et `engagement`, sans aucune arête entre eux ni vers un autre module — vérifié par `cargo tree`, pas supposé. **Seize codes d'erreur** au catalogue du noyau, **deux modules de contrats** dont l'en-tête dit pourquoi les événements de la base n'y figurent PAS, **quatorze noms d'événements** ajoutés à `contracts/programme.rs` — ils n'étaient nulle part, et chacun les aurait écrits en littéral dans son coin. **Le domaine pur des deux modules est complet et éprouvé sans base** : la table de gardes, la convention de clé d'objet, les prédicats de la table blanche, le jeu de déclinaisons, les décalages en minutes dans les deux sens, la substitution de variables, et **l'assainisseur du courriel dont le test prouve qu'un `href="{{lien}}"` survit**. **Le stockage vit derrière un contrat à deux implémentations**, la signature S3 est écrite ici et **reproduit les vecteurs d'exemple d'AWS** — le seul moyen de séparer une erreur de code d'une erreur de configuration. **Le décorateur d'envoi est posé et composé dans l'API comme dans le worker** : les six courriels de B1 et B2 passent désormais par la liste de suppression et par le journal, **sans qu'aucun module livré ne change d'une ligne**. **Le préfixe `/sessions` est rouvert et recomposé** — la séquence a été menée d'un trait, et un test frappe les routes gardées de B5 pour prouver qu'aucune n'est devenue muette. **Deux surprises, toutes deux relevées par le harnais** : la table blanche porte **huit** couples et non six — `programme.sessions` et `training.trainings` s'y ajoutent, et le module Formations ne déclarant aucune permission, sa garde est **fermée avec son motif écrit** ; et le porteur d'un dossier se lit dans une **colonne**, pas dans la table de liaison, qu'un déclencheur alimente. **`make garage-init` importe désormais une clé fixe** et `check-db` l'appelle : `down -v` n'invalide plus les identifiants du `.env`. `cargo fmt`, `clippy -D warnings` et la suite complète au vert. | **Phase 3 — US1** : le dépôt, la déduplication par empreinte, le quota opposable. Le service **n'émet rien et n'enfile rien** : les deux déclencheurs le font, et T064 compte les lignes d'outbox **et** de `platform.jobs`. |
| 2026-08-21 | **B6 découpé — 209 tâches en 12 phases** | [`specs/006-media-engagement/tasks.md`](../../../specs/006-media-engagement/tasks.md) : **209 tâches, dont 73 de test** et huit vérifications mécaniques, une phase par histoire et **trois jalons**. **Six avertissements en tête, cinq décrivant un défaut silencieux** — dont le plus grave : **`programme.registration.confirmed` n'existe pas**, et un consommateur écrit d'après le commentaire du modèle ne serait jamais réveillé. **Une dépendance que le plan n'avait pas nommée** : **l'écriture des règles de rappel doit précéder le calendrier et les envois**, contre l'ordre des priorités — rien ne sème de règle, et sans elle les deux autres histoires ne se démontreraient qu'en posant une ligne à la main en SQL. L'écriture devient leur **instrument de mesure**, comme la programmation publique en B5. **Une séquence à ne pas interrompre** : entre T116 et T117, les dix-sept routes de B5 sont **muettes** — le préfixe `/sessions` est rouvert et recomposé, et deux `web::scope` du même préfixe ne se complètent pas. **Jalon 1 à T100** : un fichier entre, devient servable et se rattache. **Jalon 2 à T151** : les rappels partent, et les écarts n° 34 et n° 108 se referment. | `/speckit-implement`. **T003 d'abord** : le schéma a changé, et sans `down -v` la compilation échoue sur un message sans rapport avec la cause. |
| 2026-08-21 | **B6 planifié — 31 décisions, 33 routes, une modification du modèle** | [`specs/006-media-engagement/`](../../../specs/006-media-engagement/plan.md) — `plan.md`, `research.md`, `data-model.md`, `contracts/` et `quickstart.md`. **2 crates, 33 routes, 5 travaux différés, 2 consommateurs d'outbox, 2 événements émis — six le sont par la base —, 16 codes d'erreur.** Contrôle constitutionnel passé **deux fois**. **Deux choses qu'aucun des trois plans précédents n'avait pu écrire** : **aucune entorse au principe VIII**, et **aucune écriture hors du schéma du module** — le contrôle mécanique du quickstart porte donc aussi sur `reference` et `content`, plus strict que celui de B3, B4 et B5. **Une modification du modèle, et une seule** : `engagement.session_reminder_schedule()`, **fonction de lecture** — ni table, ni colonne, ni type. Elle existe parce que l'agrégat des rappels a **deux** lecteurs et que deux agrégations divergeraient **en silence** : un nombre de destinataires faux ressemble à un nombre juste. Précédent exact de `media.attached_image()`, dont l'en-tête porte mot pour mot ce raisonnement. **La spécification est amendée, et l'amendement est daté** : le téléversement passe de trois temps à un seul geste. H1 l'annonçait comme repli, et la raison décisive est que `DraftUpload` **ne porte aucune empreinte** — le serveur doit donc la calculer, ce qu'il ne peut faire qu'en voyant passer le flux. **Cinq points tranchés plutôt que laissés à l'implémentation**, chacun parce qu'une décision tacite y produirait une faute **à l'exécution**. Deux méritent d'être connus : **une inscription reprise doit RÉACTIVER ses rappels** — la clé d'unicité porte sur (séance, personne, canal, décalage) **sans condition d'état**, les lignes annulées existent toujours, et `ON CONFLICT DO NOTHING` ne les ressuscite pas : qui se désiste puis revient ne recevrait **plus jamais rien** ; et **l'assainisseur doit laisser passer les variables d'URL** — un `href="{{lien}}"` normalisé donne un lien mort, qui ne se voit qu'à la réception du courriel. **Trois dépendances nouvelles** — `actix-multipart`, `image`, `hmac` **0.12 et non 0.13** (la 0.13 exige `sha2` 0.11, le workspace tient la 0.10). **Le SDK S3 d'Amazon est écarté** : quarante crates payés à chaque `make check-back` d'un dépôt sans intégration continue, alors qu'une erreur de signature est **bruyante et totale**, jamais silencieuse — l'argument d'`ammonia` ne s'applique pas. **Critère de bascule écrit d'avance** : une demi-journée. **Le préfixe `/sessions`, livré par B5, est rouvert** et composé par l'API : deux `web::scope` du même préfixe ne se complètent pas. | `/speckit-tasks`. **Le SQL n'a pas encore bougé** : le plan l'a décidé, l'implémentation l'écrira — et il faudra alors `down -v`, plus une entrée dans `modele.md`. |
| 2026-08-21 | **B6 spécifié — Média + Engagement** | [`specs/006-media-engagement/spec.md`](../../../specs/006-media-engagement/spec.md) : **9 histoires, 15 cas limites, 113 exigences, 29 critères, aucune modification du SQL, aucune ligne de Rust.** **Une exigence du prompt est déjà tenue, et la spécification le dit plutôt que de la refaire** : l'écart n° 32 — assainir le HTML de la présentation détaillée — a été livré par B4 le matin même, à l'écriture, sur l'**unique** chemin d'écriture de la colonne, vérifié en cherchant toutes ses occurrences dans `backend/`. Le refaire produirait un second filtre, et il ne pourrait pas vivre ici : la colonne appartient à un schéma que ces deux crates n'ont pas le droit de toucher. **L'intention est tenue là où elle a de la valeur** : le corps HTML des **modèles de courriel**, saisi par un administrateur et envoyé à des milliers de personnes. **Douze écarts nouveaux, n° 126 à 137.** Le plus grave, n° 126 : **`programme.registration.confirmed` n'existe pas**. Le commentaire du modèle annonce que la matérialisation des rappels est déclenchée par cet événement ; l'énumération des statuts ne le contient pas, et le déclencheur émet une **création** portant le statut. Un consommateur écrit d'après ce commentaire ne serait **jamais réveillé** — aucun rappel ne partirait, sans erreur, et personne ne s'en apercevrait avant le jour de la séance. Relevé en lisant le corps du déclencheur, pas le commentaire. **Deux écarts engagent l'architecture** : **aucune permission `media.*` n'existe** dans le modèle (n° 127) — le droit de rattacher devient donc le droit d'écrire sur l'entité porteuse, et un test exige qu'aucune ligne de la table blanche ne reste sans garde ; et **les courriels de B1 et B2 ne passent ni par le journal d'expédition ni par la liste de suppression** (n° 133), si bien qu'une adresse en rebond dur reçoit encore des invitations — traité en **enveloppant** le contrat d'envoi du noyau, sans qu'aucun module livré ne change d'une ligne. **Le piège n° 1 des cinq modules précédents se répète deux fois dans le même jalon** : le dépôt d'un objet **et** la matérialisation des rappels émettent et mettent en file **déjà**. **Deux crates, jamais un**, et ils ne se connaissent pas. **Une seule question au commanditaire**, en mots simples : faut-il obliger à décrire une image au moment où on la dépose ? | `/speckit-plan`. Le plan devra trancher **le seul point où le SQL pourrait bouger** : l'agrégation des rappels a deux lecteurs et doit être écrite une seule fois — une **fonction de lecture**, jamais une table ni une colonne. |
| 2026-08-21 | **B5 LIVRÉ — le programme de la COP31 est public, et l'on s'y inscrit** | **Les 156 tâches, `make check` au vert en entier** — base détruite et rechargée de zéro, seize schémas, rapport de frontières **vide**, `clippy -D warnings` sans un avertissement, **627 tests**. **L'écart n° 57 est refermé** : retenir un dossier fait naître ses séances **dans la transaction de la décision**, sur **un seul hameçon** — les deux chemins d'acceptation y passent, la reprise v1 n'écrit pas l'état et ne programme donc rien. Le planificateur, livré le 18/08, a enfin de quoi placer. **Aucun chevauchement n'est refusé** : deux séances superposées s'écrivent, et la réponse porte le conflit — c'est le contrat le plus important du module, et aucun type ne permet d'exprimer un refus de placement. **Le module est le premier consommateur d'outbox du dépôt** : B3 estampille et annonce, B5 reçoit et rend publiques les séances désignées, la garde de rejeu venant du noyau. **Le symptôme « publié mais rien de public » est reproduit puis dissipé par un test de bout en bout** — l'obligation inscrite aux points bloqués le 20/08. **Trois défauts silencieux du modèle sont corrigés dans le service, et chacun est éprouvé** : la journée de rattachement est remise à nul quand le créneau change, faute de quoi une séance déplacée du 12 au 14 novembre resterait rangée au 12 **sans que rien ne le signale** (n° 113) ; les réponses sont validées contre le formulaire **résolu**, la base ne vérifiant **rien** quand la séance ne porte pas de formulaire attaché — le cas courant (n° 114) ; et **toute écriture d'inscription prend la séance en verrou** (n° 124). **Ce dernier a été mesuré, pas supposé** : le test à cent inscriptions concurrentes sur dix places, lancé **verrou retiré**, rend **onze** confirmées ; remis, il en rend dix. **Le service n'émet aucun événement** — les deux déclencheurs du fichier émettent déjà, et un test compte **une** ligne d'outbox par inscription et **trois** après une acceptation à trois séances, pas six. **L'écart n° 7 a été corrigé plutôt que suivi** : le canal de diffusion **reste saisissable**, le déclencheur ne le posant que lorsqu'il est nul ; le refuser aurait cassé une fonctionnalité livrée. Ce qui est refusé, c'est un canal désigné **quand la diffusion est retirée** — le seul endroit où la base efface un choix en silence. **L'organisation voit trois nombres et jamais un nom** : le test balaie la charge utile **entière** à la recherche d'un nom, d'une adresse, d'une réponse. **Une dépendance nouvelle, déclarée et consignée** — `regex`, déjà transitive. **Le SQL n'a pas bougé d'une ligne.** Crate `programme` : 17 372 → **27 318 lignes**, aucun fichier au-dessus de mille | **B6** — notifications et travaux différés. Deux signaux l'attendent déjà : `programme.session.scheduled`, émis par séance à la publication, et `programme.registration.*`. **Trois points restent ouverts** : la séance d'un dossier annulé (n° 123, question posée au commanditaire) ; l'écart n° 56 et son pendant n° 119, aucune permission ne distinguant « composer la grille » de « publier » ni de « voir les inscrits » ; et l'écart n° 125, consigné — on ne peut pas annuler son inscription à une séance annulée |
| 2026-08-21 | **B5 découpé — 156 tâches en 12 phases** | **`specs/005-sessions/tasks.md` : 156 tâches, dont 67 de test**, une phase par histoire et **quatre jalons de livraison**. **Cinq avertissements sont écrits en tête**, chacun parce qu'il coûterait cher découvert en chemin : les **deux** déclencheurs qui émettent déjà — quatorze types à eux deux, et le service n'en émet aucun ; la journée de rattachement qui **ne suit pas** une séance déplacée ; le déclencheur d'inscription qui ne valide **rien** quand la séance n'a pas de formulaire attaché ; le contrôle de jauge qui **ne tient pas sous concurrence** ; et **le canal de diffusion, sur lequel l'écart n° 7 se trompe** — le refuser à la lettre casserait une fonctionnalité livrée du planificateur. **Le découpage a fait apparaître une dépendance que le plan n'avait pas nommée** : **la programmation publique doit précéder la publication**. La lecture publique se démontre en posant la date à la main, et devient alors l'**instrument de mesure** de la publication effective ; l'ordre inverse obligerait à prouver que la publication a marché sans pouvoir en constater l'effet. **Une entorse à l'ordre des priorités, assumée et dite** : les journées spéciales et la diffusion (P2) passent avant la publication (P1), parce qu'elles écrivent dans le **même fichier de service** que le placement et rendent la **même forme de réponse** — les séparer donnerait deux occasions de diverger sur la composition des conflits. **Le jalon que le prompt demande hors inscriptions est à T001–T112** : à ce point, le programme de la COP31 est public. **La question ouverte n'est pas tranchée par le découpage, et il le dit** : aucune tâche n'annule la séance d'un dossier annulé — l'écrire avant l'arbitrage reviendrait à décider à la place du commanditaire ; si l'option recommandée est retenue, **une seule fonction s'ajoute**. **Quatre contrôles mécaniques** ferment le lot, dont deux propres au module : exactement **trois** fichiers écrivant hors du schéma, et un `grep` sur l'émission d'événements qui doit ne **rien** rendre | Prochain pas : **`/speckit-implement`**. [Écarts et vérifications](../ecrans/b5-sessions.md) |
| 2026-08-21 | **B5 planifié — 27 décisions, 17 routes, une entorse** | **`specs/005-sessions/` est complet** : `plan.md`, `research.md` (**27 décisions**), `data-model.md`, `contracts/` (routes, erreurs, événements), `quickstart.md`. Contrôle constitutionnel passé **deux fois**, **aucune dépendance d'ampleur**, **aucun SQL composé dynamiquement**, **aucune modification du modèle**. **17 routes, 8 codes d'erreur, 0 événement émis par le service, 1 consommé, 0 travail différé.** **Deux écarts que seul le plan pouvait révéler, n° 124 et n° 125.** Le premier est le plus grave du module : **le contrôle de jauge de la base ne tient pas sous concurrence** — `count(*)` sans verrou sous `READ COMMITTED`, et la position en liste d'attente calculée par `max(...) + 1` sans index unique. Onze inscrits sur dix places ne se verraient **que le jour de l'activité** ; deux personnes au même rang d'attente, jamais. Toute écriture d'inscription prend donc la ligne de la séance **en verrou**, et un test à cent inscriptions concurrentes sur dix places le mesure. Le second : **le déclencheur d'inscription revalide à chaque changement d'état, annulation comprise** — on ne peut donc pas annuler son inscription à une séance annulée, et une question rendue obligatoire après coup bloquerait l'annulation d'une inscription ancienne. Consigné, non corrigé : il n'y a pas de contournement sans modifier le déclencheur. **Le module est le premier consommateur d'outbox du dépôt** : la machinerie du noyau existe depuis B1 et n'avait jamais servi qu'à la télémétrie ; elle **porte la garde de rejeu**, donc le module n'écrit aucun code d'idempotence. Conséquence à connaître avant de la subir : **sans worker, le bouton « Publier » estampille l'édition et rien ne devient public.** **La publication pose la date ET fait passer « pressenti » à « programmé »** — trois preuves concordantes le demandent, dont la feuille de style du planificateur, qui dit que `planned` est « programmée mais pas encore publique ». Ne poser que la date laisserait un état du modèle **mort**. **Une entorse au principe VIII** — revérifier la présence des réponses obligatoires, parce que le déclencheur ne vérifie **rien** quand la séance ne porte pas de formulaire attaché — et **une écriture hors schéma nouvelle**, la preuve du consentement, bornée à un fichier. **Le point d'accrochage de la naissance des séances a été mesuré** : les deux chemins d'acceptation passent par la même fonction — un hameçon, pas trois | Prochain pas : **`/speckit-tasks`**. [Écarts et vérifications](../ecrans/b5-sessions.md) |
| 2026-08-21 | **B5 spécifié — le module Sessions** | **`specs/005-sessions/` est écrit** : 9 histoires, 14 cas limites, **114 exigences**, 29 critères, **aucune modification du SQL**, **aucune ligne de Rust**. **Aucun crate n'est créé** — `backend/crates/modules/programme` porte déjà tout le schéma depuis B4, et ses dossiers internes sont nommés par agrégat justement pour recevoir ce jalon. **Treize écarts nouveaux, n° 111 à 123**, dont **un qui corrige la consigne du prompt** : l'écart n° 7 demande de refuser le canal de diffusion à l'écriture, or le déclencheur ne le pose que lorsqu'il est **nul** et n'écrase jamais un canal choisi — le refuser aurait **cassé une fonctionnalité livrée** du planificateur, une édition à deux canaux ne pouvant plus en désigner un. La consigne est traitée dans son intention : on refuse l'intervalle dérivé et l'exclusivité de salle, on accepte le canal, et on refuse un canal désigné **quand la diffusion est retirée** — là, et là seulement, la base efface en silence. **Trois écarts produiraient un défaut silencieux dès le premier usage réel** : la journée de rattachement **ne se recalcule pas** quand on déplace une séance (n° 113), alors que déplacer est le geste le plus fréquent de l'écran, et la séance serait rangée au mauvais jour sans qu'aucune erreur ne le dise ; le déclencheur d'inscription **ne valide rien** lorsque la séance ne porte pas de formulaire attaché (n° 114), alors que le formulaire applicable peut venir de l'édition ou de la plateforme ; et la promotion depuis la liste d'attente **ne vérifie pas la jauge** (n° 116), le contrôle de capacité ne portant que sur l'insertion. **Le piège des déclencheurs qui émettent déjà est vérifié dans le corps des deux fonctions**, pas déduit du précédent de B4. **Deux écarts anciens sont traités** : le n° 57 (rien ne crée la séance à l'acceptation) devient l'histoire n° 1 ; le n° 11 est **tranché** — une réponse « pays » est le code ISO à deux lettres, validé contre le référentiel. **Une seule question au commanditaire** : que devient la séance d'un dossier annulé après acceptation | Prochain pas : **`/speckit-plan`**. Deux frontières à y trancher — l'écriture de la preuve de consentement à une réponse sensible, et le moment de la naissance des séances. [Écarts et vérifications](../ecrans/b5-sessions.md) |
| 2026-08-21 | **B4 — épreuve au navigateur (T159, partielle)** | Session `agent-browser --headed` sur **l'API en fonctionnement** et sur le front en développement. **Sur l'API réelle, pas le harnais** : `GET /api/docs` sert **37 opérations** portant les quatre étiquettes du module et les **six codes d'erreur** ; les six chemins littéraux répondent **401 et non 404** — l'ordre d'enregistrement tient sur le serveur monté, et le contrôle négatif `/proposals/list/encore` rend bien 404 ; les cinq écritures répondent **403 `IDENTITY_ORIGIN_REJECTED`, jamais 404** — montées, et gardées par la vérification d'origine avant le routage. **Sur les quatre écrans**, qui tournent encore sur les **mocks** : ce n'est pas le comportement de l'API qui est prouvé mais **l'accord des formes**, champ pour champ. La liste affiche ses facettes dans l'ordre exact que le service rend — statuts et formats dans l'ordre du cycle de vie, les trois signaux ensuite. La fiche d'évaluation rend tout ce que la composition sert, et porte en toutes lettres « la demande de correction est toujours partagée avec l'organisation » — **l'écart n° 99 est écrit sur l'écran** ; un administrateur global y lit « Lecture seule, vous n'avez pas le droit de noter » — **l'écart n° 50, visible**. L'espace organisation et le dossier du déposant passent le **balayage** : ni note, ni rang, ni note pondérée, ni nom de membre du comité. **Une conséquence de l'écart n° 108 rendue concrète** : la frise d'un dossier retenu porte une étape « Programmé » et le dossier du déposant un onglet « Activités et rappels » — l'API les sert **vides** jusqu'à B5 et B6, et B7 doit le savoir avant de croire à une régression. **T159 reste ouverte** : les parcours du quickstart sont des parcours d'API — voile inspecté sur la charge utile, consolidation relue en base, renvoi après clôture, séance inchangée —, couverts par les tests d'intégration et non par une session au navigateur tant que le front n'appelle pas l'API. | **B7 refermera T159**, quand le front et l'API seront reliés. Prochain module : **B5, les séances et le planificateur** |
| 2026-08-21 | **B4 — phases 9 à 11, les pièces, la reprise v1 et la finition — 🏁 B4 EST LIVRÉ (T139–T160)** | **Les trente-sept routes du contrat sont montées, frappées sur la vraie application, et documentées.** Deux comptes écrits à deux endroits : le test de montage et le contrôle OpenAPI. **Un défaut réel attrapé par là** — le scope `/admin/proposals` avait été écrit mais pas monté, et la route rendait 404 en silence ; c'est exactement ce que ce test existe pour voir. **Le module ne détruit pas ce qu'il n'a pas créé** : détacher une pièce retire le rattachement, l'objet stocké demeure — `media.assets` a son propre cycle de vie, et un même fichier peut être rattaché à deux dossiers. Le test relit l'objet **après** le détachement, et c'est pour cette assertion qu'il existe. **La déduction des transitions v1 n'émet rien**, et c'est tout le sujet : elle écrit dans le journal **sans passer par la mise à jour de l'état**, donc sans réveiller le déclencheur — émettre huit mille événements de dossiers décidés il y a deux ans déclencherait autant de courriels. Elle est **rejouable** parce que la condition « journal vide » est *dans* la requête d'insertion, et non un contrôle préalable. **Une frise qui ne ment pas a demandé une décision que le découpage n'avait pas vue** : une reprise qui ne recopie pas la date d'origine laisse la création à l'instant de l'import, c'est-à-dire **après** le dépôt — la frise afficherait « dossier créé » en dernier. L'ouverture est donc bornée par les deux autres dates. Et deux lignes au même instant ne suffisaient pas : **`platform.uuid_v7()` n'est pas ordonné à l'intérieur d'une milliseconde**, l'ordre serait tombé au hasard — mesuré, pas supposé. **Elle ne devine ni l'évaluation ni une demande de correction** : ce qui n'est pas dans les dates n'est pas déductible, et l'inventer serait pire qu'un trou. **L'effacement purge les thématiques à la main** (écart n° 94) : table polymorphe, aucune cascade, et la fonction annoncée par le modèle n'existe pas. **Aucun travail différé n'est enregistré**, vérifié plutôt que supposé. **9 routes de plus, 37 sur 37. 7 tests de plus.** | **T159 reste ouverte** : éprouver **à la main**, au navigateur, les parcours du quickstart. `make check` en entier est passé au vert — volume détruit, schéma rechargé de zéro, **525 tests** — mais aucun test d'écran n'existe encore : le voile et le balayage de l'espace organisation sont prouvés sur la charge utile de l'API, pas sur ce que l'écran affiche. Puis **B5, les séances et le planificateur** |
| 2026-08-21 | **B4 — phase 8, corriger et renvoyer (T128–T138)** | **Un écart nouveau, n° 110, et il fermait une porte que personne ne voulait fermer** : les fondations rendaient un dossier **retenu** non modifiable, ce que ni le contrat d'erreurs — « dossier rejeté, retiré, annulé, ou édition terminée » — ni l'arbitrage du commanditaire du 17/08 ne demandent. Une organisation retenue qui repère une coquille trois jours avant sa séance n'aurait eu aucun moyen de la corriger. Le découpage l'avait vu sans le dire : T138 demande de vérifier qu'un dossier **retenu** se corrige sans que sa séance bouge. **Et c'est le test le plus cher à ne pas avoir** : une séance a un créneau **arbitré**, une salle, des inscrits prévenus, des rappels programmés ; recopier dessus le créneau *souhaité* d'un dossier corrigé déplacerait une séance à laquelle quarante personnes se sont inscrites, sans que personne l'ait demandé. Le test change titre, créneau, format et durée, puis relit la séance champ par champ. Le commentaire qui l'interdit est à l'endroit exact où la tentation existe. **Le renvoi et le dépôt sont deux gestes portés par deux routes** : la fenêtre de l'appel ne borne que le premier dépôt — le comité demande une correction à huit jours de la clôture, l'organisation répond après l'échéance, et lui opposer la clôture serait lui reprocher un délai qu'elle n'a pas choisi. Le plafond, lui, s'applique aux deux. **Le geste n'est plus déduit de l'état**, et c'est une correction apportée à US1 : déduire ferait franchir la clôture à un dossier corrigé par la route de dépôt. Chaque route refuse l'état qui n'est pas le sien, **après avoir enregistré la saisie**. **L'heure murale fait l'aller-retour** — 14:30 à Belém, relue 14:30, 17:30 en UTC : c'est la seconde lecture qui prouve la conversion. **Les textes provisoires ne reviennent jamais au formulaire** : le test les trouve en base et les cherche en vain dans la recomposition. **Une édition terminée ferme la modification**, règle que la base ne tient nulle part. **2 routes de plus, 30 sur 37. 7 tests de plus.** | **Phases 9 et 10 — les pièces, l'historique et la reprise v1 (T139–T152)** |
| 2026-08-21 | **B4 — phase 7, l'espace organisation (T116–T127)** | **Le test central ne vérifie pas des champs : il balaie la charge utile entière.** C'est la seule forme qui prouve quelque chose — vérifier que la note est nulle laisserait passer celle qui arriverait par un champ ajouté demain. Il cherche les **valeurs** dans la réponse sérialisée : la note, la note pondérée, le texte d'une délibération, celui d'une note personnelle, le nom d'un membre du comité. **Trois écarts nouveaux, n° 107 à 109.** Le n° 109 est le plus sérieux : `participants` existe au contrat « pour ne pas résoudre les noms un par un », et FR-077 interdit qu'un **nom de membre du comité** atteigne le déposant — les deux ne peuvent pas être vrais ensemble sur une demande de correction, qui est le cas ordinaire. **FR-077 l'emporte, filtrage à la source** : seuls les auteurs membres de l'organisation porteuse sont nommés, le message garde son identifiant d'auteur, et l'écran affichera un libellé neutre. Relâcher la règle est une ligne à retirer ; une fuite de nom ne se reprend pas — **à arbitrer**. Le n° 107 : la liste des membres d'une organisation n'a aucune porte, le module Organisations n'exposant pas de route de liste — la lecture est faite ici, comme **élargissement** de la lecture hors schéma n° 6, non comme porte nouvelle. Le n° 108 : les séances et leurs rappels appartiennent à B5 et B6, les listes partent **vides, jamais absentes** — un champ absent ferait échouer l'écran. **L'adhésion active est le seul droit d'entrée** : un administrateur de l'édition n'entre pas dans l'espace d'une organisation dont il n'est pas membre — il a la fiche du comité pour cela —, et une adhésion en attente n'ouvre rien. **Le déposant pose la résolution, le comité la retire** (écart n° 35) : un déposant qui retirerait celle du comité effacerait un arbitrage. Le compteur de demandes ouvertes suit les deux gestes, et c'est pour cela qu'aucun n'émet d'événement. **6 routes de plus, 28 sur 37. 5 tests de plus.** | **Phase 8 — US6 : corriger et renvoyer (T128–T138)** |
| 2026-08-21 | **B4 — phase 6, la fiche d'évaluation et le voile (T094–T115)** | **US4 est livrée : onze tables en une réponse, et rien qui ne devait pas sortir.** **Le voile n'est pas un filtre** : quand il est baissé, la requête qui lit les revues des pairs **n'est pas exécutée**, et le test cherche les points forts et la note personnelle d'un pair dans la réponse **sérialisée** — pas dans les champs qu'on soupçonne. Lire puis vider laisserait la donnée à portée d'un champ oublié dans un type de sortie ; ne pas lire supprime la classe entière de défauts. **Le décompte, lui, est lu** : compter n'ancre pas. **Trois cas, et le troisième décide de la forme de la règle** : une revue en **brouillon** ne lève rien, le voile se lève **à la seconde où sa propre revue part**, et il ne descend jamais sur **qui décide sans noter** — masquer les notes à qui doit trancher rendrait la décision impossible. **La consolidation est appelée dans la transaction, et les agrégats sont RELUS en base** : c'est le seul contrôle qui dise quelque chose de l'écart n° 98, dont l'absence serait muette — le classement du comité serait faux sans qu'aucune erreur ne le signale. **Un écart nouveau, n° 106** : le refus de plafond de note sortait en **500** là où le contrat annonce un 422. Le déclencheur lève un `check_violation` **sans nom de contrainte** — une vraie contrainte `CHECK` porte toujours le sien —, et le catalogue du noyau, qui ne connaît que des couples (code, contrainte), le rangeait dans le repli anonyme. Le noyau rend désormais le message français du modèle tel quel, comme il le fait déjà pour le garde d'état. **Noter exige une affectation, lire n'en exige pas** — les deux règles sont décorrélées, et un déport ferme la notation sans fermer la lecture. **Le déport date l'affectation, il ne l'efface pas** : la ligne demeure avec son motif, et c'est elle qui interdit une réattribution silencieuse. **Une pièce en quarantaine est rendue sans adresse** : c'est cette nullité qui commande l'avertissement plutôt que le bouton. **Une demande de correction écrite « comité » ressort partagée**, et un seul événement part — sur le message partagé, jamais sur une délibération. **La composition écrit, et c'est assumé** : elle pose l'accusé de lecture, passe par la porte d'écriture du noyau, et lit l'état « déjà ouvert » **avant** l'appel qui le pose. **`repo/cross.rs` découpé en deux avant d'atteindre la limite** — `mod.rs` porte ce qui décide, `fiches.rs` ce qui s'affiche, même espace de noms. **4 routes de plus, 20 sur 37. 13 tests de plus, 504 au total.** | **Phase 7 — US5 : l'espace organisation (T116–T127)** : une composition propre au soumissionnaire, et le balayage de la charge utile entière |
| 2026-08-21 | **B4 — phase 5, le comité pilote sa liste (T077–T093)** | **US3 est livrée : tout l'écran de la liste tient en une réponse.** Les lignes viennent de `programme.v_proposal_dashboard` **telle quelle** — onze sous-requêtes déjà composées, qu'on ne recompose pas — et **les sept facettes se comptent dessus**, jamais par une seconde requête : demandées à part, elles seraient mesurées à un autre instant, et le « Retenu (17) » du filtre finirait par ne plus correspondre aux lignes affichées. **Une seule exception, et le modèle dit pourquoi** : les dossiers non lus viennent de la fonction qui prend le lecteur en paramètre — « non consulté » n'est pas une propriété du dossier mais une relation entre un dossier et un lecteur, la même ligne étant lue par l'un et pas par l'autre. **Le test ne compare pas les décomptes à des nombres écrits à la main : il recompte, filtre par filtre, sur les lignes de la réponse.** Un jour où la composition ferait deux lectures, il tomberait pour la seule raison qui vaille. **Le périmètre est vérifié deux fois**, à la route et au service — une garde qui ne vit que dans la couche HTTP n'en est pas une —, et ses trois états ne se confondent pas : global, détaché, et **vide → refus explicite, jamais une liste vide**. Les confondre afficherait « rien à traiter » à qui n'a aucun droit. **Six identifiants forgés en identifiant d'édition mènent au même refus qu'un inexistant** — un dossier, une organisation, une personne, un appel, l'identifiant nul, un tiré au hasard : quatre désignent des objets bien réels, et c'est ce qui rend le test discriminant. **L'affectation groupée rend compte de chaque dossier** : appliqués **plus** écartés **égale** la sélection, et le déporté ne se confond pas avec le déjà confié — réattribuer un dossier dont on s'est retiré effacerait une déclaration d'impartialité, et `recused_at` n'est pas une suppression. **Douze dossiers confiés émettent douze événements, jamais un pour le lot** : un consommateur qui reçoit un lot doit le déplier lui-même, et son échec porterait alors sur douze effets au lieu d'un. **Le moment annoncé depuis US2 est arrivé** : `GET /proposals/{id}` est monté, et six chemins littéraux entrent désormais en concurrence réelle avec lui, sur la même méthode. Le découpage `chemins_litteraux` / `chemins_de_dossier` a tenu, et le test de montage le vérifie sur la vraie application. **Deux écarts nouveaux, n° 104 et n° 105.** Le n° 104 est le plus sérieux : le contrat du front décrit le dossier comme la ligne de table, **agrégats d'évaluation compris**, et les deux routes qui le rendent sont ouvertes **par l'adhésion** autant que par le périmètre — le déposant aurait donc reçu sa propre note, que FR-077 lui interdit. Le masquage est **à la source** : ce qui n'est pas envoyé ne peut pas fuiter, et un filtrage à l'affichage devrait être refait dans chaque écran, chaque courriel et chaque export. **6 routes de plus, 16 sur 37. 10 tests de plus, 91 dans le module, 491 au total**, `cargo fmt`, `clippy -D warnings` et `cargo test --workspace` au vert, **aucune arête** vers `identity`, `org` ni `event`. | **Phase 6 — US4 : la fiche d'évaluation, et le voile (T094–T115)** : onze tables en une réponse, l'accusé de lecture posé par une lecture qui écrit, la consolidation des notes appelée dans la transaction du dépôt d'une revue, et le voile de l'aveugle qui **n'exécute pas** la requête plutôt que de filtrer son résultat |
| 2026-08-21 | **B4 — phase 4, la machine à états — 🏁 LE JALON DU PROMPT EST ATTEINT (T063–T076)** | **US2 est livrée, et avec elle le jalon T001–T076 : l'appel de la COP31 reçoit des dossiers.** Une organisation dépose, corrige et retire ; la machine à états tient. **Les transitions offertes tiennent en une requête**, et les deux voies y sont distinctes : le lecteur est **porteur** et la règle l'y autorise, **ou** la règle nomme une permission qu'il détient **sur l'édition du dossier**. Les confondre casserait les deux extrémités de la table — le retrait par l'organisation ne nomme *aucune* permission, et la mise en évaluation n'est *pas* ouverte au porteur. Un même dossier vu par trois personnes rend trois menus différents, chacun conforme à la table : la déposante retire (motif exigé), le noteur demande des corrections (motif exigé), le décideur retient sans motif et rejette avec. **Et le décideur ne peut pas demander de corrections** — le rôle d'administration ne détient pas `programme.review.write` : c'est l'écart n° 50, une ligne de la table des droits, pas une fatalité du code. **Une transition acceptée écrit UNE ligne d'outbox, pas deux** — le contrôle qui justifiait l'avertissement n° 1 du découpage, et le seul qui dise quelque chose d'un doublon. **Les deux refus du garde se distinguent par le moment, jamais par le texte** : `restrict_violation` rend le message français du déclencheur **repris mot pour mot**, `not_null_violation` rend « motif exigé » — sûr parce que la transaction n'écrit que deux colonnes nullables. **La colonne de décision est écrasée, le journal garde tout** (écart n° 97) : une demande de correction motivée, puis un renvoi **sans** motif qui **efface** la colonne, puis un retrait motivé — trois écritures, une seule valeur en colonne, tous les motifs au journal avec leur auteur. C'est ce qui interdit à un écran de lire la colonne. **L'action groupée évalue dossier par dossier** — une sélection peut traverser deux éditions — et **écarte avant de toucher** : tenter les douze et traduire douze exceptions coûterait douze transactions avortées pour la même réponse. Un dossier hors périmètre rend **le même écart qu'un inexistant**, et **sans numéro de dossier** : l'écart ne dit pas à qui forge une sélection que le dossier existe ailleurs. **Une correction de commentaire, et elle vaut d'être dite** : j'avais écrit que l'ordre d'enregistrement des routes provoquait un 405 et qu'un défaut venait d'être rattrapé. **Mesure faite : Actix ne rend pas 405** — quand la méthode ne correspond à aucune route de la ressource, il poursuit, et le chemin finit sur la route par défaut, donc en **404**. Le risque de capture n'existe donc **que si les méthodes se recouvrent**, ce qui n'est pas encore le cas — mais le sera à US4 avec `GET /proposals/{id}`. Le découpage en chemins littéraux et chemins de dossier est posé **avant** ce moment, et les commentaires disent maintenant ce qui a été mesuré. **4 routes de plus, 10 sur 37. 12 tests de plus.** | **Phase 5 — US3 : le comité pilote sa liste (T077–T092)** : la liste en une réponse, ses sept facettes comptées sur les mêmes lignes, et le refus explicite sur périmètre vide |
| 2026-08-21 | **B4 — phase 3, une organisation dépose son dossier (T031–T062)** | **US1 est livrée** : du premier enregistrement automatique à la confirmation de dépôt, avec le même numéro de bout en bout. **Six routes montées sur 37**, et un test les frappe sur la vraie application — d'autant plus nécessaire que deux préfixes sont partagés. **Un écart nouveau, n° 102, et il bloquait la fonctionnalité entière** : la spécification avait vu que l'adresse d'URL manquait au premier enregistrement (n° 95) ; **elle n'avait pas vu que les trois textes obligatoires butent sur le même vide**. `title`, `objectives` et `detailed_presentation` sont `NOT NULL` et `platform.i18n_text` **refuse un français vide** — or le formulaire commence par les organisations, si bien qu'au premier enregistrement automatique ces trois champs n'ont jamais été touchés. Le service pose donc un **texte provisoire**, remplacé dès la première frappe, **refusé au dépôt** — un dossier ne part pas au comité en s'appelant « Dossier sans titre » — et effacé à la recomposition. Sans lui, le repli d'adresse d'URL ne servait à rien : la ligne échouait deux colonnes plus loin. **Le réessai suffixé passe par un point de reprise**, et il le fallait : une violation d'unicité **avorte la transaction** en PostgreSQL, et sans `SAVEPOINT` le premier homonyme aurait rendu la transaction inutilisable — le réessai aurait échoué sur « current transaction is aborted », c'est-à-dire au deuxième dossier portant le même titre. **Les trois refus de recevabilité sortent en 200 avec leur valeur** — l'échéance, le plafond —, classés avant l'écriture, et une **course est reclassée plutôt que lue au texte** du message du déclencheur. **Le brouillon est enregistré avant toute décision de recevabilité** : si l'appel ferme entre le chargement de la page et le clic, l'organisation ne perd pas en plus ce qu'elle venait de saisir. **Un second écart, n° 103** : le contrat déclare qu'un intervenant connu **sans compte** « reste modifiable », ce que le plan n'avait pas prévu — ne rien écrire aurait produit le pire comportement possible, un enregistrement qui réussit et ne change rien, sans un mot. L'écriture est bornée à trois colonnes et **seulement en l'absence de compte** ; dès qu'un compte existe, l'identité est verrouillée et le refus **nomme la personne**. **L'heure murale fait l'aller-retour** : saisie à 14:30 à Belém, relue à 14:30, et 17:30 en UTC — c'est la seconde lecture qui prouve que la conversion a eu lieu et qu'on n'a pas stocké la chaîne telle quelle. **Le porteur ne peut pas être son propre co-organisateur**, et ce refus n'est pas de la coquetterie : le `ON CONFLICT` du déclencheur de synchronisation ferait basculer la ligne en `lead` au prochain enregistrement, en silence, et le dossier perdrait un co-organisateur. **Une annonce par organisation ajoutée, et aucune au réenregistrement** — un brouillon s'écrit toutes les deux secondes. **26 tests du module de plus**, `make check-back` au vert. | **Phase 4 — US2 : la machine à états, lue et jamais réécrite (T063–T077)** : les transitions offertes en une requête, la tentative, et la traduction des deux refus du garde distingués par le moment |
| 2026-08-21 | **B4 — phases 1 et 2, les fondations du module Propositions (T001–T030)** | `backend/crates/modules/programme` existe et **ne dépend de personne** : `cargo tree` ne rend aucune arête vers `identity`, `org` ni `event`, y compris en dépendances de développement — la fabrique de test appelle donc les services, jamais la vraie application, dont les tests de route vivent dans `crates/api/tests/`. **Le contrat d'événements du module ne porte AUCUNE charge utile de changement d'état, et son en-tête dit pourquoi à l'endroit exact où l'on serait tenté d'en ajouter une** : `tg_guard_proposal_status()` émet déjà les huit, et un service zélé enverrait tout en double — deux courriels par transition, visibles seulement en production. C'est le piège de B1 et B2, à l'envers de B3. **Six codes d'erreur au catalogue du noyau, et pas un de plus** : sept refus métier sont déjà des membres d'union du contrat du front et sortent en 200, et **aucun code n'est ajouté pour la recevabilité** — ses trois refus sont des réponses portant des valeurs, pas des erreurs. **Dix règles pures, 34 tests sans base** : l'adresse d'URL repliée sur titre vide et suffixée sur collision, avec la **marge de trois signes** sans laquelle un titre de cent soixante caractères n'échouerait qu'à la deuxième tentative — c'est-à-dire à la première collision, en production ; les huit longueurs alignées sur celles du front et **comptées en caractères, pas en octets** — les compter en octets reviendrait à refuser le français ; la liste blanche HTML relevée sur la barre d'outils réelle de l'éditeur ; le voile de l'aveugle dans ses **quatre** combinaisons, dont celle de l'administrateur qui décide sans noter et n'est donc pas voilé ; la recevabilité classée **dans l'ordre du déclencheur**, sans quoi le contrôle préalable et le dernier mot diraient deux choses différentes du même dépôt. **Une divergence assumée avec B3** : là où B3 ignorait un code de thématique inconnu faute de code d'erreur pour le dire, ce module le **refuse en le nommant** — accepter en silence une pastille périmée ferait déposer un dossier introuvable sur tout filtre. **Deux pièges du modèle rencontrés à l'écriture** : la liste de thématiques doit être **dédoublonnée avant l'insertion**, sans quoi deux fois la même pastille rendrait un décompte inférieur à la liste et le contrôle accuserait un code valide ; et `media.assets` porte `original_filename`, nullable, et non `file_name` — relevé par le refus de SQLx à la compilation, pas par une relecture. **Le périmètre remonte sur trois niveaux**, et le test **compare deux refus** plutôt que d'en vérifier un : un dossier hors périmètre et un dossier inexistant rendent le même code, le même message, le même champ. **`/organizations` est refactorisé — du code livré est modifié** : `org` expose ses routes du préfixe séparément, `api` compose une seule fois, aucune route n'a changé de chemin, et les vingt et une routes de B2 restent atteignables. **43 tests du module, 440 au total**, `cargo fmt`, `clippy -D warnings` et `cargo test --workspace` au vert. | **Phase 3 — US1 : une organisation dépose son dossier (T031–T053)** : les dépôts, la dérivation d'adresse d'URL avec son réessai suffixé, la conversion d'heure murale en base, et le classement des trois refus avant l'écriture |
