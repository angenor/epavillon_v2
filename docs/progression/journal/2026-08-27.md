# Journal — 2026-08-27

> Extrait de la [progression](../../PROGRESSION.md). Une ligne par session, la plus récente en haut.

| Date | Session | Fait | À suivre |
|------|---------|------|----------|
| 2026-08-27 | **B9 EST LIVRÉ — les deux crates, les neuf routes, et la dernière dette de données simulées est fermée** | Les 146 tâches de [`tasks.md`](../../../specs/007-direct-tableaux-de-bord/tasks.md). **Deux crates créés** — `live` et `analytics` —, **9 routes**, **1 travail différé**, **0 événement émis par le code** (la base en émet deux), **3 codes d'erreur**, **2 lignes de semis**, **1 section de configuration au noyau**, et **aucune dépendance nouvelle au workspace**. `make check-api-contract` compte désormais **0 route en attente**, contre 8 en ouvrant la session. **Les cinq portées remontent, et c'est le test qui tient tout l'écran** : `live.incidents` n'a aucune colonne d'édition pour `session`, `event_day` et `organization` — la lecture passe par `live.event_incidents()`, et un `grep` qui vaut un test refuse tout `WHERE incidents.event_id = $…` dans le crate. **Le contrôle de périmètre et la lecture sont indissociables**, puisque c'est la même fonction qui les porte. **Le crate n'émet rien, et un test compte les lignes d'outbox** : exactement une par publication, exactement une par retrait — le jour où quelqu'un ajoute un `emit_event` « pour faire comme les autres », le compte double et le test casse. **`published_by` est vérifié sur la valeur de la colonne**, pas sur l'audit. **Le refus de configuration est mesuré** : un intervalle de rafraîchissement égal ou inférieur à la fenêtre d'anti-rebond refuse le démarrage en nommant les deux réglages — c'est le seul défaut du jalon qui serait entièrement silencieux, et un test vérifie aussi que la fonction du modèle bloque bien une tranche **déjà réussie**. **Les contrôles de périmètre et d'autorisation ont été déplacés des routes vers `service/`** : les laisser dans un gestionnaire Actix les rendait inéprouvables sans une arête de développement vers `api`, que le contrôle de frontière du principe II refuse. **Une décision d'implémentation non prévue par le plan** : `ActiveIncident` gagne `target_id` et `target_label`, faute de quoi le bandeau public ne pourrait pas nommer son sujet sur une page qui parle de trente activités — deux champs facultatifs ajoutés, aucun changé. **`UiIncidentBanner` gagne une propriété `targetLabel`** pour la même raison : « cette activité » ne nomme rien quand le bandeau coiffe tout un programme | **Rien n'a été vérifié au navigateur contre l'API réelle** : la suite d'intégration tourne sur base jetable, le contrat est vert, mais les deux écrans du back-office et la page des programmations n'ont pas été rouverts. **Les quatre projections non lues sont rafraîchies quand même** — les retirer serait modifier le modèle pour un gain nul. **Aucun consommateur d'outbox ne déclenche de rafraîchissement**, et c'est une décision : l'effet serait invisible et l'écart au plus d'un quart d'heure. À reprendre si l'intervalle s'allonge au-delà de l'heure |
| 2026-08-27 | **B9 découpé — 146 tâches en 9 phases, trois jalons** | [`tasks.md`](../../../specs/007-direct-tableaux-de-bord/tasks.md) — **69 tâches de test**, **88 parallélisables**, et une répartition qui suit les six histoires : US1 29, US2 25, US3 22, US4 10, US5 9, US6 5, plus 46 tâches d'amorçage, de fondations et de polissage. **Trois jalons** : l'écran des messages entièrement branché (T001–T084), les deux écrans qui lisent la plateforme avec des chiffres frais (T001–T123 — **le jalon utile**), la dernière dette de données simulées fermée (T001–T137). **US2 passe devant US1**, contre l'ordre de la spécification : le tableau de bord affiche les incidents actifs de l'édition, et un incident n'existe que si quelque chose sait en poser un — livrer US1 d'abord ferait vérifier sa cinquième famille d'alerte sur une liste vide. **Cinq avertissements en tête, dont quatre produisent un défaut entièrement silencieux** : `down -v` obligatoire, les deux fonctions de publication qui **émettent déjà**, `published_by` laissé **nul sans erreur** par une écriture hors du noyau, l'intervalle de rafraîchissement qui **arrête la chaîne sans trace** s'il est plus court que l'anti-rebond, et le filtre de périmètre écrit à la main qui laisserait fuir **trois portées sur cinq**. **Quatre contrôles mécaniques** valent des tests : aucune arête entre modules, aucune écriture hors schéma (`platform` et `reference` compris), aucun appel à `kernel::events::emit`, aucun `WHERE` sur `incidents.event_id` | **Le prochain pas est `/speckit-implement`.** Le MVP s'arrête à la phase 3 (US2) : l'écran des messages lit la plateforme réelle et le contrat tombe de 8 routes en attente à 5 — la dette recule visiblement. **En équipe** : une personne prend US2 → US3 → US5 (la chaîne du direct), une autre US1 → US4 (la chaîne de la mesure) ; les deux ne partagent qu'une fonction SQL, jamais un fichier |
| 2026-08-27 | **B9 planifié — et le plan a rouvert la spécification deux fois** | [`plan.md`](../../../specs/007-direct-tableaux-de-bord/plan.md) — **29 décisions techniques**, **9 routes**, **2 crates**, **1 travail différé**, **0 événement émis par le code**, **3 codes d'erreur**, contrôle constitutionnel **sans entorse au principe VIII** et **sans aucune dépendance nouvelle** — une première dans la phase B, tout le calcul vivant en base. Cinq artefacts : `research.md`, `data-model.md`, `contracts/{routes,errors,events}.md`, `quickstart.md`. **Deux points de la spécification étaient faux, et aucun ne se voyait en lisant le modèle.** (1) Le tableau de bord était gardé par le seul périmètre, alors que le modèle porte `analytics.dashboard.read` et que `GET /api/health` la teste **depuis B1** — or elle n'est pas attribuée à `programmer`, qui a pourtant un périmètre d'administration, et c'est avec un tel compte que la règle métier n° 8 a été **vérifiée au navigateur sur cet écran le 17/08** : garder la garde telle quelle aurait refusé l'écran au compte qui l'a validé. Une ligne au catalogue des rôles, aucune élévation. (2) **La page publique d'une activité n'existe pas** — `pages/` n'en porte aucune et `useApi.ts` le dit lui-même : l'histoire n° 5 aurait été « livrée » avec une route qu'aucun écran n'appelle. L'exposition passe sur la **page des programmations**, à l'échelle de l'édition, par la fonction **descendante** ; et la question que D4 reportait se tranche par une règle qui existe déjà — trois bandeaux au plus, le plus grave en tête, le reste en « +N ». **Et une mesure faite parce que le contraire aurait coûté une journée** : `REFRESH MATERIALIZED VIEW CONCURRENTLY` est réputé refusé dans une fonction et dans un bloc transactionnel ; s'il l'était, `refresh_all(true)` journaliserait **huit échecs sans lever**. Éprouvé sur la base du dépôt : huit vues, huit succès, dans les deux cas. **Deux modifications du modèle, aucune n'est un changement de schéma** — le seuil de l'écart n° 43 et l'attribution de permission | **Le prochain pas est `/speckit-tasks`.** **`down -v` obligatoire avant de compiler** : deux lignes de semis ont changé, et le schéma n'est chargé qu'au premier démarrage du conteneur. **Trois pièges nommés d'avance**, tous silencieux : un intervalle de rafraîchissement plus court que la fenêtre d'anti-rebond **arrête la chaîne sans trace** ; un service qui émet « pour faire comme les autres » double l'outbox ; une écriture qui contourne la porte du noyau laisse `published_by` **nul sans erreur** |
| 2026-08-27 | **B9 spécifié — Direct + Tableaux de bord, le jalon qui ferme la dernière dette de données simulées** | [`specs/007-direct-tableaux-de-bord/`](../../../specs/007-direct-tableaux-de-bord/spec.md) — **6 histoires, 12 cas limites, 71 exigences, 14 critères, 4 décisions tranchées**, aucun marqueur de clarification. [Écarts et vérifications](../ecrans/b9-direct-tableaux-de-bord.md), [décisions](../decisions/2026-08-27.md). **Le constat d'abord, la spécification ensuite** : le modèle sert déjà tout — les six fonctions et la table du schéma `live` § 6, les huit projections et les deux vues du schéma `analytics`, les deux modules déclarés dans `platform.modules`, la permission `live.incident.publish`, la taxonomie `incident_kind` à neuf termes. **Une seule modification, et ce n'est pas un changement de schéma** : une ligne de réglage au semis, pour le seuil de l'écart n° 43, ouvert depuis le 17/08. **Deux affirmations du dépôt sont déjà fausses et l'ont été mesurées** : la fabrique d'incidents annonce « sept routes » quand il y en a huit, et trois fichiers annoncent « trois écrans en données simulées » quand la vitrine est basculée depuis B8. **Deux choses réputées à faire le sont déjà** : `GET /api/health` sert `v_operational_health` depuis B1 — la refaire produirait deux routes pour une vue —, et le worker sait armer une chaîne récurrente six fois. **Les quatre points que le prompt laissait ouverts sont tranchés** : la composition du tableau de bord vit dans le crate `analytics` sous `repo/cross/` et pourquoi cela ne rompt pas la frontière (D1) ; le seuil d'urgence passe dans `platform.settings` plutôt que dans une colonne d'appel (D2) ; une portée globale **ne se retire pas** depuis une édition, la permission se vérifiant sur la portée réellement visée (D3) ; l'exposition publique du bandeau **entre** dans ce jalon, en dernière priorité et bornée à la page d'une activité (D4). Sept écarts, **n° 143 à 149**. **Cette session n'a modifié aucun fichier de `frontend/`, de `backend/` ni de `docs/database/`** | **Le prochain pas est `/speckit-plan`.** Une question au commanditaire, non bloquante : quand un message d'entretien s'affiche sur tout le site, l'équipe d'une seule COP doit-elle pouvoir l'enlever elle-même ? La position tenue est la réversible. **Le bandeau sur la programmation complète et sur l'accueil reste au jalon suivant** : « quel message s'affiche sur une page qui parle de trente activités » n'est pas tranché. **À l'implémentation** : `down -v` avant de compiler, la ligne de semis n'étant chargée qu'au premier démarrage du conteneur |
| 2026-08-27 | **La présentation d'une édition affichait ses balises** | Écart signalé par le commanditaire sur `/evenement/[slug]`. La description d'une édition est saisie dans **l'éditeur riche** du back-office (`AdminEventsI18nField … rich`, arbitré en B3 : « même éditeur que la présentation détaillée d'une proposition ») et arrive donc en HTML restreint ; la page publique la rendait par interpolation, si bien que `<p>`, `<ul>` et `<a>` s'affichaient tels quels dès qu'une vraie édition venait de l'API. **Les données d'exemple ne pouvaient pas le montrer** : leurs descriptions sont écrites en texte nu, et c'est pourquoi la vérification au navigateur de A3 était passée à côté. `EventPresentation` rend désormais la description par `UiRichContent`, qui porte la feuille `.rich-text` — même allure à la saisie et à la publication, aucune couleur venue du contenu. **Le même champ est repris ailleurs en aperçu** : le bloc d'appel de l'accueil (`HomeCallSection`) en fait un paragraphe court, où rendre des titres et des listes n'a pas de sens — il passe par `richTextToPlain()`, nouvel utilitaire (`app/utils/rich-text.ts`) qui dépouille le balisage, sépare les blocs par une espace et décode les entités que l'éditeur produit. `UiRichContent` s'en sert aussi pour juger un contenu vide, à la place de sa propre expression régulière. `highlights` reste du texte simple : son champ de saisie est un `textarea`, pas l'éditeur. **Vérifié** : `make check-front` au vert — typecheck, construction, contrat d'API (139 appels, 8 routes en attente, inchangé). | **Rien n'a été revu au navigateur contre une édition réelle** : la correction se démontre sur données d'exemple par un texte nu, pas par du HTML. **Les données d'exemple mentent sur la nature du champ** — les sept descriptions d'édition de `mocks/event.ts` sont du texte nu là où l'écran de saisie produit du HTML ; les passer en balisage restreint ferait travailler l'écran hors ligne sur ce qu'il rencontrera vraiment. **Le champ est peut-être lu ailleurs sans être rendu** : un `<title>` ou une méta de partage tirée de la description afficherait ses balises de la même façon — `richTextToPlain()` est écrit pour ces emplois. |
