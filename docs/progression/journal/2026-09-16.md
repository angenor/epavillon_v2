# Journal — 2026-09-16

> Extrait de la [progression](../../PROGRESSION.md). Une ligne par session, la plus récente en haut.

| Date | Session | Fait | À suivre |
|------|---------|------|----------|
| 2026-09-16 | **La liste des éditions du back-office quitte le tableau** | `AdminEventsEditionsList`, sur le modèle de la liste des propositions : vignette, statut, métadonnées à icônes, dossiers reçus, barre de programmation. L'API joint `cover` à chaque ligne — [A10](../ecrans/a10-evenements.md) | Relancer l'API pour voir les vignettes contre la base réelle |
| 2026-09-16 | **`/programmations` reçoit son propre bandeau** | Premier jet (réutilisation d'`EventHero`) refusé par le commanditaire. Bandeau dédié `EventProgrammeHero` : titre du pavillon, activités, organisations, pays, dernière mise à jour, sélecteur des autres programmations ; édition par défaut en cours → à venir → passée. **Modèle** : `sessions.listing_changed_at` (horaires, titre, salle, statut) et deux colonnes de `v_edition_stats` — [A3](../ecrans/a3-evenement-public.md) | **Migration de production à faire** (§ 13). **Disque plein en cours de session** : Postgres local arrêté, `cargo clean` (34 Go) puis redémarrage ; base intacte, `cargo test -p programme -p event` passe. `canal_par_defaut_unique` a échoué une fois puis passé trois fois : test instable |
| 2026-09-16 | **Les vrais logos d'ePavillon remplacent ceux de l'IFDD et de l'OIF** | Fichiers renommés : `public/logos/svg/epavillon.svg` (marque complète, verticale) et `epavillon-symbole.svg` (pictogramme), leurs PNG en `png/…@4x.png`. Variantes `-inverse.svg` créées pour fond sombre : **seul le trait noir `#231F20` passe en blanc**, les cinq couleurs ne bougent pas. Les quatre logos IFDD/OIF sont supprimés. En-têtes public, authentification et back-office : **le pictogramme seul, sans le mot « ePavillon »** (demande du commanditaire), en 40 px, le nom porté par le texte alternatif — le logo complet, vertical, serait illisible à cette hauteur. Pied de page : marque complète inverse. Le favicon provisoire devient le pictogramme, trait blanc quand l'onglet est sombre. Vérifié au navigateur, thèmes clair et sombre | Les PNG ne sont utilisés nulle part (réserve pour courriels ou partage social). Le texte du logo est en police système : son rendu varie d'une machine à l'autre — une version vectorisée, si le service communication en a une, serait plus fidèle. Le pied de page ne mentionne plus l'IFDD/OIF que par le copyright |
| 2026-09-16 | **La section d'appel de l'accueil devient une affiche** | Aplat institutionnel, couverture de l'édition à droite fondue de gauche à droite, rebours sur verre. Second dégradé de la charte, `.fade-inverse-start`, arbitré par le commanditaire — [A15](../ecrans/a15-accueil.md) | Le libellé d'édition de la COP31 est tronqué en base (« …sur le ») : à corriger au back-office |
| 2026-09-16 | **L'espace organisation renvoyait vers des écrans qui n'existent plus, et un enregistrement perdu ne se voyait pas** | **Quatre liens morts** : le bloc « ce qui attend une action » d'`/mon-organisation` est composé **par l'API**, qui portait encore les chemins d'avant le renommage des écrans — `/proposer?dossier=…`, `/espace/dossiers/…`, `/espace/membres`. Le clic tombait sur la page « introuvable » du site. Les quatre pointent désormais vers les chemins réels (`/deposer-une-proposition`, `/mon-organisation/dossiers/…`, `/mon-organisation#membres`) ; le lien d'une séance sans compte rendu ouvre **l'onglet des séances** du dossier plutôt qu'un paramètre que l'écran ignorait. **Aucun test ne les couvrait** : ce sont des chaînes, et rien ne les confronte au routage du site. **Second point, signalé par le commanditaire** : un enregistrement automatique en échec ne se lisait qu'en une ligne au bas d'un formulaire de six étapes — qui ferme l'onglet ne la voit pas, et laisse un dossier vide en base que personne ne relance. Ajout d'une **alerte en tête de formulaire** avec le message de l'API et un bouton « Réessayer », et d'un **garde-fou `beforeunload`** tant qu'une modification n'est pas écrite. La reprise du brouillon, elle, fonctionne : vérifiée au navigateur après la correction des rôles — le formulaire rouvre `CDP31-00002` avec son numéro et son horodatage | **L'API doit être recompilée et redémarrée** pour que les liens corrigés sortent : le binaire en cours est l'ancien. **Pas de test automatique sur ces chemins** — un contrôle qui confronterait les cibles de l'API au routage du site reste à écrire. Le cache de compilation Rust ayant été supprimé (disque plein), la première compilation est longue |
| 2026-09-16 | **Autocomplétion des intervenants, et un chemin d'API que personne ne pouvait joindre** | Le champ « Adresse électronique » d'un intervenant propose désormais, dès trois caractères, les personnes que la plateforme connaît : **adresse confirmée, ou intervenant d'une activité RETENUE**, et seulement celles qui acceptent de figurer dans les listes (`is_directory_visible`). Un dossier en cours d'examen ne fait entrer personne — ce serait dire qui a été proposé. La recherche est un **préfixe d'adresse**, huit réponses au plus : sans ces bornes, une autocomplétion est un export de la liste de contacts. Retenir une suggestion pré-remplit les champs par **la même opération** que la recherche par adresse exacte. Rien n'est bloquant : ne rien trouver laisse la saisie à la main. Nouvelle route `GET /people/suggest`, trois tests sur les bornes. **Et un défaut ancien, découvert en l'essayant** : `/people/lookup` **ne répondait pas non plus** — `/people/{id}`, déclarée avant par le module d'identité, capturait les deux chemins et échouait à en faire un UUID. **Le test des routes ne pouvait pas le voir** : sans session, tout répond 401 avant que le chemin ne soit lu. La recherche d'intervenant par adresse n'avait donc jamais fonctionné contre l'API réelle. Ordre corrigé dans `crates/api/src/lib.rs`, et **test avec session** (`routes_people_litterales.rs`) | **Question ouverte au commanditaire** : l'autocomplétion révèle, à qui tape trois lettres, le nom et l'adresse de personnes inscrites. Les bornes limitent la casse, pas le principe. À confirmer, ou à restreindre aux seuls intervenants déjà retenus |
| 2026-09-16 | **La liste de suggestions ne se donnait pas pour un choix, et la civilité restait verrouillée vide** | Deux retours du commanditaire sur l'autocomplétion. **La liste passait pour du texte d'aide** : l'aide du champ — « Elle ne sera pas publiée… » — s'intercalait entre le champ et la liste, et personne ne cliquait. L'aide s'efface désormais tant que la liste est ouverte ; celle-ci colle au champ, porte son intitulé (« Personnes déjà connues de la plateforme »), une icône et le mot « Reprendre » sur chaque ligne. **La civilité était grisée après sélection**, même quand le profil n'en portait aucune — or un compte se crée sans civilité et le programme annonce « Mme Awa Sow Fall ». Elle redevient modifiable dans ce seul cas, avec la raison écrite sous le champ, et **l'API COMPLÈTE sans jamais écraser** (`completer_civilite`, `WHERE civility IS NULL`) : ce que la personne a déclaré elle-même ne bouge pas. Test des deux sens | — |
| 2026-09-16 | **« 0 membre du comité évalue le dossier » : un décompte qui ne devait pas être affiché** | L'écran de confirmation annonçait le nombre de revues exigées par l'appel. Il vaut **zéro** sur la COP31, et le déposant lisait « 0 membre du comité de sélection évalue le dossier » juste après avoir envoyé son dossier. **Deux raisons de retirer le nombre, pas de le corriger** : une campagne peut arbitrer sans revues enregistrées, et **des membres du comité peuvent n'avoir aucun compte sur la plateforme** — le décompte ne mesure donc pas ce qu'il prétend. La suite des opérations de la confirmation est désormais **le même texte éditable** que celui de la relecture (`calls_for_proposals.submission_next_steps`) : une seule liste à tenir. Sans texte saisi, l'écran retombe sur trois étapes de base, sans aucun chiffre. **Le texte par défaut du modèle est réécrit** — il annonçait « au moins deux membres » —, la base locale rattrapée, et le test du champ vérifie maintenant l'absence de décompte plutôt qu'un nombre de lignes | — |
| 2026-09-16 | **Première migration de la production, et le dossier des sauvegardes qui n'était pas exclu** | La production portait le schéma du **02/09** : aucun outil de migration n'existe, `docs/database/` n'est chargé qu'à la création du volume. Écart **mesuré** par comparaison des deux `pg_dump --schema-only` : colonnes `summary` et `activity_type_code`, texte « après l'envoi » et défaut du format sur l'appel, vue du tableau de bord (catégories et vignette), déclencheur du rôle d'organisation. Script **éprouvé sur une copie complète de la production**, puis joué : sauvegarde, construction des images, migration (`COMMIT`), redémarrage. Données préservées (2 personnes, 1 compte, 1 organisation) et rôle `org_manager` rattrapé. **Schéma de production identique au modèle** après coup, à la partition de courriels près. Un oubli trouvé par cette dernière comparaison — un commentaire de colonne dont le texte contient « ; » — corrigé sur place. `financedurable` à 200 avant et après. Méthode consignée au § 13 de `DEPLOIEMENT.md`. **Au passage** : `sauvegardes/`, où `./deploy.sh backup` rapatrie la base, **n'était pas exclu de git** — un `git add` général aurait versionné une copie de la production. Ajouté à `.gitignore` | Le rattachement public (cPanel institutionnel) reste à faire, comme au 02/09 |
