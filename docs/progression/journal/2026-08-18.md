# Journal — 2026-08-18

> Extrait de la [progression](../../PROGRESSION.md). Une ligne par session, la plus récente en haut.

| Date | Session | Fait | À suivre |
|------|---------|------|----------|
| 2026-08-18 | Page « En cours de maintenance » (A14) | **CE N'EST PLUS À LA PAGE DE SAVOIR QU'ELLE EST FERMÉE, C'EST AU ROUTAGE.** Le prompt l'exige mot pour mot — « le routage doit la servir automatiquement quand le drapeau de fonctionnalité du module est désactivé, sans toucher aux pages elles-mêmes ». D'où **un middleware GLOBAL** (`middleware/feature-flag.global.ts`), **un registre** (`utils/feature-modules.ts`) et **une seule page** (`pages/maintenance/[module].vue`) pour les six modules. Six tests recopiés dans six pages, c'était six occasions d'en oublier un — et un module qu'on croit fermé alors qu'il s'affiche. **Le modèle a été complété d'un drapeau** : `directory.enabled`, pour l'annuaire. Les cinq autres existaient depuis A10, contrairement à ce qu'affirmait `CLAUDE.md` — corrigé. **L'APPARIEMENT SE FAIT PAR NOM DE ROUTE, PAS PAR CHEMIN** : une page déclare deux adresses par `defineI18nRoute` (`/communaute`, `/en/community`), et comparer des chemins obligerait à connaître les deux, dans les deux langues, plus le préfixe de langue ; le nom de route ne varie que d'un suffixe `___fr`. **LE DÉTOURNEMENT JOUE DANS LES DEUX SENS** — le drapeau éteint mène à la page de maintenance, le drapeau allumé en RAMÈNE. Sans le second sens, une adresse partagée trois mois plus tôt annoncerait comme fermé un module ouvert entre-temps, ce qui est exactement le contraire de l'honnêteté que le prompt demande. **Une clé inconnue rend 404** (`validate`), sans quoi `/maintenance/nimportequoi` deviendrait un générateur d'écrans plausibles. Aucune date annoncée, aucune illustration de chantier : une pastille d'attention, ce que l'espace portera, et trois renvois vers ce qui répond aujourd'hui. **Nettoyage collatéral** : le bloc `nav.maintenance` quitte `_nav.json` pour `components/maintenance-state.json` et `pages/maintenance.json` — la convention du projet est un fichier par écran, et les deux phrases d'espace y étaient en double avec les pages qui les affichaient. 1 page, 1 middleware global, 1 store, 1 registre, 1 fichier de mocks, 1 méthode d'API, 4 fichiers de traduction. `make check-db` au vert sur base neuve (six drapeaux `<module>.enabled` en base), `typecheck` et `build` au vert, les deux sens du détournement éprouvés au serveur de développement. | Le jalon front est complet : A1 à A14 sont faits. Reste à ouvrir la phase API (B0). Le module `live` n'a toujours aucun prompt d'API — à rattacher à B6 ou à ouvrir en propre. Quatre des six modules fermés n'ont AUCUNE page (`/publications`, `/formations`, `/outils`, `/messagerie` répondent 404) : leurs noms de route sont déjà inscrits au registre, il n'y a rien à faire le jour où ils en auront une |
| 2026-08-18 | Barre publique : espaces et menu du compte | **LA BARRE NE PORTE PLUS QUE DES ESPACES, ET LE COMPTE A SA BULLE.** Trois entrées demandées — Programmations, Communauté, Négociations — et « Mon organisation » plus la déconnexion déplacées dans un menu déroulant sous une bulle d'initiales (`UiUserMenu`), ouvert au survol. **Le survol ne peut pas être le seul geste** : il n'existe ni au doigt ni au clavier, le déclencheur est donc un vrai bouton (`aria-expanded`, `aria-haspopup`) qui répond aussi au clic et à la flèche bas, et Échap rend le focus. La fermeture au survol est retardée de 160 ms, sinon le curseur ne traverse jamais l'espace entre le bouton et le panneau. **Pas de photo : `identity.people` n'a aucune colonne d'avatar** — la bulle rend les initiales, qui sont une donnée réelle, plutôt qu'une silhouette générique. **Communauté et Négociations n'existaient pas** : deux pages créées avec `UiMaintenanceState`, parce qu'une entrée de barre qui répond 404 est pire que pas d'entrée (leçon d'A3). Les deux modules restent fermés ; seule leur annonce change. `myOrganizationTo` rend désormais un chemin BRUT — il était déjà préfixé par `localePath`, et le menu l'aurait préfixé deux fois. | Ouvrir Communauté quand `org`/`engagement` auront leurs écrans ; brancher les deux pages sur `platform.feature_flags` le jour où un drapeau les couvre — aucun ne le fait aujourd'hui. |
| 2026-08-18 | Messages d'incident (A13) | **L'ÉCRAN DU DIRECT — celui qu'on ouvre pendant que la salle attend.** Une activité déborde, un intervenant tarde, une diffusion s'interrompt : le bandeau doit paraître dans la minute et disparaître dès que c'est réglé. **Le modèle a été complété d'abord, sur deux points.** (1) La taxonomie `incident_kind` de `080_live.sql` ne portait pas le **débordement sur le créneau suivant**, que le prompt nomme pourtant en premier — ce n'est ni un retard (il en CAUSE un, ailleurs) ni un changement d'horaire (rien n'a été décidé) : c'est un fait constaté en direct, et le faire passer pour l'un des deux aurait faussé tout historique d'incidents. (2) Le back-office n'avait AUCUNE fonction pour lire autre chose que les incidents ACTIFS : `active_incidents_for_event()` filtre sur les quatre conditions d'affichage, or cet écran doit montrer ce qui va parler, ce qui attend une décision et ce qui a parlé. **`live.event_incidents(event, at)` rend les cinq états avec l'état CALCULÉ EN BASE**, la cible résolue et l'auteur de la publication ; `active_incidents_for_event()` est réécrite au-dessus d'elle, ce qui supprime le second balayage de portée — deux balayages qui divergent, et le même incident s'affiche au tableau de bord sans apparaître ici. **Un défaut d'interface trouvé en éprouvant, pas en relisant** : le `watch` qui vide la cible au changement de portée effaçait aussi la cible qu'un PRÉ-REMPLISSAGE venait de poser — le raccourci « Signaler un débordement » arrivait sur un formulaire dont l'activité s'était effacée toute seule ; la remise à zéro appartient au gestionnaire de choix, pas à un observateur. 3 pages, 6 composants, 1 utilitaire pur, 1 fichier de contrats, 1 fichier de mocks d'écran, 1 fabrique d'API, 4 fichiers de traduction. **RECENTRÉ SUR LE DIRECT LE MÊME JOUR, après retour du commanditaire** : « les messages d'incident, c'est pour signaler des dysfonctionnements liés à la diffusion d'une activité en direct ». Le premier jet était l'écran que décrit le prompt — cinq portées, neuf natures, entrée par le formulaire — et se lisait comme un gestionnaire de bandeaux générique. L'écran s'ouvre désormais sur **le poste de direct** : les activités du jour, leur état d'antenne, et quatre gestes par activité (retard, débordement, panne technique, diffusion interrompue) qui ouvrent le formulaire déjà rempli de la portée, de la cible, de la nature, de la gravité suggérée et de la fenêtre — la fin du créneau perturbé. Le formulaire complet reste dessous, les cinq portées du modèle avec lui. Les données de démonstration sont recentrées : le téléversement en panne et la prolongation de l'appel n'avaient rien à faire là. `make check-db` au vert sur base neuve, `typecheck` et `build` au vert. | A14 — page « En cours de maintenance » et drapeaux manquants (formations, messagerie). Le module `live` n'a toujours aucun prompt d'API : à rattacher à B6 ou à ouvrir en propre |
| 2026-08-18 | Utilisateurs et rôles (A12) | **LE DÉFAUT STRUCTUREL DE LA V1, PRIS PAR SA CAUSE : huit rôles GLOBAUX dans un ENUM.** Être « révisionniste » valait pour toutes les COP, et confier un webinaire à son responsable avait imposé une page d'administration séparée, écrite dans l'urgence et en partie codée en dur. Le RBAC scopé existait dans `030_identity.sql` depuis le premier jour ; il n'avait aucune surface. Trois écrans la lui donnent — la LISTE (une pastille par attribution, portée comprise, jamais un nom de rôle nu), la FICHE en quatre onglets dont « ce que cette personne peut faire, ET OÙ », et la file RGPD. **Le modèle a été corrigé d'abord, sur deux points, et le second était une faille d'usage** : (1) `role_assignments` ne portait ni `revoked_by` ni `revoked_reason` — `note` est le motif de l'OCTROI et ne peut pas servir deux fois, si bien qu'une révocation ne laissait qu'une date nue ; (2) **`identity.role.assign` n'était accordée à AUCUN rôle** — seul `super_admin` la détenait, par l'effet du trigger, et le back-office des rôles n'aurait donc servi qu'au compte technique pivot. Accordée à `admin`, elle n'ouvre aucune élévation : la portée de l'attribution suit celle de l'administrateur, éprouvé en base. **Deux défauts d'interface trouvés en éprouvant, pas en relisant** : `.filter(isAssignmentActive)` passait l'INDEX comme instant de référence — toutes les attributions paraissaient à venir, la colonne des rôles était vide ; et `UiDrawer` ne s'ouvrait pas s'il était monté déjà ouvert, défaut que `UiModal` avait corrigé au prompt A2 et que le tiroir traînait encore. 3 pages, 10 composants, 2 utilitaires purs, 1 fichier de contrats, 1 dossier de mocks en quatre fichiers, 2 mocks d'entité (`platform`, `privacy`), 1 fabrique d'API, 10 fichiers de traduction. `useApi.ts` atteignait mille lignes : l'espace organisation (A5) en est sorti dans sa propre fabrique, sans qu'une ligne d'écran change | Prompt A13 — messages d'incident |
| 2026-08-18 | Organisations et fusion des doublons (A11) | **LE DÉFAUT N° 1 DE LA V1, PRIS PAR LES QUATRE BOUTS.** « 2 personnes ont créé deux fois la même organisation et on voit les 2 sur la plateforme sans possibilité de fusionner » : le SQL posait quatre verrous depuis le premier jour, aucun n'avait de surface. Quatre écrans les rendent utilisables — la LISTE (`mv_organization_scorecard`, présente dans le modèle et affichée nulle part, triée par score de confiance **croissant** : la première ligne est toujours la fiche à regarder), la FILE des doublons présumés (triée par similarité décroissante, motifs nommés du plus probant au moins — un domaine de courriel partagé est une preuve, un même pays ne prouve rien), l'écran de FUSION, et la FICHE d'une organisation. **Ce qui fait la valeur de l'écran de fusion n'est pas la comparaison, c'est le DÉCOMPTE.** Il est lu dans `org.organization_references` — le registre que chaque module alimente — et non dans une liste de tables écrite dans un composant : le jour où Formations déclare ses deux colonnes, elles sont comptées sans qu'on y touche. Trois sorts y sont distingués, jamais confondus : transféré, **supprimé car déjà présent côté cible** (dédoublonnage), supprimé. **C'est ce décompte qui a trouvé un défaut du MODÈLE** : le registre déclarait `organization_domains` sans `dedupe_on`, si bien qu'une fusion laissait la fiche absorbante avec deux lignes pour `osed-sahel.org` — un doublon fabriqué à l'instant où l'on en corrigeait un autre. Corrigé dans le SQL d'abord, puis répercuté. **`merge_organizations()` ne prend aucun choix de champ**, et le prompt en demande : les valeurs retenues sont donc un `UPDATE` de la cible, **avant** l'appel et dans la même transaction — obligation inscrite pour B2. **Le périmètre d'administration a dû être lu autrement** : une organisation n'appartient à aucune édition. La permission de lecture ouvre l'écran quelle que soit sa portée, la LISTE est filtrée sur les éditions administrées et le dit, la FUSION exige la portée globale. **Deux défauts transverses corrigés au passage** : le `sr-only` de `UiCounter` étendait la page de 163 px sous 375 px (la barre d'onglets d'A10 en souffrait aussi), et l'historique affichait ses instants et ses statuts en valeurs brutes. Fusion éprouvée de bout en bout au navigateur : sens inversable, quatre arbitrages, motif obligatoire, confirmation par le nom de la fiche absorbée (le sigle accepté, un nom faux refusé), 11 éléments déplacés, file remise à zéro, fiche absorbée toujours consultable derrière son bandeau de renvoi. | **A12 — utilisateurs et rôles.** Quatre points consignés ci-dessous : le choix de champ à porter par l'API dans la même transaction que la fusion, la portée des permissions à confirmer, l'âge de la projection matérialisée à afficher, et `compute_trust_score()` que rien n'appelle. **`useApi.ts` atteint 987 lignes** : la prochaine fabrique d'API doit en sortir avant tout ajout. |
| 2026-08-18 | Zone de saisie du texte enrichi — correction (UiRichText) | **DÉFAUT SIGNALÉ : « impossible d'écrire, seule la première ligne répond au clic alors que la zone est vaste ».** Exact, et le défaut ne datait pas de cet écran : `EditorContent` rend une ENVELOPPE qui contient l'élément `contenteditable`, et la hauteur minimale était posée sur l'enveloppe. L'élément éditable gardait donc la hauteur de son contenu — une ligne sur un champ vide de huit — et tout clic en dessous tombait dans le vide. **Le composant étant partagé, le formulaire de dépôt (A4) en souffrait depuis le 17/08 sans que ce soit relevé.** La classe `rich-text`, le rembourrage et la hauteur minimale passent sur l'élément ÉDITABLE, par `editorProps.attributes`. **Un second défaut est tombé avec le premier** : la règle `.rich-text > * + *` de `main.css` vise les ENFANTS DIRECTS ; posée sur l'enveloppe elle ne rencontrait qu'un seul enfant et ne s'appliquait à aucun paragraphe. Le texte s'écrivait serré et s'affichait aéré une fois publié — exactement ce que l'en-tête de `main.css` interdit. Deux paragraphes portent désormais 12 px de marge dans l'éditeur comme au rendu. **Et un troisième, d'accessibilité** : un `contenteditable` n'est pas un contrôle de formulaire, un `<label for>` ne s'y associe pas et l'identifiant était posé sur l'enveloppe. Il va sur l'éditable, avec `role="textbox"`, et `aria-describedby` / `aria-invalid` / `aria-required` suivent l'aide et l'erreur — le message de validation de ce champ n'était annoncé à personne. Vérifié au navigateur sur les DEUX écrans : haut, milieu et bas de zone visent l'éditeur, le clic en bas donne le focus, la frappe entre. | — |
| 2026-08-18 | Gestion des événements — retours du commanditaire (A10, reprise) | **Quatre demandes, dont une qui a rouvert le MODÈLE.** **(1) Coordonnées GPS** : rien ne les portait. `event.events` gagne `latitude`/`longitude` et `ck_events_coordinates` — les deux ou aucune. Deux `numeric` plutôt que PostGIS : rien ne demande de calcul spatial, seulement d'ouvrir un plan. Base rechargée de zéro, les trois contraintes éprouvées sur une ligne réelle (latitude seule refusée, latitude 95 refusée, point de Belém accepté). **(2) Liste de pays exhaustive** : le semis passe de 29 à **249 entrées**, la norme ISO 3166-1 entière. Les codes ont été vérifiés mécaniquement avant d'être écrits — unicité des trois codes, concordance avec les 29 lignes déjà curées, validité de chaque fuseau ; les noms viennent des données CLDR de l'exécution, aucun n'est recopié. Le bloc curé PRIME, « République démocratique du Congo » ne devient pas « Congo-Kinshasa ». Côté mocks, `countries-iso.ts` génère les 231 fiches manquantes plutôt que d'ajouter quatre mille lignes de recopie. **(3) Fuseaux exhaustifs** : la liste écrite à la main cède la place à `Intl.supportedValuesOf('timeZone')` — **419 options** au lieu de 20, exhaustives par construction, et l'alias `Europe/Geneva` qui avait fait tomber tout le formulaire ne peut plus s'y glisser. Onze fuseaux courants restent remontés en tête, et les villes où l'IFDD travaille gardent leurs accents. **(4) Slug composé pendant la frappe** : `utils/slug.ts` rejoue `platform.slugify()`, **comparé à la fonction SQL sur six cas — identiques**. Il suit le LIBELLÉ et non le titre (`cop31`, pas `cop31-conference-des-nations-unies-sur-les-changements-climatiques`), cesse de suivre dès qu'on y touche, et **ne suit jamais en modification** : le slug d'une édition publiée est une adresse que des courriels et des moteurs de recherche portent déjà. **(5) Description en texte enrichi**, comme la présentation détaillée d'une proposition (A4) : `I18nField` gagne un mode `rich` qui monte `UiRichText` sous `ClientOnly`. | **A11 — organisations et fusion des doublons.** Un point à reprendre avec l'IFDD : le statut OIF des 220 pays ajoutés, laissé à `none` faute de source qui fasse autorité. |
| 2026-08-18 | Gestion des événements (A10) | **AUCUNE MODIFICATION DU MODÈLE : pour la deuxième fois du jalon, le SQL suffisait.** `060_events.sql` portait déjà tout — les dix-huit champs d'une édition, `ck_events_physical_location` qui exige un lieu dès qu'on n'est pas en ligne, `ux_calls_one_per_event` qui tient la cardinalité 0..1 de l'appel, `event.programme_tracks` pour les journées spéciales, `seed_default_criteria()` pour la grille et `call_reviewers` pour le comité. **Trois écrans, six onglets, quinze écritures.** La liste des éditions (filtres et tri dans l'URL, chaque ligne dans SON fuseau — la COP31 en heure de Belém et le cycle PACO en heure de Paris sur la même page), le formulaire d'une édition (partagé entre création et modification), et la fiche à six onglets : journées du calendrier, journées spéciales, lieux et salles, canal de diffusion, appel à propositions avec sa grille pondérée, comité de sélection. **Ce qui a demandé le plus de soin n'est pas la saisie, ce sont les CONSÉQUENCES SILENCIEUSES.** Retirer une salle, supprimer un jour, désactiver un canal ne casse rien en base — `ON DELETE SET NULL` détache les séances sans un mot. Chaque écriture rend donc `sessions_detached`, et l'écran le chiffre avant de confirmer. Supprimer un fil, lui, CASCADE sur `programme.session_tracks` : c'est du travail éditorial perdu, et la confirmation le dit. **Trois refus vérifiés au navigateur** : `ux_programme_tracks_code`, `ux_events_slug` et `ck_events_physical_location` remontent au bon champ, en français. **Deux corrections trouvées en éprouvant l'écran** : `Europe/Geneva` est un ALIAS que la base de fuseaux de Node refuse, et il emportait la liste entière — le formulaire restait indéfiniment sur son squelette, sans rien dire ; le calcul du décalage est désormais défensif, la page rend son erreur, et le fuseau canonique `Europe/Zurich` porte le libellé « Genève ». **Un garde-fou ajouté** : générer le calendrier d'un cycle de webinaires étalé sur l'année proposait 302 journées vides. `event_days` est le calendrier d'une conférence ; au-delà de quarante jours, la fenêtre avertit avant de confirmer. **Le fichier de mocks a été découpé en trois** (`core`, `detail`, `writes`) : d'un seul tenant il faisait 1 474 lignes, au-delà du garde-fou de `CLAUDE.md`. | **A11 — organisations et fusion des doublons.** Trois écarts consignés ci-dessous, dont deux à arbitrer : la génération des journées reste applicative faute de trigger, et `event_days` n'a pas de sens pour une série de webinaires. |
| 2026-08-18 | Graphiques du tableau de bord — ApexCharts et indicateurs de tête (A6, révision) | **DEUX DEMANDES DU COMMANDITAIRE : « des cartes de statistiques plus intéressantes » et « de vrais graphes au lieu des barres maison ».** **DÉPENDANCE NOUVELLE : `vue3-apexcharts` 1.11 sur `apexcharts` 6.10** — la première bibliothèque de graphiques du projet. Les cinq tracés dessinés à la main (un entonnoir, deux courbes en SVG étiré, deux répartitions en `div`) deviennent de vrais graphiques, avec axes, infobulles, annotations datées et étiquettes qui se placent seules. **SIX INDICATEURS DE TÊTE OUVRENT LA ZONE DES CHIFFRES** — dépôts, jours avant la clôture, avancement du comité, taux d'acceptation, séances créées, inscriptions : un graphique répond à « comment cela évolue », il ne répond pas à « où en est-on », qui est pourtant la question qu'on se pose en ouvrant l'écran. **CHACUN SE TRACE À UNE COLONNE DU MODÈLE**, sans exception (`mv_proposal_funnel`, `mv_reviewer_workload`, `mv_daily_registrations`, `event.effective_deadline()`) ; le contrat de l'écran gagne `kpis` et `TrendPoint.moyenne_7j`. **LA MOYENNE MOBILE SUR SEPT JOURS ÉTAIT DANS LE MODÈLE DEPUIS LE PREMIER JOUR ET N'ÉTAIT AFFICHÉE NULLE PART** : `moyenne_mobile_7j` devient la seconde série des deux courbes, celle qui dit si ça accélère. **AUCUNE COULEUR N'EST ÉCRITE DANS LES OPTIONS** : ApexCharts veut des couleurs littérales, `useChartTheme` les lit dans les jetons de design par un élément témoin, à l'exécution, donc dans la nuance du thème actif. Sur une barre dont la teinte vient de la base (`taxonomy_terms.color_hex`), l'encre du chiffre est choisie **par mesure de contraste** entre les deux jetons de texte — un blanc fixé en dur disparaîtrait le jour où un administrateur choisit un jaune pâle. **SIX DÉFAUTS TROUVÉS EN ÉPROUVANT L'ÉCRAN, dont deux qui trompaient la lecture.** (1) Le greffon chargeait les ~500 ko d'ApexCharts sur **toutes** les pages, page publique d'événement comprise — mesuré dans le navigateur ; l'import est devenu dynamique, et la bibliothèque ne part qu'au premier graphique rendu. (2) La fusion d'options d'ApexCharts **perd les fonctions** : au premier changement de thème, les formats d'axe disparaissaient (« 0.00, 1.00 ») et la seconde série gardait la couleur du thème précédent — blanche sur fond blanc. Le tracé est désormais remonté par une clé qui porte le thème et la langue. (3) La palette était relue en réaction au magasin de préférences, donc **avant** que `app.vue` n'ait posé `data-theme` sur le `<html>` : les graphiques gardaient un thème de retard. Un `MutationObserver` ne peut pas se tromper de moment. (4) `forceNiceScale` sur une série de comptes produisait « 1, 2, 2 » et perdait la graduation la plus haute — le pas est calculé pour tomber sur des entiers, avec une graduation de respiration au-dessus du pic. (5) La bibliothèque réserve pour un libellé d'axe la largeur du texte **tronqué**, puis lui ajoute ses points de suspension : « Agriculture et alimentation » se faisait couper au commencement (« griculture… »). (6) La jauge d'avancement du comité était peinte en rouge parce que la carte signale des retards : « 31 revues rendues sur 39 » se lisait comme « 80 % d'échec ». Une jauge mesure une avancée ; l'alerte est passée sur la pastille d'icône. **DEUX RAPPORTS FAUX ÉCARTÉS, ET C'EST LE JEU DE DONNÉES QUI LES A RÉVÉLÉS** : « 18 séances sur 16 dossiers retenus » — `sessions_programmees` compte les séances issues de **tout** dossier de l'appel, retenu ou non, les deux ensembles ne sont pas emboîtés, et la carte ne montre donc plus de dénominateur ; et la variation sur sept jours n'est ni verte ni rouge — une hausse des dépôts est une bonne nouvelle, une hausse des retards non, et la même couleur pour les deux apprend à lire la flèche à l'envers. **LES JOURS DES PROJECTIONS SONT DÉSORMAIS FORMATÉS EN UTC**, ce qui corrige un décalage latent : ces jours sont des dates civiles découpées en UTC, et les rendre dans le fuseau de Belém les reculait d'un jour. Éprouvé au navigateur : les sept graphiques dessinés, bascule clair ↔ sombre **sans rechargement** (palette, encre, infobulles suivent), 375 px sans défilement horizontal, libellés d'axe complets, aucun graphique chargé sur la page publique. `npm run typecheck` et `npm run build` au vert ; le paquet d'entrée client perd 80 ko, ApexCharts vit dans un fragment séparé de 496 ko chargé à la demande. | **A10 — gestion des événements.** Deux points laissés ouverts : « combien d'activités retenues attendent encore un créneau » n'a **aucune projection** pour le porter — le chiffre viendra avec elle, pas avec une soustraction fausse ; et les répartitions gardent des barres horizontales plutôt qu'un camembert, décision inchangée depuis A6 |
| 2026-08-18 | Planificateur de créneaux (A9) | **LE MODÈLE A ÉTÉ CORRIGÉ D'ABORD, sur deux points, et le premier était consigné comme bloquant depuis le 16/08** (écart n° 10, « à faire avant A9, pas pendant »). **(1) `detect_conflicts()` remontait en gravité BLOQUANTE toute paire de séances simultanées de l'édition**, y compris un atelier tenu dans une salle VIRTUELLE, qui n'occupe pourtant aucun mètre carré du pavillon : le planificateur aurait affiché un conflit matériel qui n'en est pas un, et l'équipe aurait appris à ignorer son bandeau d'alerte — exactement ce qu'une alerte ne doit jamais devenir. La branche « stand unique » ne retient plus que les séances installées dans une salle PHYSIQUE (`enforce_room_exclusivity`), et **écarte les deux séances d'une MÊME salle**, que la branche `room` dit mieux en nommant la salle : chaque double réservation était comptée deux fois. **Le critère est le LIEU, jamais le format** — une table ronde `online` diffusée depuis le studio du pavillon l'occupe bel et bien, une séance `hybrid` posée en salle virtuelle non. Corollaire : une séance SANS SALLE n'occupe rien, ce qui est l'état normal d'une activité retenue et pas encore installée. **(2) `publication_readiness()` glissait un `tstzrange` BRUT dans un texte d'affichage** — « (["2027-11-12T14:00:00-03:00",…) » au milieu d'une phrase française, mesuré à l'écran. La fonction rend désormais `occurs_at`, un instant que l'interface situe dans le fuseau de l'édition et la langue du lecteur ; elle gagne au passage le contrôle **« Séance sans lieu ni précision de lieu »**, que les mocks appliquaient sans que le SQL le connaisse. Base rechargée de zéro, `make check-db` vert, les deux signatures vérifiées en base. **1 page** `app/pages/admin/programmation.vue`, **5 composants** sous `app/components/admin/planner/` (bandeau de conflits, panneau des activités à placer, carte d'activité, grille salle × heure, panneau de séance, récapitulatif de publication), **1 utilitaire pur** (`utils/planner.ts` — filtres, facettes, index des conflits par séance, conversions d'heure), **1 fichier de contrats** (`types/admin-planner.ts`), **1 fichier de mocks** (`admin-planner.ts`, qui rejoue les trois dérivations du trigger `tg_sessions_derive_fields()`), **2 fichiers de traduction** ; `useApi()` se découpe une seconde fois (`composables/api/planner.ts`, sept appels dont quatre écritures) et tient à 945 lignes. **RIEN NE REFUSE UN CHEVAUCHEMENT, ET CELA SE LIT DANS LES TYPES** : `schedule()` rend la séance ET les conflits recalculés pour toute l'édition, jamais une erreur « créneau occupé ». Éprouvé au navigateur — une carte déposée à 09:00 sur un créneau déjà pris a été acceptée, **les deux blocs sont passés au rouge avec leur compteur**, et le bandeau de 4 à 5 chevauchements. **PLACER, DÉPLACER, REDIMENSIONNER, RETIRER SONT UNE SEULE ÉCRITURE**, parce que la base n'en distingue pas : `room_id`, `starts_at`, `ends_at`. « Retirer » n'est pas supprimer — la séance retourne au panneau avec son créneau souhaité. **CINQ SÉANCES DES DONNÉES SIMULÉES PERDENT LEUR SALLE** : sans elles, le panneau latéral et son compteur n'avaient aucune matière, et l'état « retenue mais pas encore installée » n'existait nulle part dans le jeu. **DEUX CHEMINS POUR PLACER, TOUJOURS LES DEUX** — le glisser-déposer ne fonctionne ni au clavier ni sur une tablette ; le panneau de séance fait la même chose en deux temps, et c'est le seul chemin offert sous 1024 px. **QUATRE DÉFAUTS TROUVÉS EN ÉPROUVANT L'ÉCRAN, dont un qui annulait tout l'écran** : vue-cal ne charge son module de glisser-déposer qu'à sa CRÉATION et seulement si l'édition est déjà ouverte — or le droit d'arbitrer arrive après le premier rendu, la grille naissait donc en lecture seule et le restait, sans le moindre message (`editable` fait désormais partie de la clé du composant) ; le bouton « Enregistrer » du panneau était actif à l'ouverture, faute de comparer des INSTANTS plutôt que des chaînes (`…-03:00` et `…Z` désignent la même seconde) ; « Aucun événement » s'affichait une fois par colonne de salle ; et le bandeau de conflits coupait « 4 chevauchements » en deux à 375 px. Vérifié dans un navigateur : bandeau et détail des quatre chevauchements avec lien vers chaque cas, **le faux conflit du 17 novembre a disparu** (séance en salle virtuelle face à une séance physique), placement par glisser-déposer et par sélection en deux temps, compteur de 5 à 4 activités restantes, récapitulatif de publication bloqué à 8 points avec leurs dates lisibles, bandeau VERT et « tout est placé » sur le cycle PACO, 375 px sans défilement horizontal, anglais complet, thème clair, zéro clé brute. **REPRIS LE MÊME JOUR APRÈS DEUX RETOURS DU COMMANDITAIRE.** (1) « Pas besoin de plusieurs colonnes, tout se passe dans la même salle, on a un seul stand » : les colonnes de salles sont retirées, et surtout **le jeu de données est aligné sur la règle métier n° 3** — le pavillon déclarait DEUX salles physiques (Baobab et Fromager), ce qui laissait croire à deux activités simultanées possibles ; elles n'en font plus qu'une, `ROOM.stand`. Vérifié : les trois paires simultanées du jeu restent trois, aucune n'apparaît ni ne disparaît, et la programmation publique affiche « Stand du pavillon ». Un bloc porte désormais le nom de sa salle SEULEMENT quand ce n'est pas le stand — c'est ce qui explique qu'une séance en ligne ne produise aucun conflit face à sa voisine. (2) « L'affichage en jour une colonne, puis en semaine plusieurs colonnes jours » : le calendrier gagne une bascule **Jour / Semaine**, portée par l'URL (`?vue=semaine`) — le jour pour poser un créneau à l'heure près, la semaine pour voir l'équilibre du programme, qu'on ne voit pas en feuilletant douze jours un par un. Le glisser-déposer fonctionne dans les deux vues (éprouvé : dépôt sur une colonne de jour, salle attribuée « Stand du pavillon », créneau juste), et la semaine tient à 375 px sans défilement horizontal. (3) « Ajoute mois et années » : le calendrier compte désormais **quatre vues** — jour, semaine, mois, année. (4) « En vue mois, on peut juste afficher le nombre d'activités comme pour la vue années » : le mois et l'année ne montrent plus de blocs mais un **décompte par case**, teinté par le pire conflit qu'il contient — la journée à reprendre se repère sans rien ouvrir. (5) « En vue mois aussi on doit pouvoir glisser-déposer » : le dépôt y fonctionne, avec une règle explicite — la grille du mois n'a pas d'axe horaire, donc **seule la DATE vient du dépôt** ; l'heure est celle demandée dans le dossier, à défaut celle que la séance porte déjà, à défaut 9 h. Sans cette règle, la bibliothèque aurait déduit l'heure de la position verticale du curseur dans la case — sans erreur, sans avertissement, et faux. L'année reste en lecture : une case y vaut un mois, aucune date ne s'en déduit sans l'inventer. (6) « En mode nuit, les lignes du tableau ne sont pas visibles » : la grille était tracée en `--color-separator`, soit gris-800 sur un fond presque aussi sombre. Elle suit désormais `--color-border`, et le jour retenu est teinté à l'accent au lieu des gris pâles de la bibliothèque. (7) « Les filtres du bloc À placer ne sont pas nécessaires, en plus d'être encombrants » : les quatre listes déroulantes mangeaient toute la hauteur visible du panneau — **aucune activité n'était lisible avant de défiler**. Recherche et tri restent dehors, les trois filtres se replient derrière un bouton qui porte leur nombre, et des jetons rappellent lesquels sont actifs — un filtre qu'on ne voit pas fait chercher longtemps une activité qui est bien là. `npm run typecheck` et `npm run build` au vert. | **A10 — gestion des événements.** Trois manques relevés en chemin : **aucune permission ne distingue « composer la grille » de « publier le programme »** (l'écran teste `programme.session.schedule` pour les deux, comme le modèle le prévoit) ; **rien ne crée les séances à l'acceptation d'un dossier** — l'écran suppose qu'elles existent sans salle, ce que l'API devra faire (écart n° 57) ; et le chemin « publication effective » n'a pas pu être éprouvé sur les données simulées, qui portent des conflits volontaires |
| 2026-08-18 | Fiche d'évaluation (A8) | **AUCUNE MODIFICATION DU MODÈLE : pour la première fois du jalon, le SQL suffisait.** `reviews`, `review_scores`, `review_assignments`, `review_criteria`, `proposal_comments` et `proposal_transitions_allowed` portaient déjà tout ce que l'écran demande — jusqu'à `calls_for_proposals.blind_review`, qui décide du voile, et `recusal_reason`, qui trace le déport. **1 page**, **10 composants** sous `app/components/admin/review/`, **1 utilitaire pur**, **1 fichier de contrats**, **1 fichier de mocks**, **2 fichiers de traduction**. **LE VOILE DE L'ÉVALUATION EN AVEUGLE EST APPLIQUÉ À LA SOURCE, PAS À L'ÉCRAN** : tant que ma revue n'est pas déposée, les revues de mes pairs ne sont PAS dans la réponse — les masquer en CSS les laisserait lisibles dans l'onglet réseau, c'est-à-dire un voile qui ne cache rien. Ce qui reste visible sous le voile est le NOMBRE et l'avancement nominatif : savoir que deux revues existent n'ancre personne, et l'on relance un collègue en retard sans connaître sa note. **`blind_review` de l'appel COP31 bascule à `true`** (valeur par défaut du modèle), l'arbitrage du commanditaire du 16/08 restant ouvert — les deux modes sont écrits, la COP30 simulée reste à découvert. **DEUX DÉFAUTS HÉRITÉS TROUVÉS EN ÉPROUVANT L'ÉCRAN, dont un bloquant** : `useApi().identity.administeredEvents()` réimplémentait le périmètre d'administration avec `['super_admin', 'admin', 'programmer']` **codés en dur**, quand `identity.administered_events()` teste la PERMISSION `programme.proposal.read_all` — un membre du comité se voyait donc refuser l'accès à la fiche des dossiers qu'on venait de lui confier (« Accès refusé », mesuré) ; et M. Duchesne, membre du comité, figurait comme PANÉLISTE d'un dossier qu'il note. **`useApi.ts` A COMMENCÉ À SE DÉCOUPER** — la dette ouverte le 17/08 : les cinq appels de cet écran vivent dans `composables/api/proposal-review.ts`, montés par une fabrique qui reçoit `call` et `send`. Le fichier tient à 932 lignes au lieu de 1 050. Parcours complet éprouvé au navigateur : notation des six critères, total pondéré recalculé en direct (26 / 40 → 13 / 20), dépôt, **voile levé à la seconde**, moyenne du dossier passée de 15,25 à 14,50, message partagé confirmé avant envoi, décision « Retenir » appliquée. `npm run typecheck` et `npm run build` au vert. | **A9 — le planificateur de créneaux** (vue-cal, glisser-déposer, conflits non bloquants). Un seul arbitrage reste ouvert, la valeur par défaut de `blind_review` : **« qui a le droit de demander des corrections » a été tranché le 18/08** — le rôle `admin` n'a pas `programme.review.write` par défaut, et cela se changera au besoin depuis la gestion des rôles (A12), puisque l'autorisation est une donnée (écart n° 50) |
| 2026-08-18 | Liste des propositions (A7) | **LE MODÈLE A ÉTÉ CORRIGÉ D'ABORD, pour la septième fois du jalon.** `v_proposal_dashboard` portait l'avancement des revues et le classement, mais **rien de ce qui identifie un dossier dans un tableau de quarante lignes** : ni format, ni pays, ni thématiques, ni co-organisateurs, ni qui l'évalue, ni ce qui est en retard. L'écran aurait rechargé quatre tables et refait les correspondances lui-même — c'est-à-dire perdu la raison d'être de la vue, et reproduit le défaut n°1 de la v1 sur les libellés de thématiques. La vue gagne **treize colonnes** (§ 7) et le fichier une fonction, **`programme.unread_proposals_for(personne, édition)`** : « non consulté » est une relation entre un dossier et un LECTEUR, elle ne peut pas être une colonne. Objets chargés et éprouvés sur PostgreSQL 17. **1 page**, **4 composants**, **2 utilitaires purs**, **1 fichier de contrats**, **4 fichiers de mocks**, **2 fichiers de traduction**. **`mocks/permissions.ts` APPARAÎT ICI, ET C'EST LE PREMIER ÉCRAN QUI TIENT VRAIMENT LA RÈGLE** « tester une permission, jamais un rôle » : les rôles étaient simulés depuis A0.3, mais rien ne disait ce qu'un rôle PERMET — les écrans en étaient réduits à « cette personne administre-t-elle quelque chose ? », un test de rôle déguisé. Affecter demande `event.call.manage`, décider `programme.proposal.decide`, chacune **avec sa portée**. **Trois défauts trouvés en éprouvant l'écran dans le navigateur** : `line-clamp-2` n'existait pas dans la feuille produite (titres sur six lignes), une pastille thématique insécable imposait le quart de la largeur du tableau, et « Aucun révisionniste affecté » s'affichait sur des dossiers portant trois revues rendues. `npm run typecheck` et `npm run build` au vert. | **A8 — la fiche d'évaluation.** La ligne du tableau y mène déjà (`/admin/propositions/:id`), comme le tableau de bord depuis le 17/08 : **la route n'existe pas encore et rend un 404**. C'est le prochain prompt |
