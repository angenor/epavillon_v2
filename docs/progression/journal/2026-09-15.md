# Journal — 2026-09-15

> Extrait de la [progression](../../PROGRESSION.md). Une ligne par session, la plus récente en haut.

| Date | Session | Fait | À suivre |
|------|---------|------|----------|
| 2026-09-15 | **Liste des propositions du back-office : un tableau devient une liste, avec vignette (A7)** | Sur demande du commanditaire : **colonnes « Thèmes » et « Dossier » retirées**, et `AdminProposalsTable` (sur `UiTable`) remplacé par `AdminProposalsList` — rangées composées : case, **vignette de couverture** (placeholder icône `image` si absente), statut, titre-lien étendu à la rangée, métadonnées à icônes (porteur +N, pays, format, dépôt), puis revues en segments et note /20 avec rang. Le tri passe par une liste « Trier par » + bouton de sens (le tri par numéro de dossier n'est plus proposé). **Modèle d'abord** : `v_proposal_dashboard.cover` (`media.attached_image`, rôle `cover` du dossier), vue remplacée **à chaud** en base de dev ; `LigneDePilotage.cover`, `.sqlx` régénéré, `make openapi` + contrat au vert, `nuxt typecheck` à 0. Icône `image` ajoutée à `UiIcon`. **Vérifié au navigateur sur données d'exemple** (thème sombre, clair, 375 px sans défilement horizontal) | **Aucun écran ne dépose encore d'image de couverture sur un dossier** : toutes les rangées montrent le placeholder tant que le formulaire de dépôt ou la fiche d'évaluation n'offre pas ce champ. Non éprouvé contre l'API réelle. Production : la vue est à recharger |
| 2026-09-15 | **Formulaire de dépôt : cinq ajustements du commanditaire (`docs/observations1.md`)** | **Résumé retiré** du formulaire et du modèle (`programme.proposals.summary` supprimée ; la séance garde son propre `summary`, rédigé par l'IFDD — elle n'est plus recopiée depuis le dossier). **Thématiques obligatoires** et **catégories à choix multiple et obligatoires** : `activity_type_code` supprimée, les catégories passent par `reference.entity_terms` comme les thématiques (`repo/themes.rs` généralisé par `Classement`), `v_proposal_dashboard` expose `category_codes`/`categories`, et le dépôt refuse un dossier sans l'une ou l'autre (422 nommant `theme_codes` ou `category_codes`) — jamais le brouillon. **Format** : `allowed_formats` vaut `{in_person}` par défaut ; un appel qui n'ouvre qu'un format l'affiche sans le faire choisir et le pose d'office. **Phrase « Seuls les formats ouverts… » retirée.** **« Ce qui se passe après l'envoi » éditable au back-office** : nouvelle colonne `event.calls_for_proposals.submission_next_steps` (texte, une étape par ligne, défaut fr/en), saisie dans `CallPanel`, rendue par `StepReview` — bloc masqué si vide. Base de dev migrée **sans `down -v`** (la catégorie du dossier existant reportée en rattachement) ; schéma complet rechargé dans une base jetable, zéro erreur. Vérifié : `cargo clippy --workspace -D warnings`, `cargo test --workspace` vert — 24 tests d'`engagement` (`rappels`, `regles`) ont d'abord échoué parce que Docker s'est arrêté en cours de suite, et repassent après `make up`, `nuxi typecheck` à 0, `make openapi` + contrat, `make sqlx-prepare` | **Production** : le schéma y est chargé à l'ancienne — recharger la base (elle ne porte aucune donnée métier) avant de déployer. **L'appel COP31 doit être passé en présentiel seul** au back-office s'il ouvre encore les trois formats : le défaut ne touche que les nouveaux appels. **Non vérifié dans un navigateur.** Le nombre d'évaluateurs n'est plus lu sur l'appel dans ce bloc : il s'écrit dans le texte |
| 2026-09-15 | **Le droit de déposer n'était attribué à personne (écart n° 74), et les occurrences se figent à une** | **Mesuré sur un compte fraîchement inscrit** : adhésion active, organisation créée, et pourtant `FORBIDDEN` au dépôt. Les permissions se lisent sur `identity.role_assignments` ; **aucun chemin d'écriture n'attribuait `org_manager` ni `org_member`**, ni la création d'organisation, ni l'approbation d'une demande, ni l'acceptation d'une invitation. Le défaut ne se voyait pas parce que **les seuls comptes existants étaient super-administrateurs**. Corrigé **en base**, comme le rattachement principal juste à côté : `tg_memberships_sync_role` pose le rôle sur la portée `organization` dès qu'une adhésion est active, le révoque quand elle cesse de l'être ou change de rôle. Trois chemins d'écriture ne peuvent pas s'en souvenir chacun de leur côté. Base locale rattrapée pour les adhésions déjà actives. **Occurrences souhaitées** : champ en lecture seule à 1 — la colonne accepte toujours plusieurs occurrences, seule la saisie est fermée | **Production** : le rattrapage des adhésions existantes est à rejouer si la base n'est pas rechargée. Toujours **pas de vérification au navigateur** |
