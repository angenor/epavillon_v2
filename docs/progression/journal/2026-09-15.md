# Journal — 2026-09-15

> Extrait de la [progression](../../PROGRESSION.md). Une ligne par session, la plus récente en haut.

| Date | Session | Fait | À suivre |
|------|---------|------|----------|
| 2026-09-15 | **Formulaire de dépôt : cinq ajustements du commanditaire (`docs/observations1.md`)** | **Résumé retiré** du formulaire et du modèle (`programme.proposals.summary` supprimée ; la séance garde son propre `summary`, rédigé par l'IFDD — elle n'est plus recopiée depuis le dossier). **Thématiques obligatoires** et **catégories à choix multiple et obligatoires** : `activity_type_code` supprimée, les catégories passent par `reference.entity_terms` comme les thématiques (`repo/themes.rs` généralisé par `Classement`), `v_proposal_dashboard` expose `category_codes`/`categories`, et le dépôt refuse un dossier sans l'une ou l'autre (422 nommant `theme_codes` ou `category_codes`) — jamais le brouillon. **Format** : `allowed_formats` vaut `{in_person}` par défaut ; un appel qui n'ouvre qu'un format l'affiche sans le faire choisir et le pose d'office. **Phrase « Seuls les formats ouverts… » retirée.** **« Ce qui se passe après l'envoi » éditable au back-office** : nouvelle colonne `event.calls_for_proposals.submission_next_steps` (texte, une étape par ligne, défaut fr/en), saisie dans `CallPanel`, rendue par `StepReview` — bloc masqué si vide. Base de dev migrée **sans `down -v`** (la catégorie du dossier existant reportée en rattachement) ; schéma complet rechargé dans une base jetable, zéro erreur. Vérifié : `cargo clippy --workspace -D warnings`, `cargo test --workspace` vert — 24 tests d'`engagement` (`rappels`, `regles`) ont d'abord échoué parce que Docker s'est arrêté en cours de suite, et repassent après `make up`, `nuxi typecheck` à 0, `make openapi` + contrat, `make sqlx-prepare` | **Production** : le schéma y est chargé à l'ancienne — recharger la base (elle ne porte aucune donnée métier) avant de déployer. **L'appel COP31 doit être passé en présentiel seul** au back-office s'il ouvre encore les trois formats : le défaut ne touche que les nouveaux appels. **Non vérifié dans un navigateur.** Le nombre d'évaluateurs n'est plus lu sur l'appel dans ce bloc : il s'écrit dans le texte |
