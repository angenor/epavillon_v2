# Journal — 2026-08-20

> Extrait de la [progression](../../PROGRESSION.md). Une ligne par session, la plus récente en haut.

| Date | Session | Fait | À suivre |
|------|---------|------|----------|
| 2026-08-20 | **B3 — phase 4 : le périmètre borne aussi les événements (T042–T053)** | **US2 est livrée** : la liste bornée par le périmètre avec ses facettes, le détail des six onglets en une réponse, le sélecteur du back-office, et le refus sur URL forgée. **Trois choses valent d'être retenues.** (1) **Le contrat du front documente une permission qui n'existe pas** — `programme.proposal.review` ; le code réel du modèle est `programme.review.write`, celui-là même que les données simulées emploient. Il vit dans `repo/cross.rs` et non dans `domain/permissions.rs` : ici, c'est un **critère de liste**, pas un garde — il ne protège aucune route. (2) **Une jointure sur `identity.people` s'était glissée dans `repo/tracks.rs`** pour résoudre le nom du responsable d'un fil : la première fissure dans la frontière que `repo/cross.rs` existe pour tenir. Retirée le jour même — c'est exactement l'accroissement invisible que l'en-tête de ce fichier annonce. (3) **`time::Time` ne se sérialise pas en `HH:MM:SS`** mais en ISO 8601 complet : la plage d'accueil du pavillon est rendue **en texte par la base**, plutôt que remise en forme en Rust — ce qui aurait fait une seconde écriture de l'heure. **Deux simplifications dictées par la relecture** : la liste et le détail lisaient la même édition par deux requêtes voisines de vingt-cinq colonnes, ramenées à une seule paramétrée par le périmètre et un identifiant facultatif ; et les décomptes de la liste, pris une par une, tiennent maintenant en une requête par `unnest`. **Les facettes se dérivent des lignes déjà lues** et jamais du catalogue : proposer au filtre toutes les séries de la plateforme donnerait à un compte détaché la liste de ce qu'il n'administre pas — divulguer par la facette ce que la liste vient de masquer. **Le test du détail porte une séance réelle**, placée en salle, diffusée et rattachée à un fil : elle doit se retrouver dans les cinq compteurs qui la concernent et pas sur le canal général. Sans elle, tous les décomptes vaudraient zéro et le test ne prouverait rien. | **T049 reste ouverte, et c'est exact** : les six routes paramétrées par un identifiant d'enfant naissent en phases 5, 7 et 9 ; le garde et son épreuve sur les six cibles sont livrés, chaque route qui arrive cochera sa part. Six dépôts sont créés avec leur **seule lecture** — `tracks`, `venues`, `channels`, `calls`, `criteria`, `committee` —, le détail en ayant besoin dès maintenant ; leurs écritures viennent avec leurs phases. Suite : **phase 5, l'appel unique et sa grille (T054–T068)**, qui referme le jalon que le prompt demande. |
| 2026-08-20 | **B3 — phase 3 : une édition existe, avec un sigle qu'on peut épeler (T025–T041)** | **US1 est livrée** : créer et modifier une édition, la règle du sigle, la traduction des six contraintes, et le calendrier daté dans le bon fuseau. **Trois choses valent d'être retenues.** (1) **T014 n'avait jamais été écrit** — un `cd` en échec au début d'une commande composée avait avalé le corps du fichier, et rien ne le signalait : la vérification portait sur la première ligne, identique dans l'ébauche de la phase 1. Le fichier compilait parce que personne ne le référençait encore. Rétabli en ouvrant la phase 3. (2) **La traduction des contraintes branche sur le NOM de la contrainte, pas sur le texte du message** — ce qui a obligé à faire remonter l'erreur **brute** du dépôt jusqu'au service : `repo::editions::inserer` et `modifier` rendent un `sqlx::Error`, sur l'idiome posé en B1 (`repo/admin_users.rs`). Traduire dans le dépôt aurait perdu le nom, seule chose sur laquelle on ait le droit de brancher. (3) **`PG_DIAG_DATATYPE_NAME` rend le nom du domaine NU** — « slug », pas « platform.slug » : le schéma voyage dans un champ à part. Mesuré sur la base (`SELECT 'Mauvais Slug'::platform.slug`) après qu'une hypothèse contraire eut fait sortir un slug mal formé en erreur 422 anonyme au lieu d'un refus de formulaire. **Deux choix de conception dictés par le modèle** : `numeric` n'a pas de décodeur dans les caractéristiques SQLx du workspace, donc les coordonnées passent par `::float8` en lecture et `$n::float8::numeric(9,6)` en écriture, comme le score de B2 ; et la liste des fuseaux vient de `pg_timezone_names`, **le dictionnaire même** contre lequel `platform.timezone_name` vérifie ce qu'on écrit. **Le test du fuseau est écrit pour tomber si le calcul se fait en UTC** : la COP31 s'y termine le 20 novembre à 22 h à Belém, soit le **21** en temps universel — douze journées d'un côté, treize de l'autre. 23 tests unitaires et 26 tests d'intégration ; `make check-back` au vert. | **Phase 4 — US2 : le périmètre borne aussi les événements (T042–T053)** |
| 2026-08-20 | **B3 — phases 1 et 2 : le crate existe et ses fondations tiennent (T001–T024)** | **`backend/crates/modules/event` est créé** et ne dépend que de `kernel` et de `contracts` — `cargo tree -p event | grep -E 'identity|org'` ne rend rien. **Trois codes d'erreur** au catalogue du noyau (`EVENT_GLOBAL_SCOPE_REQUIRED`, `EVENT_CRITERION_HAS_SCORES`, `EVENT_UNKNOWN_REFERENCE`), et la traduction des contraintes du module ajoutée à `pg_error.rs`. **Deux pièges d'ordre y ont été traités en écrivant** : les clés étrangères du module devaient être déclarées **avant** la garde par sous-chaîne de B1, sans quoi `events_country_id_fkey` sortait en `IDENTITY_UNKNOWN_REFERENCE` ; et les unicités du module **avant** le fourre-tout `("23505", _)`, qui les aurait toutes rendues en conflit anonyme. **Le cas des domaines est outillé plutôt que deviné** : `pg_error::data_type()` lit `PG_DIAG_DATATYPE_NAME` par un `try_downcast_ref` vers `PgDatabaseError` — vérifié sur sqlx 0.9, la méthode existe —, parce que le nom de contrainte d'un domaine est celui du domaine (`slug_check`) et ne dit ni la table ni la colonne. **Six charges utiles d'événements** dans `contracts/event.rs`, dont celle de la publication qui porte le **prédicat exact** des séances à publier. **Les six résolutions d'ascendance** sont écrites dans `repo/cross.rs`, sous l'en-tête qui énumère les neuf lectures hors schéma autorisées ; celle du canal rend un **double `Option`**, parce qu'un canal général de la plateforme n'est ni introuvable ni hors périmètre. **Le préfixe `/admin/planner` est composé une seule fois dans `api`**, cinq lignes posées avant que le défaut de B2 ne se reproduise en B5. **La clôture automatique des appels** appelle `event.effective_deadline()` plutôt que de recopier son expression, émet elle-même — aucun déclencheur du module n'émet — et se replanifie sur une grille ancrée à l'époque Unix, pour que dix redémarrages dans le même créneau ne produisent qu'un seul travail. `make check-back` au vert. | **Phase 3 — US1 : l'édition, son sigle et son calendrier (T025–T041)** |
| 2026-08-20 | **B3 découpé — 129 tâches en 11 phases** | **`specs/003-evenements/tasks.md` : 129 tâches, dont 31 tests, une phase par histoire et quatre jalons de livraison.** **Trois avertissements sont écrits en tête du découpage**, parce que chacun coûterait cher s'il était découvert en chemin. (1) **Supprimer un critère d'évaluation détruit les notes, et l'ordre RÉUSSIT** — `ON DELETE CASCADE` : le refus (T060) et le test qui vérifie que **les notes sont toujours là après le refus** (T067) vont ensemble, sans la seconde moitié le test ne prouve rien. (2) **Le préfixe `/admin/planner` sera partagé avec B5** : il se compose dans `api` dès les fondations (T018), pas quand le défaut se reproduira. (3) **Trois chemins littéraux doivent être déclarés avant leur homologue paramétré**, sans quoi `public` est capturé comme adresse d'URL. **Le découpage a fait apparaître une dépendance que le plan n'avait pas nommée** : la phase 4 bloque les phases 7 à 9, parce que **toutes les écritures d'onglet rendent la composition entière** — `service/detail.rs` doit exister avant la première d'entre elles. **Le parallélisme le plus rentable** : les phases 3 et 6 ne partagent aucun fichier, deux histoires P1 menées de front. **Le jalon que le prompt demande** est T001–T068 : édition, périmètre, appel et grille — à ce point la campagne de la COP31 peut être ouverte. | `/speckit-implement` sur B3 |
| 2026-08-20 | **B3 planifié — 20 décisions, 37 routes, une seule entorse** | **`specs/003-evenements/` est complet** : `plan.md`, `research.md` (**20 décisions**), `data-model.md`, `contracts/` (routes, erreurs, événements), `quickstart.md`. Contrôle constitutionnel passé **deux fois**, **une entorse justifiée**. **37 routes, 6 événements, 3 codes d'erreur, 1 travail différé, aucune dépendance nouvelle, et — contrairement à B2 — aucun SQL composé dynamiquement.** **Deux écarts de plus, n° 91 et 92, dont un que seul le plan pouvait révéler.** (91) **Supprimer un critère d'évaluation détruit les notes, en silence** : `xmod_fk_review_scores_criterion` est `ON DELETE CASCADE`, l'ordre réussit, et l'argumentaire d'une décision de sélection disparaît — précisément ce que la v1 n'avait pas. Le service compte et refuse ; **c'est l'unique entorse au principe VIII de tout le plan**. (92) **Cinq tables du module ne sont pas auditées** — journées, fils, lieux, salles, canaux : « qui a renommé la salle Baobab ? » n'a pas de réponse. Consigné, non compensé. **Quatre décisions structurantes.** (1) **Les jours civils se calculent en base, dans le fuseau de l'édition** — deux bases de fuseaux dans une même chaîne, c'est ce qui a fait tomber le formulaire du front sur `Europe/Geneva`. (2) **Tout ce qui détache une séance le compte AVANT** : les clés sont `ON DELETE SET NULL`, après coup le chiffre serait zéro. (3) **Le préfixe `/admin/planner` est composé une seule fois dans `api`**, avant que B5 n'y vienne — le patron de `/people`, appliqué avant que les trois routes muettes de B2 ne se reproduisent. (4) **La publication annonce au lieu d'écrire**, avec le **prédicat exact** des séances, pour que le chiffre annoncé et l'effet obtenu viennent du même raisonnement. **Deux ajouts additifs au contrat du front**, ignorés jusqu'à B7 : le sigle proposé et le refus de suppression d'un critère. | `/speckit-tasks` sur B3 |
| 2026-08-20 | **B3 spécifié — le module Événements** | **`specs/003-evenements/spec.md` : 8 histoires utilisateur, 16 cas limites, 99 exigences fonctionnelles, 25 critères de réussite, et AUCUNE modification du SQL.** `060_events.sql` relu en entier : les huit sections couvrent déjà tout ce que le prompt demande. **Trois vérifications changent la spécification.** (1) **Aucun déclencheur du module n'émet d'événement de domaine** — le fichier ne porte que deux déclencheurs d'audit, sur les éditions et sur les appels : le piège n° 1 rencontré en B1 (anonymisation) et en B2 (fusion), où la base émet déjà ce que le service s'apprête à émettre, **n'existe pas ici**, et le service émet tout lui-même. (2) **Le rôle `programmer` ne détient AUCUNE permission `event.*`** — l'écart n° 5 d'A10 supposait qu'un chargé de programmation pouvait composer les journées spéciales ; le semis dit le contraire, seuls `admin` et `super_admin` détiennent la gestion des événements et celle des appels. (3) **La publication de la programmation traverse deux schémas** : la date vit sur `event.events`, la visibilité de chaque séance sur `programme.sessions.published_at`, que `v_public_schedule` filtre. La frontière retenue est l'outbox — B3 pose la date et annonce, la Programmation consomme. **L'écart n° 9 est traité par l'option A** : le sigle reste facultatif en base et devient obligatoire dans le SERVICE pour une édition tenant un pavillon, de 2 à 12 caractères, avec **une valeur par défaut proposée** — un refus qui ne propose rien fait chercher une convention que personne n'a écrite. **Le périmètre d'administration borne les six onglets et les quinze écritures**, création comprise, qui exige la portée globale. **Deux écarts d'A3 se referment** : les éditions publiques deviennent une ressource (n° 26) et la page d'une édition embarque ses trois images (n° 25). **Quatre écarts nouveaux, n° 87 à 90.** | `/speckit-plan` sur B3. Deux points restent à arbitrer avec le commanditaire : la granularité du calendrier d'une série de webinaires (302 journées vides sur `paco2027`) et l'absence de permission `event.*` sur le rôle de programmation |
| 2026-08-20 | **Les deux constats laissés ouverts, et le typecheck qui ne tournait plus** | **« REMETTRE DANS LA FILE » NE REMETTAIT RIEN — corrigé, backend et mocks.** L'écran range les paires sur `reviewed_at`, que *toute* décision renseigne : un report posé sur une paire écartée la laissait parmi les tranchées. Le service ne savait reprendre qu'une paire **déjà reportée** — c'est-à-dire jamais le cas qui compte, puisqu'on se trompe en écartant, pas en reportant. La règle est maintenant : **un report sur une paire déjà sortie de la file l'y ramène**, quelle qu'ait été la décision, en effaçant les trois colonnes. **Et une paire fusionnée ne se rejuge plus** : la réécrire effaçait la trace de la fusion sans la défaire — l'API refuse. Trois tests (`paire_ecartee_revient_dans_la_file.rs`) et vérification au navigateur : la paire IMRE / Chaire de Montréal repasse de « déjà tranchées » à « à trancher ». Côté mocks, deux défauts jumeaux : `settledDecisionOf()` était lu avec `??`, si bien qu'une décision remise à nul retombait sur celle du jeu de données. **`useApi.ts` REPASSE SOUS LE GARDE-FOU** : 1019 → 807 lignes, le bloc `proposals` (A4 · A7, deux cents lignes) sorti dans `composables/api/proposals.ts` selon le patron déjà en place. **LE TYPECHECK DU FRONT NE TOURNAIT PLUS DEPUIS UN MOMENT, ET PERSONNE NE LE VOYAIT** : `vue-router` était épinglé `^4.5.0` dans `package.json` alors que Nuxt 4.5.2 dépend de `^5.2.0`. La dépendance explicite gagnait, `nuxt typecheck` échouait sur la résolution du greffon `vue-router/volar/sfc-route-blocks` — **avant d'avoir vérifié le moindre type**, donc en silence sur les vraies erreurs. Aligné sur `^5.2.0` : `nuxt typecheck` passe, `npm run build` passe, et le navigateur confirme accueil, programme, back-office, dépôt et invitation dans les deux langues. **T124 exécuté** : `make check` en entier, base détruite et rechargée. | Relancer `make garage-init` avant la prochaine session : le stockage objet est reparti de zéro. |
| 2026-08-20 | **Front — l'écran d'acceptation d'une invitation, et l'enquête sur le bouton de fusion** | **L'ÉCRAN QUI MANQUAIT EXISTE : `app/pages/invitation.vue`.** Le lien du courriel d'invitation menait à un 404 — le défaut que B1 avait déjà payé. La page tient les cinq issues sur le chemin exact que compose `mail.rs` (`/invitation` et `/en/invitation`, **sans `defineI18nRoute`** : traduire ce chemin casserait les liens déjà partis). **Elle n'exige aucune session** — le jeton est la preuve d'adresse, et la personne invitée n'a le plus souvent pas de compte : `middleware: 'guest'` ne redirige personne, il résout seulement la session avant le rendu. Éprouvée au navigateur, français et anglais : acceptation (« Vous avez rejoint le ROAC »), lien périmé, lien déjà utilisé, lien invalide, aucun lien. **Les suites diffèrent** : connectée, la personne va à son espace ; sinon l'écran propose **les deux** — créer son mot de passe d'abord, se connecter ensuite —, parce que l'API ne dit pas si un compte existe derrière l'adresse. Ajoutés : `AcceptInvitationResult` (`types/organization-workspace.ts`), `composables/api/invitation.ts`, `acceptInvitation()` dans les mocks, **trois jetons de démonstration** (`invitation-valide`, `-expiree`, `-utilisee`) et `i18n/locales/{fr,en}/pages/invitation.json`. `npm run build` passe. **LE BOUTON « EXAMINER LA FUSION » NAVIGUE — le défaut consigné ne se reproduit pas.** Huit conditions éprouvées au navigateur (chargement direct, navigation interne, après ouverture et fermeture d'une modale, aller-retour, 375 / 1280 / 1920 px, français et anglais, clic souris réel) : l'écran de fusion s'ouvre à chaque fois sur la paire OSED. **La piste `localePath()` est écartée par la mesure** : instrumenté dans la page, il rend `/admin/organisations/fusion?gauche=…` en français et `/en/admin/…` en anglais — la forme objet `{ path, query }` se résout correctement, et deux autres écrans l'emploient déjà. La seule chose durcie est réelle mais mineure : `openMerge()` **attend** désormais la promesse de `navigateTo`, qu'il ignorait — un refus de navigation ne laissait aucune trace. | Deux constats faits en chemin, consignés dans [`points-bloques.md`](../points-bloques.md) : « Remettre dans la file » **ne remet pas** la paire dans la file (la répartition se fait sur `reviewed_at`, que `deferred` renseigne), et `useApi.ts` tient **1019 lignes** — au-dessus du garde-fou, à découper au prochain écran |
| 2026-08-20 | **B2 — le module Organisations est livré** | **`/speckit-implement` EXÉCUTE LES 129 TÂCHES.** Le crate `backend/crates/modules/org` existe : **21 routes, 6 travaux différés, 6 événements, 11 codes d'erreur, 29 fichiers de test d'intégration sur base réelle**. Clippy passe sans avertissement, aucun fichier ne dépasse 1000 lignes, `cargo tree -p org` ne montre **aucune arête** vers un autre crate de module. **LA CIBLE CHIFFRÉE EST TENUE, ET LARGEMENT** : la recherche répond en **55 ms au 95ᵉ centile sur 5 001 fiches**, pour une cible de 150 ms — médiane 48 ms, pire cas 64. Aucun des trois remèdes prévus par R2 n'a servi, et **aucune ligne de `docs/database/` n'a été modifiée**, comme annoncé. **Les trois pièges prévus par le plan n'ont rien cassé** — c'est tout l'intérêt de les avoir cherchés avant : la fonction de fusion émet elle-même son événement (le service ne le refait pas, et le dit à l'endroit où l'on serait tenté de l'ajouter), l'unicité des adhésions ignore le statut (un unique ordre avec reprise conditionnelle, éprouvé sur cent demandes concurrentes), et les arbitrages de fusion viennent **après** l'appel (une fusion arbitrant le nom légal aboutit, et échouerait dans l'autre ordre). **Le service de jetons est remonté dans le noyau à comportement constant** : les quatre tests de B1 sur la vérification d'adresse, la réinitialisation et le rejeu **n'ont pas été réécrits** et restent verts. Le défaut n° 81 est corrigé — une personne créée par invitation obtient désormais un compte et son lien —, et le test de B1 qui affirmait l'inverse est réécrit en disant pourquoi il avait tort. **Cinq défauts que seul le code pouvait révéler**, dont deux que seule une requête HTTP réelle pouvait montrer : **trois routes sur vingt et une étaient muettes** — deux `web::scope` du même préfixe ne se complètent pas, Actix retient le premier et rend 404 sans essayer le second, et ni la compilation, ni les tests de service, ni la documentation engendrée ne pouvaient le voir ; et une charge utile **exigeait un identifiant que le chemin portait déjà**, obligeant le client à l'envoyer en double. Le premier est désormais tenu par `crates/api/tests/routes_org.rs`, qui frappe **les vingt et une routes sur la vraie application** et n'admet aucun 404 — la leçon de B1 répétée : un test qui appelle un service n'éprouve pas le montage. Les trois autres : cent créations concurrentes sortaient en « service indisponible » parce que la transaction perdante retenait **deux** connexions du pool avant d'être rendue ; la liste du back-office était **vide sur une base neuve**, jointe par l'intérieur à une projection qu'un travail différé seul rafraîchit ; et une faute d'orthographe connue se faisait passer pour une dénomination posée par la base, la normalisation effaçant les accents qui la distinguaient du nom légal. Les trois sont corrigés et inscrits dans `pieges.md`. **Une réserve, mesurée et assumée** : le décompte de transfert d'une fusion est exact au chiffre près sur **dix-sept lignes du registre sur dix-huit** — la dernière est déplacée par un trigger d'adhésion avant que la boucle n'y arrive. Rien n'est perdu, et le corriger demanderait de reproduire l'effet d'un trigger dans un décompte, ce que le principe VIII interdit | **Éprouvé au navigateur le même jour (T068, T125)** : invitation émise par une référente → travail en file avec son jeton → worker → relais du site → **courriel lu dans Mailpit**, avec « Bonjour Invité·e » (le nom n'est pas déduit de l'adresse), son lien et ses sept jours ; le **premier essai a échoué** en `MAIL_RELAY_UNREACHABLE` et **la reprise a réussi**. Acceptation **sans session** : adhésion active, adresse marquée vérifiée, rejeu refusé « déjà utilisé ». Côté site : l'accueil, la garde de connexion, l'espace organisation, la liste du back-office **avec ses facettes comptées**, le refus « droit nécessaire : consultation des organisations » — jamais une liste vide — et la file des doublons avec la paire OSED et ses quatre motifs. **Deux constats pour B7** : le lien d'invitation mène à une page qui n'existe pas — le défaut que B1 avait déjà payé —, et le bouton « Examiner la fusion » ne navigue pas. Reste — l'API et le worker, eux, sont éprouvés pour de vrai : inscription, vérification, connexion, recherche sur l'ancien sigle, rattachement d'office, création, doublon exact nommé, refus de fusion sans portée globale ; le worker réarme sa chaîne, balaie par tranches, recalcule les scores et rafraîchit la projection, et **la charge utile du courriel mort est bien effacée**. Puis `make check` en entier — qui **détruit la base** et impose `make garage-init`. Ensuite **B3** |
| 2026-08-20 | B2 — les tâches sont découpées | **`/speckit-tasks` PRODUIT `tasks.md` : 129 TÂCHES EN 11 PHASES, dont 29 tests.** Une phase par histoire utilisateur, chacune éprouvable seule, avec son point de contrôle. **Quatre jalons de livraison**, et le premier compte : **T001–T035**, la recherche répond juste et les deux lectures rendent enfin des résultats différents — le défaut n° 1 de la v1 est **prévenu**, et cela se démontre en une minute sur la fiche IFDD **que le semis fournit déjà**, avec ses cinq dénominations et ses deux domaines vérifiés. Puis **T001–T054** (l'écran de rattachement A2 entièrement servi), **T001–T099** (le back-office tient le référentiel au quotidien), **T001–T129** (la fusion). **Deux avertissements sont écrits en tête du fichier, parce qu'ils se paieraient à l'exécution.** (1) **La phase 2 touche du code LIVRÉ ET ÉPROUVÉ** : le service de jetons remonte dans le noyau et l'inscription d'`identity` est corrigée — sans quoi US4 n'a que de mauvaises issues. Le déplacement se fait **à comportement constant**, et les quatre tests de B1 sur les jetons ne sont **pas réécrits** : ils sont la preuve. (2) **L'ordre des deux écritures d'une fusion est l'inverse de celui que `progression/api.md` et l'en-tête d'`admin-organizations.ts` annoncent encore** — les deux documents fautifs sont corrigés par T126. Les quatre obligations minimales du principe X ont chacune leur tâche nommée — chemin nominal (T030, T075), URL forgée (T076), invariants traduits (T089, T117, dont un message repris **mot pour mot**), écriture dans l'outbox (T116, **qui compte les événements plutôt que de vérifier leur présence**). Et huit critères de réussite ont le test qui les tient et qu'aucun autre ne tiendrait, dont les 150 ms sur 5 000 fiches (T033) et le décompte de fusion comparé ligne de registre par ligne de registre (T113). Toujours aucun code Rust. | **`/speckit-implement`**. Commencer par les phases 1 et 2, qui sont bloquantes, et ne pas ouvrir d'histoire avant que `cargo tree -p org` ne montre zéro arête entre modules **et** que les tests de B1 sur les jetons soient repassés au vert sans avoir été touchés |
| 2026-08-20 | B2 — le plan est écrit | **`/speckit-plan` PRODUIT `plan.md`, `research.md`, `data-model.md`, `contracts/` (routes, erreurs, événements) ET `quickstart.md`.** Le contrôle des dix principes est passé **deux fois**, avant et après conception : **une entorse, justifiée** — le décompte de transfert d'une fusion **compose son SQL** depuis le registre des références, parce que c'est la seule façon que le chiffre annoncé avant et le chiffre rendu après viennent du même raisonnement ; énumérer les tables en Rust rendrait le décompte faux au premier module qui se déclare au registre, **et personne ne le verrait**. **19 décisions techniques**, 21 routes, 11 codes d'erreur, 6 événements, 6 travaux différés. **Trois décisions structurent le reste.** (1) **Les deux lectures de la recherche sur-lisent** : la limite est appliquée **à l'intérieur** de la fonction, donc filtrer après coup rendrait moins de lignes que demandé, en silence — dix demandées, trois écartées, sept rendues. (2) **Le service de jetons à usage unique remonte dans le noyau** : trois des cinq finalités du modèle n'appartiennent pas à `identity` — l'invitation est le geste de ce module, la confirmation d'un intervenant sera celui de B4 — et aucun crate de module ne peut dépendre d'un autre ; les durées par finalité vivaient **déjà** dans la configuration du noyau depuis B1. (3) **La cible de 150 ms se mesure avant de se traiter**, et le semis doit ressembler à un vrai référentiel : 5 000 noms tirés au hasard n'ont presque aucun trigramme commun, et la mesure serait excellente et fausse. **Six écarts de plus, n° 81 à 86**, dont un défaut de B1 que seul B2 pouvait révéler : **une personne créée par invitation ne pouvait pas se créer de compte** — l'inscription branche sur « adresse connue » et envoie un rappel, alors que l'invitation crée une personne **sans compte** ; le courriel serait parti, le lien aurait marché, l'adhésion se serait activée, et l'invité n'aurait jamais pu se connecter. Deux autres comptent : **l'expression de table qui porte le terme cherché est matérialisée**, donc l'usage de l'index trigramme dépend du planificateur — c'est le seul point qui pourrait faire manquer les 150 ms, il est **mesuré et non supposé**, et modifier le SQL vient en **dernier** dans l'ordre des remèdes ; et **FR-041 n'avait aucun point d'application** — refuser le départ du dernier référent exige une route qui retire un membre, qu'aucun écran n'appelle : elle est livrée, parce qu'une règle spécifiée sans endroit où s'exercer est lettre morte. Au passage, un constat qui fait gagner une demi-heure : **le semis donne plus qu'on ne croit** — l'IFDD y arrive avec ses cinq dénominations et deux domaines vérifiés, dont un en rattachement automatique, si bien que deux des trois parcours s'éprouvent sans rien semer. Toujours aucun code Rust. | **`/speckit-tasks`**, puis `/speckit-implement`. Commencer par le crate `org` et le déplacement des jetons dans le noyau, qui est bloquant pour l'invitation ; ne pas ouvrir d'histoire avant que `cargo tree -p org` ne montre zéro arête entre modules. Les trois portes hors `Makefile` restent les mêmes qu'en B1 — graphe de dépendances, taille des fichiers, chaque code d'erreur dans l'OpenAPI engendré |
| 2026-08-20 | B2 — les organisations sont spécifiées | **`/speckit-specify` PRODUIT `specs/002-organisations/`.** 8 histoires utilisateur priorisées, 14 cas limites, **84 exigences fonctionnelles** en 10 groupes, 12 entités du modèle, 18 critères de réussite mesurables, 13 hypothèses. **Les cinq exigences du prompt et les quatre obligations relevées en A11 sont toutes traitées.** L'écart n° 23 devient **deux lectures au-dessus de la même fonction de base**, qui n'est pas modifiée : celle destinée à une personne n'admet que les fiches portant une ressemblance de dénomination, celle destinée à la revue des doublons rend tous les signaux — et le domaine continue d'**alimenter le score** de la première, ce qui est son seul emploi juste ici. L'écart n° 24 est confirmé et la question qui vient avec est tranchée : **le retrait du dernier référent actif est refusé** tant qu'aucun autre n'a été désigné, un administrateur pouvant passer outre, la fiche étant alors signalée « sans référent ». Le score de confiance est tranché en **travail différé et non en trigger** — c'est une aide au tri, pas un invariant, et un trigger renchérirait chaque adhésion. **Onze écarts relevés, n° 70 à 80, dont trois qui auraient cassé à l'exécution.** (1) **L'ORDRE DES DEUX ÉCRITURES DE LA FUSION EST L'INVERSE DE CELUI INSCRIT EN A11** : l'unicité du nom ne porte que sur les fiches **vivantes**, donc tant que la source n'est pas passée `merged`, la cible ne peut pas reprendre son nom légal — les arbitrages viennent **après** l'appel, dans la même transaction. (2) **L'ADRESSE D'URL NE PEUT PAS ÊTRE ARBITRÉE** : son unicité ne connaît **aucune** condition de statut, et la libérer casserait précisément ce que la fusion promet. (3) **L'UNICITÉ DES ADHÉSIONS PORTE AUSSI SUR LES RÉVOQUÉES** : les données simulées créent une seconde ligne là où la vraie base refuserait — une personne refusée ne pourrait plus jamais redemander. S'y ajoutent deux constats d'autorisation : la permission de consultation des organisations est **détenue par le rôle d'utilisateur ordinaire** et ne peut pas garder le back-office seule, et **rien n'attribue ce rôle aujourd'hui** — garder la recherche derrière elle refuserait tout nouvel inscrit. Et le piège n° 1 d'`identity` se répète à l'identique : **la fonction de fusion émet elle-même son événement** et marque elle-même la paire de la file. Aucune modification du SQL, aucun code Rust : `backend/crates/modules/` ne contient toujours que `identity`. | **`/speckit-plan`** sur `specs/002-organisations/` : le crate `org`, la forme des deux lectures de recherche, le semis de 5 000 organisations qu'exige SC-002, et l'inscription du piège d'événement dans `contracts/events.md`. Puis `/speckit-tasks` et `/speckit-implement`. **Une question au commanditaire**, inscrite dans `points-bloques.md` : que devient une organisation dont l'unique référent s'en va ? |
| 2026-08-20 | B1 — phases 9, 10 et 11 : **B1 EST TERMINÉ** | **LES 126 TÂCHES SONT FAITES, ET `make check` PASSE EN ENTIER (T100–T126).** Trois phases d'un coup. **(9) La chaîne différée est éprouvée, plus supposée** : la purge des jetons devient une tâche récurrente **qui se replanifie elle-même** — la clé d'unicité porte le jour visé, et le démarrage du worker ne fait que **réarmer** la chaîne au cas où sa dernière occurrence serait morte avant d'avoir posé la suivante. `GET /api/health` rend les quatorze indicateurs de `analytics.v_operational_health` **sans recalculer un seul seuil** : le modèle porte déjà la décision, la redoubler ferait deux vérités. `GET /api/ready` **sollicite réellement le pool** — un processus qui répond pendant que sa base est injoignable n'est pas prêt. Cinq tests, dont trois dans le noyau. **Le point de contrôle est tenu à la main** : inscription provoquée, worker arrêté (rien ne part), worker relancé — **un seul courriel**. **(10) Une demande d'effacement s'honore sans détruire les statistiques d'une COP passée** : la file RGPD exige la portée **globale**, ou 403 — jamais une file filtrée, qui donnerait l'illusion d'un traitement complet à qui n'en voit qu'un morceau. L'anonymisation ne répond qu'à une demande d'**effacement**, et le service **n'émet pas** `identity.person.anonymized` : la fonction de base l'émet déjà, et deux lignes s'écriraient sans erreur — le piège n° 1 du module. Vérifié par une inscription à une session : les minutes de présence survivent à l'effacement. **(11) La documentation OpenAPI est engendrée, jamais écrite** : 26 chemins annotés auprès de leur gestionnaire, et **les 20 codes d'erreur stables engendrés depuis le catalogue du noyau** — un code ajouté apparaît au prochain démarrage, un code oublié n'existe pas. **141 tests, 143 requêtes préparées, `make check` en entier au vert avec zéro avertissement**, plus gros fichier à 588 lignes, aucune arête entre deux crates de module. **Deux défauts trouvés en éprouvant à la main, et non par un test.** (1) **Trois exemplaires du même courriel** sont partis pour une seule inscription : le relais du site retenait l'identifiant du message **après** l'envoi, alors que le doublon réel est **concurrent** — le client abandonne au bout de quinze secondes et réessaie deux secondes plus tard, pendant que le premier envoi est encore en cours. L'identifiant est désormais réservé **avant**, et rendu si l'envoi échoue. (2) **`platform.audit_log.actor_label` n'était renseignée par personne**, alors que la colonne existe pour « rester lisible après anonymisation RGPD » : sans elle, une personne qui exerce son droit à l'effacement fait devenir « Utilisateur anonymisé » toutes ses décisions passées. Le modèle est corrigé — le trigger lit le nom à l'écriture, seul instant où il existe encore. Quinze écarts relevés, n° 55 à 69. | **B1 est fini. Le jalon qui ouvre l'appel à propositions de la COP31 est complet.** Prochain pas : **B2 — Organisations**, avec la règle métier n° 1 (une organisation, plusieurs dénominations) qui est le défaut n° 1 de la v1. Deux dettes datées et non tenues, toutes deux pour **B7** : l'API ne pose **aucun en-tête CORS** — le site appelle avec `credentials: 'include'` depuis un autre port, et aucun appel de navigateur n'aboutira sans elle —, et le mock du site range la fenêtre de validité invalide sous `scope_not_allowed` là où l'API rend `IDENTITY_ROLE_WINDOW_INVALID` en 422 |
| 2026-08-20 | B1 — phase 8, confier un rôle sur la bonne portée | **UN WEBINAIRE SE CONFIE À SON RESPONSABLE SANS LUI OUVRIR LE RESTE DE LA PLATEFORME (T089–T099).** Le scénario de l'histoire mené à la main : une administratrice détachée sur une édition attribue **là** (accepté), sur une autre et globalement (refusés), et **ne peut pas retirer un rôle global** qu'elle n'aurait jamais pu accorder. C'est cette seconde moitié qu'on oublie : le retrait se vérifie sur la portée de **l'attribution visée**, pas sur celle de l'acteur. **Le refus du trigger ressort mot pour mot** *(obligation n° 3 du principe X)* — le modèle sait déjà écrire quelles portées un rôle admet, et le reformuler produirait un second libellé qui se périmerait. **Le retrait pose trois colonnes et ne supprime jamais** ; la note d'octroi reste intacte, parce que « pourquoi cette personne n'est-elle plus au comité ? » se pose six mois plus tard. **Les options d'attribution sont bornées à ce que l'appelant peut réellement accorder** — trois rôles sur onze pour une détachée —, et ce qui est hors d'atteinte reste **visible et désactivé** : le taire ferait croire à un bogue à qui cherche une édition qu'il sait présente. **110 tests.** **Deux défauts trouvés en éprouvant à la main, et non par un test** : (1) **`forbidden_scope` divulguait les rôles de la personne visée** — la route n'exigeait que la session, donc n'importe quel compte connecté lisait les attributions de n'importe qui en tentant une écriture vouée à l'échec ; il faut désormais `identity.role.assign` sur au moins une portée pour l'ouvrir (**403** sinon), et le refus ne rend plus aucune liste ; (2) **une valeur hors liste se faisait passer pour un champ fautif** — `status: "anonymized"` rendait `field: "anonymized"`, et l'écran aurait souligné une case inexistante : seuls deux messages de serde nomment un champ, les autres nomment une valeur. Neuf écarts en tout, dont un assumé : **une portée ciblée sans cible se refuse avant la base**, parce que l'autorisation passe avant l'écriture et qu'il n'y a rien à interroger sans cible — le code rendu est celui que la base rendrait. | **Phase 9 — US7 : les effets différés partent une fois, et une seule (T100–T107)** : la purge récurrente des jetons, la remontée des travaux en échec sur la route de santé, et les quatre garanties de la chaîne différée éprouvées plutôt que supposées |
| 2026-08-20 | B1 — phase 7, retrouver l'accès à un compte | **UN COMPTE PERDU SE RÉCUPÈRE SANS PASSER PAR L'ASSISTANCE (T081–T088).** Cycle mené à la main de bout en bout : demande, courriel reçu dans **Mailpit**, lien suivi, mot de passe choisi — et **l'ancien mot de passe ne vaut plus rien**, la session ouverte avec lui rendant `null` au premier appel suivant. **Deux statuts, et ils ne disent pas la même chose** : un jeton refusé sort en **200** avec son discriminant, l'écran propose de redemander un lien ; un mot de passe refusé sort en **422** sur le champ `password`, le formulaire se corrige sur place. **Le jeton est revérifié à l'envoi** (FR-042) — le contrôle préalable évite de faire composer un mot de passe pour rien, il ne vaut aucune garantie, et le test joue l'onglet ouvert la veille au soir. **Un mot de passe refusé ne brûle pas le lien** : il est jugé avant que le jeton ne soit consommé. Un changement abouti emporte trois choses dans la même transaction — compteur d'échecs à zéro, **verrou levé** (sans quoi on resterait bloqué un quart d'heure avec le mot de passe qu'on vient de choisir), **toutes les sessions coupées** avec leur motif. **88 tests.** Six écarts, dont un qui datait de la phase 6 et se voyait dans la boîte aux lettres : **les liens des courriels menaient à des pages inexistantes** — le site traduit ses chemins (`/verification-adresse`, `/en/verify-email`), les courriels pointaient vers `/auth/verify-email`, le nom du fichier de page, introuvable dans les deux langues. Et une question pesée puis tranchée autrement qu'à l'inscription : **la réponse invariable ne se comble pas ici par une écriture factice** — un jeton bidon pour une adresse inconnue mettrait en file un courriel vers un destinataire qui n'existe pas ; la transaction est ouverte avant de savoir si l'adresse est connue, et l'écart restant est de quelques millisecondes là où Argon2id en coûtait cinquante. | **Phase 8 — US6 : confier un rôle, le retirer, sur la bonne portée (T089–T099)** : l'attribution scopée, ses issues, la symétrie attribuer/retirer, et les paramètres de droits envoyés par le client qui n'ont aucun effet |
| 2026-08-20 | B1 — phase 6, l'inscription et le courriel | **LE COURRIEL ENTRE DANS LE JALON, ET IL SORT VRAIMENT (T068–T080).** Parcours mené à la main de bout en bout : inscription, courriel reçu dans **Mailpit**, lien suivi, adresse vérifiée, connexion réussie — et le même lien rejoué rend « déjà utilisé ». La chaîne complète tient : API → `platform.jobs` → worker → route privée du site → SMTP. **La réponse invariable tient par le message ET par le temps** : créer un compte coûte un hachage Argon2id de cinquante à cent millisecondes, ne rien créer coûte une lecture d'une milliseconde — le mot de passe est donc haché **dans les deux cas**, y compris quand rien ne sera écrit. C'est la leçon de la phase 3, appliquée avant de se faire prendre. **Le jeton en clair ne vit que dans la charge utile du travail**, créée dans la transaction du changement d'état et vidée dès l'envoi réussi ; un test le cherche dans la table des jetons et dans l'outbox, et ne l'y trouve pas. **Le trou connu de la file est refermé avant le premier envoi**, comme le contrat l'exigeait : un travail mort gardait sa charge utile indéfiniment. Vider celle de tous les travaux morts coûterait à `media`, `live` et `analytics` leur seule matière de diagnostic — la tâche déclare donc ce qu'elle transporte, et le worker efface ce qui porte un secret. Même examen pour le message d'échec, qui ne recopie plus ni l'adresse du destinataire ni la réponse du relais. **75 tests.** Cinq écarts, dont un qui a coûté une demi-heure : **`localhost` n'est pas `127.0.0.1`** — le site écoute en IPv4, macOS résout d'abord en `::1`, `curl` replie tout seul et le client HTTP du worker non ; un envoi sur deux échouait en disant seulement « relais injoignable ». | **Phase 7 — US5 : retrouver l'accès à un compte (T081–T088)** : la réinitialisation complète, le jeton revérifié à l'envoi, et toutes les sessions coupées au changement de mot de passe |
| 2026-08-20 | B1 — phase 5, le périmètre d'administration | **LE BACK-OFFICE SE PARTAGE SANS SE DUPLIQUER (T059–T067).** La règle métier n° 8 est désormais tenue par l'API, et non plus seulement par l'écran : huit routes de lecture, toutes gardées, et un administrateur détaché sur une édition ne voit que ce qu'on lui a confié — **y compris quand il forge un identifiant dans l'URL**. **Les trois cas du périmètre restent distincts jusqu'au bout** : global, éditions listées, et **aucun droit → refus explicite**, jamais une liste vide — c'est le défaut silencieux que le modèle décrit et que le code aurait reproduit en testant « pas global ». Une fiche hors périmètre n'est pas cachée mais **marquée** (`in_scope: false`) : la taire ferait croire à la disparition de quelqu'un qu'on sait présent. **L'origine d'une permission n'existe nulle part en base** — `effective_permissions()` rend `(permission, portée)`, assez pour autoriser, pas pour expliquer — et l'écran demandé est un écran d'explication : elle se recompose depuis les attributions. **L'historique n'a pas besoin du journal d'audit** : depuis A12, une ligne de `role_assignments` porte ses deux événements, l'octroi et le retrait, chacun avec son auteur et son motif. Le noyau gagne la permission testée **sur n'importe quelle portée**, que `has_permission()` ne sait pas rendre et dont B2 aura besoin dès sa première liste. **59 tests**, `make check-back` au vert, plus gros fichier à 422 lignes. Cinq écarts notés, dont un laissé ouvert : **`GET /people` n'est pas bornée par le périmètre parce que le contrat ne le demande pas** — si elle doit l'être, c'est `contracts/routes.md` qui change d'abord. | **Phase 6 — US4 : l'inscription et la vérification d'adresse (T068–T080)**, qui fait entrer le courriel dans le jalon : jetons à usage unique, réponse invariable sur adresse déjà connue, et la route privée d'envoi côté site |
| 2026-08-20 | B1 — les deux écarts assumés sont refermés | **CE QUI AVAIT ÉTÉ ASSUMÉ NE L'EST PLUS.** (1) **Les écritures du compteur d'échecs repassent par la porte du noyau.** Elles avaient été mises sur le pool au motif que les quatre allers-retours d'une transaction se mesurent et trahiraient l'existence d'un compte. Le raisonnement était juste, la conclusion fausse : **une transaction ne coûte que si on l'attend**. Lancée avant le hachage, comme l'écriture simple l'était déjà, elle se replie entièrement derrière lui. Mesuré après coup : **1,4 % d'écart de temps**, contre 22,8 % au premier essai et 10 % tolérés. Toute écriture du jalon passe de nouveau par `Db::write()` — et l'acteur y est même posé dès que le mot de passe est prouvé, ce que la version « rapide » ne faisait pas. Les deux documents qui affirmaient la garantie disent de nouveau vrai, ce qui vaut mieux que de les avoir corrigés vers le bas. (2) **`X-Forwarded-For` n'est plus cru sur parole.** N'importe quel client peut écrire cet en-tête : le croire revenait à laisser choisir l'adresse enregistrée dans sa propre session. `TRUSTED_PROXIES` déclare les mandataires — adresses ou préfixes, IPv4 et IPv6 —, **et le défaut ne croit personne** : sans eux, c'est l'adresse du pair qui fait foi. Avec eux, la chaîne se remonte **de droite à gauche**, en sautant les mandataires connus jusqu'au premier maillon qui n'en est pas ; un segment illisible arrête la remontée. Le tout vit dans `kernel::net`, pour que les cinq modules suivants n'aient pas à le refaire — neuf tests unitaires et un test HTTP. **Au passage**, la colonne `inet` cesse de traverser en texte : la caractéristique `ipnetwork` de SQLx rend la vérification à la compilation, et une adresse mal formée est refusée par le parseur de Rust plutôt que par PostgreSQL — donc en 422 et non en 500. `make check-back` au vert, **52 tests**. Le troisième écart reste ouvert, à la demande du commanditaire : `useApi.ts` et ses 1 008 lignes sont un chantier de front. | **Phase 5 — US3** : le périmètre d'administration, la règle métier n° 8 tenue par l'API. Elle ferme le jalon **T001–T067**, celui qui débloque B2 à B6. |
| 2026-08-20 | B1 — audit des écarts, et leur correction | **SECOND AUDIT CROISÉ : 45 CONSTATS CONFIRMÉS SUR 59, PLUS 9 TROUVÉS PAR LE CRITIQUE DE COMPLÉTUDE. TOUS TRAITÉS.** Huit lecteurs sur huit angles — contrat de routes, formes de réponse contre les types du site, exigences fonctionnelles, les seize décisions techniques, décomptes périmés, défauts du code neuf, les dix principes, configuration clé par clé —, chacun suivi d'un sceptique chargé de le réfuter. **Un défaut de gravité haute, trouvé par trois lecteurs sur huit** : un verrou échu ne rendait qu'**un** essai au compte au lieu de tous. Le compteur grimpait pendant tout le verrou et n'était purgé que si le mot de passe était juste ; à l'échéance il valait seuil+N, et la première faute de frappe reposait un quart d'heure — indéfiniment. Quelqu'un qui s'était verrouillé en cherchant son mot de passe n'avait plus droit qu'à un essai par quart d'heure, à vie. La purge appartient maintenant au chemin commun. **Deux courses fermées** : deux renouvellements simultanés ouvraient deux sessions — le verdict de la révocation atomique était jeté —, et deux échecs simultanés au seuil posaient deux verrous et deux événements. Dans les deux cas c'est la base qui décide désormais, pas une lecture antérieure. **Un corps mal formé sortait en 400 avec le texte anglais de serde**, sans code stable ni `request_id`, alors que le contrat annonçait le point livré avec la phase 3 : il rend 422 `VALIDATION_FAILED` en nommant le champ, et rien d'autre de serde ne franchit la réponse. **Et rien ne traversait HTTP** : `crates/api` devient bibliothèque et binaire pour que `routes_auth` monte **la vraie application**, intergiciels compris, sur une base jetable — neuf cas, statuts, corps, cookies, corps d'erreur. Aussi corrigés : le rejeu qui laissait ses cookies, un `X-Forwarded-For` illisible qui effaçait l'adresse de la session, une panne de base qui se lisait « déconnecté », une écriture morte sur `last_seen_at`, et une fonction morte au commentaire faux. **Quatorze affirmations fausses dans les documents, réparties sur dix-huit fichiers corrigés** — `jsonwebtoken` jamais utilisé, une liste de motifs de révocation périmée, un test nommé à l'envers, `/api/docs` promis pour la phase 3 alors qu'il vient en phase 11, des `curl` de vérification adressés à des comptes qui n'existent que dans les données simulées, et un relevé du modèle faux sur cinq de ses six chiffres parce que la requête de recomptage oubliait un schéma. La constitution passe en **1.0.1** : sa description de `check-back` omettait les deux options sans lesquelles le portail ne voit ni les tests ni le harnais. **Trois écarts assumés et écrits** : trois écritures du compteur empruntent le pool pour tenir SC-001, l'adresse d'origine d'une session est déclarative faute de mandataire déclaré, et `useApi.ts` dépasse les 1000 lignes — hors périmètre. `make check-back` au vert, **42 tests**. | **Phase 5 — US3** : le périmètre d'administration, la règle métier n° 8 tenue par l'API. Elle ferme le jalon **T001–T067**, celui qui débloque B2 à B6. |
| 2026-08-20 | B1 — la connexion et la session (phases 3 et 4) | **ON PEUT SE CONNECTER, ET LA SESSION TIENT.** 23 tâches de plus (T036–T058), **58 sur 126**, `make check-back` au vert avec **29 tests** dont 26 nouveaux — les tests d'intégration tournent tous sur base réelle et jetable. Les **six issues de connexion** sortent toutes en 200, dans l'ordre imposé : mot de passe d'abord, puis verrou, suspension, adresse non vérifiée, second facteur. La session vit, tourne et se coupe — jeton d'accès signé Ed25519 de quinze minutes **sans aucune permission dedans**, jeton de rafraîchissement dont la base ne garde que l'empreinte, rotation qui chaîne les lignes, et **rejeu qui révoque tout**. **LE TEST DE DISCRÉTION A ÉCHOUÉ AU PREMIER ESSAI, ET IL AVAIT RAISON** : 22,8 % d'écart de temps entre une adresse inconnue et un mot de passe faux, là où la spécification en tolère dix. Le hachage était pourtant identique des deux côtés, à 0,2 % près — la différence venait de ce qu'une adresse connue **écrit** son compteur d'échecs et qu'une adresse inconnue n'écrit rien. Une milliseconde et demie qu'aucun journal ne montre, et qui suffit à redire quels comptes existent. Le hachage part maintenant sur un fil dédié et l'écriture est lancée **avant** de l'attendre : elle disparaît derrière le calcul. **Écart mesuré après correction : 1,5 %.** Éprouvé à la main aussi : deux réponses `invalid_credentials` au caractère près, une connexion qui pose ses deux cookies aux bonnes portées (`Lax` sur `/`, `Strict` sur `/api/auth`), `GET /auth/me` qui rend `null` en **200** sans session, un renouvellement qui change de jeton, et une déconnexion qui réussit deux fois de suite. **Trois écarts d'implémentation** : la clé de signature se lit en 32 octets hexadécimaux ou base64 et non en PEM — et soixante-quatre signes hexadécimaux formant aussi du base64 valide, c'est la **longueur** qui départage, pas l'ordre des essais ; le renouvellement **ne repousse pas** l'échéance de la session, sans quoi douze heures deviendraient perpétuelles ; les dépendances sont compilées optimisées en développement, sans quoi Argon2id met une seconde par vérification. Aucun fichier au-dessus de 351 lignes, `.sqlx/` régénéré (42 requêtes). | **Phase 5 — US3** : le périmètre d'administration, la règle métier n° 8 tenue par l'API et non plus par l'écran. C'est elle qui ferme le jalon **T001–T067**, celui qui débloque B2 à B6. |
| 2026-08-20 | B1 — audit du socle, et correction des écarts | **AUDIT CROISÉ DU CODE LIVRÉ CONTRE LES DOCUMENTS : 53 CONSTATS CONFIRMÉS SUR 80, TOUS TRAITÉS.** Sept lecteurs, chacun suivi d'un sceptique chargé de réfuter — 27 constats sont tombés à la vérification, ce qui est le but. **Trois choses étaient réellement cassées et ne se voyaient pas.** (1) Le contrat rangeait le refus du domaine d'adresse sous le mauvais SQLSTATE, et le code l'avait fidèlement recopié : **une adresse mal écrite sortait en 500**, sur un chemin nominal puisque le principe VIII fait du domaine le validateur prévu. Mesuré sur la base : `23514 / email_check`, jamais `22P02`. (2) **Jaeger ne recevait rien** — aucun span n'était ouvert nulle part, et la couche OpenTelemetry ignore tout événement hors span ; en corrigeant, un second défaut est apparu dessous : l'exportateur tourne sur son propre fil, sans réacteur Tokio, où le client HTTP asynchrone meurt au premier envoi. Il fallait le client **bloquant**. La chaîne « une page, une trace, une ligne d'audit » est réelle depuis aujourd'hui, et vérifiée. (3) **Le worker n'obéissait pas à `Ctrl-C`** : `db.close()` attendait la connexion d'écoute du relais, gardée pour la vie du processus. Arrêt mesuré : 1 s. **Trois garanties étaient fausses** : le noyau affirmait qu'aucune écriture ne passait par le pool alors qu'il y écrivait quatre fois ; `administered_events` repliait en silence le NULL que le SQL désigne nommément comme son défaut historique ; et `make check-back` n'analysait ni les tests ni le seul fichier composant du SQL dynamiquement, alors que le compte rendu de la veille citait la commande complète — celle lancée à la main. **Deux trous de la file sont refermés** : un travail réservé par un worker tué revient à la file au bout d'un bail de 30 min (le modèle sait voir ce cas, pas le réparer), et une erreur de base au moment de clore ne tue plus la boucle ni, par ricochet, le relais d'outbox. **Le modèle des tests porte désormais l'empreinte du SQL qu'il contient** : il se périmait en silence, exactement comme le conteneur que `CLAUDE.md` met en garde. **Dix documents portaient des décomptes périmés** — 18 fichiers SQL au lieu de 19, 15 schémas au lieu de 16, un `backend/migrations/` que le projet a écarté ; là où le chiffre ne servait qu'à dire « c'est trop pour tout charger », il cède la place à une formule qui ne se périme pas. **Et la justification de l'écart n° 6 de la veille était fausse** : Actix passe bien la requête entière à un extracteur, `match_info()` compris — le choix reste bon, l'impossibilité invoquée ne l'était pas. Deux dettes sont tranchées mais non refermées, et écrites là où l'implémenteur les lira : la charge utile d'un travail mort garde son jeton en clair (avant la phase des courriels), et l'API ne pose aucun en-tête CORS (avec B7). `make check-back` au vert. | **Phase 3 — US1** : les six issues de connexion, l'ordre imposé des contrôles, et le test qui mesure l'écart de temps entre une adresse inconnue et un mot de passe faux (SC-001, sous 10 %). |
| 2026-08-20 | B1 — le socle technique est debout (phases 1 et 2) | **`backend/` EXISTE, ET IL TOURNE.** Workspace Cargo à cinq crates — `kernel`, `contracts`, `modules/identity`, `api`, `worker` —, **35 tâches faites (T001 à T035)**, `make check-back` au vert : mise en forme, `clippy -D warnings` sans un avertissement, 3 tests d'intégration sur base réelle. **Le noyau porte les onze pièces prévues** : configuration typée validée AU DÉMARRAGE (une durée mal écrite refuse le lancement, elle ne casse plus une requête), contexte de requête, catalogue d'erreurs à codes stables, traduction `(SQLSTATE, contrainte) → erreur`, **l'unique porte d'écriture qui pose `app.actor_id` et `app.request_id` elle-même**, négociation de locale, Argon2id avec son **empreinte factice calculée au démarrage**, traces OTLP, émission par `platform.emit_event()`, file de travaux, contrat d'envoi de courriel à deux implémentations. **Le garde d'autorisation est dans `kernel` comme décidé** : `cargo tree -p identity` ne montre aucune arête vers un autre module. **Éprouvé pour de vrai, pas seulement compilé** : l'API répond, `X-Request-Id` est repris du client ou engendré et revient dans l'en-tête ET dans le corps d'erreur, une écriture d'origine inconnue rend `IDENTITY_ORIGIN_REJECTED`, et un événement déposé dans l'outbox est relayé **en moins d'une seconde** par `LISTEN/NOTIFY`, avec son `correlation_id` intact ; remis à zéro et rejoué, **il n'est pas retraité** — la garde d'idempotence tient. **Trois écarts de l'implémentation** : Rust est épinglé en 1.97.1 (SQLx 0.9 exige 1.94) et **SQLx passe en 0.9**, la CLI du poste imposant sa version ; une écriture **sans** en-tête d'origine passe, seule une origine présente et inconnue est refusée — sinon les vérifications de `quickstart.md` seraient toutes refusées, sans rien protéger ; et le contrôle de frontières de `quickstart.md` **compte une ligne de trop**, la ligne racine portant elle-même `crates/modules/`. Aucun fichier au-dessus de 332 lignes. `.sqlx/` est versionné (17 requêtes). | **Phase 3 — US1** : les six issues de connexion, l'ordre imposé des contrôles, et le test qui mesure l'écart de temps entre une adresse inconnue et un mot de passe faux (SC-001, sous 10 %). Le socle qui le permet est là : l'empreinte factice existe déjà dans `kernel::crypto`. |
| 2026-08-20 | B1 — les tâches sont découpées | **`/speckit-tasks` PRODUIT `tasks.md` : 126 TÂCHES EN 11 PHASES, dont 23 tests.** Une phase par histoire utilisateur, chacune éprouvable seule, avec son point de contrôle. **Les tests ne sont pas optionnels ici** : le principe X impose des tests d'intégration sur base réelle, sans mock. Les quatre obligations minimales de ce principe ont chacune leur tâche nommée — chemin nominal (T046, T061), refus sur URL forgée (T066), invariant de la base traduit (T098), écriture dans l'outbox (T103). **UNE DÉCISION D'ARCHITECTURE EST APPARUE EN DÉCOUPANT, et il valait mieux la voir là qu'en B2** : le garde d'autorisation vit dans le **noyau**, pas dans `identity`. `has_permission()` et `administered_events()` sont des fonctions SQL que tous les modules doivent appeler, et aucun crate de module n'a le droit de dépendre d'`identity` — le placer là aurait créé l'arête interdite dès B2. `kernel` connaît le schéma `identity` comme il connaît `platform` : aucune dépendance de crate, graphe intact. Consignée en **R16** de `research.md`. **Trois jalons de livraison** : T001–T047 pour se connecter (le plus petit incrément qui vaille), **T001–T067 pour débloquer B2 à B6** — un appelant identifié, une permission testable, un périmètre borné —, T001–T115 pour ouvrir l'appel à propositions. Toujours aucun code Rust. | **`/speckit-implement`**. Commencer par les phases 1 et 2, qui sont bloquantes, et ne pas ouvrir d'histoire avant que `cargo tree` ne montre zéro arête entre modules. |
| 2026-08-20 | B1 — le plan est écrit | **`/speckit-plan` PRODUIT `plan.md`, `research.md`, `data-model.md`, `contracts/` (routes, erreurs, événements) ET `quickstart.md`.** Le contrôle des dix principes de la constitution est passé **deux fois**, avant et après conception : **aucune violation**, `Complexity Tracking` vide. **15 décisions techniques**, chacune avec ses alternatives écartées. Trois structurent le reste : **le jeton d'accès est signé et ne porte AUCUNE permission — et c'est le modèle qui l'a décidé**, `identity.sessions` ne portant qu'une empreinte de jeton de rafraîchissement, il n'existe aucune colonne où loger un jeton opaque ; **un jeton de rafraîchissement rejoué révoque toutes les sessions** ; **l'API n'enverra aucun courriel elle-même**. **UNE CONTRAINTE D'HÉBERGEMENT EST ARRIVÉE EN COURS DE PLAN** : l'API et le site sont sur deux serveurs, et **seul celui du site a le droit d'émettre du courriel**. Le worker compose le message et le remet au site par un appel HTTP privé ; le site ouvre le SMTP. La reprise d'essai ne bouge pas — un site injoignable est un travail en échec, replanifié par `platform.fail_job()`. Le noyau expose donc un **contrat** d'envoi et non un client : la fonction TypeScript du site est temporaire, générique à tous les courriels, et sera réécrite en Rust le jour de l'autorisation. **Quatre écarts de plus relevés en planifiant** : les trois clés SMTP de `.env.example` changent de lecteur ; **aucun consommateur métier n'existe encore** — un consommateur de télémétrie éprouve la garde d'idempotence en attendant B2 ; **`anonymize_person()` émet déjà son événement**, l'émettre à nouveau en écrirait deux sans qu'aucune erreur ne le signale ; une **lecture franchit la frontière vers `org`**, ce que le modèle avait prévu, à décider en B2. B1 livre **un fichier hors de `backend/`** : la route privée d'envoi du site. Toujours aucun code Rust. | **`/speckit-tasks`**, puis `/speckit-implement`. Créer les cinq crates, poser `.env.example` à jour, et vérifier les trois portes hors `Makefile` — graphe de dépendances sans arête entre modules, aucun fichier au-dessus de 1000 lignes, chaque code d'erreur documenté dans l'OpenAPI généré. |
| 2026-08-20 | B1 — le socle et l'identité sont spécifiés | **`/speckit-specify` PRODUIT `specs/001-socle-identite/`.** 8 histoires utilisateur priorisées, 13 cas limites, **64 exigences fonctionnelles** en 9 groupes, 12 entités du modèle, 13 critères de réussite mesurables, 11 hypothèses. Les quatre exigences du prompt sont toutes traitées : **écart n° 18** — seuil et durée du verrou dans la configuration du service, défauts 5 échecs et 15 minutes, et la remise à zéro du compteur enfin tranchée (connexion réussie, expiration du verrou, réinitialisation menée à terme) ; **écart n° 19** — une durée par finalité, 24 h pour la vérification d'adresse, 1 h pour la réinitialisation, aucun appelant ne posant plus d'expiration lui-même ; **écart n° 20** — l'adresse non vérifiée interdit la connexion, **sans ajouter de statut de personne** ; **ordre des contrôles** — mot de passe d'abord, le reste ensuite. **Sept écarts relevés en l'écrivant**, dont quatre qui comptent : la discrétion se trahit par le TEMPS de réponse et pas seulement par le message — Argon2id rend le raccourci mesurable là où le mock ne le montrait pas ; le **rejeu d'un jeton de rafraîchissement** n'était spécifié nulle part et révoque désormais toutes les sessions ; une **suspension ne coupait pas les sessions en cours** ; le **courriel transactionnel n'avait aucun propriétaire** dans ce jalon, `engagement` arrivant en B6 — il part du worker, à partir des événements émis. **Une question posée au commanditaire, tranchée** : le second facteur reste un emplacement réservé. **Le point laissé ouvert par B0 est clos** — `identity.administered_events` filtre sur la bonne permission, les quatre rôles attribuables sur une édition portent tous `programme.proposal.read_all`. Aucune modification du SQL, aucun code Rust : `backend/` n'existe toujours pas. **`CLAUDE.md` gagne une section** — les questions au commanditaire se posent en mots simples, sans nom de fichier ni de colonne. | **`/speckit-plan`** sur `specs/001-socle-identite/` : arborescence des cinq crates, choix de la couche d'accès, forme des erreurs, et confirmation de l'envoi de courriel par le worker. Puis `/speckit-tasks` et `/speckit-implement`. |
| 2026-08-20 | B0 — la constitution est ratifiée | **`.specify/memory/constitution.md` PASSE DU GABARIT À LA VERSION 1.0.0.** Dix principes, et non les cinq emplacements du gabarit : modèle de données faisant autorité · frontières de modules (`kernel`, `contracts`, `api`, `worker`, `modules/<nom>`, aucune dépendance croisée) · frontières vérifiables en base (`xmod_fk_*`, rapport vide) · effets de bord par `platform.emit_event()` dans la même transaction · autorisation par permission ET par portée, avec les trois cas distincts de `identity.administered_events` · SQLx vérifié à la compilation · `app.actor_id` et `app.request_id` en début de transaction · invariants de la base non réimplémentés mais traduits · erreurs à code stable et message français · tests d'intégration sur base réelle. Deux sections libres remplies — contraintes techniques (pile, limite de 1000 lignes, textes d'interface contre données métier, périmètre du jalon) et flux de travail (cycle Spec Kit, `make check`, contrat d'API au service du front existant, mise à jour de la progression). **Sept écarts relevés en l'écrivant**, dont trois qui comptent : `cross_module_fk_report` vit dans `000_bootstrap.sql` § 9 et non dans `010_platform.sql` ; `platform` et `reference` sont **exemptés** du préfixe `xmod_fk_*`, c'est la vue qui le dit ; `identity.administered_events` ne filtre que sur `programme.proposal.read_all`, ce qui reste **à vérifier en B1** contre le semis des rôles. Aucun code Rust n'est écrit — `backend/` n'existe toujours pas et `make check-back` reste inerte. | **B1 — socle et identité** : `/speckit-specify`, puis le cycle complet. Créer `backend/` et ses cinq crates ; vérifier au passage le filtre de permission de `administered_events`, et traiter les cinq obligations d'API relevées en écrivant A12. |
| 2026-08-20 | GitHub Spec Kit installé et branché sur Claude Code | **L'OUTILLAGE DE LA PHASE B EST EN PLACE, AUCUN PROMPT B N'EST COMMENCÉ.** La CLI `specify` passe de 0.16.0 à **0.16.5** (`specify self upgrade`, installée par `uv` hors du dépôt), puis `specify init --here --force --non-interactive --integration claude --script sh` dépose deux dossiers versionnés : **`.specify/`** — gabarits de spécification, de plan, de tâches et de liste de contrôle, scripts `bash`, workflow, `memory/constitution.md` encore à l'état de gabarit — et **`.claude/skills/speckit-*`**, les dix compétences du cycle. Rien n'est écrasé : le dépôt ne portait que `.claude/settings.local.json`, qui reste ignoré. **Le nom des commandes a changé depuis la 0.16** : elles s'appellent avec un tiret, `/speckit-specify` et non plus `/speckit.specify` — `docs/PROMPTS_DEVELOPPEMENT.md` est corrigé partout, et la phase B porte désormais l'état de l'installation, la commande de mise à jour, et les deux compétences nouvelles (`/speckit-converge`, qui relit le code et complète `tasks.md` ; `/speckit-taskstoissues`, qui ouvre les issues GitHub). **`.specify/.gitignore` garde `feature.json` hors du dépôt** — c'est un pointeur par poste vers la fonctionnalité courante, pas une donnée partagée. | **B0 — la constitution** : lancer `/speckit-constitution` avec le préambule et les dix principes de `docs/PROMPTS_DEVELOPPEMENT.md`, qui remplaceront le gabarit de `.specify/memory/constitution.md`. Puis B1, le socle et l'identité. |

---

## B3 — les phases 5 à 11 (T054–T129) : le module Événements est livré

**Ce qui a été fait.** Les sept phases restantes, d'un trait : l'appel unique et sa grille, les dix lectures publiques, le stand et son canal de direct, le calendrier et les journées spéciales, le comité de sélection, la publication de la programmation, et la finition. **Les 129 tâches sont cochées.**

**Ce que cela ouvre.** Le jalon que le prompt demandait : **la campagne de la COP31 peut être ouverte**, avec ses critères pondérés, et B4 a de quoi recevoir un dossier.

**Trois choses valent d'être retenues de cette session.**

1. **Le refus qui sauve les notes fonctionne, et le test le prouve en deux moitiés.** Retirer un critère d'évaluation **réussit** en base et détruit silencieusement les notes qui s'y rapportent — l'argumentaire d'une décision de sélection. Le service compte et refuse. Le test vérifie le refus **et que les notes sont toujours là après** : sans cette seconde moitié, un service qui supprimerait d'abord et refuserait ensuite passerait le premier contrôle sans sourciller.

2. **Une écriture hors du schéma du module était inévitable, et le contrôle mécanique du plan l'interdisait.** Les thématiques d'un fil vivent dans le référentiel partagé, le contrat du front les porte, et aucune fonction de pose n'existe. La règle a été **resserrée plutôt que l'écriture cachée** : l'exception tient dans un fichier, `repo/themes.rs`, qui porte sa justification en en-tête — le référentiel n'est pas un module métier, et aucune écriture vers `programme`, `media`, `identity` ou `org` n'existe. Le contrôle de `quickstart.md` doit lire ces quatre schémas et non cinq.

3. **La grille par défaut est lue en base, en écrivant.** `seed_default_criteria()` ne sait poser ses six lignes que sur un appel : on lui en donne un jetable, dans une transaction annulée dont rien ne subsiste. Trois assertions le tiennent, dont une qui compte les éditions avant et après — sans elle, chaque affichage du formulaire laisserait une édition fantôme dans la liste du back-office.

**Ce qui reste ouvert, et n'appartient pas à ce jalon.** La seconde moitié de la publication — la visibilité de chaque séance — arrive avec B5, dont le contrat de consommateur est écrit aux points bloqués. Deux arbitrages attendent le commanditaire : la granularité du calendrier d'une série de webinaires, et les permissions du rôle de programmation.
