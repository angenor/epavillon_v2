# Journal — 2026-08-26

> Extrait de la [progression](../../PROGRESSION.md). Une ligne par session, la plus récente en haut.

| Date | Session | Fait | À suivre |
|------|---------|------|----------|
| 2026-08-26 | **Les images d'une édition se téléversent — recadrées à la forme exigée, depuis l'écran** | **L'ÉCART SIGNALÉ PAR LE COMMANDITAIRE EST CORRIGÉ** : `/admin/evenements/nouveau` et `/admin/evenements/[id]` demandaient un **identifiant d'objet** saisi à la main pour chacune des trois déclinaisons — un champ que seule une personne ayant accès à la base pouvait remplir. La route de rattachement existait (B3), celle du dépôt aussi (B6) ; il manquait l'écran entre les deux. **Trois pièces neuves** : `UiImageEditor`, un recadrage sur canevas — sélection déplaçable et redimensionnable à la souris, au doigt et **au clavier**, grille des tiers, choix du format (JPEG · WebP · PNG), qualité, largeur de sortie, et le poids **mesuré et non estimé** (chaque réglage réencode et garde le fichier qui partira) ; `MediaImageField`, l'emplacement complet — choisir, recadrer, décrire, déposer, remplacer, retirer ; et `api.media`, la part manquante de `useApi()`. **LA FORME EST IMPOSÉE, PLUS APPRISE PAR LE REFUS** : la poignée est verrouillée sur `media.attachable_roles.expected_aspect_ratio`, lu par `GET /media/roles` — ce qui sort de l'écran est recevable par construction, et le trigger des 2 % redevient un filet plutôt qu'un mode d'emploi. Deux champs manquaient au type du site (`expected_aspect_ratio`, `aspect_ratio_tolerance`) : **l'obligation inscrite au contrat de la route est refermée**. **Trois pièges traités** : un PNG transparent aplati en JPEG donne du NOIR — le canevas est peint en blanc avant le dessin ; le poids annoncé au dépôt doit être **exact**, il vient du `Blob` et non d'une estimation ; et la mesure du cadre passe par un observateur de taille, une modale venant de s'ouvrir pouvant encore mesurer zéro. **Une primitive de plus dans `useApi()`** — `sendForm`, la seule écriture qui ne parle pas JSON, avec les métadonnées **avant** le fichier comme la route l'exige ; `scripts/check-api-contract.mjs` la reconnaît, son chemin est donc vérifié comme les autres. **Vérifié** : `typecheck`, `build`, `check-api-contract` au vert ; **au navigateur, sur les deux pages** — dépôt bout en bout sur la création (recadrage 32:9, description, aperçu, « Remplacer »/« Retirer »), éditeur ouvert sur la fiche COP31 en 16:9, déplacement et redimensionnement à la souris, rapport de sortie mesuré à **851 × 479** pour un 16:9 exigé, soit 0,07 % d'écart pour 2 % tolérés. **CORRIGÉ DANS LA FOULÉE, signalé par le commanditaire : « le bouton Téléverser reste grisé ».** Aucun défaut de logique — la **description de l'image**, seul champ obligatoire, était rangée en bas de la colonne de réglages, à **huit cents pixels sous le pli** d'une boîte de dialogue haute de 381 px, et rien ne disait pourquoi l'envoi était fermé. Ce que l'appelant exige passe désormais **avant** les réglages facultatifs, juste sous l'aperçu, et **le pied de la boîte dit la raison** à côté du bouton. Reproduit au navigateur avant correction, revérifié après : le champ est à 393 px, la raison s'affiche puis disparaît à la saisie, et le dépôt aboutit. **DEUXIÈME PASSE, 27/08 — « toujours grisé ».** La première correction ne suffisait pas, et la relecture a trouvé **deux vrais défauts silencieux** : `encode()` pouvait échouer par trois chemins sans rien dire (contexte de canevas refusé, `toBlob` rendant `null`, `drawImage` levant sur une sélection dégénérée), et **une image sans dimensions passait pour valide** — un SVG qui ne porte qu'un `viewBox` se charge sans erreur et mesure zéro. Plus un piège de saisie : **le français est la langue de repli et le champ ne remonte RIEN tant qu'il manque**, si bien qu'une description écrite dans le seul onglet anglais laissait l'envoi fermé sans que l'écran puisse distinguer les deux cas — le message nomme donc la langue. **Le pied de la boîte ne peut plus être muet** : cinq raisons possibles, dans l'ordre de la correction à faire. **Cause confirmée par le commanditaire : la description oubliée.** Le diagnostic tient — ce n'était pas un blocage, c'était un champ obligatoire hors de vue dont rien n'annonçait le manque. Les deux défauts silencieux trouvés en cherchant restent corrigés. | **Rien n'a été éprouvé contre l'API réelle** : la vérification a eu lieu sur données d'exemple, où le dépôt simulé rend une adresse `blob:` qui meurt avec la page. Le dépôt réel reste à voir passer — notamment le refus de quota et la déduplication, qui n'ont pas d'équivalent hors ligne. **Les mêmes emplacements existent ailleurs** : `AdminShowcaseMediaPanel` demande encore un identifiant d'objet, et le formulaire de soumission joint ses documents sans passer par le module Média. `MediaImageField` est écrit pour être repris tel quel. **Le texte alternatif n'est demandé qu'au dépôt** : le modifier ensuite suppose une route que le site n'appelle pas. |
