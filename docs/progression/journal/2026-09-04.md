# Journal — 2026-09-04

> Extrait de la [progression](../../PROGRESSION.md). Une ligne par session, la plus récente en haut.

| Date | Session | Fait | À suivre |
|------|---------|------|----------|
| 2026-09-04 | **La navigation du back-office était trop basique — et son tiroir mobile s'ouvrait au-dessus du bouton qui l'ouvrait** | Refonte de `UiSideNav` et du pied de la colonne. **Une icône par entrée** — le composant les acceptait depuis A0.4, aucun appelant n'en passait, et le guide en dessine pourtant une par ligne ; deux pictogrammes ajoutés au jeu (`sliders`, `log-out`). **Liseré vertical sur l'entrée courante** : l'aplat `accent-surface` seul ne se repère pas sur une colonne lue en diagonale. **La colonne est devenue une pile** — marque, entrées défilantes, pied fixe —, et **le pied porte le compte connecté avec sa déconnexion** : l'en-tête du back-office n'en portait aucune trace, il fallait repasser par le site public pour se déconnecter. **Trois défauts trouvés en chemin.** (1) Le tiroir mobile était rendu **dans le flux, avant l'en-tête** : appuyer sur le bouton de menu poussait le contenu de plusieurs écrans vers le bas et **rien n'apparaissait à l'endroit regardé**. Il est maintenant `fixed` sur un voile, fermé au voile, à Échap ou à l'arrivée sur la page — et `invisible` quand il est fermé, ce qui sort ses liens de l'ordre de tabulation. (2) **`border-subtle` vaut EXACTEMENT `surface-raised` en thème sombre** (`gris-800` des deux côtés, mesuré `rgb(57,56,54)`) : les trois séparations de la colonne y étaient rigoureusement invisibles. Passées en `--color-border`. (3) `UiBreadcrumb` se tait en dessous de trois maillons, mais le `v-else` du layout testait `breadcrumb.length` sans voir ce silence : **sur toutes les listes du back-office, la langue et le thème se collaient à gauche**. La place du fil est désormais réservée. Vérifié dans le navigateur, thème clair et sombre, 1440 × 900 et 390 × 844 | **Le piège des séparations vaut au-delà de cette colonne** : tout `border-border-subtle` posé sur un panneau `surface-raised` est invisible en thème sombre — à passer en revue. Le fichier d'écran : [`a0.4-composants.md`](../ecrans/a0.4-composants.md) |
| 2026-09-04 | **La production servait ses médias nulle part, et refusait toute connexion — les deux corrigés en ligne** | Le trou repéré en local existait en production, et un second, plus grave, s'est découvert en le comblant. **`APP_PUBLIC_URL` portait encore l'adresse institutionnelle** alors que la recette vit sur `https://epavillon.mefali.com/v2` depuis le 02/09 : cette valeur donne à l'API **la seule origine acceptée en écriture**, si bien qu'un `POST /auth/login` depuis l'adresse réelle rendait `403 IDENTITY_ORIGIN_REJECTED` — **personne ne pouvait se connecter en ligne**. Mesuré avant de corriger, remesuré après : 200 avec `invalid_credentials`, la réponse métier. Côté médias, quatre gestes : le relais `media-proxy` démarré, la règle `/v2/media/` déposée au vhost, `ops/init-garage-prod.sh` rejoué — il ouvre désormais le bucket en lecture web et pose l'adresse d'après `APP_PUBLIC_URL` —, et **une preuve de bout en bout** : un objet de test déposé dans le bucket est revenu par son adresse publique en 200 avec son contenu exact, ce qui prouve la chaîne Apache → nginx → Garage au lieu de la déduire. Objet et image utilitaire retirés dans la foulée. **Un défaut de `deploy.sh` corrigé en chemin** : `scp` nomme le port `-P`, pas `-p` — qui signifie chez lui « préserver les horodatages » ; les deux appels réutilisaient les options de `ssh` et prenaient le numéro de port pour un fichier à copier, si bien que la cible `vhost` n'avait jamais pu fonctionner. **`financedurable.francophonie.org` mesuré à 200 avant, pendant et après chaque geste** | **Rien ne bloque plus les médias, en local comme en ligne.** Reste à créer une édition en ligne pour l'éprouver depuis le back-office plutôt que par un objet de test. **Le jour du relais institutionnel, `APP_PUBLIC_URL` reprend l'adresse définitive et `ops/init-garage-prod.sh` est à rejouer** — sinon les médias resteront sur l'adresse de transport. `NUXT_PUBLIC_SITE_URL` reste bâti sur l'adresse définitive, ce qui ne concerne que les liens `hreflang` et les URL canoniques |
| 2026-09-04 | **Le téléversement d'image ne cassait pas : c'est l'aperçu qui pointait vers un domaine inexistant** | Signalé comme « impossible de téléverser une image » sur `/admin/evenements/nouveau`, avec un `ERR_NAME_NOT_RESOLVED` en console. **Le dépôt réussissait** — l'objet était bien écrit dans Garage. Ce qui échouait, c'est l'affichage : `media.object_url()` compose l'adresse depuis `platform.settings` → `media.public_base_url`, et le modèle (`docs/database/050_media.sql` § 8) y met la valeur de **production**, `media.epavillonclimatique.francophonie.org` — un nom qui **n'existe pas dans le DNS** (NXDOMAIN). Le navigateur ne résolvait donc rien, et l'image cassée passait pour un dépôt raté. Le relais local existe depuis le 24/08 (`ops/media-proxy.conf`, port 3920) et le geste correctif était documenté, mais **à faire à la main, une fois** : `down -v` recharge le modèle et remet la valeur de production **en silence** — la base avait justement été rechargée. Le geste est désormais **outillé** : `make garage-init` ouvre le bucket en lecture web (`bucket website --allow`) et pose le réglage sur le relais, et `check-db` l'appelle déjà — donc plus rien à refaire après un `down -v`. Nouvelle cible `make media-base-url` pour le reposer seul, port lu dans `.env` (`MEDIA_PROXY_PORT`). Vérifié : les trois bannières déjà déposées répondent 200 en `image/webp` par le relais — elles avaient été déposées trois fois de suite dans les deux minutes précédant le signalement, **ce qui prouve que le dépôt marchait**. **La production a le même trou**, et le chemin retenu tient en une ligne de vhost : `/v2/media/` sur l'ORIGINE DU SITE, relayé vers le même nginx. Un sous-domaine `media.…` aurait demandé une entrée DNS à l'OIF — délai incompatible, § 1 de [`DEPLOIEMENT.md`](../../DEPLOIEMENT.md) — et un certificat de plus ; le chemin ne demande rien et **suit le domaine du jour**, donc survivra au relais institutionnel. Préparé sans rien déployer : service `media-proxy` au compose de production, `ProxyPass` placé **avant** `/v2/` (après, Nuxt rendrait sa 404 à la place de chaque image, avec un code 200), et `ops/init-garage-prod.sh` qui pose le réglage d'après `APP_PUBLIC_URL`. § 12 de `DEPLOIEMENT.md` | **Le geste n'était outillé nulle part** : il vivait dans la documentation, à refaire à la main après chaque `down -v`. C'est réglé ici et en production ; à surveiller le jour où un troisième environnement apparaîtra |
