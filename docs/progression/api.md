# API — suivi des prompts

> Extrait de la [progression](../PROGRESSION.md). Les prompts sont dans [PROMPTS_DEVELOPPEMENT.md](../PROMPTS_DEVELOPPEMENT.md).

| Prompt | Module | État | Notes |
|--------|--------|------|-------|
| B0 | Constitution Spec Kit | ✅ 20/08 | `.specify/memory/constitution.md` en **1.0.0** — dix principes, deux sections de contraintes, gouvernance et procédure d'amendement. [Écarts et vérifications](ecrans/b0-constitution.md) |
| B1 | Socle + Identité | ✅ **TERMINÉ 20/08** — **les 126 tâches**, `make check` au vert en entier | **B1 EST TERMINÉ, ET LE JALON QUI OUVRE L'APPEL À PROPOSITIONS DE LA COP31 EST COMPLET.** `make check` passe **en entier depuis la racine** — base détruite et rechargée de zéro, seize schémas, rapport de frontières vide, projections analytiques rafraîchies, site compilé, `clippy -D warnings` **sans un avertissement** —, avec **141 tests** et **143 requêtes préparées**. Plus gros fichier de `backend/` : **588 lignes**. `cargo tree -p identity` : **aucune arête** vers un autre crate de module. **La chaîne différée est éprouvée, plus supposée** : purge des jetons récurrente **qui se replanifie elle-même**, `GET /api/health` qui rend les quatorze indicateurs de `analytics.v_operational_health` **sans recalculer un seul seuil**, `GET /api/ready` qui **sollicite réellement le pool**. Point de contrôle tenu à la main : inscription, worker arrêté (rien ne part), worker relancé — **un seul courriel**. **Une demande d'effacement s'honore sans détruire les statistiques d'une COP passée** : la file RGPD exige la portée **globale**, ou 403 — jamais une file filtrée ; l'anonymisation ne répond qu'à une demande d'**effacement** ; le service **n'émet pas** `identity.person.anonymized`, la fonction de base l'émettant déjà. **La documentation OpenAPI est engendrée, jamais écrite** : 26 chemins annotés auprès de leur gestionnaire, et les **20 codes d'erreur stables** engendrés depuis le catalogue du noyau — `GET /api/docs`, fermée en production. **Deux défauts trouvés en éprouvant à la main** : trois exemplaires du même courriel (le relais retenait l'identifiant **après** l'envoi, alors que le doublon est concurrent), et `platform.audit_log.actor_label` que **personne ne renseignait** alors que la colonne existe pour rester lisible après anonymisation. Quinze écarts, n° 55 à 69. Avant cela, le même jour : **phases 1 à 8** — 99 tâches sur 126, **le jalon qui débloque B2 à B6 fermé**. **ON SE CONNECTE, ET LA SESSION TIENT.** Workspace Cargo à cinq crates (`kernel`, `contracts`, `modules/identity`, `api`, `worker`), 5 356 lignes de Rust hors tests, aucun fichier au-dessus de 363. `make check-back` au vert : mise en forme, `clippy -D warnings` sans un avertissement, **52 tests** dont 35 sur base réelle et jetable et 10 qui traversent HTTP. 40 requêtes préparées versionnées. Les **six issues de connexion** sortent en 200 dans l'ordre imposé ; le jeton d'accès est signé Ed25519, quinze minutes, **sans aucune permission dedans** ; la rotation chaîne les lignes et un **rejeu révoque toutes les sessions**. La discrétion est tenue en message **et en temps** — écart mesuré 1,5 %. Un audit croisé du 20/08 a rendu **45 constats confirmés, tous traités**, dont un défaut de gravité haute : un verrou échu ne rendait qu'**un** essai au compte au lieu de tous. Les deux écarts d'abord assumés sont refermés depuis : **toute écriture passe par la porte du noyau** — la transaction se replie derrière le hachage, écart de temps mesuré à 1,4 % — et **`X-Forwarded-For` n'est cru que d'un mandataire déclaré**, `TRUSTED_PROXIES`, vide par défaut. [Écarts et vérifications](ecrans/b1-socle-identite.md). **QUELQU'UN S'INSCRIT, REÇOIT SON COURRIEL ET SE CONNECTE** : la chaîne complète — API, file de travaux, worker, route privée du site, SMTP — est éprouvée à la main jusqu'à Mailpit. La **réponse invariable** tient par le message et par le temps (le mot de passe est haché même quand rien n'est créé) ; le **jeton en clair** ne vit que dans la charge utile du travail, vidée dès l'envoi ; les **trois refus de jeton** sortent dans l'ordre du contrat, « déjà utilisé » avant « périmé ». Le **trou connu de la file** — un travail mort gardait sa charge utile — est refermé, sans coûter aux modules dont elle est la seule matière de diagnostic. **75 tests**, 60 requêtes préparées. Avant cela : **LE BACK-OFFICE SE PARTAGE SANS SE DUPLIQUER** : huit routes de lecture gardées, la règle métier n° 8 tenue par l'API et non plus par l'écran. Les **trois cas du périmètre** restent distincts — global, éditions listées, **aucun droit → refus**, jamais une liste vide ; une fiche hors périmètre est **marquée** (`in_scope: false`), pas cachée. L'**origine** d'une permission, qui n'existe nulle part en base, se recompose depuis les attributions ; l'historique se lit sur `role_assignments`, qui porte ses deux événements depuis A12. Le noyau gagne la permission testée **sur n'importe quelle portée** — `has_permission()` ne répond que pour une portée précise —, dont B2 aura besoin dès sa première liste. **59 tests**, 48 requêtes préparées, plus gros fichier à 422 lignes. Un écart laissé ouvert : `GET /people` n'est pas bornée par le périmètre **parce que le contrat ne le demande pas**. **UN COMPTE PERDU SE RÉCUPÈRE SANS PASSER PAR L'ASSISTANCE** : demande, courriel, lien suivi, mot de passe choisi — et l'ancien ne vaut plus rien, la session ouverte avec lui rendant `null`. **Deux statuts qui ne disent pas la même chose** : un jeton refusé sort en **200** avec son discriminant, un mot de passe refusé en **422** sur le champ `password`. Le jeton est **revérifié à l'envoi** et pas seulement au contrôle préalable ; un mot de passe refusé **ne brûle pas le lien**. Un changement abouti remet le compteur d'échecs à zéro, **lève le verrou** et **coupe toutes les sessions**. Corrigé au passage, et il datait de la phase 6 : **les liens des courriels menaient à des pages inexistantes** — le site traduit ses chemins, les courriels citaient le nom du fichier de page. **88 tests**, 97 requêtes préparées. **UN WEBINAIRE SE CONFIE À SON RESPONSABLE SANS LUI OUVRIR LE RESTE DE LA PLATEFORME** : l'attribution se vérifie sur la portée **visée**, le retrait sur la portée de **l'attribution visée** — sans cette seconde moitié, une administratrice d'édition défait un rôle global. **Le refus du trigger ressort mot pour mot** ; le retrait **pose trois colonnes et ne supprime jamais** ; les options d'attribution sont bornées à ce que l'appelant peut réellement accorder, le reste restant **visible et désactivé**. Deux défauts trouvés en éprouvant à la main : `forbidden_scope` **divulguait les rôles de la personne visée** — la route exige désormais `identity.role.assign` quelque part, et rend 403 sinon —, et une **valeur** hors liste se faisait passer pour un **champ** fautif. **110 tests**, 109 requêtes préparées, plus gros fichier à 588 lignes. Prochain pas : **B2 — Organisations**, avec la règle métier n° 1 (une organisation, plusieurs dénominations), qui est le défaut n° 1 de la v1. **Les en-têtes CORS sont posés le 20/08, avancés de B7** — ils manquaient, et sans eux aucun appel de navigateur n'aboutissait : **les huit histoires sont désormais éprouvées depuis un vrai navigateur**, ce qui montre ce que `curl` ne montre pas — les cookies de session invisibles à `document.cookie`, et le jeton de rafraîchissement confiné à `/api/auth` par son `Path`. Reste une dette pour **B7** : le mock du site range la fenêtre de validité invalide sous `scope_not_allowed` là où l'API rend `IDENTITY_ROLE_WINDOW_INVALID` en 422 |
| B2 | Organisations | ✅ **LIVRÉE 20/08** — `backend/crates/modules/org` : 21 routes, 6 travaux différés, 6 événements, 11 codes d'erreur, 30 fichiers de test, 246 tests verts sur le workspace. **Recherche à 55 ms au 95ᵉ centile sur 5 001 fiches** (cible 150), **sans une ligne de SQL modifiée**. L'ordre des écritures d'une fusion est **corrigé partout** — ce fichier et l'en-tête d'`admin-organizations.ts` disaient « avant l'appel » ; c'est **après**, dans la même transaction, et la garantie d'A11 est intacte. Le décompte de transfert est exact au chiffre près sur **dix-sept lignes de registre sur dix-huit** : la dernière est déplacée par un trigger d'adhésion avant que la boucle n'y arrive, et rien n'est perdu. Chaîne du courriel **éprouvée jusqu'à Mailpit au navigateur**. Reste `make check` en entier, qui détruit la base. Avant cela, le même jour : 🟨 spécifiée, planifiée et découpée | [`specs/002-organisations/`](../../specs/002-organisations/spec.md) — 8 histoires, 14 cas limites, **84 exigences**, 18 critères de réussite. [Écarts et vérifications](ecrans/b2-organisations.md). **Aucune modification du SQL.** L'écart n° 23 devient **deux lectures au-dessus de la même fonction de base** : celle destinée à une personne n'admet que les fiches portant une ressemblance de dénomination, celle de la revue des doublons rend tous les signaux — et le domaine **continue d'alimenter le score** de la première. L'écart n° 24 est confirmé (qui crée est référent, qui rejoint est membre) et la question qui vient avec est tranchée : **le retrait du dernier référent actif est refusé** tant qu'aucun autre n'a été désigné. **Les quatre obligations d'A11 sont traitées, et la première est CORRIGÉE** : (1) les choix de champ de la fusion sont bien un `UPDATE` de la fiche CIBLE dans la MÊME transaction, mais **APRÈS** `org.merge_organizations()` et non avant — `ux_organizations_name_country` ne porte que sur les fiches vivantes, et la cible ne peut reprendre le nom légal de la source qu'une fois celle-ci passée `merged` ; (2) la liste exige `org.organization.read` **ET** un périmètre d'administration non vide, la permission étant accordée au rôle `standard` et ne pouvant donc pas garder le back-office seule ; la fusion exige `org.organization.merge` en portée **GLOBALE** ; (3) la liste **relit statut, sceau, score de confiance et pointeur de fusion sur `org.organizations`**, les compteurs restant projetés et rafraîchis par un travail différé ; (4) `compute_trust_score()` est tranchée en **travail différé, pas en trigger** — aide au tri et non invariant, avec une clé d'unicité par organisation. **Onze écarts, n° 70 à 80**, dont trois qui auraient cassé à l'exécution : l'ordre des écritures de la fusion, l'**adresse d'URL non arbitrable** (unique sans condition de statut), et l'**unicité des adhésions qui porte aussi sur les révoquées** — une personne refusée ne pourrait plus jamais redemander. Et le piège n° 1 d'`identity` se répète : **la fonction de fusion émet elle-même son événement** et marque elle-même la paire de la file. **Le plan est écrit le même jour** — `plan.md`, `research.md` (**19 décisions**), `data-model.md`, `contracts/` (21 routes, 11 codes d'erreur, 6 événements, 6 travaux différés), `quickstart.md`. Contrôle constitutionnel passé deux fois : **une entorse justifiée**, le décompte de transfert d'une fusion composant son SQL depuis le registre des références — seule façon que le chiffre annoncé et le chiffre rendu viennent du même raisonnement. Trois décisions structurantes : **la lecture filtrée SUR-LIT** (la limite est appliquée à l'intérieur de la fonction, filtrer après coup rendrait moins de lignes que demandé) ; **le service de jetons à usage unique remonte dans le noyau** (trois des cinq finalités n'appartiennent pas à `identity`, et les durées par finalité y vivaient déjà) ; **la cible de 150 ms se mesure avant de se traiter**, avec un semis à distribution réaliste. Six écarts de plus, **n° 81 à 86**, dont un défaut de B1 que seul B2 pouvait révéler : **une personne créée par invitation ne pouvait pas se créer de compte**. Et la règle de frontière qui vaudra pour B3 à B6 est posée : un module **lit** hors de son schéma quand la question porte sur ses propres entités, il n'y **écrit** jamais et n'appelle jamais un autre module. **Le découpage est écrit le même jour** — `tasks.md` porte **129 tâches en 11 phases, dont 29 tests**, une phase par histoire, quatre jalons de livraison. Deux avertissements en tête : **la phase 2 touche du code livré** (jetons remontés dans le noyau, inscription corrigée) et **l'ordre des écritures d'une fusion est l'inverse de celui que ce fichier annonçait**, corrigé par T126. Prochain pas : `/speckit-implement` |
| B3 | Événements | ✅ **LIVRÉE 20/08 (T001–T129)** | `backend/crates/modules/event` : **37 routes**, **6 événements de domaine**, 3 codes d'erreur, 1 travail différé, 32 fichiers de test. **Le jalon que le prompt demandait est atteint** : l'appel à propositions de la COP31 peut être ouvert, avec sa grille pondérée. **Le refus qui sauve les notes** est l'unique entorse au principe VIII du module — retirer un critère porteur de notes réussirait en base et détruirait l'argumentaire d'une décision de sélection ; le test vérifie le refus **et que les notes sont toujours là après**. **Les écarts n° 25 et n° 26 se referment** : la page publique d'une édition porte ses trois déclinaisons d'image et son appel résolu **en une requête**, et le critère de publicité vit dans la vue du modèle. **Une dérogation, assumée et bornée** : poser les thématiques d'un fil écrit dans `reference.entity_terms` — la seule écriture hors du schéma `event`, isolée dans `repo/themes.rs`, le contrôle mécanique du plan étant trop large d'un schéma. Avant cela, le même jour : **phases 1 à 4** — **US1 et US2 étaient livrées** : une édition existe avec son sigle et son calendrier daté dans son fuseau, et le périmètre d'administration la borne — liste filtrée, facettes comptées sur les mêmes lignes, refus explicite sur périmètre vide, et six identifiants forgés menant au même refus qu'un identifiant inexistant. Le détail rend ses **six onglets en une réponse**, sur une seule connexion et dans une transaction en lecture seule. **6 routes montées sur 37**, 69 tests, `cargo test --workspace` au vert. **T049 reste ouverte** : les six routes d'onglet qu'elle vise naissent en phases 5, 7 et 9 ; le garde et son épreuve sur les six cibles sont livrés. Prochain pas : **phase 5, l'appel unique et sa grille (T054–T068)**, qui referme le jalon demandé. Avant cela, le même jour : **Le découpage est écrit** — `tasks.md` porte **129 tâches en 11 phases, dont 31 tests**, une phase par histoire et **quatre jalons de livraison**. **Trois avertissements en tête** : la suppression d'un critère d'évaluation **réussit** en détruisant les notes (le refus T060 et le test T067 vont ensemble) ; le préfixe `/admin/planner` sera partagé avec B5 et se compose dans `api` dès les fondations (T018) ; trois chemins littéraux doivent précéder leur homologue paramétré. **Une dépendance que le plan n'avait pas nommée** : la phase 4 bloque les phases 7 à 9, toutes les écritures d'onglet rendant la composition entière. **Le jalon que le prompt demande est T001–T068.** Prochain pas : `/speckit-implement`. Avant cela, le même jour : **Le plan est écrit le même jour** — `plan.md`, `research.md` (**20 décisions**), `data-model.md`, `contracts/` (**37 routes**, 3 codes d'erreur, 6 événements, 1 travail différé), `quickstart.md`. Contrôle constitutionnel passé **deux fois** : **une entorse justifiée**, le refus de supprimer un critère porteur de notes. **Aucune dépendance nouvelle, et aucun SQL composé dynamiquement** — contrairement à B2, tout passe la vérification à la compilation. **Deux écarts de plus, n° 91 et 92** : (91) **supprimer un critère d'évaluation détruit les notes en silence** — `xmod_fk_review_scores_criterion` est `ON DELETE CASCADE`, l'ordre réussit et l'argumentaire d'une décision de sélection disparaît ; le service compte et refuse ; (92) **cinq tables du module ne sont pas auditées** — journées, fils, lieux, salles, canaux —, consigné et non compensé. **Quatre décisions structurantes** : les **jours civils se calculent en base, dans le fuseau de l'édition** (deux bases de fuseaux dans une même chaîne, c'est le défaut d'`Europe/Geneva`) ; **tout détachement se compte AVANT** de détacher (`ON DELETE SET NULL` rendrait zéro) ; le préfixe **`/admin/planner` est composé une seule fois dans `api`**, avant que B5 n'y vienne — le patron de `/people`, posé avant que les trois routes muettes de B2 ne se reproduisent ; et la **publication annonce au lieu d'écrire**, en portant le **prédicat exact** des séances. Avant cela, le même jour : [`specs/003-evenements/`](../../specs/003-evenements/spec.md) — **8 histoires, 16 cas limites, 99 exigences, 25 critères de réussite. Aucune modification du SQL** : `060_events.sql` relu en entier, ses huit sections couvrent déjà tout le prompt. [Écarts et vérifications](ecrans/b3-evenements.md). **Quatre écarts nouveaux, n° 87 à 90**, dont trois qui engagent le plan. (87) **Aucun déclencheur de ce module n'émet d'événement de domaine** — deux déclencheurs d'audit, aucune fonction `SECURITY DEFINER` : le piège n° 1 de B1 et B2, où la base émet déjà ce que le service s'apprête à émettre, **n'existe pas ici**, et c'est vérifié plutôt que supposé. (88) **Le rôle `programmer` ne détient AUCUNE permission `event.*`** — l'écart n° 5 d'A10 supposait le contraire ; un chargé de programmation ne peut ni créer un fil, ni déclarer une salle, ni poser un canal. Consigné, non corrigé : ce serait modifier le modèle. (89) **Publier la programmation exige deux écritures dans deux schémas** — la date sur `event.events`, la visibilité sur `programme.sessions.published_at` que `v_public_schedule` filtre. **Tranché par l'outbox** : B3 contrôle, pose la date et annonce ; la Programmation consomme. (90) **La publication est gardée par `programme.session.schedule`** et non par `event.event.manage` — c'est la permission que le modèle attribue au rôle chargé de « publier la programmation », et le garde vivant dans `kernel` depuis B1, aucune arête entre crates n'en découle. **L'écart n° 9 est traité par l'option A** : le sigle reste facultatif en base et devient obligatoire **dans le service** pour une édition tenant un pavillon (2 à 12 caractères, lettres, chiffres, tiret), vérifié sur l'état **résultant** de l'écriture, avec une valeur par défaut proposée — un refus qui ne propose rien fait chercher une convention que personne n'a écrite. **Deux écarts d'A3 sont refermés** : les éditions publiques deviennent une ressource portant la règle « ni brouillon ni annulée » (n° 26), et la page d'une édition embarque ses **trois** déclinaisons d'image (n° 25). **Deux obligations d'A10 confirmées** : le détail embarque description, message d'accueil et images ; la **création** d'une édition exige la portée **globale**. **Deux points restent à arbitrer** : la granularité du calendrier d'une série de webinaires (302 journées vides sur `paco2027` — le plan annonce avant d'écrire, aucune borne n'est codée) et les permissions du rôle de programmation |
| B4 | Propositions | ✅ **LIVRÉE 21/08 (T001–T160)** | `backend/crates/modules/programme` : **les huit histoires, 37 routes sur 37**, 3 événements émis par le service (huit le sont par la base), 6 codes d'erreur, **aucun travail différé**, 11 fichiers de test. **Le jalon que le prompt demandait est atteint dès T076** — l'appel de la COP31 reçoit des dossiers — et le module va jusqu'au bout : la liste du comité, la fiche d'évaluation et son voile, l'espace organisation, la correction et le renvoi, les pièces, l'historique et la reprise v1. **Le voile de l'aveugle n'est pas un filtre** : quand il est baissé, la requête qui lit les revues des pairs **n'est pas exécutée**, et le test cherche la note d'un pair dans la charge utile **sérialisée** — lire puis vider laisserait la donnée à portée d'un champ oublié dans un type de sortie. Le décompte, lui, est lu : compter n'ancre pas. **La consolidation des notes est appelée dans la transaction du dépôt d'une revue, et les agrégats sont relus en base** : c'est le seul contrôle qui dise quelque chose de l'écart n° 98, dont l'absence serait **muette**. **L'espace organisation se prouve par un balayage de la charge utile entière** — vérifier champ par champ laisserait passer celui qu'on ajoutera demain. **Corriger un dossier retenu ne déplace pas sa séance** : le test change titre, créneau, format et durée, puis relit la séance champ par champ. **La reprise v1 n'émet aucun événement** — elle écrit dans le journal sans passer par la mise à jour de l'état, donc sans réveiller le déclencheur : huit mille courriels évités. **Neuf écarts nouveaux, n° 102 à 110**, dont **trois ont corrigé du code déjà livré** : le n° 106 dans le noyau — un `check_violation` levé par `RAISE` ne porte aucun nom de contrainte, et le refus de plafond de note sortait en **500** là où le contrat annonce un 422 —, le n° 110 dans les fondations de ce module — un dossier **retenu** était fermé à la correction sans que rien ne le demande —, et le refus de confondre dépôt et renvoi dans US1. **Aucun n'aurait été vu par relecture : chacun est tombé sur un test.** **Trois écarts restent à arbitrer par le commanditaire** : le n° 105 (confier un dossier à qui ne siège pas au comité n'a aucun refus au contrat), le n° 109 (le nom d'un membre du comité qui écrit au déposant — masqué, l'écran affichera un libellé neutre) et le n° 107 (la liste des membres d'une organisation n'a aucune porte, lue ici par élargissement d'une lecture existante). **525 tests**, `make check` **en entier** au vert — volume détruit, schéma rechargé de zéro —, **aucune arête** vers `identity`, `org` ni `event`. **Une tâche reste ouverte, T159** : éprouver à la main les parcours du quickstart au navigateur — le voile et le balayage sont prouvés sur la charge utile de l'API, pas sur ce que l'écran affiche, et le raccordement du front est B7. Avant cela, le 20/08 : **spécifiée, planifiée et découpée** — [`specs/004-propositions/`](../../specs/004-propositions/spec.md) — **8 histoires, 17 cas limites, 103 exigences, 27 critères de réussite. Aucune modification du SQL** : `070_programme_proposals.sql` relu en entier, ses huit sections couvrent déjà tout le prompt. [Écarts et vérifications](ecrans/b4-propositions.md). **Huit écarts nouveaux, n° 93 à 100**, dont trois qui auraient cassé à l'exécution. (93) **Le déclencheur d'état émet DÉJÀ l'événement de domaine** — `tg_guard_proposal_status()` appelle `platform.emit_event()` à chaque transition acceptée : c'est **l'inverse de B3**, le piège n° 1 de B1 et B2 est de retour, et un service qui émettrait aussi enverrait courriels et notifications **en double**, visible seulement en production. La conclusion d'un module ne se transporte pas au suivant. (95) **`proposals.slug` est NOT NULL et unique par édition, et `ProposalDraft` ne le porte pas** — le premier enregistrement a lieu à la première frappe, quand le titre est vide et que `platform.slugify('')` rend NULL : à la lettre du contrat, **le tout premier appel du formulaire échoue**. Dérivation, repli et unicité par suffixe passent au service, qui seul connaît les autres dossiers de l'édition. (98) **Rien n'appelle `refresh_proposal_score()`** — la fonction existe, son commentaire dit de l'appeler, aucun déclencheur ne le fait : sans appel explicite dans la même transaction, la note, la moyenne et l'élimination d'un dossier ne bougent jamais et **le classement du comité est faux sans qu'aucune erreur ne le signale**. S'y ajoutent (94) `platform.purge_term_links()`, annoncée par le commentaire de `reference.entity_terms` et **définie nulle part** — le service purge lui-même, ce que l'écart n° 3 demandait déjà ; (96) **un dossier peut naître dans n'importe quel état**, le garde étant posé `BEFORE UPDATE OF status` — la création pose toujours « brouillon », et c'est aussi ce dont la reprise v1 a besoin ; (97) le motif d'un **retrait par l'organisation** s'écrit dans `decision_reason` et l'écrase — le motif se lit dans le journal ; (99) **une demande de correction peut être invisible de celui qu'elle vise et compter quand même**, `is_change_request` étant indépendant de `visibility` — forcée en `submitter`, le compteur redevient honnête par construction ; (100) **`legacy.migrate_activities()` est une coquille vide**, son corps étant annoncé dans `scripts/migration/03_activities.sql`, qui n'existe pas — le dossier `scripts/` non plus : la déduction des transitions de l'écart n° 37 devient une opération **de ce module**, rejouable et sans doublon. **Les douze exigences du prompt sont traitées** — écarts n° 3, 4, 8, 27, 28, 30, 32, 35, 37, 38, 39 et les deux règles en toutes lettres : le triplet `('programme','proposals',id)` est posé par le service et **jamais reçu du client** ; les transitions offertes croisent `proposal_transitions_allowed` avec `has_permission()` **et** la qualité de porteur, le service ne rejouant pas le graphe à l'écriture ; `title` et `title_text` gardent leurs deux noms ; l'espace organisation reçoit une **composition propre** où ni note, ni rang, ni nom de membre du comité, ni inscrit nominatif n'entrent ; le fil du soumissionnaire est filtré **à la source** ; le renvoi d'un dossier corrigé est une **route distincte** du dépôt, le plafond restant vérifié dans les deux cas ; la recomposition en brouillon vit **à un seul endroit** ; et la modification d'un dossier retenu **ne propage rien** vers sa séance. **Deux points sont explicitement laissés au plan** : la frontière de la **création d'une personne inconnue** — dérogation bornée, ou contrat d'événement qui interdirait la création synchrone dont le formulaire a besoin —, et le découpage des fichiers, la fiche d'évaluation composant onze tables. **Deux questions restent au commanditaire**, la spécification tenant l'option la moins engageante : la n° 8 (le déposant voit-il sa note et son rang ? — non) et celle de l'écart n° 35 (une résolution posée par le déposant vaut-elle clôture ? — déclaration). **Le plan est écrit le même jour** — `plan.md`, `research.md` (**24 décisions**), `data-model.md`, `contracts/` (**37 routes**, 6 codes d'erreur, **3 événements émis par le service** contre **huit émis par la base**, **aucun travail différé**), `quickstart.md`. Contrôle constitutionnel passé **deux fois** : **une entorse et deux écritures hors schéma**, toutes trois justifiées. (1) **Le classement des trois refus de recevabilité avant l'écriture** — le contrat attend des réponses **portant des valeurs** que le déclencheur n'expose que dans une phrase française, et surtout le même `restrict_violation` sert aux transitions interdites **et** aux trois refus : sans classement on ne saurait pas laquelle des quatre causes s'applique, et la reconnaître au texte serait la dépendance la plus fragile possible. Le déclencheur reste le dernier mot. (2) **Les thématiques**, même dérogation bornée que B3 pour les fils. (3) **La création d'une personne inconnue** — le point que la spécification laissait au plan, **tranché en faveur de la dérogation bornée**, avec un précédent livré en B2 : la voie du contrat d'événement rendrait la création différée, donc l'intervenant absent de la réponse, donc le doublon indétectable pendant que le déposant est devant son écran. **Un neuvième écart, n° 101, relevé au plan** : `GET /proposals/:id/transitions`, que le prompt voulait pour les transitions offertes, est **déjà le journal** dans le contrat du front — le journal le garde, les transitions offertes prennent `available-transitions` plus un champ de la fiche, l'alternative que le prompt offrait lui-même. **Un défaut déjà payé, évité de justesse** : deux routes vivent sous `/organizations`, préfixe qu'un `web::scope` unique occupe — deux scopes du même préfixe ne se complètent pas, et cela a coûté **trois routes muettes en B2**. Le préfixe est refactorisé sur le patron de `/people` : **du code livré est modifié**, à annoncer en tête du découpage. **Cinq décisions tranchées dans la recherche plutôt que laissées à l'implémentation**, chacune parce qu'un choix tacite aurait produit une faute **à l'exécution** : la double émission d'événements, la dérivation de l'adresse d'URL, l'appel à la consolidation, la distinction des deux codes du garde d'état, et la composition du préfixe. **Une dépendance nouvelle et une seule**, `ammonia`, consignée comme la constitution l'exige. **Aucun travail différé** — le worker n'est pas modifié, et un test compte les gestionnaires plutôt que de le supposer. **Et découpé le même jour** — `tasks.md` porte **160 tâches en 11 phases, dont 46 de test et 97 parallélisables**, une phase par histoire et **quatre jalons**. **Le jalon que le prompt demande est T001–T076** : à ce point l'appel de la COP31 reçoit des dossiers, et le comité peut encore travailler sur la v1. **Quatre avertissements en tête**, chacun parce qu'il coûterait cher découvert en chemin, et **un encadré** pour le point resté sans réponse — qui, dans une organisation, peut corriger, renvoyer et **retirer** un dossier déposé par une collègue —, dont l'hypothèse est **isolée dans une seule fonction** pour qu'un arbitrage n'en change qu'une. **Trois dépendances que le plan n'avait pas nommées** : la machine à états ne s'éprouve pas sans le dépôt ; la fiche dépend d'une tâche de la machine à états (le champ des transitions offertes vient de la même requête) ; l'espace organisation dépend du fil filtré de la fiche — l'écrire deux fois produirait deux filtres dont le second divergerait, exactement le défaut que le filtrage à la source doit empêcher. L'histoire de la reprise v1 est, elle, **complètement indépendante**. Prochain pas : `/speckit-implement` |
| B5 | Sessions | ✅ **TERMINÉ 21/08** — **les 156 tâches**, `make check` au vert en entier | **LE PROGRAMME DE LA COP31 EST PUBLIC, ET L'ON S'Y INSCRIT.** `make check` passe **depuis la racine** — base détruite et rechargée de zéro, seize schémas, rapport de frontières **vide**, `clippy -D warnings` sans un avertissement —, avec **627 tests**. **L'écart n° 57 est refermé** : retenir un dossier fait naître ses séances **dans la transaction de la décision**, sur **un seul hameçon**, et une acceptation rejouée n'en double aucune. **Aucun chevauchement n'est refusé** — deux séances superposées s'écrivent et la réponse porte le conflit ; aucun type de ce module ne permet d'exprimer un refus de placement. **Premier consommateur d'outbox du dépôt** : B3 estampille et annonce, B5 rend publiques les séances désignées, et un **test de bout en bout reproduit le symptôme « publié mais rien de public »** — worker arrêté — avant de le dissiper. **Trois défauts silencieux du modèle sont corrigés dans le service et chacun est éprouvé** : la journée de rattachement remise à nul quand le créneau change (n° 113), la validation contre le formulaire **résolu** et non attaché (n° 114), et le **verrou de ligne** sur la séance (n° 124) — **mesuré, pas supposé** : cent inscriptions concurrentes sur dix places rendent **onze** confirmées verrou retiré, dix verrou posé. **Le service n'émet aucun événement** : une ligne d'outbox par inscription, trois après une acceptation à trois séances. **L'écart n° 7 a été corrigé plutôt que suivi** — le canal de diffusion reste saisissable. **L'organisation voit trois nombres et jamais un nom**, éprouvé par un balayage de la charge utile entière. **Une dépendance nouvelle**, `regex`, déjà transitive et consignée. **Le SQL n'a pas bougé.** Crate `programme` : 17 372 → **27 318 lignes**, aucun fichier au-dessus de mille. [Écarts et vérifications](ecrans/b5-sessions.md) |
| B5 (historique) | Sessions | 🧩 découpée 21/08 | **156 tâches en 12 phases, dont 67 de test**, quatre jalons, et **le jalon que le prompt demande hors inscriptions à T001–T112**. **Cinq avertissements en tête**, dont celui qui **corrige l'écart n° 7** : le canal de diffusion reste saisissable. **Une dépendance que le plan n'avait pas nommée** : la programmation publique doit précéder la publication, faute de quoi celle-ci se prouverait sans qu'on puisse en constater l'effet. Avant cela, planifiée le même jour : **27 décisions, 17 routes, 8 codes d'erreur, 0 événement émis, 1 consommé — le premier du dépôt —, 0 travail différé, une entorse et une écriture hors schéma nouvelle.** **Deux écarts que seul le plan pouvait révéler** : le contrôle de jauge de la base **ne tient pas sous concurrence** (n° 124), et le déclencheur d'inscription **revalide à chaque changement d'état**, annulation comprise (n° 125). **Sans worker, publier n'a aucun effet visible** : ce module est le premier à consommer un événement de domaine. Avant cela, spécifiée le même jour : [`specs/005-sessions/`](../../specs/005-sessions/spec.md) — **9 histoires, 14 cas limites, 114 exigences, 29 critères, aucune modification du SQL, aucune ligne de Rust.** **Aucun crate n'est créé** : `backend/crates/modules/programme` porte déjà tout le schéma depuis B4, et ses dossiers internes sont nommés par agrégat pour recevoir ce jalon. **Treize écarts nouveaux, n° 111 à 123**, dont **un qui corrige la consigne du prompt** — l'écart n° 7 demande de refuser le canal de diffusion à l'écriture, or le déclencheur ne le pose que lorsqu'il est **nul** et n'écrase jamais un canal choisi : le refuser aurait cassé une fonctionnalité livrée du planificateur. **Trois écarts produiraient un défaut silencieux** : la journée de rattachement ne se recalcule pas quand on **déplace** une séance (n° 113), le déclencheur d'inscription ne valide **rien** quand la séance ne porte pas de formulaire attaché (n° 114), et la promotion depuis la liste d'attente ne vérifie pas la jauge (n° 116). **Deux écarts anciens sont traités** : le n° 57 devient l'histoire n° 1, et le n° 11 est tranché — une réponse « pays » est le code ISO à deux lettres. **Une question au commanditaire** : que devient la séance d'un dossier annulé après acceptation. [Écarts et vérifications](ecrans/b5-sessions.md) |
| B6 | Média + Engagement | 🏁 **JALON 3 ATTEINT 22/08 — PHASES 1 À 11 LIVRÉES** | **T001 à T198 : les neuf histoires sont livrées.** Un fichier entre, devient servable, se rattache à ce qu'il illustre ; l'organisation lit son calendrier de rappels **sans un nom** ; le rappel part **une fois et une seule** ; les textes se corrigent sans redéploiement ; chacun choisit ce qu'il reçoit et une adresse morte cesse d'être sollicitée. **Et le disque ne se remplit plus tout seul** (US9, 22/08) : orphelins listés du plus lourd au plus léger **variantes comprises**, suppression refusée sur un objet encore rattaché **en disant combien de fiches l'utilisent** (écart n° 128), purge qui **efface réellement du stockage** et n'échoue pas sur un objet déjà parti, réconciliation des compteurs de quota, et préparation des partitions du journal d'expédition (écart n° 137). **Trois chaînes récurrentes de plus**, sur le patron de B1 : elles posent leur propre occurrence suivante, et le démarrage du worker les réarme **sans doublon**. **856 tests**, `clippy -D warnings` sans un avertissement, aucune écriture hors schéma, aucune arête entre crates de module. **Reste la phase 12** : les trente-trois routes sur la vraie application, les seize codes d'erreur dans l'OpenAPI engendrée, les sept parcours du quickstart **sur Garage réel** — seul moment où S3 est exercé —, et `make check` en entier depuis la racine. [Écarts et vérifications](ecrans/b6-media-engagement.md) |
| B7 | Raccordement du front | ✅ **22/08** | **Le site parle à l'API.** Client TypeScript **engendré** (`make openapi`, 156 chemins, 175 opérations, 65 codes) et vérifié par `make check-api-contract` — 128 appels, 123 formes toutes définies, 19 routes en attente nommées. **152 écarts tranchés**, contre-expertise sur les plus lourds. **4 routes ajoutées au socle transverse** (pays, langues, taxonomies, drapeaux **résolus**), **7 corrections dans des modules livrés**, **5 noms de contrat rectifiés**, et l'export refuse un `operation_id` en double. **`make check` au vert en entier, 857 tests.** **L'obligation CORS relevée en auditant B1 est levée** : les en-têtes sont posés, l'origine autorisée est celle d'`APP_PUBLIC_URL`, et une écriture depuis une autre est refusée en `IDENTITY_ORIGIN_REJECTED` — vérifié au navigateur comme en ligne de commande. [Écarts et vérifications](ecrans/b7-raccordement.md) |
