# Suivi de Guide Négo

> **Ce fichier est la mémoire de Guide Négo entre deux sessions.** Il remplace, pour ce projet, la progression de l'ePavillon : on n'écrit ni dans `docs/PROGRESSION.md` ni dans `docs/progression/` — [ADR-017](adr/017-le-suivi-vit-dans-progress-md.md). Toute session le lit en arrivant et le met à jour en partant.

## État

| Étape | État | Note |
|---|---|---|
| Cadrage et documentation | ✅ 18/09/2026 | Ce dossier |
| Maquette — direction artistique | ✅ 19/09/2026 | « Typographique », Atkinson Hyperlegible Next, quatre onglets et bouton « Aa » — [ADR-018](adr/018-direction-typographique-quatre-onglets.md) |
| Maquette | ✅ 20/09/2026 | 18 pages dans [design/ecrans/](design/ecrans/), jetons et composants dans [design/passation/](design/passation/), écarts tranchés dans [05-design.md](05-design.md) ; police embarquable fournie, icône provisoire : le symbole inversé de l'ePavillon ; le back-office se construit sur celui de l'ePavillon |
| Amendement de la constitution (1.1.0) | ✅ 20/09/2026 | Section « Guide Négo », principes XI à XIV, deux contraintes ; I à X inchangés |
| Correctif du modèle : vecteurs | ⏳ À faire | Avant toute indexation |
| 0a — Coquille et système de design | 🟡 Recette faite sauf l'appareil réel, 21/09/2026 | [spec](../../specs/008-guide-nego-coquille/spec.md) · [plan](../../specs/008-guide-nego-coquille/plan.md) · [tâches](../../specs/008-guide-nego-coquille/tasks.md) — 73 tâches, branche `008-guide-nego-coquille` ; **72 livrées** : drapeau, fondations du design, garde des lectures, coquille installable, hors-connexion, thème, page des composants, recette. **T071 déroulée au navigateur le 21/09** — § 1, § 2 (hors installations) et § 3 vérifiés sur la version construite, **un défaut corrigé** (« Prête hors connexion » ne paraissait qu'à la deuxième ouverture). **Reste sur appareil réel** : l'installation Android puis iPhone, et le débit bridé. La procédure d'ouverture est au § 14 de [DEPLOIEMENT.md](../DEPLOIEMENT.md) |
| 0b — Compte et admission | 🟡 Recette faite sauf l'appareil réel, 22/09/2026 | [spec](../../specs/009-guide-nego-compte-admission/spec.md) · [plan](../../specs/009-guide-nego-compte-admission/plan.md) · [recherche](../../specs/009-guide-nego-compte-admission/research.md) · [modèle](../../specs/009-guide-nego-compte-admission/data-model.md) · [trois contrats](../../specs/009-guide-nego-compte-admission/contracts/) — cinq récits, 52 exigences, quatorze critères ; **le crate `negotiation` naît à cette étape** ; deux questions tranchées par le commanditaire : la portée d'un code se choisit à sa création (une COP, ou tout Guide Négo), et la réponse à une demande part par courriel. Rien n'existe en base pour les codes, les usages, les demandes, le réseau ni le type d'appareil ; la permission, le rôle et la portée existent déjà. **Phases 1 à 7 livrées les 21 et 22/09** — 112 tâches sur 124. La base locale a été migrée **sans être détruite**, `negotiation` est monté dans l'API et le worker, et **les cinq récits sont entiers** : la session dit d'où elle vient et dure 90 jours glissants, le code d'invitation ouvre l'accès (neuf issues en 200, quota tenu par la base, essais comptés par personne), **l'IFDD tient ses codes** (créer, lister, révoquer, voir les usages, retirer un accès), **durcit l'admission sans redéployer** (trois modes, file des demandes, deux décisions, deux courriels), et **le verrou dit ce qu'il ferme** — `GnVerrou`, « Mon accès » dans ses cinq états. **Phase 8 déroulée le 22/09** — 122 tâches sur 124 : migration répétée sur une copie de la base et schémas recomparés, parcours mené au navigateur sur la **version construite**, 84 mesures d'écran, non-régression du site, `make check-safe` au vert API arrêtée. **Reste T112**, qui ne se fait que sur un Android et un iPhone réels |
| 0c — Thématiques, « Ma journée », profil | 🟡 Close côté code, 22/09/2026 — mise en ligne et appareil réel au § 15 de DEPLOIEMENT.md | [spec](../../specs/010-guide-nego-accueil-profil/spec.md) · [plan](../../specs/010-guide-nego-accueil-profil/plan.md) · [recherche](../../specs/010-guide-nego-accueil-profil/research.md) · [modèle](../../specs/010-guide-nego-accueil-profil/data-model.md) · [trois contrats](../../specs/010-guide-nego-accueil-profil/contracts/) · [recette](../../specs/010-guide-nego-accueil-profil/quickstart.md) — quatre récits, 44 exigences, douze critères, sur les écrans « 06 Thématiques », « 07 Ma journée », « 08 Ma journée hors connexion », « 11 Profil » et « 12 À propos ». **Deux questions tranchées par le commanditaire le 22/09.** (1) *Les thématiques de négociation sont un vocabulaire à elles* — `negotiation_theme` dans `reference.taxonomy_terms`, semé des dix thématiques de la maquette, comme `activity_theme` l'est pour les activités : une donnée, jamais une liste dans le code. Les sessions (3a) et les alertes (3b) s'y rattacheront ; **le Pavillon garde `activity_theme` et n'est jamais filtré par « mes thématiques »**. Pas d'écran d'administration des vocabulaires : le site n'en a pour aucun. (2) *Les textes qui engagent ont une source unique* — un fichier Markdown `fr`/`en` par texte, avec sa version en tête, embarqué dans l'API et servi par une route publique ; **la version servie remplace le réglage `PRIVACY_POLICY_VERSION`**, `identity.consents` enregistre celle que l'API a servie, et un test refuse un texte modifié sans nouvelle version. Les pages `/confidentialite` et `/conditions-utilisation` du site, aujourd'hui liées et absentes, se construiront dessus, hors de cette étape ; un éditeur pourra venir plus tard derrière la même route. **Trois divergences avec la maquette, inscrites** : les thématiques suivent le compte (son écran « À propos » dit l'inverse) ; **les trois interrupteurs de consentement ne sont pas livrés** — aucun n'a d'effet à cette étape, et un interrupteur sans effet trompe (constitution, XII), [écart 40](05-design.md) ; la cloche attend 3b. **Laissé au plan** : où vit le lien personne ↔ thématiques — `identity.negotiator_profiles.specializations`, qui existe déjà, ou une table propre. **Une vérité, pas deux**. **Phase 1 livrée le 22/09** sur la branche `010-guide-nego-accueil-profil` : le bloc `auth` est sorti de `useApi.ts` (973 → 895 lignes) vers `composables/api/auth.ts`, sans qu'aucun des appelants ne bouge. **Phase 2 livrée le 22/09** : vocabulaire `negotiation_theme` et dix termes, table `negotiation.theme_subscriptions`, base locale **migrée** deux fois par le script rejouable et schémas comparés. **Phase 3 livrée le 22/09** : `GET` et `PUT /negotiation/me/themes`, empreinte sur les codes triés, `If-Match` et le `412`, trois codes stables, seize tests sur base réelle. **Phase 4 livrée le 22/09** : la file d'écritures différées, qui n'existait pas — magasin `ecritures`, `file.ts` pur, trois déclencheurs, vidange à la déconnexion —, et le vocabulaire lu avec le drapeau ; quinze tests sans navigateur. **Phase 5 livrée le 22/09 — le MVP de l'étape est tenu** : l'écran « Mes thématiques » sert le premier choix et la modification, le choix passe par la file avec son empreinte, la garde de première entrée le propose une seule fois, et le `412` s'affiche avec le message de l'API. **Phase 6 livrée le 22/09** : « Ma journée » et ses cinq blocs, chacun vide en une ligne, l'avatar dans l'en-tête, le jour seul en sous-titre. **Phase 7 livrée le 22/09** : le profil porte le nom, le pays, « Mon suivi », « Affichage », « Application » ; « Mes téléchargements » mesure la place — la libérer vient avec les documents, à l'étape 1. **Phase 8 livrée le 22/09** : `GET /legal/{cle}`, une source pour le site et l'application, les textes **en attente du texte de l'IFDD**, la version servie à la place de `PRIVACY_POLICY_VERSION`, « À propos » et `GnTexteLong`. **Phase 9, recette, le 22/09** : quickstart déroulé sur la version construite, **cinq défauts trouvés et corrigés** (code redemandé à un compte admis, `412` dit seulement sur l'écran des thématiques, boucle d'ouverture et choix perdu quand le stockage est refusé, adresse longue qui sortait de sa ligne à 320 px), SC-007 gardé par `check:guide-nego`. **Reste sur appareil réel** : T096 à T098, dont T112 de 0b |
| 1 — Documents | 🟡 Close côté code, 24/09/2026 — phases 1 à 13 livrées ; restent la recette des réservés (T102) et l'appareil réel (T116), au § 15 de DEPLOIEMENT.md — **critère de sortie atteint sur poste** — l'essai rend l'issue A, les fichiers privés sont privés, le modèle est en base, le worker extrait, l'API sert les documents, le back-office publie le guide, le téléphone garde et efface les copies, la bibliothèque et la fiche se lisent sans compte et sans réseau, le guide se lit en salle sans réseau, avec sommaire, recherche et réglages, « Mes documents » rend la place d'un geste, et un expert corrige un passage sans republier | [tâches](../../specs/011-guide-nego-documents/tasks.md) · [spec](../../specs/011-guide-nego-documents/spec.md) · [plan](../../specs/011-guide-nego-documents/plan.md) · [recherche](../../specs/011-guide-nego-documents/research.md) · [essai](../../specs/011-guide-nego-documents/essai-extraction.md) · [modèle](../../specs/011-guide-nego-documents/data-model.md) · [quatre contrats](../../specs/011-guide-nego-documents/contracts/) · [recette](../../specs/011-guide-nego-documents/quickstart.md) · [ADR-021](adr/021-pdfium-dans-le-worker.md). **Relue par le commanditaire le 22/09** : téléchargement sans compte (écart 41), « Nouveau » = moins de sept jours et jamais ouvert, aperçu de chaque page et « ouvrir tel quel », triangle rouge (écart 20 élargi), « Marquer » non livré (écart 42), thématiques par `entity_terms`, rôle `expert`. **Phase 1, l'essai, le 23/09** sur la branche `011-guide-nego-documents` : **PDFium tient les huit critères** sur le vrai guide, extraction complète en 1,8 s — pas de service Python. **Le téléphone garde tout**, tranché par le commanditaire : texte (74 Ko) et images des 19 pages à tableau ou figure (4,1 Mo, plus que le PDF). R7 ajusté, `origin` porte le texte de sa zone. **Phase 2 le 23/09** : bucket `epavillon-prive` fermé au web, la garde des documents ouverte, `MediaFileField`. **Phase 3 le 23/09** : le modèle migré sans destruction. **Phase 4 le 23/09** : le worker extrait, le vrai guide en 18,5 s. **Phase 5 le 23/09** : l'API, 25 routes. **Phase 6 le 23/09** : le back-office des documents, recette § 1 sur le vrai guide. **Phase 7 le 23/09** : la plomberie hors connexion, prouvée sans navigateur. **Phase 8 le 23/09** : la bibliothèque et la fiche. **Phase 9 le 23/09** : le lecteur, recette § 3 sur la version construite. **Phase 10 le 23/09** : lire confortablement. **Phase 11 le 23/09** : « Mes documents », la place et ce qui s'efface — sauf la recette des réservés, faute d'un compte avec l'accès. **Suite : phase 12**, la note de correction |
| 1b — Le lecteur montre le PDF d'origine | 🟡 Phases 1 à 6 et 10 livrées, 24/09/2026 — point de contrôle de la phase 6 atteint : **le guide se lit sur ses pages, en ligne et en mode avion** ; [ADR-022](adr/022-pdfjs-dans-le-client.md), [grille](../../specs/012-guide-nego-lecteur-pdf/essai-lecteur.md). Restent les phases 7 (repérer un passage), 8 (« Texte agrandi »), 9 (notes en marge), 11 (recette) ; FR-029 au navigateur ; T084 sur appareil réel | [tâches](../../specs/012-guide-nego-lecteur-pdf/tasks.md) · [spec](../../specs/012-guide-nego-lecteur-pdf/spec.md) · [plan](../../specs/012-guide-nego-lecteur-pdf/plan.md) · [recherche](../../specs/012-guide-nego-lecteur-pdf/research.md) · [essai](../../specs/012-guide-nego-lecteur-pdf/essai-lecteur.md) · [modèle](../../specs/012-guide-nego-lecteur-pdf/data-model.md) · [trois contrats](../../specs/012-guide-nego-lecteur-pdf/contracts/) · [recette](../../specs/012-guide-nego-lecteur-pdf/quickstart.md), branche `012-guide-nego-lecteur-pdf` partie de `011-guide-nego-documents` — six récits, douze critères ; **trois hypothèses tranchées par le commanditaire le 24/09** — « Texte agrandi » remplace « ouvrir tel quel » et suit par défaut le verdict de l'extraction, le choix « Pages · Texte » prend la place de « Marquer » dans la barre dépliée (écarts 43 et 44), la copie gardée porte un numéro de format. `main` fusionnée dans la branche, qui reçoit la règle des clés d'API. Décidé le 24/09 ; prompt dans [04-roadmap.md](04-roadmap.md). **L'étape 1 ne se fusionne pas dans `main` avant ce cycle** |
| 2 — FAQ et lexique | ⏳ À faire | Ouvre la recherche globale |
| 3a — Sessions : l'agenda et son import | ⏳ À faire | L'import se construit sans attendre l'accord |
| 3b — Sessions : signalements et notifications | ⏳ À faire | Ouvre le centre de notifications |
| 4 — Réunions de la Francophonie | ⏳ À faire | |
| 5 — Pavillon de la Francophonie | ⏳ À faire | API existante |
| 6 — Échanges | — | Après le MVP ; Capacitor d'abord |
| 7 — Assistant IA | — | Après le MVP |
| 8 — Formations et quiz | — | Après le MVP |
| 9 — Restitutions | — | À confirmer sur le terrain |

## Préalables

| Préalable | État |
|---|---|
| Accord écrit du secrétariat de la CCNUCC | Demande en cours — ne bloque pas |
| Droits d'indexation et de quiz sur les guides | ✅ Acquis |
| Comptes Apple et Google de l'OIF | ✅ Ouverts |
| Experts validateurs | ✅ Désignés et disponibles |
| Vidéos des 16 modules de formation | ✅ Disponibles |

## Points ouverts

- **La réinitialisation générale du site atteint Guide Négo**, relevé le 24/09 à l'essai 1b : la préface de Tailwind, importée par `frontend/app/assets/css/main.css`, pose `* { box-sizing: border-box; margin: 0; padding: 0; border: 0 solid }` sur toute la page, application comprise — contraire à FR-026 de 0a (« aucune règle du site ne doit altérer l'application »). `assets/guide-nego/base.css` reprend `box-sizing: border-box` pour lui-même, ce qui la masquait. Elle décalait la couche de texte de pdf.js ; 1b s'en protège par une règle `content-box` sous `.pdfViewer` ([ADR-022](adr/022-pdfjs-dans-le-client.md)). **Non corrigée à cette étape** : la borner demanderait de sortir Guide Négo de la couche de base de Tailwind, et de revoir tous ses composants, qui comptent sur elle.
- ~~Le périmètre d'administration ne couvre pas les espaces de négociation.~~ **Tranché le 21/09 par le commanditaire** : pour Guide Négo, « son périmètre » veut dire **global**. Seul un administrateur de la plateforme entière tient les codes, les usages, les demandes et le mode d'admission ; la garde des douze routes est `negotiation.space.manage` **sur la portée globale** (`Requires<SpaceManage>`), et **surtout pas `RequiresAnyScope`** — le rôle `admin` porte cette permission et s'attribue aussi sur un événement, si bien qu'un administrateur d'une seule édition franchirait « n'importe quelle portée » alors qu'aucun espace de négociation n'est rattaché à une édition. **Aucune fonction de périmètre nouvelle**, aucun lien espace ↔ événement, aucun semis de rôle : `identity.administered_events()` reste ce qu'elle est. Corrigé dans `contracts/api-admin.md`, `spec.md` (FR-044 et SC-008), `plan.md` et T067, T070, T073. **Construit et éprouvé le 22/09** : les douze routes portent `Requires<SpaceManage>`, et `perimetre_url_forgee.rs` refuse toute garde plus large — y compris en relisant le code des trois fichiers de routes, commentaires retirés.
- **Confier un espace de négociation à une personne qui n'administre pas la plateforme** — un référent de COP, par exemple. Rien n'est construit pour cela, et c'est délibéré. Il faudrait le rôle `space_lead`, `negotiation.space.manage` sur la portée `negotiation_space`, et une fonction sœur d'`administered_events()` qui rende les espaces confiés. **Une spécification à part, si l'IFDD le demande.**
- ~~**`frontend/app/composables/useApi.ts` est à 973 lignes**, le garde-fou est à mille : **vingt-sept lignes de marge**.~~ **Levé le 22/09, phase 1 de 0c** : `auth` vit dans `composables/api/auth.ts`, `useApi.ts` est à 895 lignes, les 14 appelants sont inchangés. La phase 5 n'y a posé que le montage du back-office de Guide Négo (neuf lignes), son code vivant dans `composables/api/admin-negotiations.ts`. Le bloc `auth` y est une constante : c'est le premier candidat à sortir dans `composables/api/auth.ts`, sans rien changer à ses appelants. **Le prochain bloc écrit en ligne franchit la limite** — la phase 7 devra sortir `auth` avant d'y toucher.
- ~~R3 — une seule adresse par écran, et le français quel que soit le téléphone.~~ **Levé le 21/09**, vérifié en T001 au navigateur. La route non localisée s'affiche en `fr`, `/en/guide-nego` rend 404, et le cookie de langue du site reste à sa valeur — `plugins/guide-nego-langue.client.ts` le rétablit après le module i18n, qui le réécrivait sans condition. Consigné dans [research.md § R3](../../specs/008-guide-nego-coquille/research.md).
- Les lignes « En attente » du journal de maquette : des données à fournir, pas du code.
- **Les deux textes qui engagent sont à fournir par l'IFDD** — politique de confidentialité et conditions d'utilisation, `fr` et `en`. Ils se déposent dans `backend/crates/kernel/src/legal/` (`etat: publie`, la date d'entrée en vigueur pour version) ; le mode d'emploi est dans `LISEZMOI.md` du même dossier. Aucun outil ne les écrit.
- **L'hébergeur à nommer dans « À propos »** : le commanditaire le dira avant qu'on l'écrive. D'ici là, l'écran n'en nomme aucun.
- **L'accord du secrétariat de la CCNUCC** sur la reprise du calendrier des sessions : demande en cours. L'étiquette de source n'en parlera qu'une fois l'accord obtenu.
- Écart de clés i18n **préexistant** au site, relevé le 21/09 en comparant les 105 fichiers : `pages/home.json` porte `aside.description` en anglais et pas en français. Sans effet sur Guide Négo, dont les 12 fichiers nouveaux concordent clé pour clé.
- **« Réservé aux négociateurs » sur la page de maintenance du module Négociations du site** (`i18n/locales/fr/pages/maintenance.json`) — la forme du lexique de Guide Négo est « négociatrices et négociateurs ». Hors du périmètre de 0b, donc **non corrigé** : c'est un texte du site.
- **Créer un compte ne consigne aucun consentement**, constaté le 22/09 : aucune ligne dans `identity.consents` après une inscription, alors que le commentaire de `frontend/app/pages/auth/register.vue` dit l'API chargée de l'écrire. Seule l'inscription à une séance qui pose une question sensible en écrit un. Préexistant, hors de Guide Négo : **à trancher par le commanditaire** — consigner l'accord aux conditions à la création du compte, ou corriger le commentaire.
- **La mise en ligne de 0a, 0b et 0c est préparée, pas exécutée** — [§ 15 de DEPLOIEMENT.md](../DEPLOIEMENT.md) : trois migrations dans l'ordre, répétition sur une copie de la production, vérifications du site, puis la recette sur téléphones réels en une séance. Le commanditaire l'exécute.
- **Les objets désignés par une colonne sans être rattachés** — `media.find_orphan_assets()` ne regardait que `media.attachments`. Le registre `media.asset_references` (étape 1) corrige le cas des documents de Guide Négo, **seuls déclarés**. Restent à déclarer, par leur module : `programme.proposal_documents.asset_id` et les colonnes d'objet de `publication`, `training`, `live`, `engagement`, `programme.sessions`. Sans effet visible tant qu'aucun écran ne liste les orphelins ; **avant d'en construire un**, les déclarer.
- Les restitutions et leur partage par cercle : proposés, à confirmer par entretiens.
- **Étape 1 — limites connues de l'extraction**, relevées au contre-examen et laissées : un filigrane ou une bande latérale répétés hors des marges deviennent une figure sur chaque page, et le document passe « tel quel » ; un fond de couleur pleine page dessiné en tracé (et non en image) masque le filet des notes, qui restent dans le texte. Le vrai guide n'a ni l'un ni l'autre.
- **Étape 1 — le jeu d'exemple des documents** ne simule ni les permissions (tout est permis hors ligne, aucun 401 ni 403) ni les images de page et le PDF (chemins relatifs, 404 sans API).
- **Étape 1 — `check-api-contract`** ne compare pas les verbes, ne lit pas une réponse annoncée à deux formes (« DocumentLibrary, ou DocumentTextHits avec q ») et ignore les constructeurs de chemin (image, PDF). Et rien ne vérifie que `backend/.sqlx` est à jour : `cargo sqlx prepare --check` gagnerait à entrer dans `check-back`.
- **Étape 1 — relevés de la recette du back-office (23/09), hors de Guide Négo, non corrigés** : les dates des listes prennent le fuseau du serveur au rendu, puis celui du navigateur (écart d'hydratation, hérité des codes) ; un expert voit aussi les entrées générales du back-office, qui lui répondent « accès refusé ». Les deux autres — l'élision de « heure d'Antalya » et la page rechargée qui passait par la connexion — sont corrigés sur `main`.
- **Étape 1 — relevés de la phase 8, laissés** : « Ajouté » d'un lien ne porte que la date, dans le fuseau du téléphone — la maquette dit « Hier, 22:40 — heure d'Antalya », mais la liste ne sert pas le fuseau de la COP ; une panne de la recherche dans le texte passe pour « aucun trouvé dans le texte », et les documents trouvés par le texte disparaissent le temps d'une frappe ; les spécimens de la planche des documents portent des types en i18n, comme ceux de 0a leurs thématiques ; « Tout se lit sans connexion », sous-titre de Ressources, ne vaut plus pour un document non téléchargé.
- **Étape 1 — `useGnLecture` compte un refus de l'API comme une panne de réseau** (`noterEchec`) : un 401 ou un 403 sur une lecture fait passer toute l'application hors connexion. Préexistant (0a) ; contourné pour les favoris seuls. Et **une route publique ne dit pas qu'un jeton a expiré** : la phase 8 le déduit côté téléphone (`lireEnPersonne`) ; un signal de l'API serait plus sûr.
- **Étape 1 — la recette des réservés de la phase 11 n'est pas faite** (T102, étapes 1, 4 et 5 du [quickstart § 5](../../specs/011-guide-nego-documents/quickstart.md)) : aucun compte de recette n'a l'accès négociateur, et l'ouvrir en base a été refusé à l'agent — c'est une attribution de droit. Il suffit d'un compte admis par un code d'invitation du back-office de 0b ; le document « Résumé réservé — recette » attend en base. Le chemin est couvert sans navigateur depuis la phase 7 (`effacerLesReserves`, la génération des réservés).
- **Étape 1 — relevés de la phase 13, laissés** : un filtre de thématique venu d'une adresse, sur une thématique qu'aucun document ne cite, s'affiche en code brut (« Thématique : gender ») — le vocabulaire servi ne porte que les valeurs citées ; sans API et sans rien de gardé, un navigateur neuf voit « pas encore ouvert », par la règle de 0a (ce qui n'a jamais été lu ouvert reste fermé) ; la recette a trouvé `localhost:8080` pris par un Adminer en IPv6 et le port 3000 par un autre projet — l'API écoute sur `127.0.0.1`, et le navigateur tente `::1` d'abord.
- **Étape 1 — relevés de la phase 12, laissés** : la signature dit « expert IFDD » quel que soit l'expert — la qualité vient du rôle, et le modèle ne porte pas d'accord (« experte ») ; un extrait sélectionné qui commence sur une puce de liste (« • … ») ne se retrouve pas et la note va en tête de page ; sur la fiche d'un document, l'expert voit les thématiques et le choix du PDF actifs, sans bouton d'enregistrement — l'API refuse, mais l'écran ne le dit qu'au refus (hérité de la phase 6).
- **Étape 1 — relevés de la phase 11, laissés** : `/guide-nego` sans barre finale est hors de la portée du service worker (`/guide-nego/`, l'adresse de départ de l'application installée) — tapée dans un navigateur, elle ne s'ouvre pas sans réseau ; « utilisés par Guide Négo » compte toute l'origine, site compris quand ils partagent un domaine ; la place d'une copie dépubliée se rend à la relecture de la liste, pas avant.
- **Étape 1 — relevés de la phase 10, laissés** : la section d'un passage est celle de sa page, pas de sa position — un passage au-dessus d'un titre porte la sous-partie qui s'ouvre après lui ; une recherche de deux lettres très courantes (« de ») surligne des milliers d'occurrences à l'ouverture du premier passage ; la ligne de page de la barre, devenue bouton, fait 32 px comme la maquette, sous les 48 de la règle ; un terme anglais touchable reste une cible en ligne, sous 44 px (exception des cibles dans le texte).
- **Étape 1 — relevés de la phase 9, laissés** : en « tel quel », la place réservée est celle d'une page A4 — une page à l'italienne glisse un peu quand son image arrive ; la recette sur Android et iPhone réels reste à faire, comme pour 0a à 0c.
- **« Au nom de {nom} »** (`guide-nego.demande.json`) n'élide pas devant un nom qui commence par une voyelle (« Au nom de Aïssatou »). Même défaut que « heure de Antalya », sur un nom propre : `zoneElides()` en donne la règle.
- **Étape 1 — deux contre-examens interrompus le 23/09**, pour tenir le délai : la relecture des renforts de tests de la phase 5 (six lots sur sept) et celle des corrections de la lecture PDF (pages tournées, pages qui ne se chargent pas). Les tests passent ; à relancer si l'on veut la même assurance qu'ailleurs.
- `make check-safe` : deux tests Rust de `identity` (`toute_ecriture_laisse_son_auteur`) ont échoué une fois le 21/09 pendant que l'API locale et un serveur de développement tournaient, puis passé seuls et dans la porte complète, API arrêtée. Aucun Rust n'avait été touché. À surveiller : ils semblent sensibles à une activité concurrente sur la base.

- `make check-safe` ne se lance plus qu'à la fin d'un cycle de travail, demandé le 21/09 : la porte complète (typecheck Nuxt, build et tests de tous les crates) coûte plusieurs minutes. Entre-temps : `npm run check:guide-nego`, `test:guide-nego`, `typecheck`.

## Journal

| Date | Fait | À suivre |
|---|---|---|
| 2026-09-24 | **Étape 1b, phases 2 à 6 et 10**, un commit par phase. **2** : `serve_as_is` devient `large_text_choice` (NULL = suit le verdict), `document_reading_modes()` porte la règle ; migration jouée deux fois, schéma comparé, aucun écart dans `negotiation`. **3** : `get_range` dans `kernel`, route `GET`/`HEAD …/file` (206, 416, 304 malgré « -br », `identity`, `no-store` pour un réservé, accès relu à chaque morceau), image publique retirée, `…/large-text`, une seule sérialisation de la lecture pesée par le worker, PDF protégé nommé. **4** (sous-agent, relu) : pdf.js legacy, détection, remplacements page et travailleur, transport de plages, ressources sous leur version, feuille bornée avec `content-box`. **10** avancée (ne dépend que de 3) : `LargeTextChoice` partagé par l'aperçu et la fiche. **5** : copie au format 2 (lecture + PDF), formats anciens effacés, règle 6 — à version égale, seule la lecture se relit. **6** : `GnLecteurPages`, `GnLecteurTexte`, attente en ligne, bascule, pages non reçues. **Défauts trouvés en vérifiant, corrigés** : un `HEAD` rendait `Content-Length: 0` (actix recalcule la longueur) — un corps vide dimensionné ; le visionneur mesuré sous l'écran d'attente laissait 125 px de vide ; les 3 s comptaient depuis la lecture et non depuis l'ouverture ; la jauge restait à 0 pendant un morceau. **Défaut antérieur corrigé** : la base modèle des tests recevait le semis de développement (`NEGO-001`) — le harnais le coupe. **Mesuré sur la version construite** (Chromium, profil Android) : première page 1,9 s, `206` seulement ; 3G lente derrière un relais qui compresse comme en production : texte lisible à 3,7 s, page à 30,6 s, aucune bascule ; mode avion : page 61 en 231 ms avec sa reprise ; copie = cache à l'octet près ; back-office au navigateur. **Incident** : le sous-agent de la phase 4 a arrêté par `pkill` le serveur de recette de l'étape 1 (port 3001) ; non relancé, faute d'accord. Et l'API de recette des ports 8080/8081 tourne sur l'ancien binaire face à une base migrée : ses routes de documents lisent `serve_as_is` et échouent | Relancer au besoin la recette 3001 (commande dans le compte rendu), et redémarrer les API 8080/8081 sur le nouveau binaire. **Phase 7** : repérer un passage sur la page (deux temps, page ±1, cas de l'essai en tests). Relancer l'extraction du guide pour recalculer `reading_bytes` (3,8 Mo annoncés, 3,19 Mo réels). FR-029 au navigateur avec un compte négociateur |
| 2026-09-24 | **Étape 1b, phase 1 close : issue A retenue par le commanditaire**, avec quatre décisions, consignées dans [ADR-022](adr/022-pdfjs-dans-le-client.md) (ADR-021 perd sa règle « le PDF ne va jamais sur le téléphone »). (1) Réseau très lent : ~25 s pour la première page du guide, **accepté mais visible** — jauge, puis au bout de 3 s « Lire le texte en attendant » et « Télécharger pour lire sans réseau » (FR-009 bis) ; la bascule ne compte que le travail de pdf.js, jamais le réseau (FR-012 bis) ; SC-003 réécrit (texte lisible en moins de 5 s). (2) iOS 16.4 : `Promise.withResolvers` remplacé dans la page et dans le travailleur. (3) Polices standard toutes gardées (+0,35 Mo). (4) Les trois défauts de l'essai au plan. **Vérifié avant d'écrire** : un transport de plages écrit ici (`PDFDataRangeTransport`) marche sur le vrai guide, aux mêmes temps, et dit ce qui est demandé et reçu — il porte la jauge et le délai. **La règle qui décalait pdf.js** : la préface de Tailwind du site, reprise par `base.css` de Guide Négo — point ouvert, non corrigé. Spec, plan, recherche (R15), contrat du lecteur, quickstart et tâches mis à jour ; le prototype reste dans `.essais/` comme banc | Phase 2 (le modèle), puis 3 à 6, un commit par phase, arrêt au point de contrôle de la phase 6 |
| 2026-09-24 | **Étape 1b, phase 1 : l'essai mesuré (T004 à T008)**, Chromium 153 en profil Android bridé ×4 et WebKit 26.6, sur la version construite dans une copie du front ; [grille](../../specs/012-guide-nego-lecteur-pdf/essai-lecteur.md). **Tient** : défilement des 90 pages sans tâche de plus de 100 ms, page visible 414 ms au plus après l'arrêt, pages à tableau 50 à 64 en 147 à 490 ms ; mémoire identique aux trois allers-retours (11 canevas, 9,9 Mpx, tas 10 Mo en haut) ; ×4 net en 365 ms (p. 59) et 297 ms (p. 55) ; copie de la p. 59 dans l'ordre, sans mélange de colonnes ; **hors connexion, serveurs arrêtés, les trois façons de charger le travailleur passent sous les deux moteurs**, XHR synchrone compris — pas d'issue B ; repérage **50/50** et notes **10/10**, en deux temps. **Ne tient pas** : la première page en 3G lente, **23 à 28 s** contre 8 — la couverture est une photo de 396 Ko, 8 s à elle seule à ce débit ; la linéarisation aggrave (47 s). **Trois défauts trouvés pour le plan** : pdf.js lit tout le fichier en ligne sauf `disableStream` et `disableAutoFetch` (2,8 Mo avant la première page) ; le `border-box` du site décale la couche de texte d'une ligne ou plus — `content-box` sous `.pdfViewer` ; pdf.js 6 ne publie plus la couche de texte, le repérage passe par le DOM. **Plancher iOS déduit, non mesuré** : blocs `static {}` → iOS 16.4 ; `Promise.withResolvers` à émuler dans la page **et** dans le travailleur, sinon iOS 17.4. Coquille : +1,3 Mo compressé, 0,95 sans les polices Liberation. **Limites** : le bridage n'atteint pas le travailleur (borne pessimiste mesurée à part, attente 510 ms) ; le hors-ligne de Playwright pour WebKit coupe même le service worker, d'où les serveurs arrêtés | **Relecture par le commanditaire** : l'issue A, et une décision — accepter ~25 s pour la première page de ce guide en 3G lente (la copie gardée reste le remède), ou autre chose. Le délai de 20 s en ligne de la seconde sécurité est à revoir : il basculerait vers « Texte agrandi » avant l'arrivée de la page. Puis T009 (ADR-022) et T010. T001 (Xcode) et T084 (iPhone, Android) par le commanditaire |
| 2026-09-24 | **Étape 1b, phase 1 commencée, puis arrêtée à la demande** (l'implémentation se fera dans une session neuve). **T002** : le guide n'a que des polices incorporées, aucun cMap ni `wasm` utile, mais **28 273 images minuscules** — les pointillés des tableaux, 1 300 à 2 400 par page sur les pages 50 à 64 : le cas le plus dur pour le rendu. **T003** : `pdfjs-dist` 6.3.289, version exacte. **Constats** : la garde de la coquille retient déjà module, visionneur et travailleur de pdf.js ; à `page-width` la page ne fait que ~300 px sur 360 — le composant calera l'échelle ; les `U+0002` de PDFium sont ici les traits d'union de mots composés coupés en fin de ligne, que la normalisation retire avec le blanc qui suit. Première page à 3,0 s sous bridage (processeur ×4, 10 Mbit/s), hors banc. **Le banc** (`banc.mjs`, Playwright 1.63, Chromium et WebKit) et le prototype sont écrits mais **n'ont rien mesuré de bout en bout** ; tout est dans `.essais/lecteur-1b/LISEZMOI.md`, avec la façon de reconstruire dans une copie du front sans toucher la recette de l'étape 1 sur le port 3001 | Reprendre la phase 1 : T004 à T008 par le banc, puis rendre le résultat de l'essai au commanditaire **avant** T009 (ADR-022). T001 (Xcode) par le commanditaire |
| 2026-09-24 | **Étape 1b : tâches et analyse.** Plan relu par le commanditaire : pdf.js et son visionneur, route unique du PDF, feuille bornée, essai d'abord — d'accord. **Tranché** : un téléphone qui n'affiche pas les pages bascule sur « Texte agrandi », avec une ligne qui le dit, jamais le visionneur du téléphone ; la bascule se décide par détection des fonctions, **et par une seconde sécurité sans mesure** — erreur de pdf.js ou première page non dessinée en 8 s sur une copie (20 s en ligne), comptée sur le téléphone, réessayée à l'ouverture suivante. **Ajouté au plan** : la normalisation des deux textes (césure `U+0002`, ligatures, insécables, apostrophes, fragments) par une fonction pure testée, et le taux de passages retrouvés mesuré à l'essai ; `Content-Encoding: identity` et `no-transform` sur la route du PDF, et **la vérification 4 du § 11 de DEPLOIEMENT.md** — un `206` sans compression à travers le relais, qui compresse (mesuré le 22/09). `/speckit-tasks` : 84 tâches en onze phases. `/speckit-analyze` : aucun critique, neuf constats appliqués — dont les images de page gardées pour l'aperçu seul, la lecture toujours dans la copie, et la feuille bornée engendrée par le contrôle lui-même. **Xcode n'est pas installé** : pas de simulateur iOS sur le poste (T001, par le commanditaire, ne bloque rien) | Phase 1 : l'essai sur le vrai guide, puis relecture avant l'ADR-022 |
| 2026-09-24 | **Étape 1b planifiée** — [plan](../../specs/012-guide-nego-lecteur-pdf/plan.md), onze phases. Trois états des lieux menés en parallèle : le back, le front, et `pdfjs-dist` 6.3.289 lu dans son archive. **Décisions** : pdf.js en build `legacy` avec son visionneur `PDFViewer` — virtualisation, plafond de canevas d'iOS, redessin net de la zone visible déjà faits (R1) ; **une route de l'API sert le PDF par plages**, public comme réservé, l'accès vérifié à chaque morceau — tous les PDF sont dans le bucket privé, et rien ne lisait de plage jusqu'ici, d'où `get_range` dans `kernel::storage` (R3) ; pdf.js gardé par la coquille, `wasm` et polices copiés par un module, feuille du visionneur copiée **bornée** à `[data-app="guide-nego"]` — environ 1,1 Mo compressé de plus (R4) ; copie de format 2, PDF et lecture, sans images (R5) ; passage repéré par la couche de texte de pdf.js, sans positions en base (R7) ; `serve_as_is` devient `large_text_choice` (NULL = suit le verdict), et une fonction SQL porte la règle (R11) ; les images de PDFium restent pour l'aperçu du back-office seul. **Un point ouvert pour le commanditaire** : pdf.js 6 exige iOS 18 — un iPhone en iOS 16 ou 17 n'affiche pas les pages ; le plan lui ouvre « Texte agrandi » quand le document l'offre | Relecture du plan par le commanditaire, dont le plancher iOS 18 ; puis `/speckit-tasks`. La phase 1 est l'essai sur le vrai guide : rien ne s'écrit avant |
| 2026-09-24 | **Étape 1b spécifiée** — [spec](../../specs/012-guide-nego-lecteur-pdf/spec.md) sur la branche `012-guide-nego-lecteur-pdf`, partie de `011-guide-nego-documents`. Six récits : lire les pages (P1), trouver un passage et y aller (P1), « Texte agrandi » (P2), la copie gardée — PDF et texte, sans image de page (P2), la note en marge (P3), le back-office (P3). Un tableau dit ce qui change dans la spec 011. **Relue par le commanditaire le même jour, trois hypothèses tranchées** : « Texte agrandi » remplace « ouvrir tel quel » — proposé par défaut si l'extraction a bien recomposé le texte, retiré sinon, changeable sans republier, la recherche et le sommaire restant toujours ; le choix « Pages · Texte » se voit, à la place de « Marquer » dans la barre dépliée et dans « Réglages », et une ligne l'annonce une fois sur écran étroit ; « Aa » n'ouvre que le lexique, donc le pictogramme de « Réglages » (deux A) change — écarts 43 et 44 inscrits dans 05-design.md ; la copie gardée porte un numéro de format, et une copie d'un format ancien s'efface. **La règle des clés d'API** (`CLAUDE.md`, `.env.example`, `.env.prod.example`) est commitée seule sur `main`, puis `main` fusionnée dans 012. Copie gardée du guide : environ 3,2 Mo, contre 3,75 Mo à l'étape 1. Les copies d'essai de l'étape 1 s'effacent (FR-031) | `/speckit-plan` : le moyen de dessiner les pages et l'ADR qui remplace la règle d'ADR-021, la mémoire, le service du PDF par plages, les positions du texte, le back-office |
| 2026-09-24 | **Le lecteur montrera le PDF d'origine** — décidé par le commanditaire après avoir lu le texte recomposé de l'étape 1. Le texte extrait reste pour la recherche, le sommaire, un mode « Texte agrandi », puis l'assistant et le lexique. Prompt de l'étape 1b dans la feuille de route. Clé OpenRouter posée dans `.env` (`OPENROUTER_API_KEY`), et la règle des clés inscrite dans `CLAUDE.md` | `/speckit-specify` de 1b, sur une branche partie de `011-guide-nego-documents` |
| 2026-09-24 | **Étape 1, phase 13 — la recette et les finitions** (T108 à T115 ; T116 reste). **La planche** gagne la note de correction, repliée, dépliée et en tête de page : les huit composants nouveaux y sont. **L'anglais relu** par un sous-agent, puis corrigé : les 31 paires concordent clé pour clé, et **77 corrections** dans 15 fichiers — surtout le calque de « lire » au sens de charger (« The library could not be read » devient « …loaded »), « offline » partout pour « sans réseau », des pluriels (« No documents »), deux phrases agrammaticales et un faux ami (« charter » pour charte graphique). **SC-011** : aucun libellé de type, de thématique ni de COP dans le code ni dans les écrans ; seules les planches en portent, comme spécimens. **Tailles** : 30 mesures sans débordement — bibliothèque, deux fiches, « Mes documents », lecteur, à 320, 360 et 390 px, clair et sombre —, ni en « Très grande » à 320 px ; états vide, erreur et accès refusé vus. **Le site** : une vignette déposée par `ImageField` part en 201 dans le bucket public et s'affiche par le relais, puis « Annuler » ne rattache rien ; les médias de l'accueil et de l'édition se chargent. **Recette** : § 2 sécurité rejouée (403, recherche muette sur le réservé, relais 404 sur son PDF), § 3 hors réseau (reprise à la page 59, recherche sans accent, 4 passages dans 90 pages). **DEPLOIEMENT.md § 15** porte l'étape 1 : quatrième migration, `ops/init-garage-prod.sh` pour le bucket privé avant de migrer, PDFium venu de l'image — **surtout pas `PDFIUM_LIB_PATH` dans `.env.prod`**, qui l'emporterait —, la dette de R4 à zéro, les lignes semées, les contrôles du site et le guide lu en mode avion après une nuit. `make check-safe`, API arrêtée : **au vert**, 1 306 tests Rust. | T102 : un compte admis par code d'invitation, puis les étapes 1, 4 et 5 du § 5. T116 et la mise en ligne : § 15 de DEPLOIEMENT.md, par le commanditaire. Ensuite, l'étape 2 — FAQ et lexique |
| 2026-09-24 | **Étape 1, phase 12 — la note de correction d'un expert** (T103 à T107). **Au back-office**, l'aperçu gagne `components/admin/negotiation/PreviewNotes.vue`, sous la forme lisible : la page est celle qu'on feuillette, l'extrait est ce qu'on **sélectionne** dans le texte (280 signes au plus, coupés à un mot : un début d'extrait suffit à l'ancrer), le texte `fr` est exigé et `en` facultatif ; les notes de la page puis celles des autres pages, vivantes et **retirées**, avec auteur et date de pose et de retrait, le retrait par confirmation. Boutons selon `can_post` et `can_withdraw`. **Dans l'application**, `GnNoteCorrection` (filet de 3 px dans la marge, triangle rouge, ligne de 48 px, boîte signée « Nom, expert IFDD — date » au bouclier vert) ; `useGnDocuments` relit les notes **après** la liste, par leur empreinte, sous la clé `corrections`, et masque celles d'un réservé gardées pour une autre personne ; `ancrerLesNotes` (`forme-lisible.ts`) pose chaque note sur le bloc qui contient son extrait, sinon sur la première ligne d'un extrait à cheval, sinon en tête de page — « tel quel », toujours en tête. `GnPageLue` enveloppe chaque bloc d'un `div` en `display: contents`, ou de la note quand il en porte une : le texte n'est pas touché. Le repli sans accents sort de `lecteur.ts` dans `repli.ts`, pour que l'ancrage le partage sans import circulaire. Quatre tests d'ancrage ; 224 tests, `check:guide-nego`, `typecheck` et `check-api-contract` au vert. **Recette § 4 déroulée sur la version construite** : le guide téléchargé d'abord, la note posée par l'expert à la page 59 paraît au lecteur sur le bon paragraphe, reste en mode avion, se déplie avec sa signature ; retirée, elle disparaît au retour du réseau et ne revient pas hors connexion ; le back-office la garde « Retirée », avec qui et quand. **Conditions de cette recette** : le port 3000 était pris par un autre projet et `localhost:8080` répondait par un Adminer en IPv6 — seconde API sur `127.0.0.1:8081` avec `APP_PUBLIC_URL=http://localhost:3001`, site construit servi sur 3001. Le mot de passe de `recette.docs.expert@example.org` a été reposé par « mot de passe oublié » et Mailpit. | Phase 13 : la recette et les finitions — planche des huit composants (dont `GnNoteCorrection`), traductions `en`, SC-011, quatre états et trois largeurs, non-régression du site, `make check-safe` API arrêtée |
| 2026-09-23 | **Étape 1, phase 11 — « Mes documents », la place et ce qui s'efface** (T096 à T101 ; T102 en partie). **« Mes documents »** remplace « Mes téléchargements » de 0c, renommée `ressources/mes-documents.vue` pour porter le nom de sa traduction : en tête « n téléchargé(s) · n favori(s) », « Sur le téléphone » avec la place de chaque copie et sa date, `GnJauge`, la ligne de persistance refusée, « Favoris » avec leur rappel et, sans compte, l'invitation ; « Tout retirer du téléphone » par `GnConfirmation` en rouge assombri, qui nomme les documents et leur place, dit les téléchargements demandés qu'il annule, et annonce son échec au lieu d'un faux succès. `utils/guide-nego/mes-documents.ts`, testé. On y arrive du profil (« 1 document · 3,8 Mo »), des Ressources (ligne nouvelle) et de la fiche quand la place manque. **« Ma journée »** montre les derniers ouverts sur le téléphone avec leur dernière page lue (« lu à 23:45, p. 8 ») ; vide sur un appareil neuf, un réservé fermé à la personne n'y paraît pas — sur un téléphone partagé, il dirait ce qu'une autre a lu. **À propos** : les téléchargements restent sur le téléphone, thématiques, favoris et accords suivent le compte. `GnLigneDocument` gagne `type` facultatif, `morceaux`, `octets` et `precision` au lieu d'une copie par écran. **Trouvé en recette et corrigé — SC-005 était faux sur la version construite** : après « Tout retirer », Chrome ne rend la place d'un cache **supprimé** qu'au déchargement de la page ; celle d'une **entrée** supprimée, aussitôt (mesuré à part). Le retrait vide donc les entrées avant le cache, et `copieIntacte` teste l'existence par `cache.keys(cle)` au lieu de lire des réponses qu'il ne consommait pas et qui retenaient leurs octets. Mesuré : 6,8 Mo → 3,0 Mo à l'instant, en mode avion. **Relecture adversariale**, quatorze constats, douze corrigés : un faux « aucun favori » hors réseau ou pour une autre personne (`connus` dans `useGnFavoris`, la file des choix rangée par personne) ; un en-tête de favoris annoncé avant leur lecture ; « Tout retirer » qui annulait sans le dire les téléchargements demandés ; l'erreur « bibliothèque pas lue » affichée sans copie, et fausse en ligne ; un succès annoncé sur un échec, toujours au pluriel ; le profil et la page qui ne comptaient pas les mêmes copies (les copies dépubliées pas encore rapprochées se comptent et se disent) ; un téléchargement relancé pendant le retrait qui réapparaissait (génération, comme pour les réservés) ; le squelette qui attendait le compte ; le retour qui ne ramenait pas au profil ; les récents d'un réservé ; l'état vide de « Ma journée » qui clignotait ; l'éditeur seul dans les récents, comme la maquette. **La jauge ne promet plus la place libre du téléphone** : le navigateur ne dit que ce qu'il accorde encore — « 3,8 Mo utilisés par Guide Négo · 4,3 Go encore disponibles ». **Recette** au navigateur sans interface (Playwright, IPv4 forcé : `localhost:8080` en IPv6 tombe sur l'adminer d'un autre projet) : sans compte, télécharger, « Mes documents », tout retirer ; connectée (`recette.0c.phase6`), deux favoris dont **un posé hors réseau, enregistré une fois** au retour (une réponse 204), **les mêmes sur un second appareil** ; 320, 360 et 390 px dans les deux thèmes sans défilement horizontal ; **sur la version construite** : l'API coupée n'efface rien, une copie dépubliée s'efface à la relecture, « Ma journée » et « Mes documents » en mode avion, application fermée puis rouverte. **Pas déroulé** : la copie réservée, la déconnexion qui l'efface, l'accès retiré — voir les points ouverts. **Donnée de recette laissée en base locale** : « Résumé réservé — recette » (`recette-resume-reserve`), copie réservée du guide qui partage son fichier et ses pages. **Contrôles** : `test:guide-nego` (220), `test:site`, typecheck, `check:guide-nego`, `check-api-contract` ; `make check-safe` attend la fin du cycle | Phase 12 : la note de correction ; la recette des réservés quand un compte a l'accès |
| 2026-09-23 | **Étape 1, phase 10 — lire confortablement** (T090 à T095). `utils/guide-nego/lecteur.ts` : la recherche dans le document, sans accents ni casse, sur un index replié une fois par document ; chaque position renvoie au texte d'origine, qu'on surligne lettre pour lettre ; NFKD (« co2 » trouve « CO₂ »), tirets et blancs ramenés ; deux lettres au moins. **Cent pages en 19 ms** (SC-009) ; le vrai guide : « progres collectifs », 4 passages. Le lecteur ouvre depuis sa barre : **le sommaire** (`GnLigneSommaire`, `GnLecteurSommaire`, dans `GnPanneauLecteur`) — chapitre en cours déplié, section en cours marquée, saut de page ; **la recherche** (`GnLecteurRecherche`) — « n passages dans N pages — sans réseau », page, section, extrait, « vous êtes ici », 200 passages listés au plus ; **les occurrences** (`GnOccurrence`) — surlignage plein de la courante, clair des autres, d'un bout à l'autre ; **les réglages** (`GnReglagesLecture`) — taille 17/20/24 par `GnChoixTailleLecture`, thème **par `useGnTheme` de 0a**, et la taille ajoutée au profil pour que « Aussi dans votre profil » soit vrai ; **le terme touché** — feuille titrée du terme, **sans traduction ni définition**, avec les pages où il paraît. `GnSegmentsLus` rend les segments ; `GnFeuilleBasse` gagne `fermeture`, `GnEntete` `retourBouton`. **Relecture adversariale**, dix constats, corrigés : la barre était inutilisable au clavier (la ligne de page devient le bouton qui la déplie et donne le focus à la première action ; la barre ne se replie plus sous un focus) ; une recherche coupait un terme anglais en deux boutons et la feuille s'ouvrait sur un morceau ; après un saut la barre disait la page précédente — l'observateur suit désormais **une ligne au tiers de l'écran**, et un saut (`sauter`) pose la page atteinte sans voir défiler les voisines ; le bandeau d'occurrence couvrait la barre dépliée ; une lettre seule figeait le téléphone ; un tableau refermé cachait l'occurrence suivante ; le focus se perdait après un saut ; une frappe rapide surlignait un passage d'une autre liste ; `CO₂`, les tirets et les doubles blancs ne se trouvaient pas ; deux doublons retirés (les tailles, l'en-tête du panneau). **Vérifié au navigateur** : 320, 360 et 390 px × deux thèmes × trois tailles, page, sommaire et recherche, **sans défilement horizontal** (T095) ; saut de chapitre, taille qui garde la page, feuille du terme, clavier. **Contrôles** : `test:guide-nego` (213), `test:site`, typecheck, `check:guide-nego`, `check-api-contract` ; `make check-safe` attend la fin du cycle | Phase 11 : « Mes documents » — favoris, copies, tout retirer, la persistance refusée dite |
| 2026-09-23 | **Étape 1, phase 9 — le lecteur, le critère de sortie** (T082 à T089 ; T088 déjà fait en phase 8). `utils/guide-nego/forme-lisible.ts` : grammaire close — un `kind` inconnu ou un segment mal formé s'ignore, `term` implique l'italique, notes en fin de page, la section du pied (dernière entrée de niveau 1 ou 2 : « 3.6. Adaptation » et non « 3.6.1 »), la reprise ; les 1 009 blocs et le texte entier du vrai guide passent sans perte. `useGnLecteur` : la copie **entière** d'abord, le réseau ensuite ; images gardées (adresses locales) ou lues par l'API ; page en cours par observateur d'intersection ; note au plus toutes les deux secondes, par version. `lire.vue` : en-tête d'une ligne (`GnEntete` gagne `compact`), pages recomposées, « Voir la page d'origine », mode « tel quel » en images empilées, reprise « Reprise à la page 59 — lue à … » et « Début », état « Ce document n'est pas sur votre téléphone » avec « Télécharger au retour du réseau » et un document lisible maintenant. **Composants** : `GnBarreLecture` (repliée 32 + 6 px, dépliée 106 ; sans « Marquer », écart 42 ; **Sommaire, Rechercher, Réglages n'y paraissent qu'en phase 10**, qui les branche), `GnProgression` (les deux barres faites main en phase 8 y passent), `GnPageLue`, `GnImageDePage` — tous sur la planche. **Trouvé au navigateur** : la reprise se perdait — l'observateur voyait la page 1 avant le recalage, la police chargée recomposait le texte (1 403 px de glissement), le routeur remontait en haut, et quitter le lecteur notait la page 1. Corrigé : `scrollToTop: false`, recalage après `document.fonts.ready`, suivi commencé après recalage et arrêté en quittant, ligne de reprise effacée au seul geste de la personne. **Relecture adversariale**, quatorze constats, tous corrigés : deux tableaux d'une page s'ouvraient ensemble ; la dernière page n'était jamais atteinte ; ouvrir réécrivait l'heure de la reprise ; l'image d'un réservé lue au réseau tombait en 403 après expiration du jeton, avec un message « avec le réseau » en ligne (elle passe par l'API avec rotation, et l'échec se dit, avec « Réessayer ») ; en « tel quel », faux message pendant la lecture du cache et pages qui grandissaient (place réservée au format A4, image demandée à l'approche) ; une rotation d'écran perdait la page ; un lien ou un réservé sans accès, hors connexion, recevait une promesse de téléchargement ; le retour du réseau n'ouvrait pas le document ; un réservé ouvert ne se fermait pas à la perte d'accès ; une copie illisible restait « Téléchargé » (validée, sinon retirée — `estUneFormeLisible`) ; la dernière page se perdait à la mise en arrière-plan ; la ligne de reprise cachait la fin du texte ; la reprise d'une copie gardée s'effaçait si la liste nommait une autre version. Repères de page rendus invisibles (R14). **Recette § 3 sur la version construite** (navigateur sans interface, sans compte) : copie du guide **3,77 Mo** en 20 entrées (forme lisible et 19 images) ; lu jusqu'à la page 59, hors connexion, fermé puis rouvert : « Hors connexion — lu à 20:23 », « 1 lisible maintenant », « Lien externe — réseau nécessaire » ; « Reprise à la page 59 », pied « Page 59 sur 90 · 3.6. Adaptation » ; image de page lue dans le cache ; étape 8 avec le guide, sa copie retirée hors connexion : « Ce document n'est pas sur votre téléphone », demande faite, application fermée, **parti une fois** au retour du réseau (compteur 7 → 8). `navigator.storage.persist()` refusé par Chrome sans interface, comme attendu. **Contrôles** : `test:guide-nego` (205), `test:site`, typecheck, `check:guide-nego`, `check-api-contract` ; `make check-safe` attend la fin du cycle | Phase 10 : sommaire, recherche, réglages, terme — et les brancher dans la barre ; un moyen de la déplier au clavier |
| 2026-09-23 | **Étape 1, phase 8 — la bibliothèque et la fiche** (T073 à T081, et T097 avancée). `utils/guide-nego/documents.ts` : filtres combinés, comptes par valeur, « Afficher n documents », recherche sans accents sur titre, résumé et éditeur, marques d'une ligne. `useGnDocuments` relit la liste avec son empreinte (`304` : la liste reste) et rapproche les copies à chaque réponse. **Liste** : recherche, trois filtres en feuille basse, filtres et recherche dans l'adresse, trouvé dans le texte avec sa page, hors connexion « n lisible(s) maintenant ». **Fiche** (`[id]/index.vue`, et non `[id].vue` : `lire.vue` vient à côté) : les six états, bandeau « Remplacé par » vers le bout de la chaîne, verrou de 0b sur un réservé, Favori, Partager, téléchargement sans compte et ses issues. **Composants** : `GnLigneDocument`, `GnFeuilleFiltre`, `GnBandeauRemplace`, sur la planche. **Favoris avancés de la phase 11** (T097) : sans eux, « Favori » n'aurait rien fait ; la file de 0c sert une famille de clés (`favori-<id>`, `inscrireFamille`). Ressources relie la bibliothèque ; l'ouverture dit « à lire sans réseau » (écart 41). **Tranché faute de maquette** : « À jour » se tait devant « Nouveau », et hors connexion ne reste que sur un téléchargé ; « Remplacé par » au masculin, le genre du type n'étant pas connu ; le vide après filtre dit « Aucun document pour Bulletin · Genre » — « Aucun bulletin sur le genre » demande une grammaire que le vocabulaire ne porte pas ; « Nouveau » se perd à l'ouverture de la fiche sans entrer dans les récents ; « Retirer du téléphone » passe par une confirmation. **Relecture adversariale** : un défaut critique — le jeton d'accès vit un quart d'heure et la liste, publique, ne rend pas de 401 sans lui : passé ce délai elle se lisait anonyme et **effaçait les réservés d'une personne qui a l'accès**. Corrigé par `lireEnPersonne` : une liste qui montre fermé un réservé alors que ce téléphone sait la personne connectée et son accès ouvert fait tourner le jeton et se relit ; une rotation sans réponse n'applique rien ; même règle pour le 403 d'un téléchargement réservé. Et sept autres, corrigés : la liste gardée d'une personne déconnectée montrait ses réservés à la suivante (la garde porte désormais la personne, `pourUneAutrePersonne`) ; un choix remplacé pendant son envoi était perdu par la file de 0c (elle le garde et repart aussitôt — `file-course.test.ts`) ; une lecture des favoris partie avant l'envoi éteignait l'étoile (ce qui s'affiche = lu + file ; `useGnLecture.relire` attend la lecture en vol) ; un refus de l'API sur les favoris passait pour une panne de réseau ; un téléchargement en attente ne partait pas quand une lecture réussie, sans `online`, prouvait le retour du réseau ; la flèche de la fiche perdait les filtres ; un terme désactivé s'affichait par son code. Le bandeau hors connexion prend la phrase de l'écran (`ceQuiSeLit`) : « Seuls les documents téléchargés s'ouvrent » ; l'action du message éphémère devient un jeton, `--gn-action-sur-titre` — son jaune tombait sur un aplat clair en thème sombre (défaut de 0a). **Vérifié au navigateur** à 360 px, deux thèmes : les six fiches, filtres et recherche, retour qui garde la recherche, téléchargement réel puis retrait, demande hors connexion partie au retour du réseau. **Contrôles** : `test:guide-nego` (197), `test:site`, typecheck, `check:guide-nego` (56 paires), `check-api-contract` ; `make check-safe` attend la fin du cycle | Phase 9 : le lecteur — `lireLaCopie`, `imageDeLaCopie`, jamais de page blanche ; « Lire » de la fiche mène déjà à `[id]/lire` |
| 2026-09-23 | **Étape 1, phase 7 — la plomberie hors connexion** (T063 à T072, et T072 bis). `lireEtiquete` sait `If-None-Match` et rend « inchangé » sur un `304`, appelants de 0c inchangés — les appels étiquetés sortent de `http.ts` dans `api/etiquete.ts`, testable sans Nuxt. La garde `guide-nego` monte en **v3** (`copies`, `a-telecharger`). `utils/guide-nego/copies.ts` : deux caches `gn-documents-publics` et `-reserves`, complet ou rien, rapprochement avec la liste (dépublié, devenu réservé, autre empreinte), effacements. `useGnCopies` : téléchargement en flux avec progression, rien d'écrit avant le dernier octet, annulation, compteur une fois ; file des demandes sans réseau sur les déclencheurs de 0c ; déconnexion, session finie ailleurs et accès perdu effacent les réservés, **jamais sur une API muette** (`effacements.ts`). **Deux exigences du commanditaire** : `navigator.storage.persist()` au premier téléchargement, redemandé tant qu'il est refusé ; **à chaque ouverture**, les copies se vérifient contre les caches — une copie vidée par le navigateur redevient « non téléchargée ». **Relecture adversariale** : un défaut critique — en production la base de l'API est relative (`/v2/api`) et Cache Storage rend des clés absolues : la vérification à l'ouverture aurait effacé **toutes** les copies ; corrigé par `cleDeCopie`, testé sur base relative. Et neuf autres, corrigés : une IndexedDB bloquée lue comme vide faisait tout effacer (la garde distingue désormais « illisible » de « vide ») ; une écriture de fiche ratée passait pour réussie (jugée à la validation de la transaction) ; une demande réservée partait après la déconnexion (relue avant chaque départ, et génération des réservés sous le verrou) ; une panne de rotation ou un 429 retirait une demande ; l'annulation ne la retirait pas ; un document devenu réservé partait du côté public (la file relit la liste avant de partir) ; des connexions IndexedDB ne se fermaient jamais ; un effacement raté faisait tomber la lecture du compte ; la progression pouvait passer 100 %. Clés locales préfixées `gn.`, comme `stockage.ts`. **Vérifié au navigateur** : base en v3, et depuis l'origine du site `Content-Length` et `ETag` lisibles, flux, clés absolues. **Contrôles** : `test:guide-nego` (180), `test:site`, typecheck, `check:guide-nego`, `check-api-contract` | Phase 8 : la bibliothèque et la fiche — brancher `relireLaBibliotheque` et `rapprocher` |
| 2026-09-23 | **Trois correctifs demandés par le commanditaire après la phase 6.** Sur `main`, un commit chacun : **« heure d'Antalya »** (`d50c9a1`) — chaque formule de fuseau a sa jumelle élidée, choisie dans `useDateTime`, seul endroit qui compose désormais l'étiquette ; dix composants écrivaient « heure de » en direct, dont les pages publiques de l'appel, et cinq phrases d'écran le portaient en dur ; test sur la phrase rendue (`npm run test:site`, branché sur `check-front`). **La page rechargée ne passe plus par la connexion** (`514145b`, suite de e78b57d) — la garde ne renvoie qu'une session finie ; au serveur, une page réservée sans session tranchée se rend en chargement et le navigateur la rend après la rotation (rendue sans personne, elle affichait « accès refusé ») ; « Continuer » suit `?redirect=`. Vérifié au navigateur : jeton invalide puis rechargement, session finie, « Continuer ». `main` fusionné dans 011. **Dans 011** : le refus « déjà remplacé » cite la **version** du remplaçant, plus son titre ; `creer()` et `modifier()` perdent la langue qui ne servait qu'à ce message. Tests `negotiation` (47) et HTTP (19) au vert. **Incident** : disque plein à 4 Go, Docker figé — 54 Go de cache incrémental de `backend/target` vidés ; Docker redémarré par le commanditaire, base intacte | Phase 7 : `navigator.storage.persist()` au premier téléchargement, et les copies vérifiées à chaque ouverture |
| 2026-09-23 | **Étape 1, phase 6 — le back-office des documents** (T056 à T062). Liste, création, fiche, aperçu page par page, entrée « Documents » au menu. Trois écrans écrits en parallèle, puis une relecture adversariale : douze constats, tous corrigés — la fiche d'un document B pouvait afficher A après « nouvelle version » (clé de données fixe), la relecture de l'extraction s'arrêtait au premier échec en affirmant tourner, un lien enregistré laissait déposer un PDF voué au refus, une grille de thématiques refaisait `ThemePicker`, **généralisé** en `AdminThemePicker` (vitrine et pistes inchangées). **Réutilisé** : `MediaFileField` (prop `removable` : aucune route ne détache un PDF), `useDepotMedia`, `UiSwitch`. **Ajouté** : `useApiUrl()` pour les images et le PDF, une seule table des couleurs d'état, le jeton `--admin-header-height` (la barre de l'aperçu se collait sous la hauteur de la barre du site), les huit types du modèle au jeu d'exemple (il en avait cinq). **Le menu** porte la garde par entrée : un expert ne voit de Guide Négo que « Documents ». **Contrat corrigé** : pas de `role=source` au dépôt, l'énumération n'en a pas. **Recette § 1 sur le vrai guide** : dépôt → « prête » en 35 s ; 89 tournes de page en 4 s, image comprise ; refus attendus, messages tels quels ; administrateur d'une édition refusé sur les douze routes, expert en lecture ([quickstart § 1](../../specs/011-guide-nego-documents/quickstart.md)). **Contrôles** : typecheck, `test:guide-nego` (138), `check:guide-nego`, `check-api-contract` ; `make check-safe` attend la fin du cycle. Comptes de recette `recette.docs.{admin,expert,evenement}@example.org`, rôles posés en base ; le guide, le Bulletin et un brouillon de nouvelle version restent en base pour les phases suivantes | Phase 7 : la plomberie hors connexion |
| 2026-09-23 | **Étape 1, phase 5 — l'API** (T038 à T055). Treize codes d'erreur, cinq contraintes traduites, trois permissions ; **8 routes publiques** — bibliothèque, recherche dans le texte sans accents, lecture, image de page servie par l'API depuis le bucket privé, notes, compteur, favoris — et **17 du back-office** — fiche, fichier, extraction, « tel quel », publication, nouvelle version, aperçu, PDF, notes ; types, jeu d'exemple et méthodes d'API côté site. **Tests** : huit fichiers sur base réelle, deux en HTTP ; **SC-007 prouvé** sans compte, sans accès et par adresse forgée. **Trouvé par les tests** : une seconde nouvelle version butait sur l'unicité du slug au lieu de nommer le remplaçant ; le choix « tel quel » n'était pas audité — `platform.tg_audit()` sait désormais lire une autre clé que `id` ; une note à code de langue mal formé rendait 500. **Tranché d'après le contrat** : pour un réservé sans accès, `themes_hidden` toujours vrai et `external_url` nul, l'adresse d'un lien étant son contenu (contrat mis à jour). Le jeu d'exemple suit le serveur (dépublication, verrou du lien, compteur, recherche, tris, messages), fixé par `tests/guide-nego/documents-exemples.test.ts`. **Contrôles** : 70 binaires de test Rust (`negotiation`, `kernel`, `media`, HTTP), clippy, typecheck, `test:guide-nego` (138), `check:guide-nego`, `check-api-contract`, `.sqlx` régénéré ; `make check-safe` attend la fin du cycle. **Phase 4 committée à part**, dans un arbre de travail séparé où elle compile et passe ses tests seule | Phase 6 : le back-office des documents |
| 2026-09-23 | **Étape 1, phase 4 — l'extraction** (T029 à T037). PDFium dans l'image du worker (amd64 et arm64) ; la forme lisible et les règles de R7 dans `domain/extraction/`, un fichier par règle, pures et éprouvées sans PDFium ; le travail `negotiation.document.extract` attend l'analyse du média (15 s doublées, une heure au plus), lit le PDF dans le bucket privé, écrit pages et images au nom de qui a attaché le fichier, et n'écrit rien si le fichier a changé entre-temps. Un petit PDF de quatre pages, fabriqué par un exemple, porte une difficulté du guide par règle. **Mesuré** : le vrai guide passe par le travail entier en 18,5 s, 90 images, copie gardée 3,75 Mo. **Revue adversariale** — quatre angles, seize constats, tous confirmés, onze défauts distincts. Corrigés : un sommaire qui boucle faisait tomber le worker, et le travail repartait sans fin après sa chute ; une page étroite et très haute demandait une image de plusieurs gigaoctets ; un document vide épuisait ses essais ; toutes les images tenaient en mémoire (elles partent désormais au bucket une à une, 2 000 pages au plus) ; PDFium ne se lie qu'une fois par processus ; un encadré placé avant la première ligne passait sous zéro ; un logo répété devenait une figure sur chaque page. **Le guide ressort identique** après ces corrections. **Un contre-examen des corrections** en a trouvé onze de plus, corrigés à leur tour : le travail d'une demande antérieure pouvait conclure par-dessus une relance et laisser publier avant elle — la demande entre au modèle (`document_renditions.request_id`) ; une erreur après le dépôt, ou une chute du worker, laissait des images que rien ne désigne ; une page tournée (paysage) ou une boîte d'origine décalée rendait un texte illisible ou vide — les coordonnées se ramènent au repère de la page affichée ; une page qui ne se charge pas tronquait le document sans rien dire ; un signet visant une page hors du document, le caractère nul ; deux encadrés côte à côte s'entremêlaient ; des graphiques différents dans un même cadre de gabarit devenaient du décor. Le guide ressort toujours identique. Laissé : les pages d'un ancien fichier restent en base jusqu'à la prochaine extraction réussie — l'aperçu et la publication exigent l'extraction du fichier courant (phase 5) | Phase 5 : l'API |
| 2026-09-23 | **Étape 1, phase 3 — le modèle** (T019 à T028). `negotiation.documents` : brouillon sans source, COP, date, éditeur renommé `publisher`, dépublication, un seul successeur et pas de boucle, assistant décoché par défaut. Tables `document_renditions`, `document_pages`, `correction_notes` ; rôle `expert` et ses deux permissions ; types « Résumé » et « Bulletin ». **Trouvé en écrivant le modèle** : la liste des orphelins du média ne voyait que les rattachements, et le PDF d'un document y serait apparu en tête — `media.asset_references` le corrige, prouvé par un test. Migration jouée deux fois, schémas comparés à une base modèle : aucun écart sur l'étape ; `cross_module_fk_report` conforme ; tests `negotiation`, `media`, `identity` au vert | Phase 4 : l'extraction |
| 2026-09-23 | **Étape 1, phase 2 — les fichiers privés deviennent privés** (T010 à T018). Deux déplacements purs, commités à part : le contrat de stockage passe de `media` à `kernel` (`media` le réexporte, ses tests sont inchangés), et le dépôt sort d'`ImageField` dans `useDepotMedia` — dépôt d'image vérifié au navigateur. Puis `media.private_bucket` et `media.object_location()` au modèle, migration rejouable jouée deux fois ; **un stockage par bucket** (`kernel::storage::Entrepots`), si bien que le dépôt, le traitement et la purge lisent un objet là où il est ; la garde `("negotiation","documents")` — permission globale, PDF seul, **privé imposé quoi que dise la demande** ; `altText` facultatif hors image ; `MediaFileField` au guide de style. **Vérifié** : un objet du bucket privé rend 404 par le relais média, un objet public 200 ; aucun objet `private` n'était dans le bucket ouvert. **Relevé en passant** : le conteneur `usenghor_adminer` d'un autre projet écoute le port 8080 en IPv6 — le rendu serveur de Nuxt y tombe au lieu de l'API | Phase 3 : le modèle |
| 2026-09-23 | **Étape 1, phase 1 — l'essai d'extraction** (T001 à T009). `make pdfium` (chromium/7881, épinglé sur `pdfium-render` 0.9.4), `PDFIUM_LIB_PATH`, et `negotiation/examples/essai_extraction.rs` avec ses règles en cinq fichiers. **Issue A** : les huit critères tiennent sur le vrai guide — ordre de lecture, pieds sur 87 pages, 42 notes sur 42, 16 tableaux et 8 figures en `origin`, 35 traits d'union justes, termes anglais à 8 % de faux après filtre, sommaire de référence retrouvé entier à la bonne page, numéros de page lus dans le pied. **Ce que le vrai fichier a appris** : ni signets ni étiquettes ; le flux du PDF est déjà l'ordre de lecture, sauf les formes flottantes ; PDFium garde le tiret de fin de ligne en U+0002 ; la graisse qu'il rend ne vaut rien ; les annexes ont leur propre pied ; **la p. 59 n'a aucun italique**, contrairement à la maquette. **La copie gardée pèse plus que le PDF** (4,2 Mo contre 2,9) : le commanditaire garde tout, pour lire le guide en entier sans réseau ([ADR-021](adr/021-pdfium-dans-le-worker.md)). Contrat étendu : `origin.text`, 10 % du texte resté cherchable | Phase 2 : le stockage privé, deux déplacements purs d'abord |
| 2026-09-23 | **Étape 1 : tâches écrites** — [tasks.md](../../specs/011-guide-nego-documents/tasks.md), 116 tâches en treize phases : l'essai (T001-T009, relu avec le commanditaire avant toute suite), le stockage privé (deux déplacements purs en tête), le modèle, l'extraction, l'API avec SC-007, puis un récit par phase — US1 back-office, la plomberie hors connexion en parallèle, US2, US3 (critère de sortie atteint sur poste en fin de phase 9), US4, US5, US6 —, et la recette. **Le guide de la CdP30 est reçu** : `.essais/guide-cdp30.pdf`, hors Git, 90 pages et 2,9 Mo (la maquette disait 92 pages et 9 Mo) ; pages témoins nommées ; la couche de texte perd le tiret des mots coupés (« Conventioncadre ») | T001 : créer la branche, puis l'essai |
| 2026-09-22 | **Étape 1 relue et planifiée.** Arbitrages du commanditaire reportés dans la spec, [05-design.md](05-design.md) (écart 20 élargi, écarts 41 et 42) et [04-roadmap.md](04-roadmap.md) (le prompt de l'étape 1 ne réserve plus le téléchargement au compte). Plan en onze phases, R1 à R20, quatre contrats, recette. **Deux constats** : (1) `media` dépose les objets `private` dans le bucket `epavillon`, ouvert en lecture web, et n'émet aucune adresse signée — un PDF réservé aurait été public ; (2) seul `media` sait lire un objet, d'où le passage du contrat de stockage dans `kernel`, comme `kernel::mail`. Dettes notées : l'écran A12 rend une liste vide d'espaces de négociation ; 02-domaine.md l.29 cite encore `negotiation_track` | **Le commanditaire fournit le PDF du guide de la CdP30** ; créer la branche `011-guide-nego-documents` ; `/speckit-tasks` |
| 2026-09-22 | **Étape 1 spécifiée** — [spec](../../specs/011-guide-nego-documents/spec.md) et sa [liste de contrôle](../../specs/011-guide-nego-documents/checklists/requirements.md). Maquettes 03 et 04 et modèle inventoriés. **Arbitré par le commanditaire** : le texte du lecteur est tiré du PDF. **Décidé dans la spec, à contester au besoin** : une COP au plus par document ; favoris suivant le compte — le paragraphe « À propos » de 0c, qui les dit sur le téléphone, se corrige ; progression et taille du texte propres à l'appareil ; « Nouveau » = publié depuis moins de sept jours ; lecture en ligne sans compte ; « Marquer » non livré (écart à inscrire) ; le triangle rouge de la note contredit l'écart 20 (à inscrire) | `/speckit-plan` — trancher `entity_terms` ou table propre pour les thématiques ; vérifier la permission de l'expert |
| 2026-09-22 | **0c close côté code — la mise en ligne de 0a, 0b et 0c préparée.** Trou relevé par le commanditaire : `guide_nego.enabled` n'était semé que par `900_seed.sql`, chargé sur une base neuve ; aucune migration ne le portait, la production ne l'a pas, et la bascule du § 14 y rendait `UPDATE 0`. **`specs/008-guide-nego-coquille/migration.sql`** l'insère éteint, `ON CONFLICT DO NOTHING` — joué deux fois en local sans toucher le drapeau ouvert, et le cas de la production simulé dans une transaction annulée : `UPDATE 0` avant, `UPDATE 1` après. **§ 14** : la bascule doit rendre `UPDATE 1`, sinon migrer 0a d'abord. **§ 15, nouveau** : les trois migrations dans l'ordre (008, 009, 010 — `replaced_by` d'ADR-020 est porté par 010, rien d'autre n'a bougé dans `docs/database/`), leur répétition sur une copie de la production avec deux passages, la comparaison des schémas **et une requête des lignes semées**, que la comparaison des schémas ne voit pas ; `AUTH_SESSION_TTL_APP` (90 j) et `AUTH_REFRESH_GRACE` (60 s) ont leur défaut, **rien à ajouter à `.env.prod`** ; les vérifications du site juste après le redémarrage ; la recette sur Android et iPhone en une séance sur deux jours, T071 de 0a, T112 de 0b, T096 à T098 de 0c. **Constat** : créer un compte ne consigne aucun consentement — point ouvert | Le commanditaire exécute le § 15 |
| 2026-09-22 | **0c, phase 9 — recette** (T086 à T095), sur la version construite servie au port 3000 et l'API réelle, avec quatre comptes créés par l'inscription. **§§ 1 à 3** : dix termes servis sans session, `activity_theme` inchangé à 17 ; suivi de bout en bout, retrouvé sur un second navigateur ; les dix gestes du tableau conformes — `400` nommant le code refusé, `412` sans écriture, `304`, même empreinte en `fr` et en `en`, deux `PUT` identiques sans une ligne d'audit, `actor_id` partout ; la base refuse un terme d'`activity_theme`, `left_at` posé au retrait. **§ 4** : les écrans rouverts hors connexion avec leur heure, bandeau une fois par épisode, retour à « Synchronisé à » sans rechargement ; trois choix successifs hors connexion partis en **une** requête, avec le dernier état ; départ à l'ouverture et sur `online` ; **deux appareils** — Finance survit, aucune trace en base du choix en retard ; **téléphone partagé** — file vidée à la déconnexion, rien ne part. **§§ 5 à 7** : « Ma journée » vide sans erreur, avec et sans compte, sans cloche ; profil conforme, thème propre à l'appareil ; « À propos » sans interrupteur, textes lisibles sans compte ; **les deux contrôles des textes mordent** (empreinte, grammaire) ; la version consignée est vérifiée par `programme/tests/consentement.rs`, la base locale n'ayant aucun consentement. **SC-001** : 2,2 s d'attente machine de la connexion à « Ma journée » — le reste est de la saisie. **§ 8** : 36 mesures (six écrans, 320/360/390 px, deux thèmes) sans débordement ni cible sous 44 px, anneau de focus au clavier partout. Garde : 154 adresses en 200. **Cinq défauts corrigés.** (1) Un compte **déjà admis** se voyait redemander son code sur un second appareil : `apresLaConnexion()` dans `parcours.ts`, et la connexion pose la marque d'ouverture — sans quoi « Ma journée » renvoyait à « Continuer en visiteur ». (2) Le `412` ne se disait que sur l'écran des thématiques, où la personne est rarement quand le réseau revient : la mise en page l'annonce par `GnMessageEphemere` là où elle se trouve. (3) Stockage refusé, « Continuer en visiteur » ramenait à l'ouverture **en boucle** : les clés vivent en mémoire le temps de la visite. (4) IndexedDB refusé, un choix fait **en ligne** se perdait en silence : `magasinDesEcritures` retombe sur un magasin en mémoire. (5) À 320 px, une adresse électronique longue sortait de sa ligne dans le profil : `overflow-wrap: anywhere` dans `GnLigneReglage`. Trois tests de plus. **SC-007 gardé** : `check:guide-nego` lit les dix libellés dans le semis SQL et refuse leur recopie dans une traduction ou un littéral du code — exemptés les données d'exemple et les spécimens de la planche, dont l'introduction ne dit plus « livré à l'étape 0a ». Quickstart corrigé : `entity_table` et `key`/`locale`. **Hors étape, relevé** : une requête d'`identity` (`find_for_login`) ne compilait plus — les lignes de recette ont fait choisir à PostgreSQL un « Hash Right Join », que SQLx lit à l'envers ; `p.id` est annoté `"id!"` et `.sqlx` régénéré (un fichier). Clé `aside.description` en trop dans `en/pages/home.json` du site, depuis le 24/08. **Non vérifié** : le contenu du back-office — aucun compte d'administration de recette, et l'octroi d'un rôle a été refusé ; sa garde renvoie bien à la connexion. *Pièges d'automate* : `scroll-behavior: smooth` fait manquer un clic simulé, et l'automate ne fait pas défiler vers un bouton sous la ligne de flottaison — un vrai toucher n'est pas concerné, vérifié par `elementFromPoint` ; le hors-ligne simulé ne vaut que pour l'onglet actif ; après une reconstruction, le service worker sert l'ancienne version au premier chargement. Portes : `make check-safe` **au vert, API arrêtée** — 1 133 tests Rust, `test:guide-nego` 119, contrat à 189 chemins sans route en attente, `check:guide-nego` (111 fichiers), construction | **Sur appareil réel** : T096 à T098, dont T112 de 0b — puis l'étape suivante de la feuille de route |
| 2026-09-22 | **0c, phase 8 — US4, À propos et les textes qui engagent** (T067 à T085), avec la consigne du commanditaire : **personne n'a écrit les textes, et aucun outil ne les écrit**. Les quatre fichiers de `kernel/src/legal/` naissent **en attente du texte de l'IFDD** — un en-tête (`etat: en_attente`, `version: 2026-01`), aucun corps — et `LISEZMOI.md` dit comment les déposer. `kernel::legal` les embarque, lit l'en-tête, sert chaque texte avec **repli sur le français**, et dit les constructions que le rendu ne connaît pas. **Trois contrôles qui mordent** : l'empreinte figée par version (un texte modifié sans nouvelle version échoue, et l'échec donne la ligne à inscrire), la grammaire close sur les fichiers réels (tableau, note, image, bloc de code, citation, titre `#`, HTML : échec à la ligne), une version par texte et, pour un texte publié, **la date d'entrée en vigueur pour version**. **`GET /legal/{cle}`**, publique, corps sérialisés une fois au montage, `ETag`, `304`, `Content-Language`, 404 sur clé inconnue ; en attente, `status: "pending"` sans corps. **`PRIVACY_POLICY_VERSION` disparaît** — champ, défaut, validation, `ProgrammeConfig` tout entier, qui ne portait que lui, et la ligne de `.env.example` — ; l'inscription lit `kernel::legal::version("privacy")`, **toujours `2026-01`** : elle enregistre exactement ce qu'elle enregistrait. Aucune preuve existante n'est réécrite. **La comparaison d'empreinte passe dans `kernel`** (`kernel::empreinte`), pour qu'une troisième route n'en écrive pas une seconde — les deux tests unitaires ont suivi. Côté application : `GnTexteLong` rend la grammaire close **sans jamais injecter de HTML** (une adresse `javascript:` reste du texte, testé) ; `useGnTexte` garde chaque texte ; « À propos » dit l'éditeur, **l'origine des sessions sans parler d'accord**, ce qui reste sur le téléphone et ce qui suit le compte **sans nommer d'hébergeur ni de restitutions**, les deux textes — « Texte en préparation par l'IFDD » —, les licences (police SIL OFL 1.1, bibliothèques MIT), **aucun interrupteur d'accord** ; « l'édition » est la COP climat qui se tient, sinon la prochaine (`editionDuGuide`, testée), et la version l'identifiant de la construction. **Vérifié au navigateur à 360 px**. *Piège de développement* : une page dans un dossier nouveau n'est pas vue par le serveur de développement tant que le fichier n'est pas touché. Portes : `cargo test` kernel 39, programme 241, negotiation 76, api 69 au vert, `clippy -D warnings`, `make openapi` (189 chemins, 158 formes), `check-api-contract`, `test:guide-nego` **116 au vert**, `check:guide-nego` (111 fichiers), `npm run build` | **Phase 9, recette** — puis `make check-safe`, API arrêtée. **T112 de 0b reste due** |
| 2026-09-22 | **0c, reprise 3 — « Libérer » ne vide que les documents.** Demandé par le commanditaire : le geste de la phase 7 effaçait l'accès, les thématiques, le vocabulaire — demain l'agenda du jour —, quelques dizaines de kilo-octets qui font marcher l'application sans réseau. **Il ne libérera que les documents téléchargés, à l'étape 1** ; à cette étape il n'y a rien à libérer, donc **pas de bouton** — un geste sans effet trompe (XII), comme les interrupteurs d'accord. « Mes téléchargements » garde son état vide et la place occupée. Retirés : `liberer()`, `GARDES_DE_LA_COQUILLE`, la confirmation et ses traductions ; `viderLesGardes()` revient à sa forme d'avant. **Écart inscrit** dans la spécification ; le scénario 5 du récit 3, FR-024 et la diminution de SC-010 passent à l'étape 1, notés dans la feuille de route avec la consigne de ranger les documents là où leur suppression se mesure. Le point ouvert sur la place qui ne baisse pas disparaît. Au passage, l'hypothèse de la spécification sur le fuseau de « Ma journée », restée à l'ancienne décision, est alignée sur R11 | **Phase 8, US4** : « À propos » et les textes — sans écrire ni la politique ni les conditions |
| 2026-09-22 | **0c, reprise 2 — la réponse de rotation perdue n'est pas un vol** ([ADR-020](adr/020-la-reponse-de-rotation-perdue.md), tranché par le commanditaire, nuance R3 de `specs/001`). Quand la réponse de `/auth/refresh` se perd au retour, le navigateur représente l'ancien jeton ; R3 strict y voyait un vol et coupait **toutes** les sessions de la personne. Désormais, **l'ancien jeton présenté dans la minute, tant que sa remplaçante n'a jamais été renouvelée**, révoque celle-ci (`response_lost`) et ouvre une session neuve — une seule vivante ; hors de ces bornes, tout est coupé. **Modèle d'abord** : `identity.sessions.replaced_by`, en dernière colonne, migration rejouable jouée deux fois sans `down -v`, `docs/progression/modele.md`, `data-model.md` de 001 et une note sous R3. Réglage `AUTH_REFRESH_GRACE` (60 s ; zéro rend R3 strict), dans `.env.example`. **Deux tests anciens rejouaient aussitôt après la rotation** et attendaient la coupure — exactement le cas que la règle accepte désormais : ils rejouent maintenant au-delà de la minute ; la course de deux renouvellements tient l'invariant « jamais deux sessions vivantes ». Sept tests nouveaux : cinq sur base réelle (reprise, deux reprises de suite, rejeu après la minute, rejeu après usage de la remplaçante, jeton d'une remplaçante écartée) et un en HTTP (la reprise pose de nouveaux cookies). `cargo test -p identity` 130, `-p api` 66, `-p kernel` 28 au vert, `clippy -D warnings`, `make sqlx-prepare`. **Relevé en passant** : deux lignes nues ajoutées au `.env` (l'adresse et le mot de passe du compte de recette) empêchaient toute compilation Rust — le lecteur de `.env` les refuse ; elles sont passées en commentaire, leur contenu intact | **Reprise 3** : « Libérer » ne vide que les documents — pas de bouton à cette étape |
| 2026-09-22 | **0c, reprise 1 — une API muette n'est pas une déconnexion.** Demandé par le commanditaire : la correction de la phase 7 avait un trou. Réseau saturé, `/auth/me` répond « personne » (jeton périmé), puis `/auth/refresh` échoue ; `rotate()` tenait cette panne pour une session finie, **effaçait le témoin**, et la session de quatre-vingt-dix jours était perdue pour rien. **La rotation a maintenant trois issues** (`utils/rotation.ts`, pure) : renouvelée ; finie — 200 `expired` ou 401 — ; injoignable — tout le reste. **Le témoin ne s'efface que sur « finie »**, et `refreshSession()` garde son booléen. Une requête dont la rotation est injoignable lève une panne (`ApiUnreachableError`), plus un 401. Côté Guide Négo, la décision tient dans `relireLeCompte()` (`utils/guide-nego/compte.ts`, pure, sur le modèle de `resoudreDrapeau`) : injoignable **lève**, et `useGnLecture` garde l'état connu avec son heure. **Le site y gagne, et un second trou y est fermé** : son store tenait une API muette pour une session finie, et surtout, **au rendu serveur**, qui ne peut pas tourner le jeton, il concluait quand même et effaçait le témoin de toute session vieille d'un quart d'heure — un rechargement de page suffisait à déconnecter. Le serveur ne conclut plus ; le navigateur tourne et décide. **Vérifié au navigateur**, jeton d'accès invalide et `/auth/refresh` coupé : Guide Négo garde l'avatar et le témoin ; le site garde le témoin, affiche « Liaison interrompue avec la plateforme », puis retrouve « AD » au retour du réseau, sans reconnexion. **Laissé tel quel, et à trancher** : pendant la panne, l'en-tête du site montre « Se connecter » — c'est sa règle écrite (`layouts/public.vue` : « l'entrée de connexion, ce qui est vrai tant qu'on ne sait pas »). Le site n'a pas de tests unitaires du client HTTP ; `test:guide-nego` **110 au vert** (5 nouveaux : les issues, et leur croisement avec une garde connectée ou non), `typecheck`, `check:guide-nego`, `check-api-contract` | **Reprise 2** : la réponse de rotation perdue, tolérance d'une minute |
| 2026-09-22 | **0c, phase 7 — US3, Profil et réglages** (T057 à T066). **Le profil** ([reglages.vue](../../frontend/app/pages/guide-nego/ressources/reglages.vue), 248 lignes, pas de découpe) : en tête, l'avatar de 48 px à côté du nom, qui devient le titre, et le pays en sous-titre (`GnEntete` gagne `avatarDuTitre`, `GnAvatar` une taille `grand`) ; « Mon suivi » — « Mes thématiques » avec les noms croisés au vocabulaire gardé, **le nombre** en repli, jamais un code brut ; « Mes téléchargements » avec la place ; « Mon accès » de 0b, qui quitte le groupe « Compte » pour y venir — ; « Affichage » inchangé ; « Application » — « À propos et confidentialité », puis « Dernière synchronisation », l'heure sans fuseau, qui n'est pas une action — ; le compte et la déconnexion de 0b, avec « Vos téléchargements restent sur le téléphone ». Le pays se garde avec le compte (`EtatDuCompte.paysId`) et son nom avec le référentiel (`useGnPays`, lecture `pays`). **`GnJauge`** : 6 px, `--gn-jauge-fond`, toujours le nombre écrit ; sur la planche dans les deux thèmes. **`useGnPlace`** : `navigator.storage.estimate()`, et `placeDe()` rend **nul** — jamais zéro — quand le navigateur ne mesure pas. **« Mes téléchargements »** : l'état vide, la jauge, la libération derrière une confirmation. **Libérer ne vide pas tout** : le drapeau et le compte restent (`GARDES_DE_LA_COQUILLE`) — sans eux, l'application ouverte sans réseau se croirait fermée ou déconnectée —, et la file n'est jamais touchée. **Un défaut de 0b trouvé en vérifiant, et corrigé** : `useGnSession` relisait `/auth/me` sans rotation du jeton ; or `/auth/me` ne rend jamais 401, et répond « personne » dès que le jeton d'accès d'un quart d'heure expire. **L'application se croyait déconnectée le lendemain matin**, session de quatre-vingt-dix jours intacte. Elle tente désormais une rotation puis relit, comme le store du site ; éprouvé au navigateur avec un jeton d'accès invalide. **Vérifié au navigateur à 360 px** : l'avatar mène au profil, les trois groupes, la jauge, la confirmation, et la garde réduite à `compte` et `drapeaux` après libération. **Un écart, inscrit aux points ouverts** : la place affichée ne diminue pas après libération — Chrome ne rend la place d'IndexedDB qu'au compactage. Et **la ligne « À propos » mène à un écran qui n'existe pas encore** : il vient à la phase 8. Portes : `npm run typecheck`, `npm run test:guide-nego` **105 au vert** (5 nouveaux), `npm run check:guide-nego` (104 fichiers), `check-api-contract`, `npm run build` | **Phase 8, US4 — À propos et les textes qui engagent** : `kernel/legal`, `GET /legal/{cle}`, les deux contrôles, la bascule de `PRIVACY_POLICY_VERSION`, « À propos », `GnTexteLong`. **T112 de 0b reste due** |
| 2026-09-22 | **0c, phase 6 — US2, « Ma journée »** (T046 à T056), avec les **quatre précisions du commanditaire**. (1) **Un bloc vide tient en une ligne** sous son en-tête de groupe — ce qui manque, et quand cela viendra —, comme la maquette le fait pour les Réunions de la Francophonie : quatre `GnEtatVide` à la suite auraient fait l'écran d'une panne. L'ordre des blocs vit dans `utils/guide-nego/journee.ts`, et un test le tient. `GnEnteteGroupe` gagne une `note` en graisse normale, qui porte « mis à jour à 11:35 » en ligne et « connus à 11:35 » sans réseau. (2) **Le sous-titre ne porte que le jour** — « Mardi 22 septembre ». Nommer le fuseau de l'appareil trompe : « Istanbul » à Antalya, « Lome » et « Ndjamena » sans accent, « Douala » pour Yaoundé, « heure moyenne de Greenwich » à Dakar. Le fuseau nommé viendra en 3a, du lieu de l'édition (`edition.city` d'abord) ; R11, FR-012 et T051 sont réécrits. (3) **« Aujourd'hui » se recalcule au retour au premier plan** (`useGnAujourdhui`) : une application restée en mémoire n'ouvre pas le lendemain sur la veille. (4) **`GnAvatar` revient aux initiales sur une erreur de chargement**, pas seulement quand l'image manque — hors connexion, l'image ne vient pas, et elle n'a pas sa place dans la garde de la coquille. L'avatar est une cible de 48 px qui ouvre le profil ; `GnEntete` le porte **à la place** du retour, `GnEcran` le relaie. Aucun compte n'a d'image à ce jour — `identity.people` n'en porte pas —, la prop est prête. La planche le montre en clair et en sombre, image absente comprise, et ne le range plus parmi les absents. **Vérifié au navigateur à 360 px**, sur un compte de recette local (`recette.0c.phase6@example.org`) : sans compte, l'invitation et les blocs ; connectée, la garde de première entrée envoie **une fois** aux thématiques, le choix part (`PUT` puis relecture, deux suivis en base) et ramène à « Ma journée » avec « AD » ; en mode avion, bandeau et « connus à », puis « Synchronisé à » au retour, **sans rechargement**. **Deux choses apprises en vérifiant** : l'API locale tournait sur un binaire antérieur à la phase 3 (404 sur les thématiques) — reconstruite et relancée ; et la barre flottante de Nuxt DevTools couvre le bas de l'écran à 360 px, où un clic automatisé tombe dessus. **Corrigé en passant** : l'indicateur d'étapes des thématiques collait au filet de l'en-tête. Portes : `npm run typecheck`, `npm run test:guide-nego` **100 au vert** (6 nouveaux, dont l'absence de « Thème » et de « Programme » seul dans l'accueil et les thématiques), `npm run check:guide-nego` (99 fichiers), `check-api-contract`, `npm run build` | **Phase 7, US3 — Profil et réglages** : `GnJauge`, `useGnPlace`, les téléchargements et leurs quatre états, « Mon suivi », « Application », le nom et le pays en tête. **T112 de 0b reste due** |
| 2026-09-22 | **0c, entre les phases 5 et 6 — l'empreinte traverse un relais qui la réécrit.** Demandé par le commanditaire : le relais institutionnel du § 11 de `DEPLOIEMENT.md` compresse en brotli, et Apache ajoute alors « -br » ou « -gzip » à l'`ETag` ; l'égalité brute de `service/themes.rs` aurait rendu `412` à **chaque** choix fait hors connexion, avec un message faux — invisible en développement comme en recette, qui ne compressent pas. **Une seule fonction compare désormais**, `negotiation::domain::empreinte::correspond`, pour `If-Match` comme pour `If-None-Match` : elle garde les 32 caractères hexadécimaux, `W/`, guillemets et suffixe tombent, une liste correspond si l'un de ses éléments correspond, et une valeur illisible ne correspond à rien. Le calcul de l'empreinte, qui était écrit deux fois (`acces.rs`, `domain/themes.rs`), vit au même endroit. `me/access` et `me/themes` rendent `Cache-Control: private, no-cache`, 304 compris. Tests : deux unitaires, deux sur base réelle (`thematiques_relais.rs` : « -br », « -gzip », `W/` acceptés ; autre empreinte, « -br » d'une autre, valeur illisible, `*` et vide → 412 sans écriture), un en HTTP (304 à travers `W/"…-gzip"` et `"…-br"`, en-tête de cache sur les deux routes). `DEPLOIEMENT.md` § 11 gagne la liste des vérifications du jour du `mv`, dont **un choix fait hors connexion qui survit au retour du réseau par l'adresse institutionnelle**. `cargo test -p negotiation` 78 au vert, `cargo test -p api` (thématiques, CORS) au vert, `clippy -D warnings` | **Phase 6, US2 — « Ma journée »**. **T112 de 0b reste due** |
| 2026-09-22 | **0c, phase 5 — US1, Mes thématiques** (T038 à T045). **Le MVP de l'étape est tenu.** `api/guide-nego.ts` porte `mesThematiques()` et `suivreDesThematiques(codes, empreinte?)`, servies par **deux primitives nouvelles de `api/http.ts`** — `lireEtiquete` et `ecrireEtiquete`, qui rendent `{ valeur, empreinte }` : c'est la première fois que le site lit un **en-tête de réponse**, et rien ne le permettait. Elles ne posent pas `If-None-Match` — un `304` ne se distingue pas d'un échec pour le client HTTP, et ces listes sont minuscules. **Un défaut trouvé et corrigé au passage** : `middleware/cors.rs` n'exposait que `X-Request-Id`, donc le navigateur **cachait l'`ETag` au code** ; l'empreinte serait restée nulle en développement sans qu'une seule erreur ne le dise, et un choix pris hors connexion serait reparti sans garde d'ancienneté. `check-api-contract.mjs` **ne voyait pas non plus les deux appels** : sa recherche ne connaissait que `call`, `send`, `sendForm` et `pending` — elle connaît maintenant les deux primitives étiquetées, et compte 166 appels. `utils/guide-nego/thematiques.ts` est **pur**, comme `acces.ts` : vocabulaire gardé, libellé résolu avec repli sur le français puis sur le code, codes voulus triés et dédoublonnés, validation à zéro refusée, récapitulatif dans l'ordre du vocabulaire, et la garde de première entrée. `useGnThematiques` fait les **deux** lectures et l'écriture : **l'empreinte est gardée DANS la valeur** — sortie de là, elle se perdrait à la fermeture de l'application, et le choix repartirait sans `If-Match` ; le choix s'applique à l'écran **avant** d'être parti, sinon les cases reviendraient en arrière sous les doigts. L'écran sert deux usages — étapes et « Suivre ces thématiques » au premier choix, retour au profil et « Enregistrer » ensuite — et porte ses quatre états, l'état vide disant qu'aucune thématique n'est proposée **sans bloquer l'entrée**. Le parcours mène désormais aux thématiques après un accès ouvert, et `PLUS_TARD` naît pour les sorties qui annoncent « Ma journée ». **Un piège d'outillage à connaître** : dans `useApi.ts`, `export const NO_ADMIN_SCOPE = { … }` immédiatement suivi de `export function useApi()` fait **disparaître `useApi` des auto-imports de Nuxt** — 137 erreurs « Cannot find name 'useApi' » sans qu'aucun fichier ne soit en cause ; la latence des données simulées reste donc déclarée dans `useApi.ts` et se **passe en paramètre** à la fabrique, au lieu d'être exportée depuis `http.ts`. Portes : `npm run typecheck`, `npm run test:guide-nego` **94 au vert** (8 nouveaux), `npm run check:guide-nego` (96 fichiers, 54 paires), `node scripts/check-api-contract.mjs`, `npm run build`, `cargo check -p api --tests` | **Phase 6, US2 — « Ma journée »** : `GnAvatar` et la planche, l'avatar relayé par `GnEcran`, les cinq blocs dans l'ordre et leurs états vides, le titre et son fuseau, l'état sans compte, le comportement hors connexion, le contrôle du vocabulaire. **T112 de 0b reste due** |
| 2026-09-22 | **0c, phase 4 — la plomberie hors connexion** (T026 à T037). `garde.ts` passe en version 2 avec le magasin `ecritures`, sans toucher à `lectures`. `utils/guide-nego/file.ts` est **pur, à dépendances injectées** — magasin, expéditeurs par clé, personne connectée — pour se tester sous `node --test` : une intention porte clé, corps entier, empreinte, personne et heure ; une nouvelle intention sur la même clé remplace la précédente **en gardant l'empreinte de la première** ; `decider()` classe la réponse — succès et 412 et refus définitif retirent l'entrée, panne et 5xx (et 408, 429) la gardent — ; 412 et succès relisent ; 412 et refus définitif signalent ; deux départs simultanés partagent la même promesse ; aucune fonction ne lève. `useGnFile` n'apporte que ce que le navigateur seul fournit : IndexedDB, la personne, la traduction d'une `ApiRequestError` en réponse, l'avis à montrer ; **chaque écran qui écrit s'y inscrit avec sa clé** — la file ne connaît aucun écran, c'est ce qui la fera servir au parcours (2) et aux signalements (3b). **Un piège évité en relisant** : la file et ses expéditeurs portent des fonctions et ne vont pas dans un `useState`, que le rendu serveur sérialise — ils vivent au niveau du module, côté client. `useGnConnexion` écoute `online` (elle n'écoutait qu'`offline`), la mise en page fait partir la file à l'ouverture, sur `online` et au retour au premier plan, `useGnSession.deconnecter()` vide la file **avant** de fermer la session. `EtatDuCompte` gagne `id`, sans quoi une intention ne pourrait pas être tenue à son compte. `useGnThematiques` lit le vocabulaire `negotiation_theme` par la route publique des termes, depuis la mise en page, et le garde comme le drapeau ; les libellés se résolvent dans la langue de la personne avec repli sur le français. Les mocks portent la taxonomie et ses dix termes, même semis que le SQL. `check-guide-nego.mjs` **refuse désormais tout `*.test.ts` hors de `tests/`**. Portes : `npm run test:guide-nego` **86 au vert** (15 nouveaux, dont l'annonce de retour en ligne), `npm run typecheck`, `npm run check:guide-nego` (94 fichiers, 54 paires) | **Phase 5, US1 — Mes thématiques** : `mesThematiques()` et `suivreDesThematiques()` dans `api/guide-nego.ts`, les suivis dans `useGnThematiques`, l'écran de choix avec `GnCase`, la garde de première entrée, le message du 412, les quatre états, le test d'écran. **T112 de 0b reste due** |
| 2026-09-22 | **0c, phase 3 — les thématiques côté API** (T010 à T025). Trois codes au catalogue de `kernel` — `NEGOTIATION_THEMES_EMPTY` (400), `NEGOTIATION_THEME_UNKNOWN` (400), `NEGOTIATION_THEMES_STALE` (412). Dans `negotiation` : `domain/themes.rs` (formes `MyThemes` et `ThemesPayload`, empreinte de 16 octets sur les codes triés et dédoublonnés), `repo/themes.rs` (lire les vivants, fermer les absents par `left_at`, ouvrir les nouveaux, sans toucher aux inchangés), `service/themes.rs` (transaction par `Db::write`, comparaison de l'`If-Match` **dans** la transaction, résolution des codes sous `negotiation_theme` pour nommer le code refusé), `routes/themes.rs` (`ETag`, `304`, `412`), OpenAPI et montage. **Un choix de construction à connaître** : le verrou par personne est un `pg_advisory_xact_lock` sur son identifiant, et non un `SELECT … FOR UPDATE` des suivis — celui-ci ne verrouille rien quand la personne n'a encore aucune ligne, et c'est précisément le premier choix que deux appareils peuvent envoyer ensemble. **Seize tests** : treize dans le crate (chemin nominal, doublons, 412 sans écriture, empreinte courante acceptée, rejeu sans trace d'audit, sans `If-Match`, trigger de vocabulaire en direct et code nommé par l'API, liste vide, terme désactivé déjà suivi ou nouveau, fermeture puis reprise, audit) et trois en HTTP (401 sur les deux routes et sur une URL forgée, `ETag` → 304 → 412 de bout en bout, 400 nommant le code). `cargo test -p negotiation` 74 au vert, `-p kernel` 28 au vert, `make openapi` (188 chemins, 157 formes, toutes définies), `make sqlx-prepare`, `npm run typecheck` | **Phase 4, la plomberie hors connexion** : magasin `ecritures`, `file.ts`, `online`, départ à l'ouverture et au premier plan, vidange à la déconnexion, vocabulaire lu avec le drapeau, `check-guide-nego.mjs` étendu, six tests sans navigateur. **T112 de 0b reste due** |
| 2026-09-22 | **0c, phase 2 — le modèle** (T004 à T009). `020_reference.sql` : la taxonomie `negotiation_theme` et ses dix termes, avec le commentaire qui la distingue d'`activity_theme`. `100_negotiations.sql` : `negotiation.theme_subscriptions`, patron de `network_memberships` trait pour trait. `migration.sql` rejouable, **jouée deux fois sur la base montée** — jamais `down -v` —, puis une base modèle `epavillon_modele_0c` chargée depuis `docs/database/` et les deux `pg_dump --schema-only` comparés après tri : **les objets de 0c sont identiques**, et les seuls écarts sont ceux que 0b avait déjà nommés (partition `email_messages_202612` créée par le worker, trois contraintes reparenthésées par la restauration) plus des alias `AS text` de vue, artefact de rendu. Point de contrôle : `GET /api/reference/taxonomies/negotiation_theme/terms` sert les dix termes **sans une ligne de Rust**, sur l'API déjà en place. Consigné dans `docs/progression/modele.md` (ADR-017). *Relevé en passant* : la base locale porte plus de cent cinquante bases `epavillon_test_*` orphelines, laissées par des tests interrompus — le harnais les purge par préfixe de modèle, pas celles-là | **Phase 3, le crate** : trois codes d'erreur, formes `MyThemes` et `ThemesPayload`, domaine, dépôt, service, routes `GET`/`PUT /negotiation/me/themes` avec `ETag` et `If-Match`, huit tests sur base réelle, `make openapi`. **T112 de 0b reste due** |
| 2026-09-22 | **0c, phase 1 — la place** (T001 à T003), sur la branche `010-guide-nego-accueil-profil`. Le bloc `auth` de `useApi.ts` sort dans `composables/api/auth.ts` : `createAuthApi(deps)` porte les neuf méthodes telles quelles, `useApi()` le monte avant le littéral de retour parce que `createGuideNegoApi` en a besoin, et les onze types d'`~/types/auth` quittent les imports du fichier commun. **Le diff se lit comme un déplacement** : aucune signature retouchée, aucun des 14 appelants modifié. `useApi.ts` passe de 973 à **895 lignes**. Portes : `npm run typecheck`, `npm run build`, `make check-api-contract` (164 appels, 155 formes) au vert | **Phase 2, le modèle** : vocabulaire `negotiation_theme`, dix termes, `negotiation.theme_subscriptions`, migration rejouable appliquée deux fois — jamais `make check` —, ligne dans `docs/progression/modele.md`. Puis la phase 3, le crate. **T112 de 0b reste due** |
| 2026-09-22 | **0c — tâches et analyse.** `tasks.md` : **103 tâches**, neuf phases, un commit par phase ; le MVP tient aux phases 1 à 5. `/speckit-analyze` a rendu **zéro critique, 93 % de couverture**, et **dix corrections, toutes appliquées**. **La plus utile était muette** : huit tâches écrivaient leurs tests dans `frontend/test/guide-nego/` alors que le dépôt a `frontend/tests/` et que la porte est le glob `tests/guide-nego/*.test.ts` — onze tests auraient été écrits puis **jamais exécutés**, la porte restant verte. Corrigé, et **`check-guide-nego.mjs` refuse désormais tout `*.test.ts` sous `frontend/` hors de `frontend/tests/`** (T032 bis) : un test que la porte ne ramasse pas est pire qu'un test absent. **Quatre lacunes de couverture comblées** : les quatre états n'étaient exigés que de l'écran des thématiques (T060 bis, T082 bis) ; SC-007 — aucun libellé de thématique dans le code ni les traductions — n'était vérifié nulle part (T090 bis) ; le chronomètre de SC-001 manquait (T088) ; et le cas « terme désactivé, déjà suivi ou nouveau », écrit au contrat, n'avait aucun test (T021 bis). **Une ambiguïté levée** : FR-036 exigeait que l'enregistrement d'un accord « soit disponible » — c'est **déjà le cas**, et un implémenteur aurait pu construire une route d'accord que FR-035 interdit pourtant d'afficher ; l'exigence dit maintenant qu'il n'y a rien à construire. **Un ordonnancement corrigé** : `GnAvatar` était créé en phase 5 alors que l'écran des thématiques n'en porte pas — il passe en phase 6, qui **ne dépend plus de rien** et pourrait précéder la phase 5. **Et une précision du commanditaire** : le contrôle sur le mot « Thème » ne porte que sur les traductions des thématiques et de « Ma journée » — ailleurs, dans les réglages d'affichage et sur la planche, le mot est légitime, il désigne l'apparence | `/speckit-implement`, sur la branche `010-guide-nego-accueil-profil`. **T112 de 0b reste due** |
| 2026-09-22 | **0c — trois reprises du plan, demandées par le commanditaire.** (1) *La file d'écriture était idempotente, mais pas sûre entre deux appareils* — et le cas est banal en COP : téléphone hors réseau à 10:00, tablette en ligne à 11:00, téléphone de retour à 12:00, et l'intention ancienne **effaçait le choix récent sans que personne ne le voie**. L'idempotence protège du rejeu, pas de l'ancienneté. Une intention porte donc **l'empreinte de l'état sur lequel elle a été prise**, l'envoie en `If-Match`, et l'API rend **412** ; l'application abandonne — jamais de fusion, personne n'aurait choisi la liste obtenue —, relit, et le dit. Un `PUT` sans `If-Match` reste accepté : l'écran en ligne vient de lire. **L'empreinte porte les codes triés, pas le corps rendu**, sinon deux appareils en `fr` et en `en` auraient deux empreintes pour un même état. **Conséquence trouvée en l'écrivant, et heureuse : le corps ne peut donc pas porter de libellés** — sinon un appareil qui change de langue recevrait un 304 et garderait les anciens. Il ne rend que des codes ; le client a déjà le vocabulaire par la route publique des termes. Une source de libellés au lieu de deux, et un contrat plus court. **Trois règles de file qui tiendront au-delà des thématiques**, puisque le parcours (2) et les signalements (3b) emploieront la même : une entrée porte l'identifiant de la personne et ne part jamais sous un autre compte (un téléphone se prête, au stand) ; la file se vide à la déconnexion ; elle part **aussi à l'ouverture de l'application** — un téléphone rouvert le lendemain n'émet pas d'`online`. Six tests de file sans navigateur, huit tests d'API sur base réelle. (2) *La sortie du bloc `auth` devient la phase 1*, un commit à part qui ne fait que déplacer : aucune méthode nouvelle, aucun des 14 appelants touché, et **rien d'autre du site ne bouge de toute l'étape** — sauf la ligne de `programme` en phase 8. (3) *Un second test rend les deux fichiers Markdown réels avec la grammaire close* et échoue sur toute construction inconnue, en nommant la ligne : les textes seront écrits par quelqu'un qui n'a pas à connaître les bornes du rendu, et un tableau rendrait du charabia sans prévenir. **Spécification ajustée** : FR-009 bis, SC-005 bis, et le cas limite des deux appareils, qui promettait « sans message d'échec » et ne le peut plus quand le choix vient de loin | `/speckit-tasks`, puis `/speckit-analyze`. **L'implémentation se fait sur la branche `010-guide-nego-accueil-profil`**, par tranches, un commit par phase |
| 2026-09-22 | **0c — le plan** (`/speckit-plan`) : recherche, modèle, trois contrats, recette, neuf phases. **Les deux points laissés ouverts sont tranchés.** (1) *Le suivi des thématiques vit dans une table propre*, `negotiation.theme_subscriptions`, sur le patron exact de `network_memberships`. **`identity.negotiator_profiles.specializations` est écarté, et la raison est nette : la table n'a de ligne pour personne** — ni le semis, ni le backend (zéro occurrence de `negotiator_profiles` dans `backend/crates/`), ni un trigger n'en crée. Y ranger « mes thématiques » fabriquerait un profil de négociateur désigné à chaque premier clic sur une case, et `space_members.negotiator_profile_id` se mettrait à pointer vers des profils nés d'un geste d'écran. S'ajoutent la sémantique inverse (compétence attestée contre préférence révocable) et le `text[]` sans index ni garde de vocabulaire, qui est le défaut même que la v2 a corrigé. `reference.entity_terms` écarté aussi : elle classe des contenus, pas des personnes, n'est pas auditée, et `platform.purge_term_links()` **n'existe pas** malgré son commentaire — les lignes d'une personne supprimée resteraient orphelines. (2) *Le bloc `auth` sort de `useApi.ts`* en phase 2, avant toute autre écriture : 973 lignes sur 1000, environ 70 déplaçables, **14 appelants inchangés**, et la fabrique reçoit `deps` et non le motif à primitives restreintes, à cause du troisième argument de `checkPasswordResetToken`. **Deux trouvailles qui changent le travail.** *La route publique des termes de taxonomie existe déjà* (`GET /reference/taxonomies/{code}/terms`) : semer `negotiation_theme` suffit à servir les dix thématiques, **sans une ligne de Rust** — seules les routes d'écriture du suivi sont à écrire. Et *l'écriture différée hors connexion n'existe pas* : un seul magasin IndexedDB, `useGnConnexion` n'écoute que `offline` et jamais `online`, `api/http.ts` pose `retry: 0` délibérément. La constitution l'exige (XI) et la spécification deux fois : **0c la construit**, bornée aux thématiques, et c'est le `PUT` en bloc — donc idempotent — qui la rend sûre. *La doctrine de 0b tient* : un accès ne se met pas en file, on ne peut pas annoncer un droit avant de l'avoir. **Les textes vivent dans `kernel`**, pas dans un module : `programme` lit la version à l'inscription et ne peut dépendre d'aucun crate de module (principe II) ; la route publique se pose dans `api`, à côté de celle du référentiel. Ce sera **la première route du dépôt à servir un contenu embarqué** — vérifié, aucun `include_str!` de contenu nulle part, et aucun `text/*` servi. `PRIVACY_POLICY_VERSION` disparaît de la configuration ; `registration.rs:122` change d'une ligne. **Écarté et dit** : le « modifié depuis » de `03-api.md`, que rien n'implémente encore dans le dépôt — l'empreinte de contenu suffit à dix termes, et il se justifiera sur les documents de l'étape 1. **Deux dettes relevées en chemin**, hors périmètre : `platform.purge_term_links()` promise et absente, et `reference.terms_of` qui ne filtre pas `is_active` quand `term_badges` le fait | `/speckit-tasks`, puis `/speckit-analyze` et `/speckit-implement`. **La phase 1 touche `docs/database/`** : migration rejouable, jamais `make check`, et la ligne dans `docs/progression/modele.md`. **T112 de 0b reste due**, sur Android et iPhone réels |
| 2026-09-22 | **0c — spécification arrêtée** (`/speckit-specify`). `specs/010-guide-nego-accueil-profil/` : quatre récits — choisir ses thématiques et les retrouver ailleurs, « Ma journée » qui s'ouvre vide sans erreur, le profil, et « À propos » avec ses textes —, 44 exigences, douze critères, liste de contrôle qualité au vert. **Les deux questions posées ont été tranchées le jour même** (voir la ligne d'état) : un vocabulaire `negotiation_theme` à semer, et une source unique pour les textes qui engagent, servie par l'API au site comme à l'application. **Ce que le modèle ne porte pas** : le vocabulaire `negotiation_theme` et ses dix termes, et le lien entre une personne et ses thématiques — **rien n'existe**, et le plan doit choisir entre `identity.negotiator_profiles.specializations` (un tableau de codes, sans intégrité référentielle, déjà là) et une table propre : une vérité, pas deux. Les consentements, eux, existent avec leur vue d'état courant, et rien n'est à semer tant qu'aucun accord n'est offert. **Rien n'est à ajouter pour « Ma journée »** : ses blocs restent vides jusqu'aux étapes 1, 3a, 4 et 5. **Trois choses relevées en rédigeant**, à reprendre au plan : `useApi.ts` est à 973 lignes pour un garde-fou à 1000 — le bloc `auth` sort en premier ; l'en-tête ne porte aujourd'hui ni avatar ni cloche — **l'avatar est de cette étape**, la cloche de 3b ; et l'écart 32 nomme cette étape, la ligne « Dernière synchronisation » perd son fuseau. **Écart 40 inscrit** dans `05-design.md` | `/speckit-plan` avec le prompt de plan commun, puis `/speckit-tasks`. **T112 de 0b reste due**, sur Android et iPhone réels |
| 2026-09-22 | **0b, phase 8 — la recette** (T104 à T111 ter). **Trois défauts trouvés, et corrigés.** (1) *Le back-office ne savait pas dire combien de personnes appartiennent au réseau* — il ne rendait que les usages par code, et SC-007 demande les deux. Or le compte des appartenances **n'est pas la somme des usages** : une même personne peut entrer par deux codes du même réseau, et son appartenance survit à la révocation du code qui l'a apportée — sommer `used_count` aurait annoncé trois membres pour deux, puis un de moins à la première révocation. `NetworkSummary` porte désormais `members_count`, lu dans les appartenances vivantes par `ix_network_memberships_network`, et l'écran des codes l'affiche sous son sous-titre. Un test le prouve sur deux codes, trois usages et une révocation. (2) *« En attente » au lieu de « Demande en attente »* sur l'écran de demande, en `fr` et en `en`, quand « Mon accès » écrivait la forme du lexique : deux écrans du même parcours se contredisaient. (3) *« Programme officiel » employé seul* en libellé de bouton sur la planche de composants — le mot est banni seul, et la phrase longue du même écran écrivait déjà « le programme officiel de la CCNUCC ». **Un test de plus** : un envoi de courriel qui échoue **ne défait pas la décision** (SC-011) — relais éteint, l'accès accordé tient, le travail revient en file avec son code d'erreur stable, et le rejeu n'envoie qu'un seul message. **Le parcours au navigateur, sur la version construite** : compte → code → entrée en **23 secondes**, là où SC-001 en accorde cent quatre-vingts. Le lien du courriel ramène bien dans l'application (`/guide-nego/verification-adresse`), la confirmation renvoie à la connexion (écart 34), et les cinq cas de saisie répondent comme la table du quickstart les décrit — dont « révoqué le 19 septembre », avec sa date, et « nego 024 » reconnu comme `NEGO-024`. **Le piège n° 1 est tenu de bout en bout** : après deux rotations forcées, `client_kind` reste `app`, l'appareil est recopié, et les quatre-vingt-dix jours repartent de zéro. En base : un seul rôle `negotiator/global` malgré deux codes saisis, une seule appartenance, `app` à 90 jours contre `web` à 12 h. **84 mesures d'écran** — 14 écrans × 3 largeurs × 2 thèmes — sans un débordement horizontal, sans une cible sous 44 px et avec l'anneau de focus partout. *Deux fausses alertes à connaître si la mesure se refait* : `GnPilule` fait 40 px de dessin dans une cible de 48 gagnée par un `::after` en débord, et les champs natifs des interrupteurs et de la recherche sont plus petits que la surface qu'on touche — une sonde qui lit `getBoundingClientRect` les accuse tous à tort. **Migration répétée sur une copie de la base**, données comprises, deux passes, puis schémas recomparés à la base modèle : **aucun écart réel** — seules les partitions `engagement.email_messages_202612` et le reparenthésage de trois contraintes par la restauration, qui n'en sont pas. **Non-régression du site** : accueil, `/negociations` et back-office inchangés dans les deux thèmes, aucun service worker ni manifeste après une visite du site seul, `data-theme` absent de `<html>`, et les quatre écrans d'authentification servis sans objet `client`. Éprouvé : `make check-safe` **au vert, API arrêtée**, 47 tests Rust dans `negotiation`, 70 tests `node --test`, `check:guide-nego` (91 fichiers, 54 paires), `make check-api-contract` (164 appels, 155 formes), `verifier-garde` (132 adresses servies en 200 contre la version construite) | **T112, sur appareil réel** : installer sur un Android puis un iPhone, créer le compte depuis l'application installée, ouvrir le courriel — sur iPhone il s'ouvre dans Safari, et l'application doit relire l'état seule au retour —, vérifier `client_kind` après une nuit, et dérouler le § 2 en 3G lente. **Puis 0c**. *Trois codes d'exemple ont été semés dans la base locale pour la recette* (`NEGO-024` du réseau, `GENE-100` général sur l'espace Climat, `STOP-999` révoqué) : le semis de développement les prévoyait, mais la base a été **migrée** et non rechargée. Ils y restent, utiles au développement |
| 2026-09-22 | **0b, phase 7 (US5) livrée** (T097 à T103 ter), **plus deux ajouts demandés le jour même**. Le verrou d'un module réservé — `GnVerrou`, **le seul composant nouveau de l'étape** — et « Mon accès » dans ses cinq états. **La sortie du verrou suit le mode d'admission**, et non l'écran qui le pose : un composant qui enverrait toujours vers la saisie du code proposerait, en « approbation seule », un champ qui n'ouvre rien (FR-022), et renverrait saisir un code à quelqu'un qui attend déjà une réponse. La décision vit dans `utils/guide-nego/verrou.ts`, **pure et éprouvée sans navigateur** — c'est aussi ce qui permet de prouver FR-032 : un `granted` bricolé dans la garde locale, un champ inventé, un état recopié d'hier n'ouvrent rien, seul `state` décide, et `etatDAcces` ne retient que ce que l'API a dit. **Deux ajouts.** (1) *L'enchaînement depuis le verrou*, laissé ouvert par T093 bis : il lit `demandePossibleMaintenant` et `codeOffert`, et mène au compte, au code, à la demande ou au retour à une demande en attente ; le § 5 du quickstart porte désormais son point de recette, avec la bascule de mode pendant l'essai. (2) *La section « Guide Négo » du menu du back-office se masque* sans `negotiation.space.manage` sur la portée globale — **un champ facultatif sur `NavSection`, lu une fois par le layout**, jamais un test par page. Sans lui, un administrateur d'événement voyait trois entrées menant à trois « accès refusé » : un menu qui affiche ce qu'il fera refuser dit à la personne qu'il existe quelque chose qu'elle ne peut pas voir (SC-008). Les autres sections ne déclarent aucune permission et paraissent toujours. **Un écart inscrit (39)** : la maquette dessine deux sorties au verrou et **suppose un compte** ; sans compte, « Saisir mon code » mènerait à un écran qui redemande d'abord de s'en créer un (FR-031) — le verrou en porte donc trois dans ce seul cas, deux sinon. **Trois écarts rattrapés au passage** : 36, 37 et 38 étaient annoncés au journal du 21/09 mais n'avaient jamais été portés dans la table de `05-design.md` — un écart annoncé et non inscrit ne vaut rien. **SC-006 prouvé, et le résultat est ici** : aucun champ, aucun libellé, aucune réponse d'API de ce périmètre ne porte le genre. Le parcours ne demande pas `civility` ; `me/access` ne rend que portée, dates, libellé de code, réseaux et demande ; les seules occurrences du mot « genre » dans les traductions sont la **thématique de négociation** (« plan d'action genre », filtre de thématique), et l'appartenance au réseau vient du **code utilisé**, de rien d'autre. La maquette écrit « Négociatrice — réseau » sous la ligne de menu du profil : cette formule est genrée et **n'a pas été reprise** — la ligne porte l'état d'accès. Éprouvé : 70 tests `node --test` (dont 6 nouveaux sur le verrou), `typecheck`, `check:guide-nego` (91 fichiers, 54 paires), `make check-api-contract` (164 appels), clés `fr`/`en` comparées sur **les 105 fichiers** — seul subsiste l'écart préexistant de `pages/home.json`, déjà aux points ouverts | **Phase 8, la recette** (T104 à T113). Trois points ne se font pas depuis un poste : T105 (répéter la migration sur une copie, puis recomparer les schémas), T112 (Android réel puis iPhone : installation, retour du courriel, `client_kind` après une nuit, 3G lente) et le chronomètre de T111 (SC-001, trois minutes). **T110 exige l'API arrêtée** : les tests d'`identity` se sont montrés sensibles à une activité concurrente sur la base |
| 2026-09-22 | **0b, phases 5 et 6 (US3 et US4) livrées** (T067 à T096) : l'IFDD tient ses codes, et durcit l'admission sans redéployer. **Les douze routes du back-office exigent la portée globale** — `Requires<SpaceManage>`, et jamais `RequiresAnyScope` : le rôle `admin` porte cette permission et s'attribue **aussi sur un événement**, si bien que « n'importe quelle portée » laisserait entrer l'administrateur d'une seule édition sur une route qui *paraîtrait* gardée. `perimetre_url_forgee.rs` le tient par deux bouts — ce que la base répond à un administrateur d'édition, **et ce que les trois fichiers de routes déclarent**, commentaires retirés : monter la vraie application pour l'éprouver en HTTP demanderait une arête vers `api` que `cargo tree` refuse (principe II). **Révoquer n'est pas retirer** (ADR-006), et l'écran le montre : deux boutons, deux confirmations, et le nombre d'accès encore ouverts affiché **avant** la révocation — pas après. **Admettre est une seule transaction** : l'état de la demande, l'attribution du rôle, l'appartenance au réseau venue du code que la demande portait, l'annuaire de l'espace et le courriel mis en file ; l'événement, lui, part **de la base** par `tg_access_request_event()`. **Le mode se relit à chaque tentative, sans cache** — la bascule se voit à l'entrée suivante, prouvé sur le même état de module, sans redémarrage (SC-002). **Cinq décisions prises en construisant.** (1) *Le retrait d'accès sort aussi de l'annuaire de l'espace* : `space_members` n'accorde rien, mais laisser quelqu'un dans la liste des membres d'un espace dont l'accès vient de tomber ferait mentir l'écran qui la montre. (2) *La portée retirée est celle du code*, jamais « toutes celles de la personne » : une négociatrice admise sur la COP31 par un code et sur Guide Négo entier par un autre ne perd que le premier — un test le vérifie sur trois comptes. (3) *`GET /admin/negotiation/admission` rend des **faits**, pas des phrases* — `offers_code`, `code_opens`, `needs_approval` — et l'écran compose son texte par ses fichiers de traduction : rendre du français depuis l'API donnerait deux catalogues pour un même écran (FR-045). (4) *Le réglage s'écrit en `INSERT … ON CONFLICT`*, jamais en `UPDATE` : sur une base dont le semis n'aurait pas été rejoué, l'`UPDATE` échouerait **en silence** — l'écran dirait « enregistré » et la tentative suivante lirait l'ancien mode. Et `updated_by` est posé à la main, `platform.settings` n'ayant pas de déclencheur d'audit : sans cela, la bascule serait le seul geste du back-office dont on ignorerait l'auteur. (5) *Le champ de code **disparaît** en « approbation seule »* (FR-022), là où l'absence de réseau le laisse visible en disant ce qui manque — `codeOffertParLeMode` et `saisiePossible` sont deux prédicats distincts pour cette raison, et les confondre ferait disparaître le champ à chaque tunnel. **Guide Négo gagne sa section au menu du back-office** plutôt qu'une entrée dans « Exploitation » : ces trois écrans commandent l'entrée d'une autre application. Éprouvé : **21 suites Rust sur base réelle** dans `negotiation` (dont la concurrence de deux demandes simultanées, la transition refusée par le trigger **sans passer par le service**, et l'audit ligne par ligne), 64 tests `node --test`, `make openapi` (155 formes, toutes définies), `make check-api-contract` (164 appels), `check:guide-nego` (88 fichiers, 54 paires), `typecheck`, `make check-safe` | **Phase 7 (US5)** : `GnVerrou` et « Mon accès » (T097 à T106). Deux choses sont **déjà en place pour elle** : `useGnAcces().demandePossibleMaintenant` et `codeOffert`, que le verrou n'aura qu'à lire pour proposer la demande plutôt que le code. Puis la **phase 8** : recette, traductions `en` relues, non-régression du site, planche de composants. `make check-safe` en fin de cycle |
| 2026-09-21 | **0b, phase 4 (US2) livrée** (T048 à T066) : le code d'invitation ouvre les modules réservés. **Les neuf issues sortent en 200**, chacune avec son discriminant et son message **composé par l'API** — elle seule connaît la date de révocation et le temps d'attente restant ; un second catalogue côté écran donnerait deux textes pour un même refus. L'écran ne choisit que le ton et les sorties. **Le quota est tenu par la base** : `ck_invitation_codes_quota` et le verrou de ligne, jamais un `SELECT used_count` que la seconde requête contournerait. **Les essais se comptent par personne**, tous appareils confondus, et un test le prouve en changeant d'identifiant d'appareil au sixième essai. `GET /me/access` rend l'état **dérivé du RBAC** en une lecture, avec son `ETag`. Côté écrans : `code.vue` (étape 2 sur 3), `useGnAcces`, et `GnEtapes`, l'indicateur d'étapes que les deux écrans partagent. **Trois défauts trouvés en construisant, dont deux dans le modèle que la phase 1 avait laissés passer.** (1) *Interblocage sous concurrence* — mesuré sur quatre entrées simultanées, pas deviné : l'insertion d'un usage prend d'abord un verrou **partagé** sur la ligne du code, par la clé étrangère, et le trigger d'incrément tente ensuite de le hausser en **exclusif**. Deux transactions s'attendaient l'une l'autre, PostgreSQL en tuait une, et la seconde personne recevait une **panne** au lieu du « code épuisé » que FR-038 bis lui promet. Le service prend maintenant le verrou explicitement, dans le bon ordre, et le modèle porte l'avertissement. (2) *Le verrou d'essais ne finissait jamais* : `invitation_attempts_recent()` comptait les essais `throttled`, qui sont eux-mêmes des essais non acceptés — chaque appui pendant le verrou faisait repartir la fenêtre, et le quart d'heure annoncé à l'écran était un mensonge pour qui insiste. La fonction ne compte plus que les essais **consommés** ; la ligne reste écrite, l'administrateur doit voir qu'on a continué de frapper. (3) *`invitation_code_uses.session_id` n'aurait jamais été renseignée* : la session ne sortait pas du module `identity`, dont `negotiation` n'a pas le droit de dépendre. `RequestContext` la porte désormais, posée par l'intergiciel — quinze lignes, et une colonne du modèle cesse d'être morte. **Trois écarts inscrits** (36, 37, 38) : l'indicateur d'étapes absent de la planche, le code à sept caractères de la maquette contre huit dans son propre texte, et la sortie « Demander l'accès à l'IFDD » que US4 apportera — un bouton menant à une adresse inexistante serait pire que son absence, et chaque issue garde au moins une suite. **`?since=` écarté**, et dit : l'état vient de quatre tables et de `now()`, aucune colonne ne porte l'instant où il a changé, et un 304 fautif laisserait ouverts sur le téléphone les modules d'un accès retiré — l'`ETag` de contenu, lui, ne peut pas se tromper. Éprouvé : 13 tests Rust sur base réelle dans `negotiation` (dont la concurrence et l'audit), 4 tests HTTP de bout en bout dans `api`, 7 tests `node --test`, `make openapi`, `make check-api-contract` (151 appels, 143 formes), `check:guide-nego` (87 fichiers, 54 paires de contraste), `typecheck`, `cargo clippy -D warnings` | US3 (T067 à T079) : le back-office des codes — créer, lister, révoquer, voir les usages, retirer un accès. **Le générateur de code est déjà écrit** (`domain/code.rs`, huit caractères dont sept tirés au sort) : T072 n'a qu'à l'employer. **Et le périmètre reste à trancher** : `identity.administered_events()` rend des **éditions**, quand un code porte une portée `global` ou `negotiation_space` — les douze routes de T067 ont besoin d'un filtre qui n'existe pas encore. `make check-safe` en fin de cycle |
| 2026-09-21 | **0b, phase 3 (US1) livrée** (T024 à T047) : entrer avec son compte. **La session dit d'où elle vient** — un objet `client` facultatif sur la connexion et l'inscription, `identity.sessions` qui porte le client et l'appareil, et `GET /auth/me` qui rend la session courante sans requête de plus. **Quatre-vingt-dix jours glissants pour l'application**, sans case à cocher : `AUTH_SESSION_TTL_APP`, et la durée repart à chaque rotation — une durée fixe ferait expirer en pleine COP un compte ouvert des semaines plus tôt. **Les liens des courriels ramènent dans Guide Négo** : le client est retenu **avec le jeton**, jamais relu d'une session — au moment où le lien s'ouvre, la personne n'en a pas encore. **Le piège n° 1 est verrouillé par deux tests** : la rotation recopie le client et l'appareil de la session remplacée, et survit à deux tours. Sans eux, toute session d'application redevient « web » au premier renouvellement, **sans qu'aucune erreur ne le dise**, et le décompte des téléphones mente. Côté écrans : `compte` (avec l'attente de confirmation), `connexion` (feuille « mot de passe oublié »), `mot-de-passe-oublie`, `verification-adresse` et `nouveau-mot-de-passe`, plus la déconnexion dans les réglages, derrière une confirmation qui dit ce qu'elle laisse sur le téléphone. **Deux défauts trouvés en construisant, que le plan ne montrait pas.** (1) *Le témoin de session du site se contredisait* : douze heures de cookie pour une session de quatre-vingt-dix jours — au treizième, `ensureLoaded()` n'osait plus tenter la rotation et l'application se croyait déconnectée sans l'être, au pire moment. Sa durée suit maintenant le client. (2) *L'écart 34 promettait plus que le modèle ne permet* : « l'application relit l'état et passe à la suite » ne peut pas valoir au retour du courriel de vérification, puisque **l'inscription n'ouvre aucune session** et qu'aucune route ne peut dire « cette adresse est-elle confirmée ? » sans révéler qu'un compte existe. La suite après confirmation est donc **la connexion**, adresse reportée — l'écart est corrigé en ce sens. **Un écart de plus, inscrit (35)** : le pays se choisit dans la liste native du téléphone, dans le créneau de `GnChamp`, là où la maquette dessinait un champ de texte — 249 pays ne se saisissent pas à la main. **Rien n'a été redessiné** : les vingt-sept composants de 0a suffisent, et le seul ajout — `GnMotDePasseOublie` — sert **deux hôtes**, la feuille basse de la connexion et l'écran qu'on atteint par un lien direct. L'objet `client` se joint **en un seul endroit**, `useGnSession` et `api/guide-nego.ts` : un écran qui l'oublierait ouvrirait une session de douze heures marquée « site ». Éprouvé : 13 tests Rust nouveaux ou étendus sur base réelle, 10 tests `node --test`, `npm run typecheck`, `npm run build`, `check:guide-nego` (83 fichiers, bornage tenu ; 54 paires de contraste), `make check-api-contract` | US2 (T048 à T066) : le code d'invitation, ses neuf issues en 200, et le compteur d'essais **par personne**. **Deux points à reprendre là.** (1) `utils/guide-nego/parcours.ts` porte `APRES_LE_COMPTE = '/guide-nego'` — trois écrans y renvoient, et T062 doit le faire pointer sur l'écran de saisie du code. (2) **`frontend/app/composables/useApi.ts` est à 964 lignes** : le garde-fou est à mille, et le prochain bloc le franchit. Le bloc `auth` y est désormais une constante — c'est le premier candidat à sortir dans `composables/api/auth.ts`, sans rien changer à ses appelants. `make check-safe` en fin de cycle |
| 2026-09-21 | **0b, phases 1 et 2 livrées** (T001 à T023). **Le modèle d'abord** : `identity.sessions` reçoit `identity.session_client` et ses quatre colonnes d'appareil, `reference` la taxonomie `negotiation_network`, et `100_negotiations.sql` un § 2 bis de cinq tables et deux vues — codes d'invitation, usages, appartenance au réseau, demandes d'accès, essais. **Aucune de ces tables ne porte d'état d'accès** : `identity.role_assignments` fait foi. Le quota tient par `ck_invitation_codes_quota` et le verrou de ligne du trigger, la machine à états des demandes par un trigger, et l'événement d'outbox part dans la transaction de la décision. **La base locale n'a pas été détruite** : `pg_dump` d'abord, puis `migration.sql` joué **deux fois** — la seconde passe ne crée rien et n'échoue nulle part. Contrôle du § 13 de `DEPLOIEMENT.md` : le `pg_dump --schema-only` des schémas `identity` et `negotiation` de la base migrée est **identique**, ligne pour ligne, à celui d'une base chargée de zéro depuis `docs/database/`. **Trois corrections trouvées en faisant, que le plan seul ne montrait pas.** (1) La migration semait la description de la taxonomie en **texte simple** là où la colonne est un `platform.i18n_text` : elle aurait échoué à la première exécution. (2) Les quatre colonnes de session sont écrites **en fin de table** dans le modèle — elles arrivent par `ALTER` sur une base en service, et `pg_dump` compare les colonnes dans leur ordre d'ajout ; les placer « au bon endroit » aurait fait diverger les deux schémas pour toujours. (3) Le bloc `negotiation` de la migration est désormais **dérivé mécaniquement** du § 2 bis du modèle : un mot qui diffère dans un `COMMENT` ressortirait comme un écart et le seul contrôle dont on dispose perdrait son sens. **Le semis de développement est verrouillé** : `docs/database/` est chargé **aussi en production**, et deux codes d'invitation écrits dans un dépôt public y ouvriraient l'espace réservé à qui les lit. Le § 6 bis de `900_seed.sql` ne s'exécute que si le serveur porte `epavillon.seed_dev=on`, que `ops/docker-compose.dev.yml` pose et que la production ne pose pas. **Puis le crate** : `negotiation` naît, monté dans l'API (état, `app_data`, liste figée de `modules.rs`, document OpenAPI) et dans le worker, avec ses deux permissions typées, ses six contrats d'événements et cinq codes d'erreur stables — le contrat d'API les porte. `routes` et `admin_routes` sont **vides et plates** : `/admin` est un préfixe partagé, un `web::scope` y rendrait muettes les routes de cinq autres modules. **Deux tests posés au-delà des tâches**, et ils ont servi : `tests/frontieres.rs` prouve mécaniquement qu'aucune arête ne relie `negotiation` à un autre crate de module (principe II), et `tests/decor.rs` fait compiler et tourner le harnais sur base réelle — sans lui, `tests/commun/mod.rs` n'aurait été compilé par personne avant le premier test d'US1. `make check-db-safe` au vert, `make openapi` passé, l'API démarre et le module est monté | US1 (T024 à T047) : la session qui dit d'où elle vient, les courriels qui ramènent dans l'application, et les cinq écrans de compte. **Les deux tests qui comptent y sont T025 et T026** — la rotation qui recopie le client, et la durée de 90 jours : sans eux rien n'échoue et les chiffres mentent. `make check-safe` en fin de cycle, un commit par phase |
| 2026-09-21 | **Tâches de 0b (123) et analyse de cohérence**, corrections appliquées. Les quatre corrections du commanditaire d'abord : **la base ne se détruit plus** — `specs/009-guide-nego-compte-admission/migration.sql` livré, rejouable, contrôlé par comparaison de schémas contre la base modèle du harnais (§ 13 de `DEPLOIEMENT.md`), et `down -v` retiré partout ; **session d'application à 90 jours glissants** d'office, l'écran de connexion n'ayant pas de case « se souvenir de moi » et 12 h déconnectant une négociatrice en salle ; **les liens des courriels ramènent dans Guide Négo** — trois écrans de plus, et sur iPhone la page confirme puis renvoie, l'application relisant l'état à son retour au premier plan (**écart 34** inscrit à `05-design.md`) ; **deux verrous en base** — essais comptés **par personne** (`device_id` se forge) et `ck_invitation_codes_quota` avec le verrou de ligne du trigger. **Puis l'analyse a trouvé deux manquements à la constitution** : le test d'URL forgée ne couvrait que 7 des 12 routes d'administration (principe V), et **aucune tâche ne vérifiait la trace de l'auteur** (principe VII) — `identity` a son test, `negotiation` n'en avait aucun. Dix tâches ajoutées. **Cinq contradictions corrigées** : la description semée en base disait encore « par personne et par appareil », contredisant l'exigence sous les yeux de l'administrateur ; six issues annoncées contre neuf réelles ; « révoquée » employé pour une demande alors qu'il qualifie un accès retiré — `cancelled` se dit désormais **« annulée »** ; une contrainte du modèle absente de la migration **et tautologique** ; et l'exemple de code `NEGO-24` de la maquette, qui porte **sept** caractères sous la phrase « vérifiez les huit caractères » — huit fait foi, l'exemple devient `NEGO-024`. Comptes rectifiés : 16 routes (non 12), 13 écrans, 5 tables, 52 exigences, 14 critères | `/speckit-implement` par tranches : phase 1 (SQL **et migration**), phase 2 (le crate), puis US1, US2, US3, US4, US5. `make check-safe` en fin de cycle, un commit par phase. **Trois arbitrages retenus faute d'avis contraire** : « annulée » pour une demande, code à huit caractères, et verrou posé seulement là où un écran existe — chaque étape suivante y pose le sien |
| 2026-09-21 | **Plan de 0b** : `plan.md`, onze points de recherche, le modèle de données, trois contrats, le guide de vérification. **La découverte qui change le plan** : il n'y a **pas de crate `negotiation`** dans `backend/crates/modules/` — il naît à cette étape, et le socle l'attend déjà (`ScopeType::NegotiationSpace` dans `kernel`, `negotiation.spaces` lue par `identity/repo/rbac.rs`, garde média `("negotiation","documents")`, ligne au registre `platform.modules`). Onze fichiers à toucher pour le monter, tous repérés. **Tranché** : (1) *la session dit d'où elle vient* par un objet `client` **facultatif** dans le corps de `login` et `register` — le site ne bouge pas —, et **la rotation recopie le client** : sans cela toute session de l'application redevient « site » au premier renouvellement et les chiffres mentent sans que rien n'échoue ; (2) *les refus d'un code sortent en **200** avec leur discriminant et leur message composé par l'API*, comme `mfa_required` le fait déjà — six refus prévus par le parcours ne sont pas six pannes, et en 4xx l'écran perdrait ses deux sorties ; (3) *la portée d'un code n'a rien demandé d'inventé* : `negotiation_space` ou `global` sont exactement les deux `allowed_scopes` du rôle `negotiator` ; (4) *les essais de code se comptent en base*, pas dans Valkey — **Valkey n'est câblé nulle part dans `backend/`** et l'y introduire serait une dépendance d'ampleur ; `01-stack.md` corrigé ; (5) *le courriel de décision vit dans le crate `negotiation`*, mis en file par `jobs::enqueue` **dans la transaction de la décision**, jamais par un appel vers `identity` ; (6) *un seul état de session* — Guide Négo réutilise le store `auth` du site, le garde-fou n'interdisant que les composants et les cookies nommés, pas la session. **Côté front, un seul composant manque** : le verrou d'un module réservé, que la planche déclare elle-même absent ; les vingt-deux autres sont livrés et ne se redessinent pas. **Cinq pièges écrits au plan**, dont la rotation qui efface le client, un cache sur le mode d'admission qui tuerait le critère de sortie, et l'unicité du code qui doit couvrir les révoqués — sans quoi l'écran « 04c » dirait « code inconnu » au lieu de « révoqué le 8 novembre » | `/speckit-tasks`, `/speckit-analyze`, puis `/speckit-implement` par tranches. **Le SQL d'abord**, base rechargée à neuf, et `docs/progression/modele.md` — la seule écriture hors `AppNego` (ADR-017). 0a garde sa recette sur appareil réel |
| 2026-09-21 | Spécification de **0b — Compte et admission** : `specs/009-guide-nego-compte-admission/`. Cinq récits — le compte, le code, les codes tenus par l'IFDD, l'admission par approbation, le verrou et « Mon accès ». **Deux questions posées au commanditaire, deux réponses.** (1) *La portée d'un code* : l'administrateur choisit à chaque création s'il ouvre **une COP précise** ou **Guide Négo en entier** — défaut proposé, l'édition en cours ; un accès lié à une COP n'ouvre que ses modules, un accès sans limite ne cesse que par un retrait explicite. (2) *La réponse à une demande* : **par courriel**, à l'admission comme au refus, la décision restant acquise si l'envoi échoue — **écart assumé** avec l'écran « 05 Demande en attente » de la maquette, qui annonce une notification ; il dira « un courriel » tant que le centre de notifications (3b) n'existe pas, **à inscrire aux écarts de `05-design.md`**. **L'état du modèle, relevé avant d'écrire** : n'existent pas les codes d'invitation partagés, le rattachement usage ↔ code, les demandes d'accès, l'appartenance au réseau, la limitation des essais de code, la clé du mode d'admission, ni le type de client et l'appareil sur `identity.sessions` (ADR-001 l'annonçait). Existent déjà et ne sont pas à créer : la permission `negotiation.space.access`, le rôle `negotiator`, la portée `negotiation_space`, `identity.has_permission`, `platform.settings`, et toute l'authentification du site — inscription, vérification, connexion, réinitialisation, rotation des sessions. Tranché aussi : la révocation d'un code **ne retire pas** les accès déjà donnés (ADR-006, deux gestes distincts) ; une personne déjà admise qui saisit le code du réseau gagne l'appartenance sans second accès ; aucun écran ne demande ni n'affiche le genre (ADR-007) | `/speckit-plan` de 0b avec le prompt commun. **Le SQL d'abord** : `030_identity.sql` (client et appareil), `100_negotiations.sql` (codes, usages, demandes, réseau), `900_seed.sql` (mode d'admission). 0a garde sa recette sur appareil réel |
| 2026-09-21 | 0a, T071 **déroulée au navigateur** (Chrome visible, version construite servie sur l'adresse publique), installations mises de côté : elles se feront à la main sur les appareils. **Un défaut trouvé et corrigé, que seul ce déroulé pouvait montrer** : « Prête hors connexion » ne paraissait **jamais à la première ouverture**, seulement à la seconde. La mise en page attendait `navigator.serviceWorker.ready`, qui n'est tenu que lorsque la garde **contrôle** la page — ce qui n'arrive qu'au chargement suivant. Autrement dit, la personne qui ouvre, entre en visiteur et referme — le geste même que décrit le guide — n'apprenait jamais que l'application tiendra sans réseau, et c'était justement l'ouverture qui avait pris le temps du réseau. `gardeConstituee()` attend désormais l'**activation** de l'inscription ; mesuré après correction : message à la première seconde, clé posée. FR-012 bis et `contracts/hors-connexion.md` rectifiés — le contrat nommait `ready`. **§ 1 entier** : les sept gestes du drapeau, dont `rollout_percent = 50` qui ferme bien l'application (pas de déploiement progressif sans compte), l'API rendue muette qui la laisse ouverte avec « Hors connexion — lu à 14:02 », et le cinquième onglet « Échanges » en quatrième position, qui disparaît quand `negotiation.channels` s'éteint. **§ 2 sans les installations** : 92 adresses de la garde servies en 200 sans redirection ; ouverture complète en mode avion, police et pictogrammes compris ; retour du réseau → « Synchronisé à » en moins d'une seconde **sans recharger** ; thème sombre appliqué à l'application seule, `<html>` et le cookie du site intacts, et fond `#101704` posé **avant** le rendu (aucun éclair en mode avion) ; cookie `epavillon_locale=en` → Guide Négo en français, cookie inchangé, site en anglais (**R3 revérifié**) ; bascule de version : deux caches puis un seul après deux rechargements, la nouvelle version s'ouvre en mode avion ; **aucune requête vers une autre origine** que l'application et l'API ; drapeau éteint pendant l'essai → l'application se ferme **malgré** la garde complète. **§ 3** : sept sections, aucun débordement horizontal à 320 · 360 · 390 px, barre d'onglets mesurée (4 onglets à 320 px : 321 px, rien ne défile ; 5 onglets : 358 · 361 · 391 px, seule la barre défile à 320 px et à l'équivalent d'un zoom de 130 %, jamais tronquée ni sur deux lignes, actif toujours dans la vue), anneau de focus de 3 px partout, arc et squelette **fixes** sous « réduire les animations », 57 symboles distincts et 42 états, et **les 28 couleurs de la maquette toutes présentes** — les deux seules en plus sont les écarts déjà arbitrés 4/27 et 33 | **Sur appareil réel** : l'installation Android puis iPhone, et le débit bridé « 3G lente ». Deux points à savoir avant : hors connexion, l'adresse **sans barre oblique finale** (`…/guide-nego`) tombe sur l'erreur du navigateur — elle est hors de la portée de la garde ; le lancement depuis l'icône utilise `./` et n'est pas touché, mais l'adresse se communique **avec** la barre oblique. Et l'ancienne version n'a basculé qu'après un `update()` explicite au premier contrôle, puis normalement aux rechargements suivants : à confirmer sur un vrai téléphone |
| 2026-09-21 | 0a, phase 8 : la recette, **sauf l'appareil réel**. **Traductions** — les 34 fichiers `en` portent exactement les clés de `fr`, aucune manquante ni en trop ; deux valeurs de démonstration de la planche de saisie restaient en français et passent par des clés (`champ-pays-valeur`, `zone-valeur`). Ce qui reste en dur est assumé et vérifié un par un : « Aa » et les spécimens de glyphes (des glyphes, hors échelle de texte — `05-design.md` règle 8-9), les noms de jetons `--gn-*` que les planches affichent **comme données**, « Guide Négo » (nom du produit, dans le manifeste et le titre d'onglet), et deux messages d'erreur internes qu'un `catch` vide avale sans jamais les afficher. **Non-régression du site, prouvée au navigateur sur construction** : accueil, back-office (tableau de bord, connectée) et `/negociations` identiques en clair et en sombre ; `/negociations` → `/maintenance/negotiation` et `/communaute` → `/maintenance/directory` comme avant — les quatre autres modules n'ont pas de page, donc 404, ce qui est leur état d'avant. **La preuve décisive du service worker** : après visite du site seul, aucun service worker et aucun manifeste ; après visite de Guide Négo, portée `…/guide-nego/` ; **de retour sur le site, la page n'est pas contrôlée** et n'a pas de manifeste. Vérifié aussi que `data-theme` reste absent de `<html>` et que les composants du site gardent leurs noms malgré la liste explicite de `nuxt.config.ts`. `make check-safe` au vert — 224 suites, zéro échec. Procédure d'ouverture écrite au § 14 de [DEPLOIEMENT.md](../DEPLOIEMENT.md), avec sa raison : `is_feature_enabled` n'a que `rollout_percent = 100` de vrai quand la personne est anonyme, et Guide Négo s'ouvre sans compte — **un drapeau à 50 % n'ouvrirait l'application à personne** | **T071**, sur un Android réel puis un iPhone : le service worker, l'installation et le mode avion ne se laissent pas éprouver depuis un poste de travail. Puis 0b. **Deux points à garder en tête** (aucun effet aujourd'hui, vérifié) : `spa-loading-template.html` écrit un fond sur `<html>` qui survivrait à une navigation de Guide Négo vers le site — il n'existe aucun lien de ce genre ; et les liens `/aide`, `/contact`, `/a-propos`… du pied de page du site ne mènent à aucune page, défaut **antérieur** à cette branche |
| 2026-09-21 | 0a, phase 7 (US5) livrée : la planche de design, `/guide-nego/composants`, en `noindex` et sans lien depuis l'application. **Onze composants de plus** — état d'erreur, chargement (arc et squelette), les trois champs de saisie, case, interrupteur, cercle de choix, onglets de filtre, pilule, marque d'état, étiquette, ligne d'information, feuille basse, boîte de confirmation — et les sept sections de la planche, avec bascule clair · sombre · côte à côte. Les 42 états ont leur vitrine, un par jeton `--gn-etat-*` : un état sans vitrine est un état que personne ne vérifie. **Mesuré au navigateur, pas à l'œil** : cinq onglets tiennent à 360 px (49 · 84 · 87 · 64 · 74) et à 390 px, quatre tiennent à 320 px sans défiler, et à 130 % de police la barre seule défile, rien n'est tronqué, l'onglet actif reste dans la vue. **Cinq défauts corrigés en chemin.** (1) Le bord d'un champ au repos suit la maquette — gris, et vert foncé au focus : l'anneau seul se perd au soleil. (2) La barre d'onglets ne redressait son défilement qu'au montage : un `ResizeObserver` le refait quand la place change, sans quoi l'onglet actif sortait de la vue à la première police agrandie. (3) `GnBouton` gagne un état de chargement — l'assemblage à la main plaçait une région vivante `role="status"` **dans** un `<button>`. (4) `GnPicto.nom` devient l'union des 57 symboles : un nom fautif donnait une icône vide sans la moindre erreur. (5) Trois jetons manquaient et étaient contournés par des espacements détournés — `--gn-squelette-hauteur`, `--gn-etiquette-air`, `--gn-colonne-largeur`. **Un piège d'outillage à retenir** : `vue-tsc -p tsconfig.json` ne vérifie RIEN (`"files": []`, références seules) ; la porte est `npm run typecheck` | Recette : T069 à T073 — traductions `en`, non-régression du site, quickstart sur un vrai Android puis un iPhone, `make check-safe`, déploiement |
| 2026-09-21 | 0a, phase 6 (US4) livrée : thème clair, sombre ou système. « Profil et réglages » n'ouvre que « Affichage → Thème », par un sélecteur segmenté utilisable aux flèches du clavier. Le choix tient dans `gn.theme`, `data-theme` se pose sur l'élément de l'application et jamais sur `<html>`, la barre système suit, et `app/spa-loading-template.html` pose le bon fond avant tout affichage — vérifié : `#101704` posé avant le rendu, gabarit absent des pages du site. **Thèmes indépendants, prouvé en croisé** : application forcée en clair, navigateur et site en sombre, cookie `epavillon_theme` inchangé à `system`. **Deux défauts trouvés en chemin.** (1) Une régression de la phase 5 : déclarer le service worker comme dossier de fichiers publics faisait rendre **404 à toute adresse profonde** de `guide-nego/` — le cache du service worker la masquait dès la deuxième ouverture, mais une première visite sur un lien partagé échouait. `sw.js` se copie maintenant dans la sortie après Nitro (`nitro:build:public-assets`). (2) Le site colore tous les `h1…h6` par une règle d'élément, qui atteignait nos titres : `base.css` reprend la main sous la borne — FR-026 vaut dans les deux sens. **Écart 33 arbitré** : le rouge de charte porte du texte blanc à 6,0:1, sous les 7:1 exigés ; l'aplat du bouton dangereux passe à `#B30E1A` (7,0:1), teinte inchangée, et le rouge de charte reste pour les textes et pictogrammes | US5 : la page des composants (T058 à T068), puis la recette (T069 à T073) |
| 2026-09-21 | 0a, relecture de la phase 5 : quatre corrections au hors-connexion. **La panne muette devient bruyante** — `scripts/guide-nego-verifier-garde.mjs <adresse>` demande les 76 adresses gardées et refuse tout ce qui n'est pas un 200 franc, redirection comprise (un navigateur refuse de servir une réponse redirigée à une navigation) ; branché sur `deploy.sh` après `update` et `deploy`, et inscrit au § 6 de `DEPLOIEMENT.md`. **Les caches de la coquille s'appellent `gn-coquille-<version>`** et le ménage ne vise que ce préfixe : les documents que gardera l'étape 1 ne partiront pas au premier déploiement — règle écrite dans `contracts/hors-connexion.md`. **La bascule de version se fait au chargement d'une page** : la page écrit « prendre-la-main » au worker en attente, il répond par `skipWaiting()`, la page se recharge une fois — sans quoi une version installée attendait une fermeture qui, sur un téléphone, ne vient jamais. FR-016 bis, le contrat et ADR-019 ajustés ; **T051 vérifiée au navigateur** : deux caches puis un seul, plus rien en attente. **Ne se retélécharge que ce qui a changé** : un fichier sous `_nuxt/` se reprend d'un cache plus ancien, seuls la page vide, les traductions, le manifeste et les icônes partent au réseau ; une installation interrompue reprend où elle s'était arrêtée. Le calcul de la liste vit maintenant dans `guide-nego/liste-de-garde.ts`, et le service worker s'éprouve hors du navigateur avec de faux `caches` — **40 tests** | US4 (thème), puis US5 (page des composants) |
| 2026-09-21 | 0a, phase 5 (US2) livrée : l'application s'ouvre en mode avion. Service worker engendré à la construction par `modules/guide-nego-garde.ts` — **76 fichiers, 1,09 Mo**, en adresses relatives, borné à `guide-nego/`. Deux pièges levés : les imports dynamiques de l'entrée entraînaient le site entier (259 fichiers) — on ne suit les dynamiques que depuis Guide Négo ; et `@nuxtjs/i18n` v10 ne met pas les messages dans le paquet quand le site rend côté serveur, il les demande à `/_i18n/<empreinte>/fr/messages.json` — sous `ssr: false` personne ne les avait injectés, et l'application s'ouvrait hors connexion en affichant ses clés. L'empreinte se lit dans le paquet qui vient d'être écrit ; introuvable, la construction échoue. Vérifié au navigateur sur construction, sans préfixe puis avec `NUXT_APP_BASE_URL=/v2/` : garde complète, ouverture en mode avion avec textes, police et pictogrammes, **aucune requête vers une autre origine**, portée `…/guide-nego/`, et le site sans service worker ni manifeste. Nouvelle construction : l'ancienne version sert toujours, deux caches, la nouvelle reste « en attente » — pas de `skipWaiting`. Bandeau hors connexion une fois par épisode, rappel dans l'en-tête, « Synchronisé à » au retour du réseau sans recharger (événement `online` → relecture) | US4 (thème), puis US5 (page des composants). **T051 reste à faire** : débit bridé et bascule effective de version demandent un vrai appareil — la fermeture d'une session Playwright vide son profil |
| 2026-09-21 | 0a, phase 4 (US1) livrée : la coquille s'ouvre sans compte. Écran d'ouverture (trois niveaux d'accès, « Continuer en visiteur » seul actif), page d'installation (adresse réelle, étapes Android et iPhone, proposition du navigateur sinon les étapes prennent le focus), cinq écrans d'onglet en état vide, lexique derrière « Aa », « Profil et réglages » dans Ressources. Composants : `GnEcran`, `GnEntete`, `GnBarreOnglets`, `GnEtatVide`, `GnBouton`, `GnEnteteGroupe`, `GnLigneReglage`. Mesuré au navigateur à 360 px : 51 · 85 · 87 · 64 · 74 = 361 pour cinq onglets, aucun libellé tronqué ni sur deux lignes, la page ne défile pas de côté ; à 320 px quatre onglets tiennent, cinq font défiler la barre seule. Manifeste et cinq icônes engendrées (le « e » du symbole vectorisé : librsvg substituait la police en silence). Vérifié : le site n'a ni manifeste ni service worker, et le cookie `epavillon_locale=en` laisse Guide Négo en français | US2 (T043 à T051), puis US4 et US5. La ligne « Profil et réglages » mène à un écran qui n'existe qu'en US4 (T056) |
| 2026-09-21 | 0a, phases 1 à 3 livrées, `make check-safe` au vert, un commit par phase. `guide_nego.enabled` semé (et inséré à la main en local) ; `/guide-nego/**` rendu côté navigateur ; R3 vérifié — la route s'affiche en `fr`, mais `@nuxtjs/i18n` réécrivait le cookie de langue du site : `plugins/guide-nego-langue.client.ts` le rétablit. Fondations : thème, mesures, police, sprite, huit rôles sombres mesurés (tableau dans `05-design.md`), `#gn-portail`. Contrôles `check-guide-nego` (bornage, feuilles et `<style>`), contrastes (50 paires), `node --test` (21). Drapeau : registre du site étendu (`closedPath`, `lastKnown`), page fermée au design de Guide Négo, verdict immédiat d'après la garde puis appliqué dans les deux sens ; API arrêtée, l'application reste ouverte — vérifié au navigateur. T041 et T042 (connexion, « hier à… ») faites en avance : la garde en dépend | US1 (T026 à T040), puis US2, US4, US5. En local le drapeau est resté **allumé à 100 %** pour la suite |
| 2026-09-21 | Tâches de 0a (73) et analyse de cohérence, corrections appliquées : tout style de composant s'écrit sous `[data-app="guide-nego"]` et rien ne se téléporte dans `<body>` (`#gn-portail`) ; « lu à » et « Synchronisé à » sans fuseau, « hier à… », « le 11 nov. à… » — écart 32 ; la barre d'onglets seule défile quand la place manque, essai à 320 px ; « Prête hors connexion » dit à la personne (FR-012 bis, lexique) ; seul le paquet `fr` est gardé | `/speckit-implement` par tranches : phases 1 à 3, puis US1, US2, US4 et US5 ; `make check-safe` et un commit par phase |
| 2026-09-21 | Plan de 0a repris après relecture : Guide Négo s'affiche en français quel que soit le téléphone et ne touche pas au cookie de langue du site ; service worker engendré à la construction par un module local (liste exacte des fichiers, précache complet, navigation cache d'abord, mise à jour en arrière-plan) — le préchauffage est abandonné ; le drapeau vaut aussitôt d'après la garde et se relit en parallèle ; écarts 30 et 31 inscrits à `05-design.md` ; branche `008-guide-nego-coquille` créée | `/speckit-tasks`, `/speckit-analyze`, puis `/speckit-implement` |
| 2026-09-21 | Plan de 0a : `specs/008-guide-nego-coquille/plan.md`, recherche, modèle, trois contrats, guide de vérification. Tranché : sept dossiers `guide-nego`, composants `Gn*` ; manifeste et service worker statiques en chemins relatifs, bornés à `guide-nego/` ; service worker écrit à la main, sans module PWA (ADR-019 à écrire) ; garde des lectures en IndexedDB ; registre des modules fermés du site étendu de deux champs ; thème hors `<html>` et hors cookie du site ; `theme.css` repris avec son bloc `:root` rentré sous `[data-app]` ; aucun cadre de test ajouté, `node --test` et trois scripts dans `check-front` | `/speckit-tasks`, `/speckit-analyze`, puis `/speckit-implement` |
| 2026-09-21 | Spécification de 0a écrite puis relue par le commanditaire : `specs/008-guide-nego-coquille/` — cinq récits, aucune clarification en attente. Tranché : drapeau `guide_nego.enabled`, allumé = activé et à 100 %, une panne d'API ne ferme jamais l'application ; cinquième onglet sur `negotiation.channels`, sans nouveau drapeau ; thème dans « Profil et réglages » dès 0a ; fondations entières, mais seuls les composants de la coquille et ceux que toute étape emploie — les autres arrivent avec leur étape (prompt 0a corrigé dans la feuille de route) | `/speckit-plan` de 0a avec le prompt commun. Le correctif des vecteurs reste à faire avant toute indexation |
| 2026-09-20 | Constitution 1.1.1, correctif : la porte de qualité est `make check-safe` ; `make check` et `make check-db`, qui détruisent la base locale, exigent l'accord du commanditaire — comme `CLAUDE.md` | Correctif du modèle (vecteurs), puis 0a |
| 2026-09-20 | Constitution amendée en 1.1.0 : section « Guide Négo » et principes XI à XIV (hors connexion, confiance, design borné, une seule porte) ; contraintes des trois agendas et du suivi ; un amendement né de Guide Négo se consigne ici. Gabarits vérifiés : rien à y modifier, ils lisent la constitution à l'exécution | Correctif du modèle (vecteurs), puis 0a. |
| 2026-09-20 | Feuille de route : le socle scindé en 0a, 0b, 0c et les sessions en 3a, 3b — une spécification par chantier ; la recherche globale s'ouvre à l'étape 2, le centre de notifications à l'étape 3b | `/speckit-constitution`, puis 0a |
| 2026-09-20 | Seconde livraison de la maquette rangée : page « Échanges », dossier `passation/` (jetons, thème, mesures, pictogrammes, composants), 29 écarts tranchés ; barre à cinq onglets à l'ouverture des Échanges | — |
| 2026-09-20 | Maquette terminée et rangée dans `design/ecrans/` ; feuille de route réécrite avec un prompt Spec Kit par étape, un prompt de plan commun et l'amendement de la constitution | `/speckit-constitution`, puis l'étape 0 — Socle |
| 2026-09-19 | Direction artistique choisie dans Claude Design, qui a posé la question avant tout prompt : « Typographique », Atkinson Hyperlegible Next, quatre onglets et bouton « Aa ». Écran de référence rangé dans `design/ecrans/`, prompts 2, 3 et 7 adaptés, ADR-018 | Prompt 2 : le système de design, avec cinq points à reprendre — rôle du jaune, relief de « En cours », filets trop pâles, pictogrammes d'état, tailles sous 15 px |
| 2026-09-18 | Cadrage : notes du porteur confrontées au modèle de l'ePavillon et à deux recherches (processus de la CCNUCC, offre de l'IFDD) ; dix arbitrages. Dossier écrit : sept documents, dix-huit ADR, le lexique de l'interface et les prompts de maquette | Maquette dans Claude Design, puis le socle |

## Mettre à jour ce fichier

1. La ligne de l'étape dans **État**.
2. Une ligne au **Journal** : ce qui a été fait, ce qui vient.
3. Un **ADR** de plus si quelque chose a été tranché ; un point ouvert de moins s'il est résolu.

Rien d'autre. Ce fichier se lit en entier à chaque session : il reste court.
